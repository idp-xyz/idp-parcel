# SA 外部资金事实采用与更正的登记面：`MapExternalFundsHandler` 零生产装配，首版采用与 sa-cc/20 新开的「更正」格今天都只有测试直写能到

Category: enhancement
Status: draft——2026-09-15 10:3x 通道 6 立票（sa-cc/20 裁决 2「更正面与首版面是同一张『SA 采用登记面』的题……本票作者顺手立一张 draft」；归 SA owner）。只写票面未动代码；取证锚 main `3a21dab7` 与分支 `mcp6-sacc20`
Blocked by: 20（更正格已在分支 `mcp6-sacc20`，进 main 前本票不开工）；要裁的三条归 SA owner

## 缺口（取证于 `3a21dab7`，逐符号名；sa-cc/20 通道 5 取证条与通道 6 认领笔各量过一遍）

- `internal/settlementaccounting/application/map_external_funds.go` `NewMapExternalFundsHandler` / `MapExternalFundsDeps`：`git grep -n -E 'NewMapExternalFundsHandler|MapExternalFundsDeps' -- cmd/ internal/ ':!*_test.go'` 除定义处外**零命中**——采用（`AdoptFact`）、映射（`Map`）、核销（`Apply` / `Reverse`）与 sa-cc/20 加的更正（`CorrectFact`）五格今天没有任何生产构造点。
- 逐类入口：**inbox**——`internal/settlementaccounting/adapters/inbox/` 只听 `parcel-pricing.evaluation.recorded` 与 `customs-compliance.duty-payment-verification.formed`，仓内没有任何银行 / 支付 / 财务系统的入向事件类型；**HTTP**——`internal/settlementaccounting/adapters/http/` 全是 `query_*` 与 intake，`cmd/parcel-api/endpoints.go` 装的 SA 端点全是 `settlementhttp.NewQuery*`；**CLI**——`cmd/` 下无 settlement 二进制。
- 后果：外部资金事实进产品只有 SA 采用这一口（ADR-0137 决定四），而这一口今天没有门——首版与更正都只有 `cmd/parcel-dispatch/assemble_test.go` 与 SA 各测试用 `sapostgres.NewExternalFundsFacts(db).Save` 直写。sa-cc/02 的信封、sa-cc/03 的 CC 消费者、sa-cc/13 的版本子表、sa-cc/20 的更正格，整条缝在生产依赖图上从 SA 这一头起就是断的。
- 装配纪律的一处暗礁（sa-cc/02 评审 Standards 非阻断 (1) 记过）：`MapExternalFundsDeps.FactHandoff` 漏装时 `handOffFact` 直接对 nil 调用 → panic；`NewMapExternalFundsHandler` 今天不像 `NewRequestBuyEvaluationHandler` 那样构造期逐口拒 nil。

## 语言从哪里来

- ADR-0137 决定四：「外部资金事实进入本产品只有 `settlement-accounting` 采用这一口……人工核实与更正也先登在 SA、再经采用信封到 CC」——去掉的是 CC 侧运维补录子命令，**保留的这一口要有门**才成立。
- UC-SA-001「同一外部资金事实通过回调、文件或人工核实重复到达，只能被采用一次；更正必须形成新来源版本」——回调 / 文件 / 人工核实三种到达方式各自对应一种入口形。
- UC-SA-005 状态行：真实财务系统来源仍 `No-Go / 待参数化`（`PAR-INT-05`）；输入表「外部资金事实」行「不复制为本地付款事实，不因接收回调自动采用」——**自动采用不在任何一票**，本票只做机制半边的门。
- ADR-0085 决定一「登记册配置写面属产品能力，进 `parcel-api` 端点表」、决定二「写准入不另立准入形」（`UnconfiguredIntake{}` 起步）、决定四「其余上下文逐册跟进，取舍按『登记频次 × 操作者角色』由实施票逐册裁」——SA 的采用与更正是不是「登记册配置写面」这一族，是要裁的 2。

## 做法（待裁后写实）

1. 装配点（见「要裁的」1）：a) `cmd/parcel-api` 端点表加 SA 第一个写端点（照 ADR-0085 形：Intake 接口 + 处理器接口 + 封闭响应形状，`UnconfiguredIntake{}` 起步；管理台写签随两阶段）；b) 新建登记 CLI（先例 `parcel-customs-register` / `parcel-pricing-register`）或在既有二进制加子命令；c) 两者都做（ADR-0085 决定一「CLI 与端点消费同一登记用例」）。
2. 命令形：首版 `AdoptFundsFactCommand` 与更正 `CorrectFundsFactCommand` 各一格，答案代数照 `FundsOutcome` 封闭集转写；`registrationjson` 若沿用 CC 那套先例则同笔。
3. 装配纪律（见「要裁的」3）：`NewMapExternalFundsHandler` 构造期逐口拒 nil（形照 `NewRequestBuyEvaluationHandler` 与 CC `NewDutyPaymentReconciliationHandler`），`FactHandoff` 装 `OutboxExternalFundsFactHandoff`，同事务 Outbox。
4. 真库装配用例一正一反在 `cmd/` 侧：采用 → 版本行 + 信封；更正 → 第二行 + 第二封。

## 红线

- 不自动采用：任何回调 / 文件到达都不在本票；本票只开人工 / 受控批量的门（UC-SA-005「不因接收回调自动采用」）。
- 不预填任何真实来源、账户、币种；夹具全 `SYN-`。
- 不改 SA 采用四格与更正格的答案代数；不改 CC。
- 共享接线文件（`cmd/parcel-api/endpoints.go`、`main.go`）动前占号。

## 完成判据（待裁后写实）

1. 装配点在：`git grep -n NewMapExternalFundsHandler -- cmd/ ':!*_test.go'` 非零。
2. 首版与更正各能从生产入口进：真库装配用例一正一反。
3. 漏装 `FactHandoff` 在构造期被拒，不再 panic。
4. ADR-0085 那套三态（未配置 / 登记册答案 / 未决）在 SA 写面上照样分得开（若走端点）。

## 地盘

`internal/settlementaccounting/{application,adapters/http}`、`cmd/parcel-api/{endpoints.go,main.go}`（共享，占号）或新建 `cmd/parcel-settlement-register`、`apps/admin-web` 写签（若走端点）。不动 `internal/customscompliance/**`。

## 要裁的

1. 入口长在哪：CLI / HTTP 端点 / 两者——归 SA owner。三选各自要新建什么见 sa-cc/20 通道 5 取证条「入口三选各自要新建什么」。
2. ADR-0085 决定四怎么读：SA 的采用与更正算不算「登记册配置写面」这一族（它登的是外部事实的引用，不是配置）；若不算，写面的准入与管理台归属另裁——归 SA owner。
3. `FactHandoff` 漏装即 panic 的装配纪律：构造期拒 nil 是不是 SA 全部 handler 的统一形（16 已给 `NewRequestBuyEvaluationHandler` 做过一处）——归 SA owner。

## 参照

[20](20-sa-external-funds-fact-holds-one-row-per-fact-and-cannot-store-a-correction.md) 裁决 2 与通道 5 取证条「要裁的 2」；[02](02-sa-external-funds-fact-adoption-hands-off-an-envelope.md) 完成记录「`cmd/parcel-api` 装配处经核不存在」与评审 Standards 非阻断 (1)；[16](16-sa-evaluation-request-and-duty-verification-adoption-standards-tails.md)（构造期拒 nil 的 SA 先例）；ADR-0137 决定四与越权风险点 6；ADR-0085；UC-SA-001；UC-SA-005。

## Comments

- 2026-09-15 10:3x · 通道 6：立票（sa-cc/20 实施中按裁决 2 顺手立）。只写票面，未动代码。
- **2026-09-15 12:2x · 取证 ← 通道 5 · 钉 `7160fe67`**（task-4e80c3bb；只读隔离树 `%TEMP%\idp-parcel-mcp5-sacc27-evidence`，未跑真库、未跑 `go test` / `go build`、未占 55432、未动任何代码；只报事实，不裁、不提方案。每条写「命令 / 文件 + 符号名 → 结果」，引用一律符号名或引文，不写行号；计数只在数本身是论点处写并钉 SHA。sa-cc/20 已进 main，票面「缺口」节钉 `3a21dab7` 的三条在 `7160fe67` 复量后仍成立，本条只补票面没量的。）

  **量到的事实（逐条）**

  - **`MapExternalFundsHandler` 的生产装配**：`git grep -n -E 'NewMapExternalFundsHandler|MapExternalFundsHandler\b|MapExternalFundsDeps' -- cmd/ internal/ ':!*_test.go'` → 全部命中在 `internal/settlementaccounting/application/map_external_funds.go`（`MapExternalFundsDeps` / `MapExternalFundsHandler` 定义、`NewMapExternalFundsHandler`、方法接收者 `AdoptFact` / `CorrectFact` / `Map` / `Apply` / `Reverse` 与私有 `existingFact` / `handOffFact` / `existingApplication` / `handOff`）外，只剩 `adapters/postgres/settlement_application_handoff.go` 头注里一处**文字提及** `MapExternalFundsHandler.Reverse`。`cmd/` 与 `internal/**/adapters` 非测试代码**零构造、零调用**。测试构造只有一处：`application/map_external_funds_test.go` `newFundsFixture` 用 `application.NewMapExternalFundsHandler(application.MapExternalFundsDeps{Facts, Mappings, Applications, Downstream, FactHandoff, Clock})` 六口全接替身（`fundsFactStoreDouble` / `fundsMappingStoreDouble` / `applicationStoreDouble` / `settlementHandoffDouble` / `fundsFactHandoffDouble` / `fundsClock`）。`NewMapExternalFundsHandler` 函数体只有 `return &MapExternalFundsHandler{deps: deps}`，不校验任何一口、不返回 error。
  - **同一族适配器的生产装配**：`git grep -n -E 'NewExternalFundsFacts\(|NewOutboxExternalFundsFactHandoff\(|NewFundsMappings\(|NewSettlementApplications\(|NewOutboxSettlementApplicationHandoff\(|NewAdoptedFundsFactView\(' -- cmd/ ':!*_test.go'` → **只命中** `cmd/parcel-dispatch/assemble.go` 的 `sapostgres.NewAdoptedFundsFactView(db)`（交给 `ccsettlement.NewSettlementAdoptedFundsFactSource`，是 CC 消费者读 SA 的只读半边）。写口 `NewExternalFundsFacts` / `NewFundsMappings` / `NewSettlementApplications` 与两只 Outbox 交接 `NewOutboxExternalFundsFactHandoff(db, store *outbox.Store, clock)` / `NewOutboxSettlementApplicationHandoff` 在 `cmd/` 非测试**零构造**。`NewOutboxExternalFundsFactHandoff` 自己对 `db` / `store` / `clock` 三口构造期拒 nil 并返 error。
  - **SA inbox 今天听哪几类信封**：`internal/settlementaccounting/adapters/inbox/` 只有两只消费者——`buy_evaluation_recorded_consumer.go`（`BuyEvaluationRecordedEventType = "parcel-pricing.evaluation.recorded"`）与 `duty_payment_verification_consumer.go`（`DutyPaymentVerificationFormedEventType = "customs-compliance.duty-payment-verification.formed"`）；`cmd/parcel-dispatch/assemble.go` 路由表里 SA 那两行是 `sainbox.DutyPaymentVerificationFormedEventType` → `routedVerifications`、`sainbox.BuyEvaluationRecordedEventType` → `routedBuyEvaluations`，装配函数分别调 `sainbox.NewDutyPaymentVerificationConsumer(db.Transactor(), inboxStore, processing)` / `sainbox.NewBuyEvaluationRecordedConsumer(...)`。仓内没有任何银行 / 支付 / 财务系统入向事件类型；SA 自己发出的 `settlement-accounting.external-funds-fact.adopted`（`external_funds_fact_handoff.go` `externalFundsFactEventType`）是出向，消费方在 CC（`ccinbox.ExternalFundsFactAdoptedEventType` → `routedFundsFacts`）。
  - **SA http 有无写面**：`internal/settlementaccounting/adapters/http/` 非测试文件为 `catalogue_intake.go` / `isolated_read_intake.go` / `unconfigured_intake.go` / `query_settlement_charges.go` / `query_settlement_funds_applications.go` / `query_settlement_operating_results.go` / `query_settlement_statements.go`；`git grep -n -E 'MethodPost|MethodPut|MethodDelete|MethodPatch' -- internal/settlementaccounting/adapters/http ':!*_test.go'` **零命中**；包内 Intake 接口只有 `CatalogueQueryIntake`。`cmd/parcel-api/endpoints.go` 装的 SA 端点是 `/settlement-charges` / `/settlement-statements` / `/settlement-funds-applications` / `/settlement-operating-results`，全走 `settlementhttp.NewQuery*Endpoint(settlementCatalogueIntake, …)`，`settlementCatalogueIntake := settlementhttp.CatalogueQueryIntake(settlementhttp.UnconfiguredIntake{})`；`cmd/parcel-api/main.go` 里 SA 只装 `sapostgres.NewChargeCatalogue` / `NewStatementCatalogue` / `NewFundsApplicationCatalogue` / `NewOperatingCatalogue` 这几只只读目录。**`settlementhttp.UnconfiguredIntake` 头注原句**：「本包只有查阅端点、没有命令面，故本类型也只实现 CatalogueQueryIntake；将来长出命令 Intake 时本类型刻意不实现，未配置装不进命令端点由编译期决定」——与同包 `IsolatedOperationsReadIntake` 头注「本包没有命令面，将来长出命令 Intake 时本类型刻意不实现，放行装不进命令端点由编译期决定」同一句式。对照 CC：`customshttp.UnconfiguredIntake` 同时实现 `ResultIntake` / `CatalogueQueryIntake` 与该包全部 `*RegistrationIntake`（`IntakeInterpretationRuleRegistration` … `IntakeDutyPaymentVerificationRegistration`），头注写「五类配置登记命令口的未配置实现（ADR-0085）……隔离读放行（ADR-0078）不实现这五个接口，写行换不了」——CC 把「刻意不实现命令 Intake」那句落在隔离读放行上，未配置类型是实现的；SA 把同一句同时写在两个类型上。只列事实。
  - **两条命令的字段与构造门**（`map_external_funds.go`）：`AdoptFundsFactCommand{TenantID domain.TenantID; Fact, Source, Payer string; Kind domain.FundsFactKind; Currency string; AmountMinor int64; Version string; OccurredAt time.Time}`，进门经 `factFrom` → `domain.NewFundsFactReference` / `NewFundsSourceRegistrationReference` / `NewFundsPayerReference`（`Payer` 去空白非空才造）/ `NewCurrencyCode` / `NewFundsFactVersion` → `domain.AdoptExternalFundsFact(spec)`，任一失败答 `FundsNotAccepted`；`TenantID` 在命令上是已构造的值类型，`AdoptFact` 不再对它校验。`CorrectFundsFactCommand{TenantID domain.TenantID; Fact, Corrects, Version string; AmountMinor int64; CorrectedAt time.Time}`（无 Source / Payer / Kind / Currency / OccurredAt，头注「从链头照抄，命令上不再接」），进门：`NewFundsFactReference(Fact)`、`NewFundsFactVersion(Corrects)`、`NewFundsFactVersion(Version)` 任一失败 → `FundsNotAccepted`；再 `version == corrects || AmountMinor <= 0 || CorrectedAt.IsZero()` → `FundsNotAccepted`；查库后 `!found || head.Fact.Version() != corrects` → `FundsNotAccepted`；`head.Fact.CorrectAmount(...)` 失败 → `FundsNotAccepted`。两条命令的幂等摘要分别是 `adoptDigest`（Source / Payer / Kind / Currency / AmountMinor / Version / OccurredAt）与 `correctDigest`（Corrects / Version / AmountMinor / CorrectedAt），头注写明两者元素不同是有意的。答案代数：`FundsOutcome` 封闭集里采用 / 更正走得到的格是 `FundsFactAdopted` / `FundsFactExisting` / `FundsFactConflict` / `FundsNotAccepted` / `FundsUndecided`（`FundsUndecidedReason` 只有 `FundsFactStoreUnavailable` 这一格与它们相关），`FundsResult.FundsHandoffReference()` 非空表示行已落、信封未出（`fundsContinuation("EXTERNAL_FUNDS_FACT_HANDOFF", …)`）。`CorrectFact` 无 `Corrects` 时不能退化成首版——两条命令是两个类型、两条方法，不共享入口。
  - **`MapExternalFundsDeps` 六口在采用 / 更正路径上各在哪里被解引用**：`Facts.FindVersion` 是 `AdoptFact` / `CorrectFact` 查库第一步；`Clock.Now()` 在造 `FundsFactRecord` 时；`FactHandoff.HandOffExternalFundsFact` 在 `handOffFact`（`FundsFactSaved` 与 `existingFact` 两条路都到）；`Mappings` / `Applications` / `Downstream` 只在 `Map` / `Apply` / `Reverse` 路径上解引用，采用 / 更正不碰。六口在类型上是同一个 `Deps`、同一只 handler。
  - **CC 那侧对应的登记面（对照物，只描形）**。**HTTP**：`internal/customscompliance/adapters/http/register_credential_and_duty.go` 每册三件——Intake 接口（`DutyPaymentVerificationRegistrationIntake.IntakeDutyPaymentVerificationRegistration(ctx, *http.Request) (application.VerifyDutyPaymentCommand, error)`）、Registrar 接口（`DutyPaymentVerificationRegistrar.VerifyPayment(ctx, command) (application.DutyReconciliationResult, error)`，方法名取用例方法名，头注讲了为什么不叫 Handle）、端点构造 `NewRegisterDutyPaymentVerificationEndpoint(intake, registrar) http.Handler`；端点体 `newDutyReconciliationEndpoint[Command any]` 只认 `MethodPost`、Intake 失败 → `writeRegistrationIntakeProblem`、编排返 error → 500 `NO_ANSWER_FORMED`；封闭响应 `dutyReconciliationRegistrationResponse{Outcome string; UndecidedReason string,omitempty}`；`writeDutyReconciliationAnswer` 把业务未决（`businessUndecided` 点名的三格）走 200 带 `undecidedReason`、其余未决折 500，形成格走 201。该文件头注原句：「外部资金事实那一口不在本文件：资金事实进 CC 只经 settlement-accounting 的采用信封（ADR-0137 Decision 四），人工补录口是第二个铸造点，票 sa-cc/07 裁决 2 去掉了它。」**装配**：`cmd/parcel-api/assemble_customs_registration.go`——每册一个 `transactional*Registration{transactor bentoapp.Transactor; inner *Handler}` 包装类型实现对应 Registrar，`WithinTransaction` 内调用例；`outbox.NewStore(db)` + `ccpostgres.NewOutboxDutyPaymentVerificationHandoff(db, outboxStore, systemClock{})` 与登记册同一只 db，头注「事务壳给的环境事务因此同时罩住核对行与 Outbox 意图——两者同生共死，形照 parcel-customs-register 的 buildRegistrar」；`customsapp.NewDutyPaymentReconciliationHandler(deps) (*Handler, error)` 返 error，装配处注释「构造门在构造期拒 nil 依赖（票 sa-cc/14 的纪律）」；`endpoints.go` 三行 `customshttp.NewRegister*Endpoint(customshttp.UnconfiguredIntake{}, …)`，注释「写准入不另立形，同挂字面量 UnconfiguredIntake{}」。**CLI**：`cmd/parcel-customs-register/main.go`——`run(ctx, args, getenv, out, errOut) int`，第一参数是子命令名（`translate.go` `allCommands` 一处列全，含 `regulatory-credential` / `duty-collaboration` / `duty-payment-verification`），唯一 flag `-input <file>`（JSON，「未知字段一律拒绝」），DSN 只从环境变量 `IDP_PARCEL_POSTGRES_DSN` 取（「登记口不猜连接串」），退出码 `exitRegistered = 0` / `exitUsage = 1` / `exitConflict = 2` / `exitUndecided = 3`（头注按 ADR-0029 恢复动作分四格），`buildRegistrar(db) (registrar, error)` 装写口 + 只读视图 + `transactor`，`systemClock{}` 给形成时刻；载荷译装在 `internal/customscompliance/adapters/registrationjson`（`translate.go` 头注：分家是为了在线端点收同源载荷、`internal` 导不进 `cmd`）。仓内 `registrationjson` 只出现在 `internal/customscompliance/adapters/` 与 `internal/visibilityexception/adapters/` 下，SA 没有。`cmd/` 下登记类二进制：`parcel-collection-register` / `parcel-customs-register` / `parcel-governance-register` / `parcel-network-register` / `parcel-pricing-register` / `parcel-ve-register`；无 settlement 二进制。`parcel-customs-register` 头注仍写「配置登记是治理动作，不是在线请求面，走独立进程而不进 parcel-api 的端点表」，与同上下文三册已在 `endpoints.go` 并存。
  - **ADR-0137 决定四原文**：「**四、外部资金事实进入本产品只有 `settlement-accounting` 采用这一口；`customs-compliance` 不开第二个铸造或补录入口，人工核实与更正也先登在 SA、再经采用信封到 CC。** SA 采用（`UC-SA-005` / `map_external_funds.go` 的四格）是事实进产品的唯一入口，UC-SA-001『同一外部资金事实通过回调、文件或人工核实重复到达，只能被采用一次』已把人工核实算作同一口的一种到达方式；CC 只拥有『与外部资金事实的可追溯关系』，它的 `ReceiveFundsFact` 是接收已采用事实的引用（票 03 的 inbox 消费者），不是铸造。……因此票 07 步一的子命令是三个（凭证 / 协作 / 付款核对），不含 `external-funds-fact`；这一条只定口径，在票 02 / 03 进 main 之前不动代码。」去掉的那一口（Context 节）：「07 步一原拟给 `cmd/parcel-customs-register` 加 `external-funds-fact` 子命令作运维补录口」。Alternatives 否决句：「CC 保留 `external-funds-fact` 人工补录口，撞键时答 `已存在` / `内容冲突`。否决：两个铸造点的冲突不是幂等问题，是归属问题」。越权风险点 6 原句：「决定四请 SA owner 复核『单一采用口』。……若 SA owner 认为运维补录该有一条不经采用四格的旁路（比如 SA 采用口不可用时的应急），那是 SA 的一格，不是 CC 的入口。另：在 02 / 03 进 main 前 CC 的 `ReceiveFundsFact` 只有测试路径能触发，这是如实的空档，不拿人工口填。」Consequences 一行：「票 07 步一 CLI 三个子命令；步二端点 + 管理台照 ADR-0085 决定四做齐（三册同族一致）」；Links：「ADR-0085 决定四：票 07 要裁的 1 的判据」。
  - **SA CONTEXT 原句**（`docs/domain/settlement-accounting/CONTEXT.md`）：Boundaries——「外部资金事实进入本产品只有本上下文的采用这一口：`customs-compliance` 按本上下文发出的采用信封接收事实引用做税费付款核对，不另设铸造或补录入口；人工核实与更正也先在本上下文采用、再传出；采用保留来源提供的每一维，含来源提供的付款人……来源未提供时事实照样采用、付款人显式记为未提供，本上下文不对它作任何判断」；Lifecycles——「外部资金事实撤销、更正或退回时，`UC-SA-005` 追加映射更正或核销撤销。两类历史都不得删除」；「结算输入已接收：……外部资金事实……的采用版本；接收不表示代垫或回收已经成立」。CONTEXT 里没有一句说采用由谁、从哪个面发起。
  - **UC 原句**：UC-SA-001 业务规则「同一外部资金事实通过回调、文件或人工核实重复到达，只能被采用一次；更正必须形成新来源版本。」UC-SA-005 状态行「外部资金引用、运营余额、未分配金额和核销编排已形成；真实财务系统交换边界、事实身份、账户、币种、付款指示和撤销/更正语义待提供，生产分支保持 `No-Go / 待参数化`」；元数据「主要参与者 | 结算运营、财务集成责任方、客户对账责任方、采购运营、信用控制责任方」、「触发 | 受控财务交换收到已确认事实、已发布对账单/审核应付待核销、外部事实撤销/更正/退回或人工复核请求」、「未受理结束 | 来源、租户、法人、账户或权限不能合法关联」；主流程步 1「校验交换边界、来源、租户、责任法人、账户和权限 → 已受理或未受理」、步 2「保全并校验外部事实/税务映射版本和有效性 → 已采用、重复、无效或待判断」、步 6「处理外部撤销、退回或更正，形成反向资金映射、核销撤销或重分配」；输入表「外部资金事实 | 财务/银行/支付系统 | ……有效性和撤销/更正关系 | 不复制为本地付款事实，不因接收回调自动采用」；权限节「外部事实采用、手工分配、自动核销规则变更、核销撤销和信用限制分别授权；结算人员不能更改银行事实」；审计节「至少记录外部事实身份/版本、采用判断……操作主体」；`AT-SA-101`「唯一财务交换边界送达已确认客户收款事实 → 只形成外部事实采用和待匹配入口」、`AT-SA-119`「同一外部事实重复通过文件和回调到达 → 只采用一次」；`PAR-INT-05`（登记册行）「现有财务系统交换边界 | 只建立一个受控财务系统边界 | 待提供 | 系统身份、交换方向、业务引用、实际付款/付款失败/追加付款/资金退回/付款撤销/事实更正结果层……」。仓内 UC-SA-* 没有一句把「人工核实」落到某个具体面（CLI / 端点 / 管理台）。
  - **ADR-0085 原文**（状态头：Accepted，**部分停用**——Decision 二「本记录新增的登记端点也在被拦之列」一句与「两族各自要过 `PAR-INT-01` 的真证据门」前半已由 ADR-0100 停用，适用场景限管理台操作者族与登记册配置写面）。决定一：「**登记册配置写面属产品能力，进 `parcel-api` 端点表。** 各上下文在 `adapters/http` 为其登记用例增设命令端点（Intake 接口 + 处理器接口 + 封闭响应形状……），装配以字面量 `UnconfiguredIntake{}` 起步。……登记 CLI 不退场——CLI 与端点消费同一登记用例，是同一能力的受控批量口与在线口，答案代数一致。」决定二：「**写准入不另立准入形。**……ADR-0078 的隔离读准入不扩到写行，其编译期排除保持。」决定三：「**管理台写表单是机制半边，随端点两阶段接线。** 表单页……如实呈现三态——未配置（403 `ACCESS_CHANNEL_NOT_CONFIGURED`）、登记册治理答案（已登记/幂等重放/内容冲突/形状不可比）、未决」。决定四：「**首切片与推进序。**……其余上下文逐册跟进，取舍按『登记频次 × 操作者角色』由实施票逐册裁；治理登记册（pilot-governance，无租户维）不入首批——治理动作的操作者授权模型特殊，单独裁。」Alternatives 否决「写面长期只走 CLI（不进端点表）」的理由：「产品就绪判据是能力覆盖承接目标客户群常规业务形态，租户运营配置员经管理台配置是常规形态；把它长期留给『能进数据库网络的工程师』等于把一项产品能力错登记为一项运维动作。」Context 节把它治的病写成「『尚未接线』与『已接线但登记册为空/渠道未配置』长同一张脸」。ADR 全文里「登记册配置」指的是「价卡、网络、商业载体」这一类（Context「租户的运营配置员要能在管理台上登价卡、登网络、登商业载体」）；没有一句定义「配置」与「事实」的边界。**既有先例**：CC 的协作事项与付款核对不是配置、是登记事实，sa-cc/07 按「同族一致」把它们与凭证册一起做进了端点表（`register_credential_and_duty.go` 头注引 ADR-0085 Decision 一），装配注释同样写「写准入不另立形，同挂字面量 UnconfiguredIntake{}」。

  **要裁的 1（入口长在哪：CLI / HTTP / 两者）——各路要动的符号只列不选**

  - **共同的底**（三选都要）：`cmd/` 某处新建 `NewMapExternalFundsHandler(MapExternalFundsDeps{…})` 的生产构造；六口今天在 `cmd/` 零构造的适配器至少要装 `sapostgres.NewExternalFundsFacts(db)` 与 `sapostgres.NewOutboxExternalFundsFactHandoff(db, outbox.NewStore(db), clock)`（后者要 `*outbox.Store`，CC 装配处是 `outbox.NewStore(db)` 同一只 db）；`Mappings` / `Applications` / `Downstream` 三口采用 / 更正不碰，接真（`NewFundsMappings` / `NewSettlementApplications` / `NewOutboxSettlementApplicationHandoff`）还是留 nil 是这一处装配自己的题，与「要裁的」3 的答案耦合——构造期若拒 nil 则六口必须全接。事务壳：SA 今天没有 `transactional*` 包装先例，CC 的形是 `WithinTransaction` 内调用例、Outbox 与登记册同事务（`assemble_customs_registration.go` 头注）。真库装配用例：`cmd/parcel-dispatch/assemble_test.go` 今天用 `sapostgres.NewExternalFundsFacts(db).Save` 直写、不经 handler；sa-cc/20 评审 Spec ② 记「`CorrectFact` × 真库 × 真 Outbox 今天零用例」并归本票。
  - **CLI**（新二进制或既有二进制加子命令）：新建 `cmd/parcel-settlement-register/`（先例形 `cmd/parcel-customs-register/{main.go, translate.go}`：`run` / `allCommands` / `-input` / `IDP_PARCEL_POSTGRES_DSN` / 四格退出码 / `buildRegistrar` / `systemClock`），命令表两条（首版 / 更正）各对一条 `application.*Command`；SA 没有 `registrationjson`，载荷 → 命令的译装要新建（CC 把它放在 `internal/<ctx>/adapters/registrationjson` 是为让端点同源，只走 CLI 时放 `cmd/` 内也行——两处都是先例外的选择）；`FundsOutcome` → 退出码的归格（`FundsFactAdopted` / `FundsFactExisting` / `FundsFactConflict` / `FundsNotAccepted` / `FundsUndecided`、`FundsHandoffReference` 非空那一格）是本入口自己的表面；测试随形 `main_test.go` / `translate_test.go` / `vertical_test.go`（真库）。加进既有二进制的话，`cmd/` 下既有登记二进制（`parcel-collection-register` … `parcel-ve-register`）都是别的上下文的，`parcel-customs-register` 头注与 ADR-0137 决定四明写不含 `external-funds-fact`。`endpoints.go` / `main.go` 零 diff；`apps/admin-web` 零 diff。
  - **HTTP 端点**：`internal/settlementaccounting/adapters/http/` 新建第一份命令文件——两个 Intake 接口（各交回 `application.AdoptFundsFactCommand` / `CorrectFundsFactCommand`）、两个 Registrar 接口（方法名若照 CC 取用例方法名即 `AdoptFact` / `CorrectFact`）、两个端点构造、一个只认 `MethodPost` 的端点体、一个封闭响应形状（`FundsOutcome.String()` 原名 + `undecidedReason` + 是否带 `FundsHandoffReference`）、`FundsUndecided` 里哪几格算业务未决（今天与采用相关的只有 `FundsFactStoreUnavailable`，按 CC `businessUndecided` 的判据它是依赖故障那半）；`UnconfiguredIntake` 加两个方法实现新接口，其头注「将来长出命令 Intake 时本类型刻意不实现」一句随之改写（或另立一个未配置命令类型——CC 无此先例）；`IsolatedOperationsReadIntake` 不实现新接口（编译期排除保持，头注那句原样成立）；`writeProblem` / `writeJSON` / `codeNoAnswerFormed` 已在 `catalogue_intake.go` 可复用，`ErrAccessChannelNotConfigured` 已在。`cmd/parcel-api`：新建 `assemble_settlement_registration.go`（形照 `assemble_customs_registration.go`：`transactional*` 包装 + `db.Transactor()` + `outbox.NewStore(db)` + 交接适配器），`endpoints.go` 表加两行 `settlementhttp.NewRegister*Endpoint(settlementhttp.UnconfiguredIntake{}, …)`（共享接线文件，票面红线已记占号），`main.go` 装配入口加一处。测试随形：`settlementhttp` 端点用例（先例 `register_credential_and_duty_test.go`）、`unconfigured_intake` 用例、`cmd/parcel-api` 装配用例。管理台：`apps/admin-web/src/pages/settlement/` 今天的页全是读面（`ChargesBillingPage` / `ReconciliationPage` / `SettlementApplicationPage` / `OperatingMetricsPage` + `api.ts`），写签为新建，两阶段照 ADR-0085 决定三；CC 写签的先例文件本次未读（见能力边界）。
  - **两者**：上两条的并集；ADR-0085 决定一那句「CLI 与端点消费同一登记用例，答案代数一致」在代码上的落点是两面共用 `application.AdoptFundsFactCommand` / `CorrectFundsFactCommand` 与 `FundsOutcome`，载荷译装若要同源就得放 `internal/settlementaccounting/adapters/registrationjson`（照 CC）。
  - **不在三选里但票面提过的**（只记防重开）：inbox 消费者路——需要一种今天不存在的来源事件类型 + 消费者 + `cmd/parcel-dispatch` 路由表项，而来源系统在 UC-SA-005 是 `No-Go / 待参数化`、输入表写「不因接收回调自动采用」，票面红线「不自动采用」已排除。

  **要裁的 2（ADR-0085 决定四怎么读：SA 采用与更正算不算「登记册配置写面」这一族）——两种读法各自依赖的事实与后果只列**

  - **文本侧**：ADR-0085 从头到尾用的词是「登记册配置写面」「登记册配置的写入」，例子是价卡 / 网络 / 商业载体，Decision 四「取舍按『登记频次 × 操作者角色』由实施票逐册裁」；没有一句定义「配置」与「登记事实」的边界，也没有一句把 SA 列进或排出。ADR-0137 Consequences 把 CC 三册「步二端点 + 管理台照 ADR-0085 决定四做齐」，其中协作事项与付款核对是事实登记不是配置——**先例已把 ADR-0085 用到了非配置的登记事实上**（CC `register_credential_and_duty.go` 头注引 Decision 一）。UC-SA-005 给的操作者事实：主要参与者含「结算运营、财务集成责任方」，触发含「人工复核请求」，权限节「外部事实采用……分别授权；结算人员不能更改银行事实」，审计节要记「操作主体」。「登记频次」仓内无任何数据（真实来源 `PAR-INT-05` 待提供）。
  - **读作「算」**：走 ADR-0085 决定一至三整套——端点进 `endpoints.go` 表、`UnconfiguredIntake{}` 起步、管理台写签两阶段、未配置 403 `ACCESS_CHANNEL_NOT_CONFIGURED` 三态；CLI 不退场。要动的符号即「要裁的」1 的 HTTP 或两者路。ADR-0100 已把管理台操作者族的准入从 `PAR-INT-01` 分出去，SA 写端点的真 Intake 候选形与 CC 三册同族。
  - **读作「不算」**：ADR-0085 不适用，写面准入形（ADR-0055 未配置格是否照用、还是另立）与管理台归属要另有一句裁决或 ADR；若只走 CLI，`parcel-customs-register` 头注「配置登记是治理动作……不进 parcel-api 的端点表」那句在 SA 侧成为现行口径，而 ADR-0085 Alternatives 已否决过「写面长期只走 CLI」——否决理由说的是「租户运营配置员」，SA 这一格的操作者按 UC-SA-005 是「结算运营 / 财务集成责任方」；两种读法在这一点上依赖同一句 UC 文本的不同读法。要动的符号即「要裁的」1 的 CLI 路。
  - **两读法共同不动的**：`FundsOutcome` 代数、`AdoptFact` / `CorrectFact` 五格、`OutboxExternalFundsFactHandoff` 信封形、CC 任何文件（票面红线）。

  **要裁的 3（`FactHandoff` 漏装即 panic：构造期拒 nil 是不是 SA 全部 handler 的统一形）**

  - **数即论点，钉 `7160fe67`**：`git grep -n -E '^func New[A-Za-z]+Handler\(' -- internal/settlementaccounting/application` → **12** 只构造器。其中返回 `(*Handler, error)` 且表驱动逐口拒 nil、包 `ErrNilDependency`（`apply_pre_acceptance_control.go` `var ErrNilDependency = errors.New("settlement accounting: dependency is nil")`）的 **2** 只：`NewApplyPreAcceptanceControlHandler`、`NewRequestBuyEvaluationHandler`（sa-cc/16 改成这形，头注「缺件一律包 ErrNilDependency（同包 NewApplyPreAcceptanceControlHandler 那张表的形）」）。其余 **10** 只不校验、不返 error：`NewSettleClaimAmountsHandler` / `NewRecordChargeAdjustmentHandler` / `NewReceiveSupplierBillHandler` / `NewMapExternalFundsHandler` / `NewConfirmChargeHandler` / `NewFormSupplierExpectedCostHandler` / `NewCutOffPublishStatementHandler` / `NewAssessAdvanceRecoveryHandler` / `NewAllocateCostsHandler` 九只形如 `return &Handler{deps: deps}`，`NewReleasePreAcceptanceControlHandler(ledger, exposures, clock)` 一只是三个位置参数直接装字段。**统一形今天不成立**——2 / 10 两种并存。
  - **哪些今天有生产构造**：`git grep -n -E 'saapplication\.New[A-Za-z]+Handler\(' -- cmd/ ':!*_test.go'` → `NewReleasePreAcceptanceControlHandler` 四处（`cmd/parcel-api/assemble_disposition.go` / `assemble_review.go` / `assemble_withdrawal.go`、`cmd/parcel-dispatch/assemble.go`，不拒 nil）、`NewRequestBuyEvaluationHandler` 一处（`assemble_evaluation_request.go`，`handler, err :=`）、`NewApplyPreAcceptanceControlHandler` 一处（`cmd/parcel-dispatch/assemble.go`，`apply, err :=`）、`NewAssessAdvanceRecoveryHandler` 一处（`cmd/parcel-dispatch/assemble.go`，不拒 nil；其装配头注写「生产装配里不放任何替身」、合同责任口接 `sapartycommercial.UnconfiguredContractResponsibility{}` 显式未配置而不是 nil）。其余八只（含 `NewMapExternalFundsHandler`）在 `cmd/` 零构造。CC 同族先例：`customsapp.NewDutyPaymentReconciliationHandler(deps) (*Handler, error)`，装配处注释「构造门在构造期拒 nil 依赖（票 sa-cc/14 的纪律）」。
  - **各路后果只列**：**(a) 只改 `NewMapExternalFundsHandler`**（形照 `NewRequestBuyEvaluationHandler`）——签名变 `(*MapExternalFundsHandler, error)`，六口逐名；唯一既有调用方 `map_external_funds_test.go` `newFundsFixture` 随形接 error（今天六口全接替身，不会拒）；新的 `cmd/` 装配处按 `handler, err :=` 接（同 `assemble_evaluation_request.go`）；一例 `TestTheHandlerRefusesANilDependencyAtConstruction` 同形。**(b) 十只全改成统一形**——十只签名 + 各自测试夹具 + 上列 `cmd/` 七处调用（四处 `Release:` 字段直接内联构造，改 error 形要先取值再装）；`NewReleasePreAcceptanceControlHandler` 的位置参数要不要顺带改 `Deps` 结构体是这一路自己的题；`NewAssessAdvanceRecoveryHandler` 的 `Contracts` 口今天接显式未配置值不是 nil，拒 nil 不改变它。**(c) 不改构造器、只在装配处保证不漏**——`FactHandoff` 为 nil 时 `handOffFact` 在 `handler.deps.FactHandoff.HandOffExternalFundsFact(...)` 解引用 panic，且是在 `Facts.Save` 已落行之后（`FundsFactSaved` 那格）——行已落、信封没出、结果没返回；`existingFact` 重放路也走 `handOffFact`。`Clock` 为 nil 在 `Clock.Now()` 处 panic，在 `Save` 之前。这些是今天的代码事实，票面「缺口」第四条已记。
  - **与「要裁的」1 的耦合**：任一入口路都只调 `AdoptFact` / `CorrectFact`，`Mappings` / `Applications` / `Downstream` 三口不在路径上；构造期拒 nil（a 或 b）意味着装配处必须把这三口也接真，否则构造就拒；不拒（c）则三口可留 nil 而 `Map` / `Apply` / `Reverse` 在生产上仍无调用方。只列。

  **能力边界**：只读 `7160fe67` 隔离树；未跑真库、未跑 `go test` / `go build` / `go vet`、未占 55432。读了：`map_external_funds.go` 全文、`map_external_funds_test.go` 夹具段、`ports.go` 资金事实相关声明、SA `adapters/http` 三份 intake 文件全文与其余文件名、SA `adapters/inbox` 两只消费者的事件类型常量、`external_funds_fact_handoff.go` 头部、CC `register_credential_and_duty.go` 与 `unconfigured_intake.go` 全文、`cmd/parcel-api/assemble_customs_registration.go` 头注与装配函数、`endpoints.go` / `main.go` 的 SA 与 CC 装配行、`cmd/parcel-customs-register/main.go` 头注与 `run` / `buildRegistrar` 签名、`translate.go` 命令表、`cmd/parcel-dispatch/assemble.go` 路由表与 SA 两只消费者的装配函数、ADR-0137 决定四 / Alternatives / 越权风险点 / Consequences、ADR-0085 全文、SA CONTEXT 含「外部资金事实」各句、UC-SA-005 元数据 / 主流程 / 权限 / 输入表 / 状态行、UC-SA-001 那一句、`PILOT-PARAMETER-REGISTER` `PAR-INT-05` 行、sa-cc/16 缺口一与完成记录。**没读**：`cmd/parcel-customs-register/main_test.go` / `vertical_test.go` 里真库装配用例的具体形；`apps/admin-web` CC 写签的先例文件（只列了 SA 四页文件名）；`sapostgres.ExternalFundsFacts` / `FundsMappings` / `SettlementApplications` 实现体；`NewAssessAdvanceRecoveryHandler` 以外八只无生产构造的 handler 各自的测试夹具怎么装；ADR-0055 / 0078 / 0100 正文（只引 ADR-0085 内对它们的转述）。grep 结果以上列命令在 `7160fe67` 复跑为准。
