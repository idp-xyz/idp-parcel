# `parcel-pricing` 没有 inbox：SA 发出的评价请求信封到不了计价，UC-SA-002 步 2 后半「`parcel-pricing` 采用……形成不可变计价输入快照」只能由测试直接调

Category: enhancement
Status: 已进 main——2026-09-14 20:4x 通道 1 推送方：`mcp4-sacc11@3062d485` 六笔重放到 main `9d720a2f` 之上，代码 tip `6876e10f`（`24b73f66→eaa532e1` / `90ed1959→65eaaf07` / `28915f3b→fd43a1f8` / `583ab00e→681bea6e` / `d272ba5f→0a8523d7` / `3062d485→6876e10f`），零冲突；非作者评审 ← 通道 1 推送方自评（两个隔离子代理鉴权错未起跑，按 parallel-sessions「没有空闲通道时推送方自己跑」；作者通道 4）两轴 0 阻断 / Standards 2 非阻断 / Spec 2 非阻断；`6876e10f` 带 DSN 全仓 113 ok / 0 FAIL，探针 PASS 非 SKIP；清点重生成零差；详见 Comments 末两条。此前 resolved——2026-09-14 20:0x 通道 4（task-d47fcaaa 续做单，接替 task-e480bd94 → task-28474d4c 两次会话 crash；分支 `mcp4-sacc11` 基 `01974923`，五笔代码 + 本笔完成记录，全文见「完成记录」；未拆前置票——裁决 4 量得入口要先过解析口才到得了「输入不可得」，拆了本票无物可验）。此前 in-progress——2026-09-14 16:1x 通道 4 认领（task-e480bd94，通道 1 派单；`/implement` › `/tdd`；分支 `mcp4-sacc11` 基远端 main `01974923`，树 `D:/tops/idp-parcel-mcp4-sacc11`；开工第一件事是裁决 4 的量，量完再定拆不拆前置票；若拆，前置票号从 23 起）。此前 ready-for-agent——2026-09-14 10:2x 通道 1 按用户 10:1x「授权代裁」（PP owner 口径）裁「要裁的」1：PP 今天**没有生产形成入口**（`NewEvaluatePricingHandler` 非测试零调用，`EvaluatePricingHandler.Handle` 收的是已成形的 `domain.EvaluationRequest`），走票面 (b)——本票先立「按评价请求形成评价」应用入口 + 评价上的「评价请求回指」一格 + 在用价卡解析读口的机制半边；PP 以消费者形式接 SA 信封；全文见文末「裁决」（含范围警示：比原估大，允许拆前置票）。取证锚 main `6bbf2bf0`。此前 draft——2026-09-10 17:5x 通道 5 立票（task-a93cb825；票 [08](08-sa-evaluation-request-orchestration-records-source-references.md)「要裁的」1 裁「走信封」时，因 PP 无 inbox 消费者先例而点名另立），只写票面未动代码；取证锚 `66cad4c4`。要裁的一条，归 PP owner
Blocked by: [08](08-sa-evaluation-request-orchestration-records-source-references.md) 的登记册与发信封半边（没有那封信封，消费者无物可收）——**08 已进 main（2026-09-11 16:3x，main 上代码 `c9aae85f` / `8d75bfc7`），信封 `settlement-accounting.evaluation-request.submitted` 载荷 `{tenantId, evaluationRequestId}`、分区主体 租户 / 评价请求、SA 读口 `EvaluationRequests`（按铸造 ID 取）在，硬阻已解；本票仍 draft，开工前要 PP owner 裁「要裁的」那一条入口形状（2026-09-11 通道 1 推送方记）**；本票落地后 08 的完成判据 4 与 [01](01-buy-evaluation-to-sa-inbox-consumer.md) 的「形成」路才走得通

## 缺口（取证于 `66cad4c4`）

- `Get-ChildItem internal/parcelpricing/adapters -Directory` → `http` / `postgres` / `sourcefeed`，**无 `inbox`**；`cmd/parcel-dispatch/assemble.go` 路由表无任何 `parcelpricing` 消费者。仓内 inbox 消费者先例在 `internal/networkrouting/adapters/inbox`、`internal/parcelshipment/adapters/inbox`、`internal/visibilityexception/adapters/inbox`。
- PP 提供方半边已在：`internal/parcelpricing/adapters/postgres/evaluation_handoff.go` 发 `parcel-pricing.evaluation.recorded`（票 01 缺口节）——PP 会**发**信封，但今天不**收**任何信封。
- 票 08 让 SA 在评价请求登记同事务发 `settlement-accounting.evaluation-request.submitted`（载荷 `{tenantId, evaluationRequestId}`）；PP 侧没有人接。

## 语言从哪里来

- UC-SA-002 步 2：「`settlement-accounting` / `parcel-pricing`：结算提交主要范围、计算目的和合格来源引用；`parcel-pricing` 采用测量、运输收费发生项、其他履约、面单及已解析商业依据形成不可变计价输入快照 → 计价输入版本或待判断」。
- PP `CONTEXT.md`（ADR-0012 决定）：`parcel-pricing`「只消费其他上下文提供的事实快照」；评价的输入是不可变计价输入快照，同一输入、版本清单与内容摘要产生相同结果（ADR-0013 Consequences 那条不变量）。
- 票 08「裁决」1：一条缝两个方向一种机制；同步调用会把 PP 的可用性压进 SA 的写路径。

## 做法

1. `internal/parcelpricing/adapters/inbox/evaluation_request_consumer.go`：收 `settlement-accounting.evaluation-request.submitted`，按 `evaluationRequestId` 经**消费侧适配器** `internal/parcelpricing/adapters/settlementaccounting/`（新；CONTEXT-MAP 加 SA→PP 一条边）读 SA 的评价请求只读口（按键取：租户 + 评价请求 ID；口径照票 [03](03-cc-inbox-consumer-receives-external-funds-fact.md)「裁决」——走只读口、取信封所指那一份、不读写侧登记面），取主要范围、计算目的、合格来源引用三件，交 PP 请求评价的应用入口（形状见「要裁的」1）。
2. PP 形成计价输入快照与评价时在评价上**回指 `evaluationRequestId`**（票 08 做法 3 靠它），随既有 `parcel-pricing.evaluation.recorded` 信封的评价内容可按引用读到。
3. 同请求重放 → PP 答`已存在`（同一请求不形成第二份评价；ADR-0013 不变量「同一评价输入……相同结果」的入口守法）；请求所指的来源引用解析不到（发生项 / 协议版本不在场）→ PP 答待判断并指名，不造输入。
4. `cmd/parcel-dispatch/assemble.go` 路由表加一行（共享接线文件，动前占号）；`application` 不得 import `settlementaccounting`。
5. 真库装配用例：入队一封 → 消费一次 → 一份计价输入快照 + 评价（或待判断）；重投不翻倍。

## 红线

- 消费者只译不算：金额、币种、换算、取整全在 PP 评价里按价卡声明（ADR-0107 / ADR-0013），消费者不预处理任何数值。
- 不改 SA 的信封形（归 08）；不改 PP 既有 `evaluation_handoff.go` 的信封形（票 01 消费它）。
- 真实价卡、真实供应商协议属实例半边 `PAR-SET-03`，夹具全是合成串。

## 完成判据

1. `git grep -w NewEvaluationRequestConsumer -- cmd/`（或等价装配符号）有非测试调用点。
2. 应用层：一封 → 一份评价（带 `evaluationRequestId` 回指）；重投 → `已存在`；来源引用解析不到 → 待判断。
3. 真库装配用例一正一反；CONTEXT-MAP 与机制清点（消费缝 PP→SA +1）同笔。

## 地盘

`internal/parcelpricing/adapters/inbox/`（新）、`internal/parcelpricing/adapters/settlementaccounting/`（新）、`internal/parcelpricing/application/`（若要裁的 1 裁「先立入口」则新编排文件）、`cmd/parcel-dispatch/assemble.go` 一行、`docs/domain/CONTEXT-MAP.md` 一条边。不动 `internal/settlementaccounting/**`。

## 要裁的

1. **（已裁，见「裁决」）** PP 请求评价的应用入口是什么形：PP 今天形成评价的入口是给谁调的（`git grep -n 'func New.*Evaluat' -- internal/parcelpricing/application` 开工前核）——若已有一个收「计价输入 + 计算目的」的应用入口，消费者直接调它并加 `evaluationRequestId` 回指一格；若今天评价只由 PP 内部或测试路径形成，本票先立「按评价请求形成评价」的应用入口。归 PP owner（入口形状与「评价回指请求」那一格是 PP 领域的事）。同时是票 08 裁决 1 的越权风险点：PP owner 是否愿意以消费者形式接。

## 裁决（2026-09-14 10:2x，通道 1 推送方按用户「授权代裁」以 PP owner 口径裁；取证 main `6bbf2bf0`）

**取证**：`internal/parcelpricing/application` 里形成评价的入口只有 `EvaluatePricingHandler.Handle(EvaluatePricingCommand{Request: domain.EvaluationRequest})`——它收的是**已成形**的评价请求（价卡版本 + 输入快照 + 证据层级 + 重放引用，`domain.NewEvaluationRequest` 四参），不收「计价输入 + 计算目的」；`NewEvaluatePricingHandler` 在 `cmd/` 与非测试代码里**零调用**，即 PP 今天没有任何生产路径形成评价，评价只由测试与 `pptest` 夹具真算出来。PP `ports` 里价卡只有 `PriceCardVersionLoader`（按版本取）与 `PriceCardCatalogueRead`（上列），**没有**「按（租户、范围、方向、目的、时点）解析在用价卡版本」的读口（参考序列 / 目录各有 `InForceResolver`，价卡没有）。`domain.EvaluationRequest` 只有 `ReplayOf`，没有指向 SA 评价请求的回指。

1. **走 (b)：本票先立「按评价请求形成评价」的应用入口**（名作者定，形照同包 `EvaluatePricingHandler`：`Deps` 拒 nil、结果格封闭）。收（租户、评价请求引用、主要范围、方向 / 计算目的、合格来源引用、计价基准时点）——全部来自 SA 评价请求读口（消费侧适配器按信封所指那一份取，口径照 [03](03-cc-inbox-consumer-receives-external-funds-fact.md)「裁决」）。三步：① 解析在用价卡版本；② 造 `PricingInputSnapshot`；③ 造 `domain.EvaluationRequest` 交**既有** `EvaluatePricingHandler.Handle`——不改它、不绕它，幂等 / 入册 / 交付照旧走它。
2. **评价上加「评价请求回指」一格**：`domain.EvaluationRequest` 加可缺席的 SA 评价请求标识（引用，不复制 SA 的任何内容），随评价入册、读口可按回指取——[08](08-sa-evaluation-request-orchestration-records-source-references.md) 判据 4 与 [01](01-buy-evaluation-to-sa-inbox-consumer.md)「形成」路要的正是这一格。回指**不是计算输入**：`MarshalEvaluationSnapshot` 的 `semanticDigest` / `planContentDigest` 不得因它而变，作者用既有快照用例钉住；它进不进 `inputDigest` 一类字段由作者按 ADR-0107「整组出自评价」的精神定，完成记录写明。
3. **在用价卡解析读口——机制半边现在立，三格封闭**：按（租户、范围、方向、目的、时点）在价卡登记册上解析，唯一命中 → 解析出版本；零命中 → **未配置**（诚实停，点名缺的是价卡）；多于一条 → **适用冲突**（未决，交人裁，不种任何优先级 / 最新优先之类的选择规则——那是实例半边与 owner 语言题）。价卡的范围 / 方向 / 目的 / 有效期今天都在 `PricingPlanVersion` 上，解析只读它们，不加列。
4. **输入快照的取值**（包裹主体、分区、计费重量、业务时点）：SA 评价请求带的「合格来源引用」（发生项 / 费用项目 / 供应商协议）能不能指到重量与分区事实，**作者开工第一件事量**；指不到就停在显式未决「输入不可得」并点名缺哪一只读口（跨上下文读口归 NO / TF / PS 那一侧，不在本票造），不猜不填、不拿默认重量顶。这一格若停住，本票的入口仍成立——它诚实地答「今天形成不了」，与 [01](01-buy-evaluation-to-sa-inbox-consumer.md) 的 `ExpectedCostUndecided` 同一种停法。
5. **PP 以消费者形式接 SA 信封——是**（票 08 裁决 1 越权风险点消解）：消费者落 `internal/parcelpricing/adapters/inbox/`（新，形照仓内三处 inbox 先例）、消费侧读口落 `internal/parcelpricing/adapters/settlementaccounting/`（新）、`cmd/parcel-dispatch/assemble.go` 路由一行 + 未决哨兵名单（ADR-0029 分格），`docs/domain/CONTEXT-MAP.md` 加 SA→PP 一条边。PP `application` 不 import SA。
6. **范围警示与拆分许可**：入口 + 回指 + 解析读口 + 消费者比立票时估的大一倍以上。**允许拆**：作者立票时可把「条 2 回指一格 + 条 3 解析读口」拆成一张前置票（同目录新号，`Blocked by` 关系写清），本票 `Blocked by` 它；**不允许**为了缩范围绕过任一未决格（条 3 的冲突格、条 4 的输入不可得格）——绕过就是替租户选价卡或替来源编重量。
7. **不改的**：`EvaluatePricingHandler` 与 `domain.EvaluatePricing` 一字不动；SA 侧不动（票面地盘原句）；不立新 ADR——回指一格与解析读口都是既有记录覆盖的形（ADR-0107 评价整组采用、ADR-0025 消费侧适配器、ADR-0029 结果分格）；若作者发现回指要改评价的守恒不变量或快照 digest 语义，停下报，那时再议 ADR。

## 完成记录

（通道 4 · task-d47fcaaa 续做单 · 2026-09-14 19:2x–20:0x · 隔离树 `D:/tops/idp-parcel-mcp4-sacc11`，分支 `mcp4-sacc11` 基远端 main `01974923`；未 rebase——main 此后到 `9d720a2f` 只有 `.scratch` / `docs` 簿记与 `cmd/parcel-dispatch/assemble.go` / `assemble_test.go` 各一处注释（通道 2 sa-cc/23），与本票的块不同，重放时推送方处理。三个会话接力：16:1x 认领后 crash 零代码；18:2x–18:3x 笔 A / 笔 B 推出、笔 C 在途 crash，推送方封存为 `0c368e45`；19:2x 起本会话续做。）

**逐笔**（分支上的 SHA；`0c368e45` 已被 force-with-lease 覆盖，此处记新旧对照）：

| 笔 | 内容 |
|---|---|
| `24b73f66` | docs(scratch)：认领，Status → in-progress（第一个会话） |
| `90ed1959` | feat 笔 A（第二个会话）：`ports/price_card_in_force.go`（`PriceCardInForceResolver`，三格封闭）+ `adapters/postgres/price_card_in_force.go`（`PriceCards.ResolveInForce`）+ 真库用例 5 条（唯一命中 / 零命中三种来路 / 两张不同身份冲突 / 同身份两版冲突 / 空键拒） |
| `28915f3b` | feat 笔 B（第二个会话）：domain `EvaluationRequestReference` + `EvaluationRequest.WithRequestReference` / `WithInput` / `PricingEvaluation.RequestReference`，快照 `requestReference,omitempty`；`semanticDigest` / `planContentDigest` 不因回指而变的钉例（`evaluation_request_reference_test.go`）；迁移 `parcel_pricing/0010_evaluation_request_reference.sql`（可空列 + 部分唯一索引 tenant+request）；`ports/evaluation_by_request.go`（`EvaluationByRequestView`）+ `Evaluations.FindByRequestReference` + 真库用例；`evaluate_pricing.go` 两处补齐取值重立请求改走 `request.WithInput`（见「裁决 7 字面」） |
| `0c368e45` → `583ab00e` | 笔 C：推送方封存的七件按 parallel-sessions「先写自己第一片 red 再读对方代码」接手——自写的「输入不可得」用例四条判据（结果格 / 三只读口定序点名 / 步 ① 跑一次再停步 ② / 零入册零交付）与封存用例逐条对上，采封存之形重切。`application/form_evaluation_from_request.go`（`FormEvaluationFromRequestHandler`）+ `_test`（8 例）；`ports/pricing_input.go`（`PricingInputResolver` / `EligibleSourceReferences`）、`ports/evaluation_identity.go`（`EvaluationIdentityFactory`）；`adapters/identity/identity.go`（`EVAL` 前缀）+ `_test`；`domain/value_objects.go` 导出 `PricingPurpose.PairsWithDirection`（+7）。相对封存改两处：受理门加发生项引用（身份 + 版本）非空 + 两条拒例；Rejects 用例一条空断言换成读口计次 |
| `d272ba5f` | 笔 D：`adapters/inbox/evaluation_request_submitted_consumer.go`（包 `ppinbox`，`EvaluationRequestSubmittedConsumer`，走 `inboxconsume` 门）+ 真库用例 5 条；`adapters/settlementaccounting/evaluation_request.go`（`TranslateEvaluationRequest`，只声明 SA 只读口 `FindByID` 一口）+ `form_on_evaluation_request_submitted.go`（`FormOnEvaluationRequestSubmittedAdapter`，五枚未决哨兵）+ 两份 `_test`（11 例） |
| 本笔（tip） | 笔 E：`cmd/parcel-dispatch/assemble.go` 装配函数 `formEvaluationOnEvaluationRequestConsumer` + `evaluationRequestSubmittedUndecidedSentinels` + 路由表一行 + `evaluationRequestFormationEvidence`；`assemble_test.go` 真库装配例 `TestASubmittedEvaluationRequestStopsHonestlyAtThePricingInputSeamThroughTheRouteTable`；`docs/domain/CONTEXT-MAP.md` SA→PP 那条边补计价侧一句；`docs/product/MECHANISM-INVENTORY.md` 在本笔代码的干净 detached 检出（`0c5c6ce1`，与 tip 只差本记录与清点两份 `.md`）重生成；本完成记录 |

**逐条对完成判据**：

1. ✓ `git grep -w NewEvaluationRequestSubmittedConsumer -- cmd/` 命中 `cmd/parcel-dispatch/assemble.go` 非测试调用点（`formEvaluationOnEvaluationRequestConsumer`），路由表 `ppinbox.EvaluationRequestSubmittedEventType` 一行；`NewEvaluatePricingHandler` 与 `NewFormEvaluationFromRequestHandler` 随之首次进入 `cmd/` 生产装配——PP 第一条生产形成路。
2. ✓ 应用层（`form_evaluation_from_request_test.go`）：一封 → 一份评价带回指（`TestFormEvaluationFromRequestFormsAndBackReferencesTheRequest`：回指在评价上、铸造一次、入册一次、意图一封、解析价卡用的是计价基准时点）；重投 → `EXISTING`（`…AnswersExistingByBackReference`：不解析、不造快照、不铸、不入册第二份）；来源引用指不到 → 停在 `PRICING_INPUT_UNAVAILABLE`（`…NamesTheMissingReadPortsWhenNoInputResolverIsWired` 点名三只读口 / `…RelaysTheResolverMissingList` 转述读口的缺项），票面「待判断并指名」在本票落成这一格与 `UNDECIDED`（依赖故障，带 Reason）两格——见「裁决 4」。另：未配置 / 适用冲突（`…StopsWhenNoOrManyPriceCardsApply`）、受理门（`…RejectsAnUnformedCommand`）、依赖故障逐口指名（`…ReportsWhichDependencyLeftItUndecided`）、构造期拒 nil。
3. ✓ 真库装配例一正一反（`assemble_test.go`）：正——SA 真登记册 + 真 Outbox 适配器落一份 BUY 请求并入队（事件类型两串相等在真库钉住），PP 册上一张适用 SYN BUY 卡 → 消费者回查 SA → 入口解析到卡 → 停在「输入不可得」（观察口原文 `ErrPricingInputUnavailable` 且点名 `transport-fulfillment`）→ `dispatch.consumer_undecided`、inbox 无账、`parcel_pricing.evaluation` 零行；反——没卡的范围停在「未配置」（原文 `ErrPriceCardNotConfigured`）、指着 SA 还没有的请求停在「还看不见」、载荷缺请求 ID 毒丸入账定稿。**「重投不翻倍」不在装配例里假装**：生产图今天到不了入册，重投由应用层回指命中 `EXISTING`（判据 2）、库上迁移 0010 部分唯一索引（`evaluation_by_request_test.go`）、inbox 认领（`ppinbox` 真库例 `…ProcessedExactlyOnce`）三处分别钉住。CONTEXT-MAP 与机制清点同笔：消费缝 24→25 组（`parcelpricing → settlementaccounting` 2 文件）、消费适配器 32→33、直投路由表 23→24、迁移 parcel_pricing 9→10、PP 生产 89→99 / 测试 86→94 / 应用编排 9→10 / postgres 13→14、端口声明 404→408；**端口缺口 +1：`parcelpricing.PricingInputResolver` 基线与精确口径都缺**——这正是裁决 4 的现场，生产装配不接任何实现（见下）。

**逐条对裁决**：

1. ✓ 入口 `FormEvaluationFromRequestHandler`（`Deps` 四口必备拒 nil、`Inputs` 可缺席；结果 `FormEvaluationFromRequestOutcome` 封闭七格：FORMED / EXISTING / PRICE_CARD_NOT_CONFIGURED / PRICE_CARD_APPLICABILITY_CONFLICT / PRICING_INPUT_UNAVAILABLE / NOT_ACCEPTED / UNDECIDED）。收（租户、评价请求引用、主要范围、方向 + 目的成对、合格来源引用三件、计价基准时点、证据层级）；三步 ① `PriceCards.ResolveInForce` → ② `Inputs.ResolvePricingInput` → 铸标识 → ③ `NewEvaluationRequest` + `WithRequestReference` 交 `EvaluatePricingHandler.Handle`（收具体类型，不绕在结构上成立）。先按回指找先到者答 EXISTING。
2. ✓ 回指一格（笔 B）：`domain.EvaluationRequest.requestReference` 可缺席，随评价入册（列 `request_reference` + 部分唯一索引），`EvaluationByRequestView.FindByRequestReference` 按回指取。**不进 `semanticDigest` / `planContentDigest`**（钉例两枚摘要不变）；**也不进 `inputDigest`**——按 ADR-0107「整组出自评价」读：回指是「为谁形成」的元数据，与 `replayOf` 同族，不是评价输入的成分；快照里带、摘要里不带。`domain` 与 `adapters/postgres` 越票面地盘字面，本条已预告。
3. ✓ 解析读口（笔 A）三格封闭，只读 `PricingPlanVersion` 上既有的范围 / 方向 / 目的 / 有效期，不加列；不种任何选择规则（同身份两版也是冲突）。
4. ✓ **量（第二个会话 16:2x，钉 `01974923`，本会话复核同意）**：业务时点指得到（`Sources().Occurrence.OccurredAt()` 就在 SA 请求上）；包裹主体指不到（发生项成员在 TF `ChargeOccurrenceRegistry.FindByKey` 写侧口上，无只读视图，且成员是包裹身份或集运单元字面分不出）；分区指不到（起讫邮编归 PS，PP 无读口）；计费重量指不到（发生项只有计数 + 单位，实重 / 尺寸归 NO 实测 / PS 申报，PP 无读口）。**落法**：`ports.PricingInputResolver` 只立形不立实现；入口对 `Inputs == nil` 答 `PRICING_INPUT_UNAVAILABLE` 并点名三只读口（TF 发生项成员对象 / NO·PS 实重尺寸 / PS 邮编路线）；生产装配**不接**任何实现——不用一个永远答「不在」的替身顶上（sa-cc/01 组合根同一取舍）。不猜、不填、不拿默认重量顶。票面做法 3「来源引用解析不到 → 待判断并指名」在本票落成 `PRICING_INPUT_UNAVAILABLE`（读口接上后由它转述「哪一件解析不到」）而不是复用领域的 `待判断`：领域 `EvaluationPending` 承诺「补齐事实就能得出价格」且是**一份已形成的评价**的状态，而这里连快照都造不出、一份评价都没有——两格不同物，不互相顶替（CONTEXT「四种结果不互相顶替」同一理由）。
5. ✓ 消费者 `internal/parcelpricing/adapters/inbox/`（新，形照 `effective_delivery_consumer.go`：只译不判、毒丸拒收入账、处理失败回滚重投）；消费侧读口 `internal/parcelpricing/adapters/settlementaccounting/`（新；只声明 SA 只读口 `FindByID` 一口，拿不到 Save；取信封所指那一份）；`assemble.go` 路由一行 + 五枚未决哨兵名单（ADR-0029 分格：请求还看不见 / 未配置 / 适用冲突 / 输入不可得 / 形成未决，恢复动作各异故不合成；`ErrUntranslatableReference` / `ErrUntranslatableAnswer` 留在名单外落 publish_failed）；CONTEXT-MAP SA→PP 那条边（08 已立）补计价侧一句。`internal/architecture` 边界门禁绿：PP `application` 不 import SA。
6. **未拆**：入口要先过解析口才到得了「输入不可得」，「已存在」靠回指读；三件在一条分支上分笔推、每笔推 origin，达到了拆票要的「小步可保」而不必立票；未为缩范围绕过任一未决格。
7. ✓ `EvaluatePricingHandler.Handle` 与 `domain.EvaluatePricing` 一字不动；不立新 ADR。**裁决 7 字面的一处越界（笔 B，第二个会话已报）**：`evaluate_pricing.go` 里 `withSeriesReadings` 与 `completeCatalogueReadings` 两处「补齐取值后重立请求」从 `NewEvaluationRequest` 四参改走 `request.WithInput`——不改 `Handle` 本体与纯函数，改的是它调用的两个包内 helper；理由在 `WithInput` 头注：四参重立会把不在四参里的回指丢掉，回指一格就是第一件会丢的。评审判。

**判断项**：① SA `PrimaryScopeReference` → PP `PricingScopeID` 按字面译（`TranslateEvaluationRequest`），是否同一标识空间归 PP / SA owner；今天两侧都是 `requiredValue` / `identifier` 的自由串，装配例用同一串 `SYN-SCOPE-11` 两侧对上。② **证据层级**：`FormEvaluationFromRequestCommand.Evidence` 由装配方声明、不给默认（入口受理门与适配器构造期都拒空）；`cmd/parcel-dispatch` 声明 `evaluationRequestFormationEvidence = S`——不是默认值，是现状的如实陈述（本仓尚无租户，价卡登记册里只有 `SYN-` 实例，从它们形成的评价只能是隔离合成 S；PS `cost_source.go` 同一取法）。某租户实例进入生产要形成 P 级评价是 PN-08 `Go` 对那一个实例的裁决，那天要改的是让证据层级按租户实例从登记册取，不是把常量翻成 P。③ 评价标识取随机铸造（`EVAL` 前缀）不从 SA 请求 ID 派生（`ports/evaluation_identity.go` 头注：身份声明不派生，幂等由回指唯一约束守）；代价是同租户同回指并发第二份撞唯一索引时 `EvaluatePricingHandler` 按新标识读不回先到者答 `UNDECIDED`，本入口转述为 `RequestEvaluationFormationUndecided`，消费者重投后按回指命中 `EXISTING`——多一次重投、零重复评价。④ `PricingPurpose.PairsWithDirection` 导出（+7）：只暴露既有私有配对表，让受理门拒配错的一对，不让它被译成「未配置」去登记一张本不该存在的卡。⑤ 越票面地盘字面的每一件：`domain/`（笔 B 回指、笔 C `PairsWithDirection`）、`adapters/postgres/`（笔 A 解析口、笔 B 回指列与读口）、`ports/`（笔 A / B / C 四个新口）、`adapters/identity/`（笔 C，新包，形照 SA / CC / PS identity）、`migrations/parcel_pricing/0010`、`docs/product/MECHANISM-INVENTORY.md`（判据 3 要的清点）；`internal/settlementaccounting/**` 零改动。

**验证**（隔离树 `mcp4-sacc11`，19:3x–20:0x，Windows 本机）：`gofmt -l` 空；`go build ./...` / `go vet ./...` 退 0；`go test -count=1 ./internal/architecture/...` ok（边界 / 标识前缀 / 分区主体 / 接线棘轮）。带 DSN `go test -p 1 -count=1 -v`：`./cmd/parcel-dispatch/ ./internal/parcelpricing/...` **PASS 513 / FAIL 0 / SKIP 0，9 包 ok**（含新装配例与 `ppinbox` 5 条真库例 PASS 非 SKIP）；此前一轮 `./internal/parcelpricing/...` 单独 PASS 449 / 0 / 0。55432 两次占 / 释均已广播。`-race` 本机无 cgo 未跑。评审按 parallel-sessions 由推送方派非作者通道；作者自跑的两轴见 Comments。

## 参照

票 [08](08-sa-evaluation-request-orchestration-records-source-references.md)「裁决」1 / 2、[01](01-buy-evaluation-to-sa-inbox-consumer.md)、[03](03-cc-inbox-consumer-receives-external-funds-fact.md)「裁决」（消费侧读口口径）；UC-SA-002 步 2；ADR-0012、ADR-0013、ADR-0107；`internal/parcelshipment/adapters/inbox/effective_delivery_consumer.go`（跨上下文 inbox 消费者 + 消费侧读口先例）；`internal/parcelpricing/adapters/postgres/evaluation_handoff.go`（PP 提供方半边）。

## Comments

- 2026-09-14 20:0x · 通道 4（task-d47fcaaa）：完成记录见上；作者自跑两轴（`/implement` Step 3，基线 `01974923`，不以此代替非作者评审）——**Standards**：注释全中文；跨文件引用只用符号名（清了三处「六样 / 五枚」这类数别处的计数）；夹具 `SYN-`；`internal/settlementaccounting/**` 零改动；architecture 全部门禁绿（边界：PP `application` 不 import SA、跨上下文翻译只在 `adapters/settlementaccounting`；标识前缀 `EVAL` 全仓唯一；路由表随消费者一起长）。**Spec**：判据 1–3 与裁决 1–7 逐条见完成记录；两处票面字面与实现措辞不同、已在记录里说明——做法 3「来源引用解析不到 → 待判断」落成 `PRICING_INPUT_UNAVAILABLE` 而非领域 `待判断`（两格不同物），裁决 7 字面在笔 B `evaluate_pricing.go` 两个包内 helper 处有一处越界（`Handle` 与纯函数未动）。**接手过程**：按 parallel-sessions 先写自己第一片 red（输入不可得格）再读封存七件，四条判据逐条对上，只差命名；一次 `go vet` 的 grep 在读封存前泄出过封存文件三行（`Evidence` 字段 / `NewEvaluationRequest(..., command.Evidence)` / 证据词表 switch），与我已在写的设计同向，如实记在这里。
- 2026-09-10 · 通道 5（task-a93cb825）：立票，未动代码。能力边界：核过 PP `adapters` 三个子目录与 dispatch 路由无 PP 消费者、仓内三处 inbox 先例；读过 UC-SA-002 步 2、票 08 裁决；**没读** PP `application` 的评价入口签名——「要裁的」1 就是这一处，开工前以代码为准。
- **2026-09-14 20:1x–20:4x · 评审 ← 通道 1（推送方自评，非作者；作者通道 4）· 钉 `mcp4-sacc11@3062d485`，基线 `01974923`**：隔离检出 `%TEMP%\idp-review-sacc11`，树干净、无未跟踪；范围六笔 29 文件 +2973/−37。按 parallel-sessions「没有空闲通道时推送方自己用 /code-review 的隔离子代理跑」起了两个只读子代理（Standards / Spec），**两个都在起跑前鉴权错**（`Authentication error`，与 09-07 20:3x 那次同一故障），改由本会话按同一 brief 逐文件读 diff 自评；除通道 1 / 4 外其余通道已 crash，无第三方可派。
  **Standards——阻断：无。非阻断 ①**：`cmd/parcel-dispatch/assemble.go` `evaluationRequestSubmittedUndecidedSentinels` 头注 `ErrPricingInputUnavailable` 那条末句「今天每一封 BUY 请求信封都停在前三格之一」与代码相反——有卡的请求停在的是第四格 `ErrPricingInputUnavailable`（`TestASubmittedEvaluationRequestStopsHonestlyAtThePricingInputSeamThroughTheRouteTable` 正例断言的正是这一格）；AGENTS「注释只写代码讲不出的东西」，讲错的比不讲更糟；改措辞零行为（如「都停在形成之前的某一格」并点名 未配置 / 适用冲突 / 输入不可得）。**非阻断 ②（判断题，Repeated Switches）**：证据层级 S / R / P 的封闭集在 `adapters/settlementaccounting` `declaredEvidence` 与 `application` `FormEvaluationFromRequestCommand.accepted` 各写一遍 switch，领域已有未导出的 `EvidenceKind.valid`；可另笔导出一处共用，不改行为。**无发现**：注释全中文；`git diff -U0` 加行正则 `\.go:\d+|第N行` 零命中；加行里的计数（三样 / 三只读口 / 三步 / 两条读法）都在同句点名、不数别处文件；`gofmt -l` 空、`go build` / `go vet` 0；ADR-0029：五枚哨兵恢复动作各异（等可见性 / 登卡 / 人裁 / 等读口 / 等依赖），`ErrUntranslatableReference` / `ErrUntranslatableAnswer` 落 publish_failed 与 `buyEvaluationRecordedUndecidedSentinels` 同形；`ErrPriceCardNotConfigured` 进名单与先例 `ErrAmountPrecisionUndeclared` 不进名单不矛盾——前者同一封信封在登卡后重投会变好，后者要换卡另评一份；ADR-0025：翻译只在 `adapters/settlementaccounting`，`application/form_evaluation_from_request.go` 只 import `ppdomain` / `ppports`；`evaluationRequestFormationEvidence = S` 是如实陈述且由装配方声明、构造期拒空，不是生产默认（AGENTS「证据层级诚实」）；`internal/settlementaccounting/**` 零 diff；`docs/product/MECHANISM-INVENTORY.md` 在 `6876e10f` 用 `tools/mechanism-inventory` 重生成零差，未手改。
  **Spec——阻断：无。非阻断 ①**：裁决 7 字面「`EvaluatePricingHandler` 一字不动」被 `completeCatalogueReadings`（handler 方法）与 `withSeriesReadings` 改走 `request.WithInput` 打破（作者笔 B 已自报）。判：为裁决 2「回指随评价入册」所必需——四参 `NewEvaluationRequest` 重立会把回指丢在补齐取值那一步；`Handle` 本体、纯函数 `domain.EvaluatePricing`、幂等 / 入册 / 交付路径未动，接受。**顺带的语义变化要点名**：旧 `completeCatalogueReadings` 在挂上目录读数时经四参重立把序列步留下的 `seriesNotes` 也丢了，而 `seriesNotes` 进 `domain.EvaluatePricing` 的 `explanation`、语义摘要盖它——新写法保留，于是「卡同时绑序列与目录、序列步留了说明、目录读数挂上」这一组合新形成的评价解释多出序列说明、语义摘要不同于旧代码会算出的值；已入册评价不受影响（重放走 `borrowSeriesReadings`，`missingSeriesBindings` 对重放答空）。是修正不是回归，但范围内没有用例钉这一组合——归 PP owner 另笔补一条单元例或在 0107 / 0099 的 Consequences 记一句。**非阻断 ②（判断项，归 PP owner）**：票面做法 3 字面「来源引用解析不到 → 待判断并指名」与 PP CONTEXT 不变量「取不到价卡、区间空档、事实缺失属待判断」的字面，与实现的三格（`PRICE_CARD_NOT_CONFIGURED` / `PRICE_CARD_APPLICABILITY_CONFLICT` / `PRICING_INPUT_UNAVAILABLE`，不形成评价）用词不同。判：裁决 3 / 4 明写「未配置」「适用冲突」「显式未决『输入不可得』」，裁决晚于做法且更具体，作者按裁决落；CONTEXT 那句分的是**已形成评价**的结果格（待判断要有方案与快照），这里连 `domain.EvaluationRequest` 都造不出，造占位快照才能落成待判断——恰是裁决 4 禁的「猜 / 填」。接受；但 CONTEXT 里没有「形成之前的停格」这一层语言，是否补一句归 PP owner。**逐条**：判据 1 ✓（`assemble.go` `formEvaluationOnEvaluationRequestConsumer` 调 `ppinbox.NewEvaluationRequestSubmittedConsumer`，路由表 `ppinbox.EvaluationRequestSubmittedEventType` 一行；`NewEvaluatePricingHandler` / `NewFormEvaluationFromRequestHandler` 首次进入 `cmd/`）；判据 2 ✓（`TestFormEvaluationFromRequestFormsAndBackReferencesTheRequest` / `…AnswersExistingByBackReference` / `…NamesTheMissingReadPortsWhenNoInputResolverIsWired` / `…RelaysTheResolverMissingList`）；判据 3 ✓（装配例正例停 `ErrPricingInputUnavailable` 且原文点名 transport-fulfillment，反例 未配置 / 还看不见 / 毒丸三格；`parcel_pricing.evaluation` 零行、未决三封 inbox 零行、毒丸一行；「重投不翻倍」三处各在：应用层回指命中、迁移 0010 `evaluation_one_per_request` + `TestSecondEvaluationForTheSameRequestIsAlreadyRecorded`、`ppinbox` `TestASubmittedRequestEnvelopeIsProcessedExactlyOnce`）；裁决 3 ✓（`PriceCards.ResolveInForce` 只读既有列，`ORDER BY plan_id, plan_version` 只定候选顺序、无 LIMIT、无「最新优先」，三格在 SQL 外按行数分）；裁决 4 ✓（`assemble.go` 不装 `Inputs`；`resolveInput` 对 nil 答 `PRICING_INPUT_UNAVAILABLE` 并列 `missingInputReadPorts`；无任何默认重量 / 分区）；裁决 5 ✓（`decodeSubmittedEvaluationRequest` 两维缺一毒丸；`EvaluationRequestSource` 只声明 `FindByID`；`HandleSubmittedEvaluationRequest` 已形成 / 已存在答 nil、五格哨兵、`NotAccepted` → `ErrUntranslatableAnswer`）；判断项 ③ ✓（`Save` ON CONFLICT DO NOTHING 撞部分唯一索引答已有记录 → `FindByID(新标识)` 不中 → `EvaluationUndecided` → `RequestEvaluationFormationUndecided`，重投 `FindByRequestReference` 命中 → EXISTING）；范围：`docs/domain/**` 只 CONTEXT-MAP 那条边一句；越地盘字面清单与判断项 ⑤ 一致、均为裁决 2 / 3 所需。作者判断项 ①②③④⑤ 全接受。
  **小结**：Standards 0 阻断 / 2 非阻断（头注一句与代码相反、S/R/P switch 两处）；Spec 0 阻断 / 2 非阻断（裁决 7 字面越界带一处未钉的解释语义变化、形成前停格与 CONTEXT 用词）。可进 main。**跑了**（隔离重放树 `%TEMP%\idp-replay-sacc11` @ `6876e10f`，内容 = 本分支 + main 至 `9d720a2f` 的前进）：`gofmt -l` 空、`go build ./...` / `go vet ./...` 0、带 DSN 全仓见下条；评审检出上只 `git diff` / `git grep` / Read。**没跑**：`-race`（本机无 cgo）。
- **2026-09-14 20:4x · 进 main 记录 · 通道 1 推送方**：`mcp4-sacc11@3062d485` 六笔重放到 main `9d720a2f` 之上（隔离检出 `%TEMP%\idp-replay-sacc11`，前一任通道 1 会话 20:0x 已 `cherry-pick` 完成、`git range-diff 01974923..3062d485 9d720a2f..6876e10f` 六笔全 `=`，零冲突——与通道 2 sa-cc/23 在 `assemble.go` / `assemble_test.go` 的两处注释不同块）：`24b73f66→eaa532e1` / `90ed1959→65eaaf07` / `28915f3b→fd43a1f8` / `583ab00e→681bea6e` / `d272ba5f→0a8523d7` / `3062d485→6876e10f`（代码 tip；本簿记笔在其上，SHA 见广播）。**验证（推送方全量一次）**：`gofmt -l` 空、`go build ./...` / `go vet ./...` 0；20:1x 先 `check_messages(waitMs 0)` 排队列（空）再广播占 55432，`6876e10f` 带 DSN 全仓 `go test -p 1 -count=1 ./...` **113 ok / 0 FAIL / 16 无测试 / 0 cached**（128 s；比 `f1be6687` 多出的三包是 PP `adapters/inbox` / `adapters/settlementaccounting` / `adapters/identity`）；探针 `TestASubmittedEvaluationRequestStopsHonestlyAtThePricingInputSeamThroughTheRouteTable`（cmd/parcel-dispatch）与 `TestASubmittedRequestEnvelopeIsProcessedExactlyOnce`（ppinbox）`-v` PASS 非 SKIP；释号广播；`tools/mechanism-inventory` 在 `6876e10f` 干净检出重生成 porcelain 空 → 不出清点笔。**评审门**：非作者评审 ← 通道 1 推送方自评（上条）两轴 0 阻断；四条非阻断随票记：Standards ① 头注一句 → A 类零行为尾巴、Standards ② S/R/P switch 两处 → 可合进同一尾巴票；Spec ① 解释语义变化未钉、Spec ② 形成前停格的 CONTEXT 语言 → 归 PP owner。**后继**：三只输入读口（TF 发生项成员对象 / NO·PS 实重尺寸 / PS 邮编路线）各归提供方上下文立票，接上后在 `formEvaluationOnEvaluationRequestConsumer` 补 `Inputs` 一行；08 判据 4 与 01「形成」路自此有回指可走。`mcp4-sacc11` → `merged/mcp4-sacc11`、远端删；作者树 `D:/tops/idp-parcel-mcp4-sacc11` 与重放树比内容后拆，评审检出 `%TEMP%\idp-review-sacc11` 拆。
