# SA 的外部资金事实一事实一行：更正版本在提供方自己就存不下，sa-cc/02 裁决 2「更正再发一封」没有落地路径

Category: bug
Status: resolved——**已进 main，2026-09-15 11:5x 通道 1 推送方**（首波批 tip `c657a5e7`；重放 `94688ce4→fcdbc86e` / `fe496cf8→8f4121ad` / `c8ead2d0→6a85d4f4` / `33116348→6dc8d247` / `63333f4d→14d86a61` / `2da94586→f25d262a`；评审 ← 通道 3 两轴 0 阻断 / Standards 2 非阻断 / Spec 3 非阻断；批 tip 带 DSN 全仓 113 ok / 0 FAIL；见 Comments「进 main 记录」）。此前——**2026-09-15 10:4x 通道 6 完工，待非作者评审进 main**（task-173daa46；分支 `mcp6-sacc20` 基 main `3a21dab7`，代码四笔 `fe496cf8` / `c8ead2d0` / `33116348` / `63333f4d`，完成记录与后继 draft 27 随末笔；完成判据 (1)–(5) 逐条见「完成记录」）。此前 in-progress——**2026-09-15 10:1x 通道 6 认领**（隔离树 `%TEMP%\idp-parcel-mcp6-sacc20`；开工先对 `3a21dab7` 重量三条取证，结果写「完成记录」判断项）。此前 ready-for-agent——**2026-09-14 22:1x 通道 1 按用户「你是业务和系统专家，自决」代裁（SA owner 口径），两条「要裁的」写入下方「裁决」节**：形取**版本子表**（身份行不动、版本各占一行，照 CC `0021`）；入口是**既有采用用例加一格「更正」**（`MapExternalFundsHandler` 新命令调 `CorrectAmount`、复用同一交接口再发一封），**不开新的在线面**——首版今天也没有生产入口，「SA 采用与更正的登记面」另立 draft 归 SA。此前 draft——2026-09-14 14:0x 通道 1 立票（sa-cc/13 完成记录判断项 ① 的发现；归 SA owner）。只写票面未动代码；取证锚 main `0bd86d42`
Blocked by: 无（要裁的已裁，见「裁决」）

## 缺口（取证于 `0bd86d42`，逐符号名）

- `migrations/settlement_accounting/0004_external_funds.sql`：`external_funds_fact` 主键 `(tenant_id, fact_id)`——一事实一行；后续 SA 迁移无一改过这道主键。
- `internal/settlementaccounting/adapters/postgres/external_funds_fact.go` `ExternalFundsFacts.Save`：`ON CONFLICT DO NOTHING`，同引用第二次 `Save` 答 `FundsFactAlreadyAdopted`——更正版本（同 `fact_id`、新 `version`、`corrects` 回指前版）落不进去。
- `internal/settlementaccounting/domain/external_funds.go` `ExternalFundsFact.CorrectAmount`：结构拷贝出回指前版的新版本——**在 SA 无生产调用点**（`git grep 'CorrectAmount(' -- internal/settlementaccounting ':!*_test.go'` 零命中）。
- `adopted_funds_fact_view.go` `AdoptedFundsFactView` 头注自己写「库里将来一事实多行（更正版本各成一行）时，这一句照样只答被问的那一版」——「将来」今天还没到。
- 于是 sa-cc/02 裁决 2「更正 / 撤销复用同一事件类型 `settlement-accounting.external-funds-fact.adopted` 再发一封、ID `<租户>/funds-fact/<事实>/<版本>`、载荷带可缺席的 `corrects`」在 SA 侧没有能产生第二封的写路径；CC 侧（sa-cc/13）已能收——`cmd/parcel-dispatch` 的正例只好另用一条事实、v1 经 CC 写口预铺、SA 只存 v2。

## 语言从哪里来

- SA `CONTEXT.md` 外部资金事实那句（sa-cc/03 裁决 2 补过「含来源提供的付款人」半句）与「它不修改外部资金事实」——更正是新版本不是修改，所以要存得下多版本。
- AT-SA-114（`CorrectAmount` 头注引）：差额与核销的重算随新有效版本另行进行。

## 做法（待裁后写实）

1. SA `external_funds_fact` 加版本维：主键加 `version`、或版本子表（形由作者定，照 CC 0021 的取舍写为何）；`FundsFactKey` 随之带版本或另立读口；新迁移，`0004` 不改。
2. 生产入口：一条「采用更正版本」的用例 / 命令（`CorrectAmount` 的调用方），走 sa-cc/02 的 handoff 再发一封。
3. `AdoptedFundsFactView` 不改（已按版本查）。

## 红线

- 不覆盖前版；更正是新版本回指前版（`Corrects`）。
- 不改 CC；CC 已能按（引用 + 版本）收。

## 完成判据（待裁后写实）

1. SA 应用层：v1 已采用，更正 v2 → 新一行回指 v1、再发一封；同版本重放`已采用`。
2. 真库：新迁移往返；`0004` 一字未动。
3. `cmd/parcel-dispatch` 正例改回同一事实两版都在 SA（sa-cc/13 那一格里「另用一条事实」的绕法退役）。

## 地盘

`internal/settlementaccounting/{domain,ports,application,adapters/postgres}`、`migrations/settlement_accounting/`（新序号）、`cmd/parcel-dispatch/assemble_test.go`（共享文件，动前占号）。CC 侧不动。

## 要裁的

1. 主键加版本还是版本子表——归 SA owner（SA 侧 `duty_payment_verification_adoption` 等表若外键到 `external_funds_fact (tenant_id, fact_id)`，取舍同 CC 0021 头注那段）。
2. 「采用更正版本」的入口长在哪（CLI / HTTP / 只有 inbox 消费者）——归 SA owner。

## 裁决（2026-09-14 22:1x 通道 1 代裁，SA owner 口径；依据是通道 5 21:15 取证条，钉 `bccb60a1`）

1. **形——版本子表，照 CC `0021`。** 取证量得 SA 没有任何外键钉在 `external_funds_fact`，所以 CC 选子表的那条理由（保外键）在 SA 不成立——但另两条成立：(i) `funds_mapping.fact_id` / `settlement_application.fact_id` 引用的是**事实身份**——映射与核销是对一条事实做的，新版本到达后「差额与核销的重算随新有效版本另行进行」（AT-SA-114、`CorrectAmount` 头注）是另一步，身份行让这两张表的引用继续指到一个确定的东西；主键加版本后 `fact_id` 单独指不到行，`FundsApplicationCatalogue.ListExternalFundsFacts` 按 `f.fact_id` 关联的聚合会逐版本重复。(ii) 与 CC 同形：同一条事实两侧都是「身份一行、版本多行」，信封 `<租户>/funds-fact/<事实>/<版本>` 两头读法一致，接手的人只记一种形。**落法**：新迁移 `settlement_accounting/00NN`（序号作者取下一个）建 `external_funds_fact_version (tenant_id, fact_id, version)` 外键到身份行，内容列（`source_ref / payer_ref / kind / currency / amount_minor / occurred_at / corrects / corrected_at / content_digest / recorded_at`）搬进子表、身份表只留身份 + 首次采用时刻（照 `0021` `DROP COLUMN` + 存量非零 `RAISE` 交人；存量为零见 `0018` 头注）；`0004` / `0018` 不改。`ExternalFundsFacts.Save` 变两步 DO NOTHING（身份行 + 版本行），`FundsFactAlreadyAdopted` 的语义落在**版本行**已在（身份已在、版本新 → 可插入，这正是本票要开的口）；`FindByKey(租户, 事实)` 交回**链头**（无后继的那一版 = 最近采用的那一版；SA 是铸造方，采用按序发生、更正必须指向当前链头——裁决 2——所以链头唯一、不需要「谁是当前」的标记）；新增 `FindVersion(租户, 事实, 版本)`；`AdoptedFundsFactView.LoadAdoptedFundsFact` 签名不变、FROM 改子表（WHERE 已带版本）；`Map` / `Apply` 照旧按 `FindByKey` 读链头（守恒对着当前有效版本算，AT-SA-114 的重算是后继题不在本票）；`ListExternalFundsFacts` 一事实一行（链头内容）+ 版本数与回指列可加、映射 / 核销聚合不因版本行重复。
2. **入口——既有采用用例加「更正」格，不开新面。** ADR-0137 决定四原句「人工核实与更正也先登在 SA、再经采用信封到 CC」、UC-SA-001「更正必须形成新来源版本」——更正是采用的一种，走 `MapExternalFundsHandler`：新命令 `CorrectFundsFactCommand{TenantID, Fact, Corrects, Version, AmountMinor, CorrectedAt}`（名字作者定），前置：事实已采用（否则`未受理`）、`Corrects` **等于当前链头版本**（指向非链头 → `未受理`——SA 是铸造方，纠正一个不是当前的版本是调用方编程错误，与 CC 作为接收方容忍乱序不同，理由写进头注）、新版本字面不等于任何已采用版本（同（事实、版本）重放 → 同摘要`已采用` / 异摘要`冲突`，照 `AdoptFact` 四格）、`amountMinor > 0`；形成走 `domain.CorrectAmount`（它由此有了生产调用方），写版本行，`handOffFact` 复用 `OutboxExternalFundsFactHandoff.HandOffExternalFundsFact`（版本与 `corrects` 从 `Record.Fact` 取，02 判断题 ⑥ 预告的正是这一格）。**不做**：CLI / HTTP / inbox 任一新面——取证量得**首版今天也没有生产入口**（`NewMapExternalFundsHandler` 零生产装配、SA http 全 query、SA inbox 无资金来源事件、`cmd/` 无 settlement 二进制），更正面与首版面是同一张「SA 采用登记面」的题：本票作者顺手立一张 draft（本目录下一个号）「SA 外部资金事实采用与更正的登记面」，要裁的（长在哪只二进制 / 端点、ADR-0085 决定四怎么读、`FactHandoff` 漏装即 panic 的装配纪律）归 SA owner；真实财务系统来源仍 `No-Go / 待参数化`（UC-SA-005、`PAR-INT-05`），不在任何一票。
3. **做法写实**（对照上面「待裁后写实」）：做法 1 → 裁决 1 的落法；做法 2 → 裁决 2 的命令与用例；做法 3 不变。
4. **完成判据写实**：(1) 应用层：v1 已采用 → 更正 v2（回指 v1）→ 版本行 +1、身份行不变、再发一封（信封 ID 带 v2、载荷 `corrects` = v1）；同 v2 重放`已采用`；回指非链头`未受理`；`FindByKey` 交回 v2、`FindVersion(v1)` 仍原样。(2) 真库：新迁移往返；`0004` / `0018` 零 diff；`ListExternalFundsFacts` 一事实一行、聚合不重复。(3) `cmd/parcel-dispatch` 正例改回同一事实两版都在 SA（经 `AdoptFact` + 更正命令或两次 `Save`，作者定）、v1 不再经 CC 写口预铺——sa-cc/13 那格「另用一条事实」的绕法退役，用例内那段注释随之删。(4) `internal/customscompliance/**` 零 diff。(5) 后继 draft 一张已立。
5. **能力边界**：裁的是结构（子表 vs 主键、入口归既有用例、链头口径）；`Save` 两步的事务边界、`ListExternalFundsFacts` 的列形、迁移搬列的细节归作者。读过：本票全文、通道 5 取证条、ADR-0137 决定四与越权风险点 6（经取证引文）、CC `0021` 头注（经引文）；**没读**：`map_external_funds.go` / `external_funds_fact.go` / `funds_application_catalogue.go` 正文、ADR-0085、UC-SA-005 正文。作者量到与代码不符，以代码为准并写进判断项。

## 参照

[02](02-sa-external-funds-fact-adoption-hands-off-an-envelope.md) 裁决 2；[03](03-cc-inbox-consumer-receives-external-funds-fact.md)；[13](13-cc-correction-version-inbound-registration-and-rereconciliation.md) 完成记录判断项 ①；`migrations/customs_compliance/0021_external_funds_fact_versions.sql` 头注（CC 侧取版本子表的理由）。

## 完成记录

（通道 6 · task-173daa46 · 2026-09-15 10:0x–10:4x · 隔离树 `%TEMP%\idp-parcel-mcp6-sacc20`，分支 `mcp6-sacc20` 基远端 main `3a21dab7`；认领笔 `94688ce4`，代码四笔 `fe496cf8`（裁决 1：迁移 + 端口 + 适配器 + 领域一句）/ `c8ead2d0`（裁决 2：更正格）/ `33116348`（判据 (3)：dispatch 正例）/ `63333f4d`（自评两轴各一条：AdoptFact 幂等按版本、注释去跨文件计数），本完成记录 + 后继 draft 27 + spec 两行随末笔。每笔提交后即推 `origin/mcp6-sacc20`。）

**开工重量三条取证（`3a21dab7`）**：`git grep -n -E 'REFERENCES|FOREIGN KEY' -- migrations/settlement_accounting/` 零命中；`git grep -n 'CorrectAmount(' -- internal/settlementaccounting ':!*_test.go'` 只命中定义；`git grep -n -E 'NewMapExternalFundsHandler|MapExternalFundsDeps' -- cmd/ internal/ ':!*_test.go'` 除定义处零命中——三条与通道 5 取证条一致，无不符。

**`/tdd`**：领域一句（`CorrectAmount` 拒更正时刻早于业务发生时刻）先红后绿；更正格（`map_external_funds_correction_test.go` 五个用例）先红（`undefined: application.CorrectFundsFactCommand`）后绿，其中「新版本等于回指」一格首轮答`冲突`而不是`未受理`，把命令自身的形提前到碰库之前才绿。**如实记**：第一片（迁移 + 适配器）的红只是编译期的 `undefined: FindVersion` 与直写 SQL 因表形变化必红，没有在旧表形上单独跑一遍新真库用例看它红——迁移与适配器改动是一体的，拆不出一个「旧库新用例」的中间态。

**逐条对完成判据**：

1. ✓ **应用层**——`TestCorrectingTheChainHeadAdoptsANewVersionThatPointsBackAndHandsOffASecondEnvelope`：v1 采用 → `CorrectFact` v2（回指 v1）→ 替身里版本 +1、身份键不变、意图 2 封且第二封键带 v2、`Record.Fact.Corrects()` = v1（真库那封的 ID 与载荷形由 sa-cc/02 的 `TestACorrectionVersionEnqueuesItsOwnEnvelopeInTheSamePartition` 钉着，本票复用同一交接口未改它）；`FindByKey` 交回 v2、`FindVersion(v1)` 金额原样无回指。`TestReplayingACorrectionAnswersExistingAndADifferentAmountUnderTheSameVersionIsAConflict`：同 v2 重放`已采用`、异金额`冲突`。`TestCorrectingANonHeadVersionOrAnUnadoptedFactIsNotAccepted`：回指非链头`未受理`且 v3 不落、更正未采用事实`未受理`。另有坏命令八格`未受理`、库错`未决`、交接失败留续办引用并重放补交。
2. ✓ **真库**——`TestAFundsFactCorrectionVersionAccruesAsASecondRowAndFindByKeyAnswersTheChainHead`：新迁移往返，身份 1 行 / 版本 2 行直读，`FindByKey` 交 v2、`FindVersion(v1)` 原样、同 v2 重放`已采用`不顶替、`AdoptedFundsFactView` 两版各答各的；`TestExternalFundsFactCatalogueListsOneRowPerFactWithTheChainHead`：两版一行、内容是链头、映射 / 核销聚合只算一遍、余额按链头金额。`git diff --stat 3a21dab7 -- migrations/settlement_accounting/0004_external_funds.sql migrations/settlement_accounting/0018_external_funds_fact_payer.sql` 空。
3. ✓ **dispatch 正例**——`TestAnAdoptedExternalFundsFactReachesTheCustomsRegisterThroughTheRouteTable` 第二格：bank-fact-2 的 v1 / v2 同一事务两次 `facts.Save` 落 SA，两封分两拍经消费者落 CC，`ListFundsFactVersions` 两版断言不变；`register.RegisterFundsFact` 预铺段与「另用一条事实」注释删。带 DSN 该用例 PASS 非 SKIP。
4. ✓ `git diff --stat 3a21dab7 -- internal/customscompliance migrations/customs_compliance` 空。
5. ✓ 后继 draft [27](27-sa-external-funds-fact-adoption-and-correction-registration-face.md) 已立（Status draft，「要裁的」三条归 SA owner，Blocked by 20 进 main）；spec 子票表加 27 一行、20 一行改口。

**逐条对裁决**：

- 裁决 1（形 = 版本子表）：`migrations/settlement_accounting/0021_external_funds_fact_versions.sql` 建 `external_funds_fact_version (tenant_id, fact_id, version)` 外键到身份行，内容列十项照裁决搬进子表、CHECK 与 0004 / 0018 逐条同；身份表 DROP 五道 CHECK 与十列，只留身份 + `recorded_at`（首次采用时刻）+ `inserted_at`，重建只含身份两列的 `scope_not_blank`；存量非零 `RAISE` 交人。`Save` 两步 DO NOTHING、`FundsFactAlreadyAdopted` 落在版本行已在；`FindByKey` 交链头（`NOT EXISTS` 后继）；新增 `FindVersion`；`LoadAdoptedFundsFact` 签名不变、FROM 经共用的 `externalFundsFactSelect` 改子表；`Map` / `Apply` 未动、照旧按 `FindByKey` 读链头；`ListExternalFundsFacts` 一事实一行（链头）、聚合不重复。
- 裁决 2（入口 = 既有用例加更正格）：`CorrectFundsFactCommand{TenantID, Fact, Corrects, Version, AmountMinor, CorrectedAt}`（名字照票面）→ `MapExternalFundsHandler.CorrectFact`；四条前置全在（事实已采用 / `Corrects` 等于链头 / 同（事实、版本）四格 / `amountMinor > 0`），头注写了铸造方与 CC 接收方纪律不同；形成走 `domain.CorrectAmount`；`handOffFact` 复用 `OutboxExternalFundsFactHandoff`，版本与回指从 `Record.Fact` 取。**不做**：CLI / HTTP / inbox 零新面。
- 裁决 3（做法写实）：做法 1 → 0021 + 适配器；做法 2 → `CorrectFact`；做法 3 → 视图只改头注一句（「将来」已到）与 WHERE 列加别名限定，语义不变。
- 裁决 4（判据写实）：见上。

**判断项**（取证与代码不符处、归作者的三格、以及超出票面字面的取舍——每条都可撤回）：

1. **链形三道库约束是本票加的、CC 0021 没有**：回指外键到本表、`UNIQUE (tenant_id, fact_id, corrects)`、`corrects IS NULL` 部分唯一索引。理由写在 0021 头注：本上下文是铸造方（ADR-0137 决定四），链必须线性，裁决 1「链头唯一、不需要标记」这句话在库上要有东西守；CC 作为接收方容忍乱序不校验链（CC 0021 头注），两侧纪律不同、形同理不同。**代价**：直接 `Save` 一个回指不存在版本的记录会撞外键报错——`TestAnExternalFundsFactRoundTripsAndSecondAdoptKeepsTheWinner` 原来那段「bank-fact-2 直接存一条回指 `bank-fact/v1` 的 v2」正是这种写法，已拆去、并入新用例（先 v1 后 v2）。评审若认为三道该只留外键到身份行、与 CC 完全同形，去掉三道约束 + `Save` 头注两句 + `TestTheDatabaseKeepsAFundsFactVersionChainLinear` 一个用例即可，其余零改。
2. **`Save` 两步的事务边界**（裁决 5 归作者）：`RequireExecutor` 没有事务就拒，两步都在调用方事务里，第二步失败第一步随之回滚——没有「身份落了、版本没落」的中间态可见。身份行 `recorded_at` 只在首版落下时写一次（DO NOTHING），就是「首次采用时刻」。
3. **版本行 `ON CONFLICT DO NOTHING` 不写冲突目标**：主键撞、第二个首版、同一前版第二次更正三样都折成`已采用`，编排读回链头作答——与 `AdoptFact` 输掉竞态那一格同形（`TestALostAdoptionRaceHandsOffTheAdoptedVersionOnceNotTheLosers` 在新表形上照旧绿，靠的就是这一条：输家的另一个版本字面撞的是部分唯一索引而不是主键）。顺序到达的同类写入在编排里被「回指必须等于链头」挡下，到不了库。应用层替身 `fundsFactStoreDouble.Save` 同样守这两道，否则那条竞态用例在替身上答`已采用`而在真库上答`已采用`的理由就对不上。
4. **`ListExternalFundsFacts` 的列形**（裁决 5 归作者）：没加版本数列。链头行的 `Version / Corrects / CorrectedAt` 三列已表达它在链上的位置；加列要同笔动 `ports.ExternalFundsFactCatalogueRow` → HTTP 转写 → 管理台类型，读面今天没有页面消费它，留给要它的那张票。前版内容不在列表上，按版本读走 `AdoptedFundsFactView`（头注写明）。
5. **`domain.CorrectAmount` 加了一条前置**：更正时刻不得早于业务发生时刻。`RehydrateExternalFundsFact` 与 0004 / 0021 的 `correction_coupled` CHECK 都已这么要求，形成门放过去的版本会在落库那一步才被拒、编排只能答`未决`；补到形成门后编排答`未受理`。领域改动一行，既有用例全绿（它们用的都是 +1h）。
6. **更正成功复用 `FundsFactAdopted`**，不新增 outcome：更正是采用的一种（裁决 2 原话），调用方从 `Fact().Fact.Corrects()` 分辨这是首版还是更正版；ADR-0029 按恢复动作分格，两者恢复动作同为「无」。
7. **`correctDigest` 只算回指 / 新版本 / 金额 / 更正时刻**，与 `adoptDigest` 元素刻意不同：同一（事实、版本）若先经 `AdoptFact` 作首版落下、再有人拿它当更正版本提，两份摘要必不相等，答`冲突`而不是把首版当成更正的重放。
8. **`CorrectFact` 先 `FindVersion` 再 `FindByKey`**：重放同一次更正时链头已经是新版本本身，先查链头会把正当重放判成「回指非链头」——头注写了。
9. **dispatch 正例用两次 `facts.Save` 而不是 `AdoptFact` + `CorrectFact`**（票面「作者定」）：该用例钉的是路由表与 CC 消费者那一侧，SA 是夹具；装 handler 要连带装 Mappings / Applications / Downstream 三个它用不到的口。**由此留一格**：handler × 真库 × 真 Outbox 的组合今天没有一条真库用例（应用层用替身，适配器各自真库）——它属 27 的「真库装配用例一正一反」，随装配点一起立。
10. **`migrations.go` / `plan.go` 零改**：`all:settlement_accounting` 目录已嵌入、`SettlementAccounting()` 读目录全部文件、`plan.go` 按模块函数接线——四件同笔在这里只剩 SQL 一件。
11. **`AdoptedFundsFactView` 头注改口**：原句「库里将来一事实多行……这一句照样只答被问的那一版」的「将来」已到，改成「0021 起」。WHERE 三列加 `v.` 限定是因为共用 SELECT 段带了别名，语义零变。
12. **清点预报**（推送方在 tip 上重生成兑底）：迁移 +1（`settlement_accounting/0021`）、端口接口方法 +1（`ExternalFundsFactStore.FindVersion`）、生产 `.go` 文件 +0（`external_funds_fact.go` 加方法、`map_external_funds.go` 加命令与方法）、测试文件 +1（`map_external_funds_correction_test.go`）、`.md` +1（票 27）。
13. **`AdoptFact` 的幂等查法随之改为先按（事实、版本）`FindVersion`、再按身份 `FindByKey`**（第四笔）：自评 Spec 轴量到——更正 v2 落下之后链头是 v2，首版 v1 的采用命令重放若照旧拿 `FindByKey` 的链头摘要去比，会答一个不存在的`冲突`；裁决 2 写的幂等单位是（事实、版本），首版也该照它。事实已有链而命令的版本字面不在链上 → `冲突`（一条事实一个首版），既有四格语义不变；`TestReplayingTheFirstVersionAfterACorrectionStillAnswersExisting` 先红后绿，既有 `AdoptFact` 三条用例照旧绿。

**自评两轴**（`/implement` Step 3 的 `/code-review`：两只隔离子代理都以「Authentication error」死在第一步——与通道 1 此前撞到的同一格；改由作者串行自跑两轴，**不代替非作者评审**）：**Standards**——发现 1 条已修：`external_funds_fact.go` `FindByKey` / `Save` 头注、`ports.go` `FundsFactAlreadyAdopted` 注、应用层替身两处注释数了 0021 里的约束（「三道」「两道」），按 AGENTS「改文档」换成点名，不留数；其余无发现（注释全中文、引用全符号名、夹具全合成、`0004` / `0018` / CC 零 diff、新 SQL `i/lf`）。**Spec**——发现 1 条已修（判断项 13）；裁决 1 / 2 与判据 (1)–(5) 逐条对过，无缺项；范围外的东西只有判断项 1 / 5 两处，各自写了理由与撤法。

**验证**（隔离树，Windows 本机，DSN 指 55432；占 / 释各广播两轮）：`gofmt -l ./internal ./cmd ./migrations ./tools` 空；`go build ./...` / `go vet ./...` 全仓 0；带 DSN `-p 1 -count=1 -v`（钉 `33116348`）：`cmd/parcel-dispatch` + `cmd/parcel-api` + `./internal/architecture/...` + `./internal/settlementaccounting/...` + `./migrations/` → **1071 PASS / 0 FAIL / 0 SKIP**（24 s）；其中 SA `adapters/postgres` 单包 145 PASS / 0 SKIP。第四笔 `63333f4d` 只改应用层与注释，其上 SA application / domain 不带 DSN ok、全仓 build / vet 0；真库那批未重跑（它不经 handler）。`git ls-files --eol` 新 SQL `i/lf w/lf`，`migrations` 行尾门 ok。**没跑**：全仓（推送方重放时带 DSN 跑一次，parallel-sessions「唯一一跑」）；`-race`（本机无 cgo）。

## Comments

- 2026-09-14 14:0x · 通道 1：立票（sa-cc/13 实施中发现）。只写票面，未动代码。
- **2026-09-14 21:15 · 取证 ← 通道 5 · 钉 `bccb60a1`**（task-a41fb596；只读代码、未跑真库、未动任何代码；只报事实，不裁、不提方案。每条写「命令 / 文件 + 符号名 → 结果」，引用一律符号名或引文，不写行号。）

  **要裁的 1（主键加版本 还是 版本子表）**

  - `git grep -n 'external_funds_fact' -- migrations/settlement_accounting/` → 只命中 `0004_external_funds.sql`（建表与各 CONSTRAINT）与 `0018_external_funds_fact_payer.sql`（`ADD COLUMN payer_ref` + `external_funds_fact_payer_not_blank`）；其余 SA 迁移不碰这张表。`git grep -n -E 'REFERENCES|FOREIGN KEY' -- migrations/settlement_accounting/` → **零命中**：SA 全部迁移里没有任何一张表外键到 `external_funds_fact`，也没有任何外键。`funds_mapping.fact_id`、`settlement_application.fact_id`（`0004`）与 `duty_payment_verification_adoption.funds_ref`（`0020`）都是裸 `text NOT NULL`，只有 `btrim(...) <> ''` 的 CHECK，无 REFERENCES。
  - `0004` 主键原文：`CONSTRAINT external_funds_fact_pkey PRIMARY KEY (tenant_id, fact_id)`；头注原句「external_funds_fact 主键取（租户+事实引用）：同一事实只采用一次。更正两半同在或同缺；更正版本不得等于当前版本」。`version text NOT NULL`、`corrects text`、`corrected_at timestamptz` 是同一行上的列；`external_funds_fact_correction_coupled` 要 `corrects <> version` 且 `corrected_at >= occurred_at`。除主键外 `0004` 没有为这张表建索引。
  - `internal/settlementaccounting/ports/ports.go` `FundsFactKey`：`{TenantID domain.TenantID; Fact domain.FundsFactReference}`，头注「外部资金事实采用的幂等键……同一事实引用只采用一次」——**不带版本**。`ExternalFundsFactStore` 方法集：`FindByKey(ctx, FundsFactKey) (FundsFactRecord, bool, error)`、`Save(ctx, FundsFactRecord) (FundsFactSaveOutcome, error)`；无 Replace、无按版本取的方法。`FundsFactRecord{Key, ContentDigest, Fact domain.ExternalFundsFact, RecordedAt}`——版本藏在 `Fact.Version()` 里，不在键上。
  - `git grep -n FundsFactKey -- internal/settlementaccounting cmd/` → 用方：`adapters/postgres/external_funds_fact.go`（`ExternalFundsFacts.FindByKey`、`scanExternalFundsFact`）、`adapters/postgres/adopted_funds_fact_view.go`（`LoadAdoptedFundsFact` 内拼键交 `scanExternalFundsFact`）、`adapters/postgres/external_funds_fact_handoff.go`（`externalFundsFactPartitionKey`，分区键不含版本）、`application/map_external_funds.go`（`AdoptFact`、`Map`、`Apply` 各按（租户 + 事实）`FindByKey`）、`cmd/parcel-dispatch/assemble_test.go`（直接 `facts.Save`）、`adapters/postgres/external_funds_test.go`、`application/map_external_funds_test.go`、`application/map_external_funds_handoff_test.go`。
  - `AdoptedFundsFactView.LoadAdoptedFundsFact(ctx, tenant, fact, version) (domain.ExternalFundsFact, bool, error)`：SQL 为 `externalFundsFactSelect` + `WHERE tenant_id = $1 AND fact_id = $2 AND version = $3`——**版本在 WHERE 里**；结构头注原句「版本进 WHERE 而不是读回再比——库里将来一事实多行（更正版本各成一行）时，这一句照样只答被问的那一版」。今天一事实一行，它「按版本查」实际成立的形是：同事实、版本字面不等 → 无行 → `found=false`（`TestTheAdoptedFundsFactViewAnswersOnlyTheVersionTheEnvelopePointsAt`；dispatch 正例里 `bank-fact/v9` 落 `dispatch.consumer_undecided` 钉的是同一格）。`ExternalFundsFacts.FindByKey` 的 SQL 只有 `WHERE tenant_id = $1 AND fact_id = $2`、用 `QueryRow`——一事实多行时返回哪一行由库决定。`FindByKey` 与 `LoadAdoptedFundsFact` 共用 `externalFundsFactSelect` 与 `scanExternalFundsFact`。
  - `ExternalFundsFacts.Save`：单条 `INSERT INTO settlement_accounting.external_funds_fact (tenant_id, fact_id, source_ref, payer_ref, kind, currency, amount_minor, version, occurred_at, corrects, corrected_at, content_digest, recorded_at) … ON CONFLICT DO NOTHING`，`RowsAffected() == 0 → FundsFactAlreadyAdopted`。版本、回指、更正时刻都作为这一行的列写入（`record.Fact.Version()` / `.Corrects()` / `.CorrectedAt()`）。
  - `internal/settlementaccounting/domain/external_funds.go` `ExternalFundsFact` 字段集：`fact, source, payer, kind, currency, amountMinor, version, occurredAt, corrects, correctedAt`——`version` 与 `corrects` 都在。`AdoptExternalFundsFact(spec)` 不接回指（`ExternalFundsFactSpec` 无 `Corrects` 字段）。`CorrectAmount(amountMinor, version, correctedAt)` 前置：接收者版本有效、`amountMinor > 0`、新 `version` 有效、`correctedAt` 非零、`version != fact.version`；产出：接收者的值拷贝，改 `amountMinor`，`corrects = fact.version`，`version = 新版本`，`correctedAt`——头注「保留原版本（值语义），新版本回指前身；差额与核销的重算随新有效版本另行进行（AT-SA-114）」。`domain/funds_rehydration.go` `RehydrateExternalFundsFact` 是读回带回指行的唯一造法，头注「Adopt 造不出带更正回指的版本——更正是转换门，读回不重放 CorrectAmount」。
  - `git grep -n 'CorrectAmount' -- internal/settlementaccounting cmd/` → 定义一处；调用全在测试：`domain/external_funds_test.go`（`TestAnAdoptedFundsFactIsAReferenceNotABalance` 子测 "an amount correction forms a new version keeping the original"；`TestAFundsFactCarriesTheSourceProvidedPayerOnlyWhenGiven`）、`adapters/postgres/external_funds_fact_handoff_test.go`（`TestACorrectionVersionEnqueuesItsOwnEnvelopeInTheSamePartition`）、`cmd/parcel-dispatch/assemble_test.go`（`TestAnAdoptedExternalFundsFactReachesTheCustomsRegisterThroughTheRouteTable` 第二格）。`internal/settlementaccounting/application` 下零命中——与票面「无生产调用点」一致。
  - CC `0021` 头注原句：「形取**版本子表**而不是主键加版本：0016 的 duty_payment_verification 外键钉在 external_funds_fact (tenant_id, fact_ref) 上——『没有接收的资金事实就没有核对』是事实身份层面的话，版本是事实的历史；主键加版本会拆掉那道外键，子表让身份行不动、版本各占一行，外键原样成立。」那道外键是 CC `0016` 的 `CONSTRAINT duty_payment_verification_funds_fact_received FOREIGN KEY (tenant_id, funds_ref) REFERENCES customs_compliance.external_funds_fact (tenant_id, fact_ref)`。对照 SA：**没有任何表外键到 `settlement_accounting.external_funds_fact (tenant_id, fact_id)`**（首条零命中）；SA 里引用 `fact_id` 的 `funds_mapping` / `settlement_application` 与引用 `funds_ref` 的 `duty_payment_verification_adoption` 全是裸文本列。`0021` 头注那句所依赖的前提（有外键钉在身份行）在 SA 迁移里不存在——只列事实。另：`0021` 把内容列从身份表 `DROP COLUMN` 搬进子表，并以 `DO $$ … RAISE EXCEPTION` 守存量；SA 侧存量状态见 `0018` 头注「存量为零（本上下文的采用今天没有生产入口），不回填」与 `map_external_funds.go` `adoptDigest` 头注「0018 之前本上下文的采用没有生产入口、存量为零」。
  - 还读 `external_funds_fact` 内容列的非测试 SQL：`git grep -n 'settlement_accounting.external_funds_fact' -- internal/ cmd/ ':!*_test.go'` → `external_funds_fact.go`（`externalFundsFactSelect`、`Save` 的 INSERT）与 `funds_application_catalogue.go` `FundsApplicationCatalogue.ListExternalFundsFacts`（`SELECT f.fact_id, …, f.version, f.occurred_at, f.corrects, f.corrected_at … FROM settlement_accounting.external_funds_fact AS f`，按 `f.fact_id` 关联 `funds_mapping` / `settlement_application` 聚合，`ORDER BY f.fact_id`）；后者经 `cmd/parcel-api/endpoints.go` 的 `/settlement-funds-applications`（`settlementhttp.NewQuerySettlementFundsApplicationsEndpoint`，`main.go` 装 `sapostgres.NewFundsApplicationCatalogue`）在线。

  **要裁的 2（「采用更正版本」的入口长在哪）**

  - 首版今天从哪里进 SA：`git grep -n -E 'ExternalFundsFacts\b|AdoptExternalFundsFact|FundsFactAlreadyAdopted' -- internal/settlementaccounting/application cmd/ internal/settlementaccounting/adapters` → 非测试命中只有 SA 自己的 `application/map_external_funds.go`（`AdoptFact` / `factFrom` 调 `domain.AdoptExternalFundsFact`）与 `adapters/postgres/external_funds_fact.go`；`cmd/` 下非测试**零命中**。再核 `git grep -n -E 'NewMapExternalFundsHandler|MapExternalFundsDeps|NewExternalFundsFacts\(|NewOutboxExternalFundsFactHandoff\(' -- cmd/ internal/ ':!*_test.go'`（排除 SA application / postgres 自身）→ **零命中**：`MapExternalFundsHandler` 今天没有生产构造点。逐类入口：**inbox 消费者**——`internal/settlementaccounting/adapters/inbox/` 只有 `buy_evaluation_recorded_consumer.go`（听 `parcel-pricing.evaluation.recorded`）与 `duty_payment_verification_consumer.go`（听 `customs-compliance.duty-payment-verification.formed`），无资金事实来源事件；**HTTP**——`internal/settlementaccounting/adapters/http/` 只有 `query_*` 与 intake 文件，`git grep MethodPost` 零命中，`cmd/parcel-api/endpoints.go` 装的 SA 端点全是 `settlementhttp.NewQuery*`；**CLI**——`cmd/` 目录下无 settlement 二进制。事实：首版今天**没有生产入口**，只有测试直写——`cmd/parcel-dispatch/assemble_test.go` 用 `sapostgres.NewExternalFundsFacts(db).Save` 直接落行，SA 各测试同。与票 02 完成记录「`cmd/parcel-api` 装配处经核不存在」、`0018` 头注「本上下文的采用今天没有生产入口」同向。
  - ADR-0137 决定四原文：「**外部资金事实进入本产品只有 `settlement-accounting` 采用这一口；`customs-compliance` 不开第二个铸造或补录入口，人工核实与更正也先登在 SA、再经采用信封到 CC。** SA 采用（`UC-SA-005` / `map_external_funds.go` 的四格）是事实进产品的唯一入口，UC-SA-001『同一外部资金事实通过回调、文件或人工核实重复到达，只能被采用一次』已把人工核实算作同一口的一种到达方式……因此票 07 步一的子命令是三个（凭证 / 协作 / 付款核对），不含 `external-funds-fact`」。去掉的那一口：同 ADR Context 节「07 步一原拟给 `cmd/parcel-customs-register` 加 `external-funds-fact` 子命令作运维补录口」——CC 侧的人工补录 CLI 子命令。保留的铸造路径：SA `map_external_funds.go` 的采用。同 ADR 越权风险点 6 原句：「决定四请 SA owner 复核『单一采用口』……若 SA owner 认为运维补录该有一条不经采用四格的旁路（比如 SA 采用口不可用时的应急），那是 SA 的一格，不是 CC 的入口」。SA CONTEXT Boundaries 落地句：「人工核实与更正也先在本上下文采用、再传出」。UC-SA-001 原句：「同一外部资金事实通过回调、文件或人工核实重复到达，只能被采用一次；更正必须形成新来源版本。」
  - 那条铸造路径今天有没有「更正」一格：`AdoptFundsFactCommand` 字段 `TenantID, Fact, Source, Payer, Kind, Currency, AmountMinor, Version, OccurredAt`——**无 `Corrects` / `CorrectedAt`**；`factFrom` 只能造首版（走 `AdoptExternalFundsFact`）。`AdoptFact` 的格：`FundsFactAdopted`（新行 + `handOffFact`）、`FundsFactExisting`（同键同摘要，`existingFact` 再交同一份）、`FundsFactConflict`（同键异摘要；注释原句「同一事实引用携带不同金额或时间：冲突保留原引用——外部更正走版本链，不在采用处顶替。冲突的那一份没有被采用，无物可交」）、`FundsUndecided` / `FundsFactStoreUnavailable`、`FundsNotAccepted`。`adoptDigest` 进摘要的元素：`Source, Payer, Kind, Currency, AmountMinor, Version, OccurredAt`（`Version` 在内）。所以同事实、新版本、新金额的命令今天走到 `FindByKey` 命中首版 → 摘要不等 → `FundsFactConflict`。**没有「更正」格。**
  - 票 02 裁决 2 原文：「**2 → 同一事件类型带回指。** ……载荷带回指（原事实引用）由消费方按引用读 SA 只读口取事实内容与更正关系。」完成记录写实的信封形：事件类型 `settlement-accounting.external-funds-fact.adopted`、ID `<租户>/funds-fact/<事实>/<版本>`、分区键 `<租户>/funds-fact/<事实>`、载荷 `tenantId / fact / version / corrects`（`corrects` 带 `omitempty`，可缺席）。发信符号：`adapters/postgres/external_funds_fact_handoff.go` `OutboxExternalFundsFactHandoff.HandOffExternalFundsFact(ctx, ports.ExternalFundsFactIntent)`；`ExternalFundsFactIntent{Record FundsFactRecord}`——参数里**没有独立的版本参数**，版本与回指从 `intent.Record.Fact.Version()` / `.Corrects()` 取，ID 拼 `externalFundsFactPartitionKey(key) + "/" + version`；键空或版本空 → 报错不入队。更正版信封的真库用例已在：`TestACorrectionVersionEnqueuesItsOwnEnvelopeInTheSamePartition`（v2 同型同区、`corrects` = 前版）——它用 `CorrectAmount` 造 v2 后直接调 handoff，不经 `AdoptFact`。票 02 判断题 ⑥（评审已同意）原句：「更正版本今天只有适配器直接入队的路径……SA 侧『更正 / 撤销追加映射更正』（UC-SA-005）的编排入口不在本票，届时复用同一交接口、载荷自带回指，无需再动适配器」。
  - `cmd/parcel-dispatch/assemble_test.go` 那一格：用例 `TestAnAdoptedExternalFundsFactReachesTheCustomsRegisterThroughTheRouteTable`，函数内注释原句「SA 今天一事实一行（settlement_accounting 0004 的主键是 (tenant_id, fact_id)），同一库上留不下 v1 与 v2 两行，所以这里另用一条事实：v1 直接经 CC 写口预铺，SA 只存 v2 那一版」。绕的那一步：`bank-fact-2` 的 v1 用 CC 的 `ccpostgres.DutyPaymentReconciliation.RegisterFundsFact(txCtx, tenant, ccports.ExternalFundsFactRegistration{…})` 直接写进 CC 登记册（不经 SA、不经信封）；SA 侧只 `facts.Save` 了 `original.CorrectAmount(9000, "bank-fact-2/v2", …)` 造出的 v2；再手工 `enqueueForBeat` 一封带 `"corrects":"bank-fact-2/v1"` 的信封。第一格（`bank-fact-1` v1）全程走 SA 直写 → 信封 → CC 消费者。

  **两路各自的实测后果**（只列要动的符号，不选）

  - **主键加版本**（`(tenant_id, fact_id)` → `(tenant_id, fact_id, version)`）：新迁移 `ALTER TABLE … DROP CONSTRAINT external_funds_fact_pkey, ADD PRIMARY KEY (…)`（`0004` 不改；SA 无外键指向它，迁移层面无外键要先拆）。代码：`ports.FundsFactKey`（加版本或另立带版本的键型）→ `ExternalFundsFactStore.FindByKey/Save` 签名或语义 → `ExternalFundsFacts.FindByKey`（现 SQL 不带 version、`QueryRow` 在多行上不确定）与 `Save`（`ON CONFLICT DO NOTHING` 的冲突键随主键变，同事实新版本从「已采用」变成可插入）→ `MapExternalFundsHandler.AdoptFact`（`FundsFactExisting` / `FundsFactConflict` 的判定今天靠 `FindByKey` 按（租户 + 事实）命中首版；多版后「同事实不同版本」不再命中同一行）、`Map` / `Apply`（都按（租户 + 事实）`FindByKey` 取事实内容给 `MapFundsToTarget` / `ApplySettlement`，`ApplySettlement` 用 `fact.amountMinor` 做守恒——多版后「取哪一版」今天在代码里没有答案）→ `FundsApplicationCatalogue.ListExternalFundsFacts`（`FROM external_funds_fact AS f` 一事实多行后，按 `f.fact_id` 关联的映射 / 核销聚合逐版本行重复，`ORDER BY f.fact_id` 无版本次序键）→ 测试：`adapters/postgres/external_funds_test.go`、`application/map_external_funds_test.go`、`adapters/postgres/catalogue_read_test.go`、`cmd/parcel-dispatch/assemble_test.go`。不动：`AdoptedFundsFactView`（WHERE 已带 version）、`externalFundsFactPartitionKey`（不含版本）、`OutboxExternalFundsFactHandoff`、`domain.ExternalFundsFact` / `CorrectAmount` / `RehydrateExternalFundsFact`。
  - **版本子表**（照 CC `0021`：身份行 `(tenant_id, fact_id)` 不动，新表 `(tenant_id, fact_id, version)` 外键到身份行）：新迁移建子表；`0004` 身份表上的内容列（`source_ref, payer_ref, kind, currency, amount_minor, version, occurred_at, corrects, corrected_at, content_digest, recorded_at`）去留是这一路自己的题（CC `0021` 选了 `DROP COLUMN` 搬走并 `RAISE EXCEPTION` 守存量；SA 存量事实见上）。代码：`ExternalFundsFacts.Save`（一次 INSERT 变身份 + 版本两步；`FundsFactAlreadyAdopted` 的语义要在「身份已在」与「版本已在」之间落定）→ `FindByKey`（键不带版本时读子表哪一行）→ `externalFundsFactSelect` / `scanExternalFundsFact`（FROM 改子表或 JOIN）→ `AdoptedFundsFactView.LoadAdoptedFundsFact`（签名不变，SQL 的 FROM 改子表）→ `FundsApplicationCatalogue.ListExternalFundsFacts`（`f.version / f.corrects / f.corrected_at / f.amount_minor` 等现读身份表列）→ `MapExternalFundsHandler.AdoptFact/Map/Apply` 与主键加版本同一道「取哪一版」的题 → 测试同上。`ports.FundsFactKey` 可保持为身份键；CC 侧版本行的形是 `ccports.ExternalFundsFactRegistration{Fact, Version, Corrects, Source, Payer, Currency, AmountMinor, OccurredAt}`。不动：分区键、handoff、domain。
  - **入口三选各自要新建什么**（共同的底：`NewMapExternalFundsHandler(MapExternalFundsDeps{Facts, Mappings, Applications, Downstream, FactHandoff, Clock})` 今天零生产装配，任一选都要新建这一处装配与 `FactHandoff` 的 Outbox 适配器装配；命令层今天没有「更正」格，任一选都要新建带回指的命令 / 用例给 `CorrectAmount` 当调用方——票 02 评审 Standards 非阻断 (1) 记过 `FactHandoff` 漏装即 panic，装配时同笔）：
    - **CLI**：`cmd/` 下无 settlement 二进制，要新建一只（既有登记类先例：`parcel-customs-register`、`parcel-pricing-register`、`parcel-network-register` 等）或在既有二进制加子命令；连 DSN、装 `MapExternalFundsHandler`。
    - **HTTP**：SA `adapters/http` 今天零写面（全 `query_*`），要新建 SA 第一个写端点 + `cmd/parcel-api/endpoints.go` 表项；ADR-0137 Links 把 ADR-0085 决定四列为「票 07 要裁的 1 的判据」、Consequences 写「步二端点 + 管理台照 ADR-0085 决定四做齐」（本次未读 ADR-0085 正文）。
    - **只有 inbox 消费者**：SA `adapters/inbox` 今天只听 `parcel-pricing.evaluation.recorded` 与 `customs-compliance.duty-payment-verification.formed`，仓内没有任何银行 / 支付 / 财务系统的入向事件类型；要新建一种来源事件类型、消费者与 `cmd/parcel-dispatch` 路由表项——而来源系统在 UC-SA-005 状态行是「真实财务系统交换边界、事实身份、账户、币种、付款指示和撤销/更正语义待提供，生产分支保持 `No-Go / 待参数化`」（`PAR-INT-05`「唯一财务系统、事实层、身份、撤销/更正和税务映射」）；UC-SA-005 输入表「外部资金事实」行写「不复制为本地付款事实，不因接收回调自动采用」。

  **能力边界**：只读 `bccb60a1` 隔离树（`%TEMP%\idp-parcel-mcp5-sacc20`），未跑真库、未跑 `go test` / `go build`、未占 55432；未读 `ExternalFundsFacts` / `AdoptedFundsFactView` / `FundsApplicationCatalogue` 以外的 SA postgres 适配器实现体，未读 CC `reconcile_duty_payment.go` 实现体，未读 ADR-0085 正文；grep 结果以上列命令在 `bccb60a1` 复跑为准。
- **2026-09-15 11:4x · 评审 ← 通道 3 · 钉 `63333f4d`（检出 `2da94586`）· 基线 `3a21dab7`**（task-a9e4e4d8，只读隔离检出 `%TEMP%\idp-review-sacc20`，merge-base = `3a21dab7`；五笔代码 + 一笔簿记，14 件 +1064 −108；由推送方代落）。**Standards——阻断：无。非阻断**：① `adapters/postgres/external_funds_fact.go` `Save` 头注 + `ports.go` `FundsFactAlreadyAdopted` 注：版本行 DO NOTHING 不写冲突目标，「回指非链头」顺序到达答`未受理`、并发到达（链形 UNIQUE 撞上）答`已采用`带赢家版本——同一业务事实两张脸（parallel-sessions「不同的绿长同一张脸」）。结构上可分：返回记录 `Fact().Fact.Version()` ≠ 命令版本，且与既有 `AdoptFact` 输竞态格同形，可留；但头注应把「顺序`未受理` / 并发`已采用`」这条不对称写明（现只写「两种都按当前链头作答」）。② `0021` 头注「写成三道约束」「三道合起来」——数的是本文件自己的约束，不是别处，AGENTS「不数别处」不触，可留；提一句是因为作者自评已把 Go 注释里同一组计数换点名，SQL 这一处与之不一致。③ `CorrectFundsFactCommand.Fact/Corrects/Version` 用 string（基线 smell Primitive Obsession）——与既有 `AdoptFundsFactCommand` 同形，仓惯例覆盖，不计。**无发现**：注释全中文；跨文件引用全符号名 / 票号 / 迁移号，无行号；`0004` / `0018` / `internal/customscompliance` / `migrations/customs_compliance` 零 diff（自跑 `git diff --stat` 核）；`0021` 子表 CHECK 与 0004 / 0018 逐条同（金额正 / 种类封闭 / 更正两半同在同缺 / 付款人 NULL 允空白拒）；存量非零 RAISE 交人，无回填编造；实例半边零预填；夹具合成；`go vet` SA 全包 + `cmd/parcel-dispatch` 0。**Spec——阻断：无。非阻断**：① 判断项 ① 三道链形库约束超出裁决 1 字面——**判为裁决 1「链头唯一、不需要标记」的结构推论，不是新规则**：`FindByKey` 用 `QueryRow` + `NOT EXISTS successor`，两次 `CorrectFact` 并发回指同一链头时，若无 `UNIQUE(tenant,fact,corrects)` 与 `corrects IS NULL` 部分唯一索引就会落两个链头、`QueryRow` 取哪行由库定，裁决 1 那句在并发下失守；红线「只实现已确认规则」管的是业务参数，不是让已裁不变式在库上成立的完整性约束。**留，不必撤**。② 判据 (3) 两次 `facts.Save` 而非 handler——**可接受**（用例钉路由表与 CC 消费者，SA 是夹具），但 `CorrectFact` × 真库 × 真 Outbox 今天零用例（应用层替身、适配器 handoff 用例直调 `CorrectAmount`）；与 27 同归，票面已记（判断项 ⑨），不另立。③ 判断项 ⑤ `CorrectAmount` 加「更正时刻不早于发生时刻」——0004 `correction_coupled` CHECK 与 `RehydrateExternalFundsFact` 已要求，形成门对齐既有已确认不变量，不是新规则；留。**无发现（逐条）**：裁决 1——子表键（租户、事实、版本）外键到身份行、十项内容列搬进子表、身份表只留身份 + recorded_at（首次采用）、`Save` 两步 DO NOTHING、`FindByKey` 链头、`FindVersion` 新增、`LoadAdoptedFundsFact` 签名不变 FROM 改子表、`ListExternalFundsFacts` 一事实一行（NOT EXISTS）✓；裁决 2——`CorrectFundsFactCommand` 字段照票面，四前置全在（`FindByKey` found / `head.Fact.Version()==corrects` / `FindVersion`+`correctDigest` 四格 / `AmountMinor>0`），走 `domain.CorrectAmount`，`handOffFact` 复用，零新面 ✓；判据 (1)(2)(4)(5) ✓；27 三条要裁的全归 SA owner、列选项不预拟 ✓。**专看十条**：1 结构推论非新规则，留；2 同形可分（版本字面），接受，头注补不对称一句；3 对齐既有不变量，不越权；4 对：更正是采用的一种（裁决 2 原话），消费方经信封 `corrects`、调用方经 `Fact().Fact.Corrects()` 可分，ADR-0029 恢复动作同为无；5 四格不变——同版本同摘要`已采用` / 同版本异摘要`冲突` / 事实有链而版本不在链上`冲突` / 新事实`采用`；唯一变化是更正后重放首版从错误的`冲突`变`已采用`，`TestReplayingTheFirstVersionAfterACorrectionStillAnswersExisting` 钉之；代价多一次读，可接受；6 四前置全在、顺序（先 FindVersion 后 FindByKey）有理；`handOffFact` 复用 ✓；7 迁移如裁决，存量 RAISE，0004/0018 零 diff，`ListExternalFundsFacts` 一事实一行 ✓；8 可接受，缺口归 27；9 CC 零 diff ✓；10 27 三条归 SA owner，无预拟 ✓。**13 判断项**：① 同意（结构推论，留）；② 同意；③ 同意但记不对称（头注补一句）；④ 同意；⑤ 同意；⑥ 同意；⑦ 同意；⑧ 同意；⑨ 同意（缺口归 27）；⑩ 同意；⑪ 同意；⑫ 同意（推送方兑底）；⑬ 同意。**汇总**：Standards 0 阻断 / 2 非阻断（最重 ① 并发与顺序同事不同答的头注补句）；Spec 0 阻断 / 3 非阻断（最重 ② CorrectFact × 真库 × Outbox 零用例，归 27）。代码可进 main。**跑了**：`go vet` SA + `cmd/parcel-dispatch` 0；`go test -count=1` SA application + domain ok（无 DSN）；`git diff --stat` 核 CC / 0004 / 0018 零 diff。**没跑**：真库（凭作者 1071 PASS）；未逐行读三份测试文件全部新断言；未读 ADR-0085。未改检出一行。
- **2026-09-15 11:5x · 进 main 记录 · 通道 1 推送方**：评审非阻断**处置**——Standards ①（`Save` 头注补「顺序`未受理` / 并发`已采用`」不对称一句）与 pp-seams/01 评审的两条注释尾巴合一张 A 类零行为候选票，不挡合入；Standards ②③、Spec ①③ 接受为残差记于此；Spec ②（`CorrectFact` × 真库 × Outbox 零用例）随 draft 27 的装配用例。**重放**：隔离检出 `%TEMP%\idp-replay-wave1`（首波第四票，在 pp-seams/02 → sa-cc/22 → pp-seams/01 之上），`cherry-pick 94688ce4 fe496cf8 c8ead2d0 33116348 63333f4d 2da94586` → `fcdbc86e` / `8f4121ad` / `6a85d4f4` / `6dc8d247` / `14d86a61` / `f25d262a`；sa-cc `spec.md` 撞一次（20 行与 21 / 22 行相邻各改），按意图解：20 行取本分支、21 / 22 行取树上、27 行新增照收；`cmd/parcel-dispatch/assemble_test.go` 与 22 那一行不同用例、自动合；`git diff 2da94586 f25d262a -- internal/settlementaccounting migrations/settlement_accounting cmd/parcel-dispatch issues/20-* issues/27-*` 只差 22 那一行。**验证（推送方全量一次，批 tip `c657a5e7`）**：`gofmt -l` 空、build / vet 0；带 DSN `go test -p 1 -count=1 ./...` **113 ok / 0 FAIL / 16 无测试 / 0 cached**（131 s）；探针 SA application `-run Correct` ok。清点在批 tip 重生成（迁移 settlement_accounting 20→21、SA 测试文件 +1）。共享 main `merge --ff-only c657a5e7` → 簿记一笔在其上 → `ls-remote` 核 `bd919b72` 未动 → `push <sha>:main`。`mcp6-sacc20` → `merged/`、远端删；作者树由通道 6 比内容后拆。
