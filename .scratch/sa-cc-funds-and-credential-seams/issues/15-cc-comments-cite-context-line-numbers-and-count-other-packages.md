# CC 注释里的「硬句 NNN」是 CONTEXT 行号、已随 ADR-0137 插句错位；`parcel-customs-register` 头注数着 `application` 包的格数——一笔改口成引文与点名，零行为

Category: chore
Status: resolved——2026-09-11 19:5x **补笔二进 main**（通道 1 推送方：通道 5 wip `0b4da637` 由原通道 1 19:00 收成 + 收尾成正式补笔 **`da52715a`**，基 main `8557afd7` 纯 ff 不换号；Spec 重核 ← 通道 1 推送方：新增引文段 74 处 71 处逐字命中、余 3 处为基线已有的票面裁决转述未改字、零非注释行、零地盘外文件；带 DSN 全量 110 ok / 0 FAIL；`mcp5-sacc15` → `merged/`、两树拆，见 Comments）；此前 resolved——2026-09-11 18:4x **进 main**（通道 1 推送方重放到 sa-cc/06 之上：代码 `747c8870` / `9e90e88b` / `caad3e20` / `3098cf61` / 补笔 `839e76dc`，票面笔 `f061b834` / `54abb795` / `7f45c45b` / `af40abf7`；补笔 Spec 重核 ← 通道 1 推送方：46 处引文全部逐字命中、零非注释行、零地盘外文件；**wip `0b4da637` 不进 main**，留在分支 `mcp5-sacc15` 归通道 5 收成正式补笔，见 Comments）；此前 resolved——2026-09-11 16:5x 通道 5 完工（task-0fb34872），分支 `mcp5-sacc15` 基 `620f7fed`，四笔代码 `459cf68e` / `d4616677` / `66d0ac75` / `c4e9adf5`（末笔是 sa-cc/06 撞点五件，见完成记录），等推送方重放进 main（与 sa-cc/06 谁后进 main 谁 rebase）。此前 in-progress——2026-09-11 16:36 通道 5 认领（task-0fb34872；分支 `mcp5-sacc15` 基 `620f7fed`，隔离树 `D:/tops/idp-parcel-mcp5-sacc15`）。此前 ready-for-agent——2026-09-11 14:2x 通道 1 推送方立票并直接转 ready（要裁的为零；收下 sa-cc/10 非作者评审 Standards ① 与 sa-cc/07 步一评审 Standards ① 两条非阻断，扩到同族全部）；取证锚 main `0b027ab8`
Blocked by: 无（硬）。**软阻**：第五波 sa-cc/04（通道 4）/ 05（通道 5）/ 07 步二（通道 2）都在 `internal/customscompliance/**` 与 `cmd/parcel-customs-register/` 动手，本票是同目录大面积注释改动，先于它们进 main 会让三条分支 rebase 时逐文件解注释冲突——**等这三票进 main 后再开工**，推送方广播后转派——**05 已 15:1x 进 main、07 步二已 16:0x 进 main、04 已 16:3x 进 main（2026-09-11，通道 1 推送方记）——软阻解除，可派**；开工时同波 sa-cc/06（通道 4）在 CC `application` / `ports` 加新代码，只注释的本票与它谁后进 main 谁 rebase；07 步二评审又点了两处同族（`adapters/http/register_credential_and_duty.go` 头注「同一套八格」与 `writeDutyReconciliationAnswer` 头注「资金事实那三格」、`apps/admin-web/src/pages/customs/api.ts` `dutyRegistrationOutcomeLabels` 注释「资金事实三格」），随本票一并收。05 评审 Spec ② 与 07 步二判断项 ①（CLI 与 HTTP 答复面都未透出 `HandoffReference()`）**不并入本票**——那是答复形改动，本票零行为；归 CC owner 另立一张小票（tasks.md 15:0x 节写的「并入 sa-cc/15」由 16:0x 节更正）

## 缺口（取证于 `0b027ab8`；数本身是论点，故写数并锚 SHA）

- `git grep -n -E '硬句\s*[0-9]+'` 在 `internal/customscompliance/**` **76 处 / 46 文件**，`cmd/parcel-customs-register/` 2 处 / 2 文件（`translate.go` `gateKeyFrom` 头注、`translate_test.go` 一条 `t.Fatalf` 文本），`apps/admin-web/src/pages/customs/` 6 处 / 4 文件（`CustomsCasesPage.tsx` ×2、`CustomsRestrictionsPage.tsx`、`api.ts` ×2、`register-rows.test.ts`）。
- 那些数字是 `docs/domain/customs-compliance/CONTEXT.md` 的**行号**，不是 CONTEXT 自带的编号（CONTEXT 里没有任何「硬句 N」标记）。实测于 `0b027ab8`：第 212 行 =「法定义务人、实际付款方和最终承担费用的客户可以不同，不能互相推导」（代码引 212 ✓）、214 = 三态「不能实现为一组互斥总状态」（✓）、216 =「门禁满足不生成放行，也不能复用于其他动作或监管边界」（✓）；**第 217 行是 ADR-0137 落地时插入的「税费付款门禁怎么读核对」那句**，其后每行下移一行——代码里的「硬句 218」（关闭前逐项盘点全部适用义务 / 任一义务未终结阻止关闭，`case_closure.go` / `obligation_inventory_view.go` / `close_customs_case_test.go` 等）今天指到的 218 行是「监管核定及法定义务由本上下文拥有……」，「硬句 219」（承接项必须指名接收责任方，`query_case_registers.go` / `registrationjson/translate.go` / `case_config_registry_test.go` / admin-web `api.ts` 等）指到的 219 行是义务盘点那句。AGENTS.md「改文档」条预言的失效模式已经发生：指错了，没有任何东西报。
- 跨包计数（AGENTS.md「计数与行号同构」）：`cmd/parcel-customs-register/translate.go` `answer` 头注与 `configurationCall` 头注（「案件配置族的八格、税费付款协作与核对族的十三格」「凭证四个 handler」）、`main.go` `configurationAnswer` 头注（「封闭八格」）与 `dutyReconciliationAnswer` 头注（「封闭十三格」）、`duty_registers_test.go` 文件头注与 `TestDutyReconciliationAnswerCoversEveryOutcome` 头注（「十三格 / 八格」）、`main_test.go` `TestConfigurationAnswerCoversEveryOutcome` 头注（「八格」）——数的是 `internal/customscompliance/application` 的 `CaseConfigurationOutcome` / `DutyReconciliationOutcome` 常量个数；那边加一格是正当改动，改的人不会路过 `cmd/`。sa-cc/07 步一评审 Standards ① 点的就是这一族（「八格」是旧例、「十三格」是新添）。
- 出处：sa-cc/10 非作者评审 Standards ①（`query_duty_collaborations.go` / `query_duty_verifications_test.go` / `CustomsRestrictionsPage.tsx` 三处「硬句 212 / 214」，当时判「沿包内旧例非新造」、归 CC owner 一笔换引文）；sa-cc/07 步一评审 Standards ①（「十三格」四处）。两条都随票记为非阻断，本票收口。

## 做法

1. **行号 → 引文**：每处 `硬句 NNN` 换成该句的一小段原词引文（≤ 20 字，用「」），数字删掉。注释里已经引了原句的（如「CONTEXT 硬句 214 明禁实现为一组互斥总状态」）只删数字、保留引文；`Covers:` 行写成 `Covers: CC CONTEXT「……」`。引文选**该句最不会改的那半**（AGENTS.md：引文至少失配可见）。逐处对着 CONTEXT 当前文本核一遍——凡是原来指错行的（218 / 219 一族），以代码语义为准找到它真正要引的那句，不按今天的行号抄。
2. **跨包计数 → 点名**：「八格 / 十三格 / 四个 handler」改成「案件配置族的格」「协作与核对族的格」「凭证等各 handler」这类不带数的写法；`TestDutyReconciliationAnswerCoversEveryOutcome` / `TestConfigurationAnswerCoversEveryOutcome` 头注写「逐格对着 `application` 的枚举表」即可——用例本身列的表是它自己的断言，不动。
3. **admin-web 六处**同法（TS 注释与 `register-rows.test.ts` 的 `Covers:`）。
4. 一笔或按包分几笔都行；**不顺手改任何非注释行**。`t.Fatalf` 文本里的「硬句 219」属测试失败信息，一并换成引文。

## 红线

- 零行为改动：`git diff` 只许出现注释行、`Covers:` 行、`t.Fatalf` / 错误文本字符串行；生产代码字符串不动（错误文本里若有「硬句 N」也换，但那属信息文本不属行为）。
- 不改 `docs/domain/customs-compliance/CONTEXT.md`——本票不给 CONTEXT 加编号来「修」引用；行号引用的解法是不引行号。
- 不改口径：改口只换指法，不改注释所讲的规则内容；发现注释讲的规则与 CONTEXT 现文不符（不只是行号错），另记一条交 CC owner，不在本票里替它改。

## 完成判据

1. `git grep -n -E '硬句\s*[0-9]+' -- internal/customscompliance cmd/parcel-customs-register apps/admin-web/src/pages/customs` **零**。
2. `git grep -n -E '十三格|八格|四个 handler' -- cmd/parcel-customs-register` 零。
3. `git diff <基线> --stat` 只含上述目录；`git diff <基线> -- '*.go'` 的 `+`/`-` 行全部是 `//` 注释、`Covers:` 或字符串字面量（评审抽验）。
4. `gofmt -l` 空、`go build ./...` / `go vet ./...` 退 0；`go test -count=1` 动过的 Go 包（不带 DSN，编译过即可——真库用例 skip 属预期，票面如实记「真库未跑、无行为改动」）；admin-web `tsc --noEmit` 0、`node scripts/run-tests.mjs` 全 pass。
5. 清点：注释改动不改清点数字，`git status --porcelain -- docs/product/MECHANISM-INVENTORY.md` 为空即可。

## 地盘

`internal/customscompliance/**`（只注释）、`cmd/parcel-customs-register/**`（只注释与测试文本）、`apps/admin-web/src/pages/customs/**`（只注释）。**不动** `docs/**`、任何其他上下文。撞点见 Blocked by 软阻——开工前 `git fetch` 核 sa-cc/04 / 05 / 07 步二 已进 main。

## 参照

AGENTS.md「改文档」（行号与计数同构那段，含 `1dbc680` 抽验）与「写代码注释」；[sa-cc/10](10-cc-credential-collaboration-and-verification-read-faces.md) Comments 非作者评审 Standards ①；[sa-cc/07](07-cc-credential-and-duty-reconciliation-registration-faces.md) Comments 步一评审 Standards ①；同族票 [ve-disc/04](../../ve-disclosure-policy-view/issues/04-ve-comments-cite-context-line-numbers-and-count-methods.md)（VE 半边）、[lc/36](../../label-channel-service-first-release/issues/36-ps-label-final-review-follow-ups-header-comments-and-constructor-guards.md)（PS 头注与构造器项）。

## Comments

- 2026-09-11 14:2x · 通道 1 推送方：立票。**只写票面，未动代码。** 能力边界：数字来自 `git grep` 与 `Select-String` 实测（`0b027ab8`），行号错位核了 212 / 214 / 216 / 217 / 218 / 219 六行；没逐处读 76 处注释各自引的是哪句——实施时逐处核。
- 2026-09-11 16:36–16:5x · 通道 5（task-0fb34872）· **完成记录**。分支 `mcp5-sacc15` 基远端 main `620f7fed`（`ls-remote` 核），隔离树 `D:/tops/idp-parcel-mcp5-sacc15`，每笔即推。五笔：`c0134511` 票面 in-progress → `459cf68e` CC 43 件（domain / application / postgres / http / registrationjson，06 撞点五件除外）→ `d4616677` `cmd/parcel-customs-register` 6 件 → `66d0ac75` admin-web 4 件 → `c4e9adf5` **06 撞点五件**（`application/verify_release_gate.go`、`domain/duty_release.go` + `_test`、`adapters/postgres/gate_condition_view_test.go`、`ports/ports.go`；放最后一笔，通道 4 16:4x 占号清单点名的文件——推送方若先重放 06，此笔可整笔丢弃由本人在 06 之上重做十处）。本笔票面 + spec 15 行。
  - **做法逐条**：**1** 84 处 `硬句 NNN` 全换（`internal/customscompliance/**` 76、`cmd/parcel-customs-register/` 2、`apps/admin-web/src/pages/customs/` 6）：逐处按代码语义对着 CONTEXT 现文核出真要引的那句，换 ≤ 20 字原词引文；已引原句的只删数字；`Covers:` 行写成 `Covers: CC CONTEXT「……」`。**行号错位不止 218 / 219 一族**（取证于 `620f7fed` 的 CONTEXT）：指对的有 143 / 147 / 148 / 150 / 159 / 185 / 186 / 187 / 212 / 214 / 216；指偏一行的有 164→163（就绪与授权独立）、165→164（授权各动作分别表达）、168→169（首次发送前不可覆盖版本）、169→170（原提交永久保留）、170→171（安全再次发送）、172→173（五道动作分别表达）、174→175（替代单元 / 案件）、175→176（拟执行意图）、191→192（法定适用时点）、208→209（部分执行 / 再次执行事实）；指偏两行的 218→220（关闭盘点 / 任一未解决阻止关闭）、219→221（移交指名接收责任方）。写的人分几批、各按当时行号抄，AGENTS.md「改文档」预言的形。**同病同治**：`生命周期 NNN`（233 / 234 / 258 / 267，今天各偏两行）六处也换引文（`case_closure.go` / `case_closure_test.go` / `manifest_candidate_view.go` / `manifest_reference.go` / `manifest_reference_test.go` / `receive_manifest_test.go` / `follow_up_target_test.go`）；`case_closure_test.go` 一处裸「219」同治。**引文本身不是原文的两处改成原文**：`close_customs_case_test.go`「任一义务未终结或未有效承接都阻止案件关闭」→「任一未解决或冲突项都阻止关闭，单个案件不存在部分关闭」；`submit_declaration_test.go`「首次实际发送前形成不可覆盖版本」→「首次实际对外发送前都必须形成不可覆盖的提交版本」——口径未变，只是让引文能被搜到。**2** 跨包计数：`translate.go` `answer` / `configurationCall`、`main.go` `configurationResult` / `configurationAnswer` / `dutyReconciliationAnswer`、`duty_registers_test.go` 头注 + `TestDutyReconciliationAnswerCoversEveryOutcome`、`main_test.go` `TestConfigurationAnswerCoversEveryOutcome`——「八格 / 十三格 / 四个 handler」→「案件配置族的格」「协作与核对族的格」「凭证等各 handler」「逐格对着 `application` 的枚举表」；07 步二评审点的 `register_credential_and_duty.go`「同一套八格」「资金事实那三格」与 `api.ts`「资金事实三格」→「案件配置族的格」「资金事实那族格」。用例自己列的表与同文件内计数（「凭证子命令三格」「撤销三格」「负向三格」等）不动。**3** admin-web 六处同法。**4** `translate_test.go` `t.Fatalf` 文本「硬句 219」→ 引文。
  - **判据逐项**：**1** `git grep -n -E '硬句\s*[0-9]+' -- internal/customscompliance cmd/parcel-customs-register apps/admin-web/src/pages/customs` → 零；另 `生命周期\s*[0-9]{2,3}` → 零。**2** `git grep -n -E '十三格|八格|四个 handler' -- cmd/parcel-customs-register` → 零。**3** `git diff --stat 620f7fed HEAD` 58 文件 / +118 −117，只含三个地盘目录；逐行核 `+`/`-` 行全部是 `//` / `*` 注释、`Covers:` 或 `t.Fatalf(` 字符串行（脚本核零例外）。**4** `gofmt -l` 空；`go build ./...` / `go vet` 退 0；`go test -count=1` CC 全部包 + `cmd/parcel-customs-register` + `internal/architecture` ok；admin-web `node node_modules/typescript/bin/tsc --noEmit -p .` 退 0、`node scripts/run-tests.mjs` 220 pass / 0 fail（隔离树经 junction 指主树 `node_modules`，已 rmdir）。**5** `git status --porcelain -- docs/product/MECHANISM-INVENTORY.md` 空，清点不变。
  - **一处纪律失误，如实记**：16:46 跑 CC 包 `go test` 时 shell 里残留着上一票设的 `IDP_PARCEL_POSTGRES_DSN`，CC `adapters/postgres`（8.6 s）与 `adapters/inbox` 实际带 DSN 跑了、**未占号**，且与通道 1 16:44–16:47 的重放全量撞在同一只 55432 上——两边都绿，但这正是占号纪律要防的事；变量已清。本票零行为，真库结果不作证据，票面按判据 4 记「真库未跑、无行为改动」。
  - **注释讲错规则（交 CC owner）**：**无**——84 处逐一核过，指错行的都只是行号偏移，注释讲的规则与 CONTEXT 现文一致；两处引文非原文属转述，口径不错，已换原句。
  - **等 06**：无未动文件——撞点五件已在末笔 `c4e9adf5`，通道 4 已收到我碰过的行清单（`send_to_session` 16:5x），谁后进 main 谁 rebase。
  - **能力边界**：逐处读了 84 + 7 处注释所在的整段与 CONTEXT 140–240 行、250–275 行；没读 `docs/domain/customs-compliance/CONTEXT.md` 其余段；没读被引用句子对应的 UC 文本。
- 2026-09-11 17:0x–17:1x · 通道 5 · **补笔 `94c87bb6`**（通道 3 评审钉 `a1d21ada` 两条非阻断同源：① 引文折行搜不中；② 「已引原句」里有转述）。同一红线同一症一并收（三地盘内、零行为、33 文件 / +55 −107）：折行引文并回一行 46 处（本票新引入的与既有的都收，只剩 `domain/credential_gate.go` 一处不动——sa-cc/06 正在那件加代码）；转述换原句 13 处（生命周期箭头两侧空格三处、`duty_release_test.go` 三态括注 / 「等判断」两处、“清关成功”引号形两处、`manifest_reference_test.go`「本上下文只接受……」、`declaration_submission_test.go` 三处、`submit_declaration_test.go`「双有效才成版」、`CustomsRestrictionsPage.tsx`「判断绑定动作与边界、不复用于其他动作」、`external_result_test.go` 八件那句）；票面 grep 之外再收六处「CONTEXT 244 / 243」裸行号（`api.ts`、`vertical_test.go`、`query_case_registers_test.go`、`case_register_catalogue_test.go`、`submit_declaration.go`、`declaration_submission.go`）。**自核脚本**（对 `CONTEXT…「…」` 逐处按省略号切段、每段在 CONTEXT.md 逐字命中）：**145 处引文 142 命中**；3 处「未命中」是引 VE CONTEXT「来源事实替代关系」与 CONTEXT-MAP「customs-compliance ↔ network-routing」的，各在各自文档里逐字命中。`gofmt -l` 空、build / vet 0、CC 全部 + CLI + architecture 不带 DSN ok、admin-web tsc 0 / 220 pass、清点不变。
- 2026-09-11 17:1x · 通道 5 · **按推送方 17:07 广播 rebase 到远端 main `45b9c468`**（02 + 09 + 36 进 main 后），八笔零冲突、force-with-lease 推，全部换号；rebase 后 gofmt / build / vet / 不带 DSN 九包 / 引文自核 145→142 / 判据 1–3 grep 全部复跑同结果。SHA 对照（旧 → 新）：`0b47ebf8→c0134511`（票面 in-progress）/ `5340618b→459cf68e`（切片 1）/ `c22864c1→d4616677`（切片 2）/ `30bd36f1→66d0ac75`（切片 3）/ `04ac89f1→c4e9adf5`（切片 4，06 撞点五件）/ `70083dfc→a1d21ada`（票面 resolved）/ `bd0dfed4→94c87bb6`（补笔）/ `7ed3b056→0802f192`（票面记补笔）。上文各处 SHA 已同步为新号；提交信里的旧号（如「在 bd0dfed4」）只在对象库可查，以本对照表为准。
- 2026-09-11 18:4x · 通道 1 推送方 · **进 main 记录**。重放在 sa-cc/06 重放 tip `9c0b7223` 之上（`%TEMP%\idp-replay-15on06`，原通道 1 17:3x 起做、crash 后本会话接管核毕）：`c0134511→f061b834` / `459cf68e→747c8870` / `d4616677→9e90e88b` / `66d0ac75→caad3e20` / `c4e9adf5→3098cf61` / `a1d21ada→54abb795` / `94c87bb6→839e76dc` / `0802f192→7f45c45b` / `74bb70cf→af40abf7`。`git range-diff` 核：九笔中七笔 `=`，切片 1 与切片 4 各少一个 hunk——`application/register_case_configuration.go` `RegisterGateFinding` 头注与 `application/verify_release_gate.go` `VerifyReleaseGateCommand` 头注那两处「硬句 216」06 已在自己的笔里换成引文，重放取 06 的版本，口径同。tip `af40abf7` 上判据 1 / 2 三个 grep 复跑仍零；带 DSN 全量 110 ok / 0 FAIL / 0 cached、admin-web `tsc` 0 / 220 pass；清点在 tip 重生成零差、不另成笔。**通道 5 的 wip `0b4da637`（22 文件 +36 −51，「CONTEXT NNN」再换引文 + 折行并回，原通道 1 17:3x 收工代提保现场）不进 main**：未成笔、无评审，留在分支上——下次开工通道 5 把它 rebase 到新 main 收成正式补笔再走 Spec 重核。分支 `mcp5-sacc15` 因此**不改名 `merged/`**、树 `D:/tops/idp-parcel-mcp5-sacc15` 保留。同族后继（归 CC owner）：sa-cc/06 评审 Standards ① 点的两处非原句引文（『』内层引号、括注去掉无省略号）可随这一笔一并收。补笔 `94c87bb6` 的 Spec 重核由推送方代通道 3 做（46 处引文全部逐字命中 CC / VE CONTEXT 或 CONTEXT-MAP、零非注释行、零地盘外文件）。
- 2026-09-11 19:2x–19:5x · 通道 1 推送方（新会话，接手时 `main = origin/main = 8557afd7`）· **补笔二进 main 记录**。上条写的「wip 归通道 5 收成」实际由原通道 1 于 19:00 代为收成：分支 `mcp1-sacc15-tail` 基 main tip `8557afd7` 一笔 **`da52715a`**（31 文件 / +61 −77，零行为只注释）= 通道 5 wip `0b4da637` 的 22 件（余下「CONTEXT NNN」行号换引文、折行引文并回一行）+ 通道 1 收尾 9 件（`query_duty_collaborations.go` / `unconfigured_intake.go` / `establish_customs_case_test.go` / `judge_credential_applicability.go` / `manage_restriction_test.go` / `reconcile_duty_payment.go` / `credential_gate.go` / `duty_payment_gate_rule.go` + `_test`：sa-cc/06 新文件两处非原句引文改原句——即 06 评审 Standards ① 那两处，随本笔收；ADR-0137「本边界」误引「此边界」改正；UC-CC-003「门禁表第 4 行」改成行标签「4. 监管凭证」；UC-CC-009 / AT-CC-345 / ADR-0052 三处转述换原句）。原会话成笔后无台账、无广播即重置，本条补记。
  - **包含性核**：`%TEMP%\idp-replay-check-15tail` detached `8557afd7` 上 cherry-pick `0b4da637` 零冲突，与 `da52715a` 比只差上述 9 件，wip 的 22 件字节一致——wip 已被完整收成，`mcp5-sacc15` 上再无未进 main 的内容（`74bb70cf` 及之前九笔已吸收于 `af40abf7`，`branch-state.ps1 -Classify` 同判）。核毕拆。
  - **判据复跑于 `da52715a`**：**1 / 2** `硬句|生命周期|CONTEXT\s*NNN` 与 `十三格|八格|四个 handler` 三地盘 grep 零。**3** 地盘外文件 0；`git diff -U0 8557afd7 da52715a` 的 `+`/`-` 行逐行核，非 `//` / `*` / `{/*` 起头的行 **0**。**4** `gofmt -l` 空；`go build ./...` / `go vet` 0；不带 DSN CC 全部包 + `cmd/parcel-customs-register` + `internal/architecture` ok；带 DSN 全量 **110 ok / 0 FAIL / 15 无测试 / 0 cached**（19:41:18→19:43:25，127 s；CC `adapters/postgres` 8.9 s 为真跑非 SKIP）；admin-web `tsc --noEmit` 0、`run-tests.mjs` 220 pass / 0 fail（隔离树经 junction 指主树 `node_modules`，已 rmdir）。**5** 清点在 tip 重生成 `git status --porcelain -- docs/product/MECHANISM-INVENTORY.md` 空，不另成笔。
  - **Spec 重核代评审**（按上条补笔一先例，用户 19:4x 裁可「需要」）：对 `da52715a` 全部 `+` 行里的 `「…」` 按省略号切段、每段在 `docs/**` 与 `.scratch/**` 全部 `.md` 逐字找——**74 段 / 71 命中 / 3 未命中**；未命中的「端点各立三个入口」「一层的译装接到另一层的端点上」「这个请求来自哪个租户」三处在基线 `8557afd7` 的 `query_credentials.go` / `query_duty_collaborations.go` / `submission_authority_view.go` / `ports.go` 里已有，是票 sa-cc/10 裁决与 `PAR-INT-01` 的转述而非 CONTEXT / UC / ADR 引文，本笔只并行未改字，不属本票口径。Standards 轴未做（零行为、只注释）。**注释讲错规则**：无新增发现。
  - **进 main**：`ls-remote` 核 `8557afd7` 未动 → 共享树 `merge --ff-only`（共享树「76 行未提交」`--ignore-cr-at-eol` 后真改动 0 件，全是 CRLF 幻影）→ `push <sha>:main`。**代码 `da52715a` 不换号、重放 0 笔 cherry-pick**；簿记一笔在其上（本条 + Status + sa-cc spec 15 行 + tasks.md 节），远端 main = 簿记笔 tip，SHA 在推后广播。
  - **处置**：`mcp5-sacc15` → `merged/mcp5-sacc15`（wip `0b4da637` 作封存出处，指针留）、远端 `origin/mcp5-sacc15` 删、树 `D:/tops/idp-parcel-mcp5-sacc15` 拆（干净）；`mcp1-sacc15-tail` → `merged/mcp1-sacc15-tail`、树 `D:/tops/idp-parcel-mcp1-sacc15-tail` 拆（干净，未推过 origin）。均不加 `--force`。本票至此无在途分支、无在途树。
  - **能力边界**：引文核靠脚本逐字匹配，没有逐处重读 31 件注释的语义；未读 CONTEXT 全文；tsc / 用例只证编译与既有用例不破，两个 `.tsx` 本笔只动注释行。
