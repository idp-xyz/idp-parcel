# `settlement-accounting` ↔ `customs-compliance` 资金与凭证那条缝：七张机制半边的票

Category: chore
Status: resolved——2026-09-24 通道 1 按 issue-tracker「Complete a parent」收口：子票 01–35 逐份读过，Status 全为 resolved（11、23、24、25、26 五张原写「已进 main」、所记代码 tip 经 `git merge-base --is-ancestor` 核过都在 main 上，同笔在其前补 `resolved · `，原文未动）；起因是通道 2 同日完成度核查点名。此前 in-progress——**2026-09-14 22:1x 通道 1 按用户「你是业务和系统专家，自决」把 19 / 20 / 21 / 22 四张 draft 的「要裁的」写入各票「裁决」节并转 ready-for-agent**（依据是 21:2x 通道 4 / 5 / 6 三份取证：22 程序仍调用方交、进记录与指纹、不加主键列；19 (a′) 形成新核对版本——覆盖承前、差额 / 有效性 `PENDING`、触发只看既往核对、资金版本进指纹与记录列，Blocked by 22 同一作者同分支；20 版本子表 + 既有采用用例加「更正」格、不开新面，登记面另立 draft；21 A-1 同形复用落 Rules 税费段、13 处引用零改动）。此前——**2026-09-14 10:2x 通道 1 按用户「授权代裁」把 11 / 12 / 13 三张 draft 的「要裁的」全部写入各票「裁决」小节并转 ready-for-agent**（11 以 PP owner 口径：先立「按评价请求形成评价」入口 + 回指一格 + 在用价卡解析读口，允许拆前置票；12 以 CC owner 口径：付款人必备否是登记的规则一格，三停格；13 以 CC owner 口径：要版本维、触发重核对不在本票）；16 / 17 已进 main（17 于 10:0x）；18 由通道 4 10:1x 自立自做中。此前——2026-09-10 17:3x 通道 5 按通道 1 派单 task-9a2ff746（用户授权代裁）把十三条「要裁的」全部写入各票「裁决」小节（B 类四条落 [ADR-0137](../../docs/adr/0137-customs-gate-judgments-are-registered-facts-driven-by-assessment-requests-payment-gate-rule-is-registered-and-funds-facts-are-minted-only-in-settlement-accounting.md)），01–07 转 ready-for-agent（03 等 02、07 步二等 10），另立 08 / 09 / 10（09 ready-for-agent 等 05；08 / 10 draft 各有要裁的）。此前 in-progress——2026-09-10 15:1x 通道 4 立目录与七张子票（全部 draft，task-9880bbc9）；子票全 resolved 本 spec 才 resolved。只写票面未动代码；取证锚 `3f485e97`

## 从哪里分出来

[unresolved-review-20260904/remaining-work-dd5ed934.md](../unresolved-review-20260904/remaining-work-dd5ed934.md) 表里两行判「仍余且机制半边且无票」：

- **五-7** SA 两件——BUY 评价 → SA 的 inbox 消费者；SA 外部资金事实采用**发信封** + CC 侧 inbox 消费者。
- **五-8** CC 四件机制——UC-CC-003 步 7「记录凭证门禁」的持久化；UC-CC-009 核对向 `settlement-accounting` 的交接（outbox 意图）；放行门禁核对读付款核对；凭证登记 / 关税协作 / 资金事实 / 付款核对的在线登记面与 admin 写面。

两处的源头都是 `mechanism-executor-triage` 三张实现票收口时写下的「不在本票 / 留给后继票的」（[06](../mechanism-executor-triage/issues/06-sa-four-executors-behind-existing-uc-steps.md)「不在本票」节、[07](../mechanism-executor-triage/issues/07-cc-four-executors-behind-existing-uc-steps.md)「没做、留给后继票的」五条）。那两张票 2026-09-04 resolved 之后这些后继没有任何票面在盯，a3a4814 与 dd5ed934 两次重核都量到它们仍在。

**为什么并成一个目录**：七件全在同一条缝上（五-7 的第二件拆成「发信封」与「消费者」两张）——PP 评价进 SA 形成预期成本、SA 采用的外部资金事实进 CC 做税费付款核对、CC 的核对结果回 SA 判实际代垫、凭证与核对各自的门禁持久化、以及让租户能登记这些事实的入口。拆散到 SA / CC 各自目录，谁先谁后的边就没人写。

## 取证（`3f485e97`，逐条可重跑）

- `git ls-files internal/settlementaccounting/adapters` → 只有 `http` / `parcelpricing` / `partycommercial` / `postgres`，**无 `inbox`**；`git ls-files internal/customscompliance/adapters/inbox` → 空。
- `git grep -n 'eventing.EventType' -- internal/settlementaccounting/adapters/postgres` → 十七个事件类型常量，无一是「外部资金事实已采用」。
- `git grep -E 'SupplierExpectedCost|BuyEvaluationAdoption' -- cmd/` → 只命中 `cmd/parcel-api/unwired_orchestration.go` 的读面桩。
- `git grep -E 'RegisterCredential|DutyPaymentReconciliation|ReceiveFundsFact|JudgeCredentialApplicability' -- cmd/` → 零。
- `cmd/parcel-api/endpoints.go` 关务行：`/customs/external-results`、四类目录登记（解释规则 / 门禁目录 / 候选口岸 / 申报路径）、`/customs-case-requirement-registrations`——没有凭证、协作、资金事实、付款核对的登记端点。
- `internal/customscompliance/application/judge_credential_applicability.go` 头注：「本用例只判、不记：步 7 写的『记录凭证门禁』那半留给就绪判断的编排」。
- `internal/customscompliance/application/verify_release_gate.go` 不引用 `DutyVerificationStore`（`git grep -n DutyVerificationStore -- internal/customscompliance/application/verify_release_gate.go` 零）。
- `internal/customscompliance/application/reconcile_duty_payment.go` 三方法 `FormCollaboration` / `ReceiveFundsFact` / `VerifyPayment` 在；无任何 `HandOff*` 调用（`git grep -n -i handoff -- internal/customscompliance/application/reconcile_duty_payment.go` 零）。

## 子票

| 票 | 一件机制 | 上下文 | Blocked by |
|---|---|---|---|
| [01](issues/01-buy-evaluation-to-sa-inbox-consumer.md) | `parcel-pricing.evaluation.recorded` → SA inbox 消费者 → 处理方按引用取评价、非 BUY 不处理、命令缺三件引用 → 未决指名等 08——**resolved（作者完工 2026-09-11 22:0x；22:5x 进 main）**（通道 1 推送方重放，与 sa-cc/16、lc/37 同批：main 上 `5cdd85ad` / `3c57811d`，批清点 `fe975e1b`；作者那一笔未成笔、由推送方 22:28 `chore(salvage)` `ddc88003` 原样封存后重放；非作者评审 ← 通道 3 两轴 0 阻断、Standards 2 / Spec 3 非阻断随票记；`fe975e1b` 带 DSN 全量 110 ok；分支 `mcp5-sacc01` 基 `02e1dfc4`；(c) 范围「信封到未决」；判据 1 随裁决改口——`FormSupplierExpectedCostHandler` 由形成路后继票装，Blocked by 08 回指 / 11；带 DSN 四包 PASS 123 / 0 FAIL） | SA | 无（(c) 范围）；形成路后继票 Blocked by [11](issues/11-pp-inbox-consumer-receives-evaluation-request-envelope.md) |
| [02](issues/02-sa-external-funds-fact-adoption-hands-off-an-envelope.md) | SA 外部资金事实采用同事务经 Outbox 发信封——**resolved，2026-09-10 19:4x 进 main**（分支 `mcp5-sacc02`，代码 tip `1af6a226`，main 重放 tip `cdd6504c`；非作者评审 ← 通道 6 两轴 0 阻断） | SA | 无 |
| [03](issues/03-cc-inbox-consumer-receives-external-funds-fact.md) | CC inbox 消费者收那封信封 → `ReceiveFundsFact`——**resolved，2026-09-10 21:3x 进 main**（分支 `mcp5-sacc03`，代码 tip `f96169d2`，main 重放 tip `343b997b`、与 lc/28 同批推出；非作者评审 ← 通道 6 两轴 0 阻断；实施中新裁付款人维 A、可缺席，CC 放宽另立 12） | CC | 无 |
| [04](issues/04-cc-credential-gate-persists-in-readiness-assessment.md) | UC-CC-003 步 7「记录凭证门禁」的持久化——**resolved，2026-09-11 16:3x 进 main**（分支 `mcp4-sacc04`，基 `7c37253f`，代码 tip `d69e18c7`；main 上代码 `a32c80eb`、批清点 `dcb74785`（与 08 同批）；非作者评审 ← 通道 6 两轴 0 阻断，非阻断四条随票记，指纹用 `String()` 归 06 顺手；独立「凭证门禁判断」登记册 + 迁移 0018 + `RecordCredentialGateHandler`，`RegisterReadiness` 的形不动；装配点今天无、如实记在票面判据 1） | CC | 无 |
| [05](issues/05-cc-duty-reconciliation-hands-off-to-settlement-accounting.md) | UC-CC-009 核对向 SA 的交接（outbox 意图）——**resolved，2026-09-11 15:1x 进 main**（分支 `mcp5-sacc05`，代码 tip `353597a2`、分支 tip `33265d0c`；main 重放 tip `4ab49c65`；非作者评审 ← 通道 1 推送方自跑两轴 0 阻断、Spec 2 非阻断随票记；分区主体 租户 / 申报范围 带口名段） | CC | 无 |
| [06](issues/06-cc-release-gate-reads-duty-payment-verification.md) | 放行门禁核对读付款核对——**resolved，2026-09-11 18:4x 进 main**（分支 `mcp4-sacc06`，基 `620f7fed`，代码 tip `455cacbd`；main 上代码 `612e9afb`、推送方清点 `9c0b7223`；非作者评审 ← 通道 1 推送方自跑两轴 0 阻断、3 + 3 非阻断随票记；「税费付款」那一道按登记规则折三态、门禁记录带三态原值 + 核对版本引用、迁移 0019；顺手笔 `23b7f671` 收 04 评审 S①；规则登记面今天无、如实记在票面判断项） | CC | 无 |
| [07](issues/07-cc-credential-and-duty-reconciliation-registration-faces.md) | 凭证 / 协作 / 付款核对三册的在线登记面（CLI + 端点 + 管理台；资金事实人工口按 ADR-0137 决定四去掉）——**步一 CLI 已进 main，2026-09-11 13:4x**（分支 `mcp2-sacc07-cli`，代码 tip `40f3d6d3`；main 重放 tip `3ec86aaa`、清点 `fa386a29`；非作者评审 ← 通道 1 推送方自跑两轴 0 阻断，非阻断两条随票记）；**步二已进 main，2026-09-11 16:0x**（`mcp2-sacc07-web` 基 `7c37253f`，代码 tip `bf8bd5ea`、清点 `56674625`；main 重放 tip `25377eec`；非作者评审 ← 通道 1 推送方自跑两轴 0 阻断，非阻断三条随票记；三端点 + `cmd/parcel-api` 装配含 05 的 Handoff 口 + 真库装配用例 + 管理台三册写签）——**两步全部 resolved** | CC | 步二 Blocked by 10 → 10 已进 main，不再阻；步一无 |
| [08](issues/08-sa-evaluation-request-orchestration-records-source-references.md) | UC-SA-002 步 2 请求评价编排——评价请求登记册（铸造 ID + 自然键唯一）同事务发信封给 PP（01 裁 (c) 时另立）——**resolved，2026-09-11 16:3x 进 main**（分支 `mcp6-sacc08` 基 `807de571`，代码 tip `e81e1240`、清点 `7dfba37d`；main 上代码 `c9aae85f` / `8d75bfc7`、批清点 `dcb74785`（与 04 同批）；非作者评审 ← 通道 4 两轴 0 阻断，非阻断六条随票记（`ErrNilDependency` 包装 + `Save` 核键归 SA owner 随下一票）；判据 1 / 2 / 3 / 5 落地，判据 4 标「等 11」；装配点取 `cmd/parcel-api` fail-fast 一格） | SA | 11 只阻判据 4「请求 → 评价引用」；登记册与发信封半边**已进 main** |
| [09](issues/09-sa-consumes-duty-payment-verification-envelope-into-advance-recovery.md) | SA inbox 消费付款核对信封 → `AssessAdvanceRecoveryHandler`（05 裁「本目录加一张」）——**resolved，2026-09-11 16:5x 进 main**：接为 UC-SA-001 步 2「结算输入采用」一格（`AdoptDutyPaymentVerification`，登记册只存核对版本引用 + 采用时刻，`AdvanceOutcome` 不动；票面做法 1「译成 AssessAdvanceCommand 交 Assess」在代码上走不通，通道 1 15:5x 裁改口）；分支 `mcp5-sacc09` 代码 tip `17167aa8`、注释笔 `cb04084e`、清点 `11da0858`；main 上代码 `a2832262`、批清点 `dfd3c3f5`（与 ve-disc/02、lc/36 同批）；非作者评审 ← 通道 3 两轴 0 阻断，非阻断三条随票记（空租户核与 `ErrUntranslatableReference` 原样上抛两条归 SA owner 随下一票） | SA | 无（05 已进 main） |
| [10](issues/10-cc-credential-collaboration-and-verification-read-faces.md) | 凭证 / 协作 / 付款核对三册的读面（伴生列表读口 + 查阅端点 + 管理台读签：凭证进 customs-cases、协作与核对进 customs-restrictions；07 裁「读面另立」）——**resolved，2026-09-11 10:5x 进 main**（分支 `mcp5-sacc10`，代码 tip `0017f49b`；main 重放 tip `99ceb975` 含清点；非作者评审 ← 通道 2 两轴 0 阻断，非阻断六条随票记） | CC | 无 |
| [11](issues/11-pp-inbox-consumer-receives-evaluation-request-envelope.md) | PP inbox 消费评价请求信封 → 形成计价输入快照与评价并回指请求（08 裁「走信封」时因 PP 无 inbox 先例另立） | PP | 08 的发信封半边 → **08 已进 main（2026-09-11 16:3x），硬阻已解**；2026-09-14 10:2x 通道 1 代裁入口形状（走 (b) 先立「按评价请求形成评价」入口 + 回指一格 + 在用价卡解析读口）→ 16:1x 通道 4 认领、三个会话接力（两次 crash，笔 C 封存 `0c368e45` 后重切）→ 20:0x resolved → **已进 main（2026-09-14 20:4x）**：分支 `mcp4-sacc11@3062d485` 六笔重放到 `9d720a2f` 之上，main 代码 tip `6876e10f`；评审 ← 通道 1 推送方自评（隔离子代理鉴权错，无空闲第三方）两轴 0 阻断 / 各 2 非阻断；带 DSN 全仓 113 ok / 0 FAIL；生产图今天诚实停在「输入不可得」——三只输入读口（TF 发生项成员对象 / NO·PS 实重尺寸 / PS 邮编路线）归提供方上下文后继票 |
| [12](issues/12-cc-funds-fact-payer-may-be-explicitly-unprovided.md) | CC 入向登记放宽付款人可缺席（03 裁决 2 取 A 时「CC 放宽另立 draft」）；「真实程序要求付款人」裁为登记的规则一格（10:2x 代裁）——**resolved，2026-09-14 13:1x 进 main**（之一 `97ba925a` 通道 4 旧会话、crash 现场封存 `7f5db322`；之二 / 之三 / 完成记录由通道 1 自办于 `mcp1-sacc12`，main 重放 tip `1ec18258` + 清点 `699c9b49`；带 DSN 全仓 110 ok；**非作者评审缺席**，用户授权自决放行 + 补评审，见票面「进 main 记录」） | CC | 无 |
| [13](issues/13-cc-correction-version-inbound-registration-and-rereconciliation.md) | CC 入向登记加版本维——SA 更正版本今天到 CC 落成 `内容冲突`、只留 inbox 痕、到不了 UC-CC-009 重新核对（03 非作者评审 Spec ①；与 12 同根异题）；10:2x 代裁：要版本维（形作者定 → 版本子表）、触发重核对不在本票——**resolved，2026-09-14 14:0x 进 main**（通道 1 自办，`c90d8606` / `e833355d` / `bab22328` / `8dbd49e2` + 清点 `0bd86d42`；带 DSN 全仓 110 ok；**非作者评审缺席**，用户授权自决放行 + 补评审；后继 19 / 20） | CC | 无 |
| [19](issues/19-cc-new-funds-fact-version-forms-a-new-verification-version.md) | 资金事实新版本到达 → 形成新核对版本（UC-CC-009 一致性节）：13 只到「登记册看得见新版本与回指」，触发落点、四口读、核对按版本读、与 05 的交接见票面「做法」；2026-09-14 22:1x 代裁：(a′) 形成新版本——覆盖 / 依据 / 程序承前版、差额与有效性显式 `PENDING`、资金版本取新版并折进指纹 + 记录列；触发只看「已有既往核对版本」不看 `corrects`——**resolved，2026-09-15 12:2x 进 main**（第三波批 tip `b6e7bd34`，重放 `6ab8e41c→e0201ca6` / `a0cb6fef→07341b8b`，完成记录笔 `dbff7f5f→1f3ed3d4`；评审 ← 通道 6 两轴 0 阻断 / Standards 2 非阻断 / Spec 3 非阻断；接手完成记录 ← 通道 2（原作者会话已换，task-d230f095）；代码 `mcp2-sacc22-19@a0cb6fef` 基 `1e74aaaf`：新编排 `RederiveDutyVerificationsOnFundsFactVersion` 挂在 `ReceiveOnAdoptedFundsFactAdapter` 接收之后同一事务、`ExternalFundsFactRegister.LoadFundsFact` 改名 `LoadFundsFactVersion` 按版本读、`DutyVerificationStore.ListVerificationsByFundsFact` 新读口、`VerifyDutyPaymentCommand.FundsVersion` 必填并追进 `verificationDigest`、迁移 `0023` `funds_version` 列 + 外键到版本子表；`0016` / `0021` / `0022` 与 SA 零 diff；批 tip 带 DSN 全仓 113 ok / 0 FAIL；判断项六条见票面，其中信封 ID 128 字节与重派路续办引用无读者归 CC owner 另票） | CC | 无 |
| [20](issues/20-sa-external-funds-fact-holds-one-row-per-fact-and-cannot-store-a-correction.md) | SA `external_funds_fact` 一事实一行（0004 主键）、`CorrectAmount` 无生产调用点：更正版本在提供方自己存不下，02 裁决 2「更正再发一封」无落地路径（13 实施中发现）；22:1x 代裁：形取版本子表（照 CC 0021）、入口是既有采用用例加「更正」格、不开新面、登记面另立 draft——**resolved，2026-09-15 11:5x 进 main**（首波批 tip `c657a5e7`，六笔重放 `fe496cf8→8f4121ad` / `c8ead2d0→6a85d4f4` / `33116348→6dc8d247` / `63333f4d→14d86a61` 等；评审 ← 通道 3 两轴 0 阻断 / 5 非阻断；此前 10:4x 通道 6 完工，task-173daa46；分支 `mcp6-sacc20` 基 `3a21dab7`，代码四笔 `fe496cf8`（迁移 `settlement_accounting/0021` 版本子表 + 链形约束、`FindByKey` 交链头、新增 `FindVersion`、`Save` 两步、目录一事实一行）/ `c8ead2d0`（`CorrectFundsFactCommand` → `CorrectFact` → `CorrectAmount` → 版本行 + 复用 handoff 再发一封）/ `33116348`（dispatch 正例第二格改回同一事实两版都在 SA）/ `63333f4d`（自评修：AdoptFact 幂等按（事实、版本）、注释去跨文件计数）；带 DSN dispatch + api + architecture + SA 全部 + migrations 1071 PASS / 0 FAIL / 0 SKIP；`0004` / `0018` / CC 零 diff；后继 draft 27 已立；清点预报：迁移 +1、端口方法 +1（`FindVersion`）、生产 `.go` 文件 +0） | SA | 无 |
| [21](issues/21-cc-context-duty-reconciliation-rules-lack-the-explicit-record-sentence-cited-by-payer-code.md) | CC CONTEXT「税费付款核对」没有「未提供必须明确记录、规则要求而缺失保持未决」那句，付款人一格的注释、`0020` 头注与 12 裁决 2 引的都是监管处置决定词条（12 补评审 Standards ①）：补句 + 改正引文归属，不改行为；要裁那句的原词，归 CC owner——22:1x 通道 1 代裁 A-1（同形复用落 Rules 税费段 + 第三格，13 处引用零改动）；**resolved，2026-09-15 10:2x 进 main**（重放 `97b05cf8→c4932992` / `08eb6d59→ac39f9a5` / `efd6549f→b62bab28`，推送方自审两轴 0 阻断；`/implement` 纯 docs；`mcp3-sacc21` 基 `3a21dab7`：认领 `97b05cf8`、之一 `08eb6d59`（CONTEXT Rules「税费、放行与案件闭环」段第 ② 条后补一句，`git grep -c` 该句于 CONTEXT 1→2；UC-CC-009「启动条件」三格 + `CC-RULE` 指向；票 12 Comments 追两句归属）+ 完成记录笔；`internal/` `migrations/` `apps/` 零 diff；13 处引用逐条 Read 核过零改动；评审门纯 .md 推送方自审；判断项四条见票面） | CC（只 docs） | 无 |
| [22](issues/22-cc-duty-verification-procedure-is-caller-asserted-and-not-recorded.md) | 税费付款核对的监管程序由调用方断言、编排不核与案件一致、核对记录不带程序（12 补评审 Standards ② + Spec ①）：错报 `procedureRef` 可绕「要求而缺失保持未决」；2026-09-14 22:1x 通道 1 代裁：程序仍调用方交、不核一致，但进记录并折进指纹、不加主键列（五处键形零改、SA 零 diff）；与 19 是同一张键的两维、同作者同分支先落——**resolved，2026-09-15 11:5x 进 main**（首波批 tip `c657a5e7`，重放 `bea83b9e→ee3603d7` / `b8969de4→2e411f63` / `4bcc16f9→2ff801b9`，清点笔批 tip 兑底；评审 ← 通道 6 两轴 0 阻断 / 各 1 非阻断；此前 11:0x 通道 2 完工，task f3329ba3；分支 `mcp2-sacc22-19` 基 `3a21dab7`，代码 `b8969de4`；`VerifyDutyPayment` 加 `procedure` + `Procedure()`、迁移 `0022` `procedure_ref`、四处读回带程序、`verificationDigest` 末尾追程序、`presentation.ts` 补 `procedureRef`；带 DSN CC postgres 256 PASS / 0 SKIP、migrate + cmd 三包 ok；判断项六条见票面） | CC | 无 |
| [14](issues/14-adopt-digest-header-and-duty-reconciliation-handler-rejects-nil.md) | 03 非作者评审 Standards ① ② 两处小改：SA `adoptDigest` 头注补「0018 起含付款人、存量零不回算」；CC `NewDutyPaymentReconciliationHandler` 构造期逐口拒 nil（形照 SA `NewApplyPreAcceptanceControlHandler`，调用点三处、`assemble.go` 一块占号）。要裁的为零——**resolved，2026-09-11 11:5x 进 main**（分支 `mcp5-sacc14`，代码 tip `62420c95`；与 lc/34 同批，批 tip `72a7ef49`；非作者评审 ← 通道 2 两轴 0 阻断） | SA 一句注释 + CC | 无 |
| [15](issues/15-cc-comments-cite-context-line-numbers-and-count-other-packages.md) | 10 评审 Standards ① 与 07 步一评审 Standards ① 的同族全收：CC 注释里「硬句 NNN」行号引用（`0b027ab8` 实测 76 + 2 + admin-web 6 处，ADR-0137 插句后 218 / 219 一族已指错一行）换引文；`parcel-customs-register` 头注「八格 / 十三格」跨包计数换点名。零行为，只注释与测试文本。2026-09-11 14:2x 通道 1 立，ready——**resolved，2026-09-11 18:4x 进 main**（重放在 06 之上，代码 `747c8870` / `9e90e88b` / `caad3e20` / `3098cf61` / 补笔 `839e76dc`；通道 3 评审两轴 0 阻断 + 补笔 Spec 重核 ← 通道 1 推送方 46 引文全命中；wip `0b4da637` 留分支不进 main）；**补笔二 19:5x 进 main**（`da52715a`：wip `0b4da637` 由通道 1 收成 + 收尾 9 件——含 06 评审 Standards ① 两处非原句引文，基 `8557afd7` 纯 ff 不换号；Spec 重核 ← 通道 1 推送方，新增引文段 71 / 74 逐字命中、余 3 处为基线已有票面转述；带 DSN 全量 110 ok；`mcp5-sacc15` / `mcp1-sacc15-tail` → `merged/`，两树拆）；此前作者完工 16:5x：84 处「硬句 NNN」+ 7 处「生命周期 NNN」全换引文（指偏一行的远不止 218 / 219 一族，对照表在票 Comments）、跨包计数全换点名；分支 `mcp5-sacc15` 四笔代码 `459cf68e` / `d4616677` / `66d0ac75` / `c4e9adf5`（末笔为 06 撞点五件） | CC（只注释） | 无硬阻；与同波 sa-cc/06（通道 4）在 CC `application` / `domain` / `ports` / `postgres` 有注释行重叠，谁后进 main 谁 rebase（撞点行清单已发通道 4） |
| [16](issues/16-sa-evaluation-request-and-duty-verification-adoption-standards-tails.md) | 08 评审 Standards ①②③ 与 09 评审 Standards (1)(2) 五条非阻断合收：`NewRequestBuyEvaluationHandler` 拒 nil 包同包既有 `ErrNilDependency`；`EvaluationRequests.Save` 核键与对象一致（照 CC `SaveVerification`）；`0019` `inserted_at` 取 (b) 保留、只在 Go 读口头注写明是库侧审计列（本仓各表通形，评审「SA 既有表无此形」前提不成立）；`AdoptDutyPaymentVerification` 空租户先答`未受理`；`ErrUntranslatableReference` 上抛路径只核不改、改不改分格落「要裁的」一条。零业务语义。2026-09-11 21:2x 通道 6 自立自做（task-63bd3adc，分支 `mcp6-sacc16` 基 `02e1dfc4`）——**resolved，21:5x 完工；22:5x 进 main**（通道 1 推送方重放，与 lc/37、sa-cc/01 同批：main 上 `a11abb96` / `33f2272f` / `c9d6f6e7` / `324aa474` / `4776b1b8` / `3049ca1b`，批清点 `fe975e1b`（本票零差）；非作者评审 ← 通道 1 两轴 0 阻断、Standards 1 非阻断（`ErrNilDependency` 文本）归 SA owner；`fe975e1b` 带 DSN 全量 110 ok；分支上代码 `9d48e8ca` / `0d25b898` / `2aa0c463` + ⑤ 注释与完成记录同笔 `dab69b4f` + 要裁的 1 裁准补笔 `ffbe1f14`；作者带 DSN SA adapters/postgres PASS 139 / SKIP 0；清点零差） | SA | 无 |
| [17](issues/17-sa-nil-dependency-text-sa01-count-comments-and-pp-synthetic-evaluation-fixture.md) | 16 评审 Standards ① 与 01 评审 Standards ① ② 三条非阻断合收：`ErrNilDependency` 文本去「pre-acceptance control」限定（它自 16 起是两只构造器共用哨兵；变量名与 `errors.Is` 不变）；01 新增的 `.go` 注释「三件引用 / 五种结果」计数换点名（`cdf17834` 上重量七句，派单点名的 `buy_evaluation_recorded_consumer_test.go` 零计数；票面 / 裁决文与 08 那批不动）；PP 合成评价夹具第三份抽成 PP 侧测试专属包 `internal/parcelpricing/pptest`（普通包、只被 `_test.go` import，照 `pgtest` 的形）——三份夹具**形同值不同**（金额 12.5 / 10、取整 None / Ceiling、引用带不带摘要、合成串各异），故规格显式、值由调用方给，三处调用点各自评价逐字段照旧。除错误文本外零行为。2026-09-14 08:5x 通道 4 按通道 1 派单 task-65ce5275 自立自做（`mcp4-tails` 基 `cdf17834`，与 lc/38 同分支）；**09:4x 完工待非作者评审进 main**：条 1 `bbc156e3`、条 2 `e46decfd`（实改八句）、条 3 `9693a4ee` 新包 + `6388e32b` 三处改调；带 DSN cmd/parcel-dispatch + PP postgres + SA parcelpricing 三包 PASS 181 / SKIP 0，三处夹具快照在 `cdf17834` 与本树逐字节同；清点预报 PP 生产 +1；五条判断项见票面 Comments | SA 两处 + PP 新包 + cmd 测试 | 无 |
| [18](issues/18-sa08-count-comments-and-pp-value-helpers-unified.md) | 17 评审 Standards ② ③ 两条非阻断合收：sa-cc/08 那批 `.go` 注释 / 测试文本里「三件引用 / 三件来源引用」跨文件计数换点名（`6bbf2bf0` 重量：派单六处之外另有 `evaluation_request_handoff.go` 生产头注与 `domain/evaluation_request_test.go` 两处同族，收；`domain/evaluation_request.go` 数同文件自己的字段，不动）；PP `evaluationValue` 改为直调 `pptest.Value`，`saTestValue` 喂的全是 SA / CC 类型、判留并写理由。零行为。2026-09-14 10:1x 通道 4 按通道 1 派单 task-c0000fd2 自立自做（`mcp4-tails2` 基 `6bbf2bf0`，与 ve-disc/06 同分支）；**10:2x 完工待非作者评审进 main**：条 1 `e7809d23`（十三句七文件）、条 2 + 完成记录同笔；不带 DSN SA / PP / cmd/parcel-dispatch / architecture ok；四条判断项见票面 Comments | SA 注释 + PP 测试 | 无 |
| [23](issues/23-cc-adopted-fact-version-must-match-envelope-and-dispatch-header-two-rows.md) | 13 补评审 Standards 2 / 3 两条非阻断合收：`HandleAdoptedExternalFundsFact` 回查交回的 `content.Version` 与信封所指相等校验——不等即停、不登记，新哨兵 `ErrAdoptedFactVersionInconsistent`（视图答非所问是装配 / 视图缺陷，不进未决名单、消费门不重投；形照 PS `ErrCarrierPickupRecordInconsistent`，`assemble.go` 名单头注记「不在」一句）；`cmd/parcel-dispatch/assemble_test.go` 资金事实正例头注「落一行」按 `0021` 起的身份行 + 版本子表与用例实际断言改写。不动 `internal/settlementaccounting/**`。2026-09-14 19:2x 通道 2 按通道 1 派单 task-e79cc5a9 自立自做（`mcp2-tails3` 基 `09596d9a`，与 lc/40 同分支）；**19:4x 完工待非作者评审进 main**：立票 `8ab6c61c`、三条 + 两票完成记录同末笔；`/tdd` 新用例 `TestASourceAnsweringAnotherVersionIsRefusedBeforeRegistering` 先红后绿；带 DSN `cmd/parcel-api` + `cmd/parcel-dispatch` PASS 129 / SKIP 0，清点零差；五条判断项见票面 Comments——**resolved，19:5x 进 main**（评审 ← 通道 6 两轴 0 阻断、本票判断项全接受；通道 1 重放三笔到 `07eaafc5` 之上，代码 tip `f1be6687`，带 DSN 全仓 110 ok） | CC 一句校验 + cmd 两处注释 | 无 |
| [24](issues/24-pp-sacc11-review-standards-tails-sentinel-comment-and-evidence-closed-set.md) | 11 评审 Standards ① ② 两条非阻断合收：`assemble.go` `evaluationRequestSubmittedUndecidedSentinels` 头注「都停在前三格之一」一句改点名三格（有卡的停 `ErrPricingInputUnavailable`，装配例正例即此格）；证据层级 S / R / P 封闭集只在领域一处——`EvidenceKind.Declared()` 导出，适配器 `declaredEvidence` 与入口 `accepted` 的两份 switch 删。零行为。2026-09-14 20:4x 通道 1 推送方自立自做（点名无人应答、其余通道全 crash；`mcp1-tails4` 基 `eca6dba1`，与 lc/41 同笔）；**20:5x 完工待进 main**（评审门单通道只能自审，票面如实写）——**21:0x 进 main**（main 未动、直接快进，代码 `5eb3caee`；带 DSN 全仓 113 ok） | PP 一个导出方法 + 四处调用、cmd 一处注释 | 无 |
| [25](issues/25-pp-sacc24-review-standards-tails-declared-comment-scope-and-sentinel-sentence-qualifier.md) | 24 评审 Standards ① 前半 + ② + Spec ① 三条非阻断合收：`Declared()` 头注「封闭集多一格时只有这里要改」收窄为「校验只有这里要改」；`assemble.go` `evaluationRequestSubmittedUndecidedSentinels` 末句全称与三分之间改分号、加限定「到得了价卡解析的那些」、两处「这一格」写成 `ErrPricingInputUnavailable` 符号名。只注释，零行为；① 后半（三句错误文本字面枚举）归 PP owner 不进本票。2026-09-14 21:1x 通道 2 按通道 1 派单 task-4edf2e4d 自立自做（评审人做自己评出的尾巴；`mcp2-tails5` 基 `0bea3610`，一笔）；**21:2x 完工待进 main**（纯注释，评审门推送方自审，票面如实写）——**21:4x 已进 main**（通道 1 重放 `b0463e1a→58ef74bd`，批 tip `09833d67` 带 DSN 全仓 112 ok / 1 FAIL——那 1 FAIL 是 main 既有 flaky `TestFundsFactVersionsAccrueAsRowsThatPointBack`，与本票零关系，立 26 修） | PP 一处注释 + cmd 一处注释 | 无 |
| [26](issues/26-cc-funds-fact-versions-test-registers-in-map-order-and-flakes-on-received-at.md) | **bug（main 既有 flaky）**：CC postgres `TestFundsFactVersionsAccrueAsRowsThatPointBack` 用 map 迭代登记 v1 / v2，顺序随机、各自一笔事务，`ListFundsFactVersions` 按 `received_at` 如实列出反序时断言「v1 走样」（推送方 `09833d67` 单跑 `-count=40` 36 / 4；作者 `5bf6eb84` 复现 36 / 4）；生产代码没错。登记顺序显式化，正序 / 反序两种到达各钉一格（反序格抽成独立用例并补 `LoadFundsFact` 交回最近接收那一版的断言）。2026-09-14 21:3x 通道 3 按通道 1 派单 task-e73efbe2 自立自做（`/tdd`；`mcp3-sacc26` 基 `5bf6eb84`，一笔）；**21:4x 完工待非作者评审进 main**：`-count=40` 两格 80/80 PASS 非 SKIP、包 `-count=1` ok、`gofmt` / `vet` 0、生产零 diff；判断项四条见票面——**22:0x 已进 main**（评审 ← 通道 4 两轴 0 阻断 / 各 1 非阻断；通道 1 重放 `5d9907aa→2b2685c3`，带 DSN 全仓 113 ok / 0 FAIL，flaky 消） | CC 一份测试文件 | 无 |
| [27](issues/27-sa-external-funds-fact-adoption-and-correction-registration-face.md) | SA 外部资金事实采用与更正的登记面：`MapExternalFundsHandler` 零生产装配，首版采用与 20 新开的更正格今天都只有测试直写能到（ADR-0137 决定四保留的那一口没有门）；要裁入口长在哪（CLI / 端点）、ADR-0085 决定四怎么读、`FactHandoff` 漏装即 panic 的装配纪律，归 SA owner。2026-09-15 10:3x 通道 6 按 20 裁决 2 顺手立；**12:3x 取证 ← 通道 5 钉 `7160fe67` 已落票面**（`af2b59db→676cc09b` 进 main）：`NewMapExternalFundsHandler` cmd/ 与 adapters 非测试零构造零调用、SA inbox 只听 PP / CC 两类信封、SA http 零写面；要裁的 3「构造期拒 nil 统一形」前提不成立——SA application 十二只 `New*Handler` 里两只拒 nil、十只不校验，且拒 nil 与要裁的 1 耦合（迫使装配处把 Mappings / Applications / Downstream 也接真）；`settlementhttp.UnconfiguredIntake` 头注「刻意不实现」与 CC 同名类型做法不同口——三条各路后果只列不选；**12:5x 通道 1 按用户「代裁」代裁（SA owner 口径），ready**：ADR-0085 读作「算」（登记事实写面与 CC 协作 / 核对同族）；入口两者都要、分两步——本票只做新二进制 `cmd/parcel-settlement-register`（形照 `parcel-customs-register`）+ 译装放 `adapters/registrationjson` + 事务壳与 Outbox 同 db + 六口全接真，端点 + 管理台写签拆后继票随完成记录立；`NewMapExternalFundsHandler` 单只改构造期拒 nil（照 16 形），十只统一形不立法——**resolved，2026-09-15 14:4x 进 main**（第六批，重放 `e1525530…bb82df21 → 0a89df09…c79a29d3`，批 tip `de822820`；评审 ← 通道 2 两轴 0 阻断 / Standards 2 非阻断 / Spec 2 非阻断；接手链：通道 3 13:1x 认领 → 五笔代码 13:11–13:27 推齐、带 DSN 904 PASS → 13:3x 写完成记录时 crash → 推送方封存 `5fb936a3` 并按 diff 代写完成记录 `03e6b951`（`bb82df21` 修正误替换）；判断项七条见票面；Spec ① SA 信封 ID 无长度上界另立 [32](issues/32-sa-external-funds-fact-envelope-id-has-no-length-bound-and-a-rejected-envelope-folds-into-a-continuation.md)，Standards 尾巴合进 [33](issues/33-sixth-batch-review-standards-tails-test-currency-code-count-words-sentinel-count-and-unanchored-byte-claim.md)；`de822820` 带 DSN 全仓 115 ok / 0 FAIL；清点 SA 98→99 / 79→80、`cmd/` 61→63 / 85→88） | SA（新二进制 + `application` 一只构造器 + `adapters/registrationjson`；`cmd/parcel-api` / admin-web 本票零 diff） | 无 |
| [28](issues/28-sa-sacc20-review-standards-tail-save-header-states-ordered-versus-concurrent-answers.md) | 20 评审 ← 通道 3 Standards ① 尾巴：`Save` 头注 / `FundsFactAlreadyAdopted` 注 / `CorrectFact` 分支注补「顺序到达`未受理` / 并发到达`已采用`」不对称一句；零行为只注释。2026-09-15 11:2x 通道 1 立——**resolved，2026-09-15 12:3x 进 main**（第四批，重放 `d6b1f773→981a6f15` / `88ca0a2e→ee3ec953`，批 tip `676cc09b`；评审门推送方自审（纯注释：三文件九增两删滤 `//` 后为空）；此前 12:1x 通道 4 认领即完工，task-1b28ece5，分支 `mcp4-sacc28` 基 `7160fe67`；判断项六条见票面；批 tip 带 DSN 全仓 113 ok / 0 FAIL；清点零差） | SA（三处注释） | 无 |
| [29](issues/29-cc-handoff-envelope-id-exceeds-framework-limit-and-rederive-path-swallows-handoff-failure.md) | CC 三只交接信封 ID = 租户 / 类型 / 范围 / 税费 / 资金 / 64 位指纹串接，真长度引用下超框架上限被 `EnqueueOnce` 拒收（19 原作者实测 138 字节）；`handOffVerification` 把失败折成续办引用、核对不翻，重派路上 `Lineage.Result.HandoffReference()` 无读者——核对行提交、信封没发、消费入账成功、无人知道；做法候选 ID 指纹化 / 缩短 / 校验前移 × 响法未决 / 硬失败 / 登续办，只列不选。2026-09-15 12:4x 通道 1 立（19 评审 Spec ② + 判断项 ③ ④ 转记）；**12:5x 通道 1 按用户「代裁」代裁（CC owner 口径），ready**：上限量实 `eventing.MaxEventIDLength = 128`（`Envelope.Validate` 确定性拒）；三只 `*EventID` 一次同改成「口名前缀 + `sha256` 十六进制」定长形，载荷五维照旧；重派路交接失败分两格整笔回滚——依赖不可用归未决重投、信封不合法归硬失败（新哨兵不进未决集合）；人重核路 05 兜底不动；SA 靠采用登记册五维主键守幂等，真库用例钉住——**resolved，2026-09-15 14:4x 进 main**（第六批，重放 `976966ee→c4be9985` / `e8d73f09→34daa57a` / `b2229462→d1ebc8c1` / 记录笔 `5d2a6807→cbe83f8a`，批 tip `de822820`；评审 ← 通道 6 两轴 0 阻断 / Standards 2 非阻断 / Spec 5 非阻断，**19 评审 Spec ② 在新代码上不再成立**；接手链：13:0x 派通道 6 → crash 零提交 → 13:1x 改派通道 2 → 读到单前 crash → 13:2x 二次改派通道 4，三笔代码 13:36–13:47、带 DSN 886 PASS，通道 4 会话又重置后新会话 14:28 按 diff 补完成记录；`fingerprintEventID` 三只同改定长 90 / 82 / 89 ≤ 128；重派路依赖不可用 → 未决重投、信封确定性拒 → `ErrDutyVerificationHandoffRejected` 硬失败整笔回滚且不进未决名单；`DutyReconciliationResult.HandoffError` 新格；判据 (4) 五维幂等真库格走生产路；判断项九条见票面——③ Subject / PartitionKey 能超 512 未哈希、靠硬失败兜，要不要哈希归 CC owner；Spec ④ 其余五只 `*EventID` 串接形另立 [34](issues/34-cc-five-remaining-event-ids-still-concatenate-references.md)；Standards 尾巴合进 [33](issues/33-sixth-batch-review-standards-tails-test-currency-code-count-words-sentinel-count-and-unanchored-byte-claim.md)；`de822820` 带 DSN 全仓 115 ok / 0 FAIL；清点 CC 零差） | CC（三只 handoff 适配器 + 核对编排 + 重派编排 + `cmd/parcel-dispatch` 第三格；SA 只加真库用例） | 无 |
| [30](issues/30-cc-sacc19-review-standards-tails-unordered-test-double-and-hand-built-fixture-keys.md) | 19 评审 ← 通道 6 Standards ① ② 尾巴：`rederiveStores.ListVerificationsByFundsFact` 替身遍历 map 无序、不守端口「时刻升序、同刻按指纹」口径；`duty_registers_test.go` 两处 seed 手拼键不走 `fundsVersionKey`；只测试文件零生产改动。2026-09-15 12:4x 通道 1 立——**resolved，2026-09-15 13:1x 进 main**（第五批，重放 `c9c9b8f5→3dc8f94c`；评审门推送方自审；此前 12:5x 通道 2 完工，task-38d5d3a7，分支 `mcp2-sacc30` 基 `e1ab9fb5` 单笔三文件 +100 −11；做法 2 做了——新格钉 19 判断项 ②「同刻两版承指纹小者」，red 后 green；两只 seed 交回 `fundsVersionKey` 的键、顺手消第三处字面键；判断项四条见票面；`3dc8f94c` 带 DSN 全仓 113 ok / 0 FAIL；清点零差） | CC（两份测试文件） | 无 |
| [31](issues/31-sa-external-funds-fact-registration-endpoints-and-admin-write-face.md) | SA 外部资金事实采用与更正的**在线登记面**（27 裁决 2 第二步）：`settlementhttp` 第一份命令文件（两只 Intake + 两只 Registrar + `MethodPost` 端点体 + 封闭响应）、`cmd/parcel-api/assemble_settlement_registration.go`、`endpoints.go` 两行 `UnconfiguredIntake{}` 起步、`UnconfiguredIntake` 加两方法并改头注「将来长出命令 Intake 时本类型刻意不实现」那句（`IsolatedOperationsReadIntake` 同句原样成立、编译期排除保持）、admin-web 写签两阶段（ADR-0085 决定三三态）；消费 27 落的 `adapters/registrationjson` 与拒 nil 的 `NewMapExternalFundsHandler`。2026-09-15 13:3x 通道 3 随 27 完成记录同笔立 ready；14:4x 27 进 main、可派——**resolved，2026-09-15 16:2x 进 main**（第八批，重放 `4c466db4…17067468 → 25d2855a…e9c2f7f8`，批 tip `0943f7da`；评审 ← 通道 2 两轴 0 阻断 / Spec 1 非阻断归 32；此前 15:2x 通道 3 完工，task-cbea007d，分支 `mcp3-sacc31` 基 `93840328` 六笔、代码 tip `08e82a88`；续办引用格折 500 `HANDOFF_NOT_SENT`、`FUNDS_UNDECIDED` 全折 `NO_ANSWER_FORMED`、`ADOPTED` 201 其余 200、两册选册表单；判断项八条见票面；`0943f7da` 带 DSN 全仓 115 ok / 0 FAIL；清点 SA 99→100 / 80→81、`cmd/` 63→64 / 88→89、端点 122→124 随作者笔） | SA `adapters/http` + `cmd/parcel-api`（共享接线文件，占号）+ `apps/admin-web` | 无（27 已进 main `de822820`） |
| [32](issues/32-sa-external-funds-fact-envelope-id-has-no-length-bound-and-a-rejected-envelope-folds-into-a-continuation.md) | **bug（resolved · 已进 main `3fe6bedf`，SA owner）**：SA 采用信封 ID = `externalFundsFactPartitionKey + "/" + version`，三引用只经 `newRequiredValue` 非空门、无长度上界，超 `MaxEventIDLength = 128` 时 `Validate` 确定性拒 → `handOffFact` 折成续办引用 → 版本行已提交、信封永不出，CLI「重跑同一命令补发同一封」在此因下为假；29 只改 CC 三只，SA 这只不在内，27 让它第一次能从生产到达。做法候选：指纹化（照 29 裁决 1，要量 CC 消费门去重）/ 不合法信封不折续办整笔回滚 / 只改 CLI 的话（防重开）。2026-09-15 14:4x 通道 1 立（27 评审 ← 通道 2 Spec ① 转记）；**15:07 取证 ← 通道 2 钉 `93840328` 已落票面**（`d2af6ba1→c84e81bd` 进 main）：bento `Store.Enqueue` 先 `Validate` 再任何 SQL，`handOffFact` 不分错误种类一律折续办不返 error——单事务 CLI 下「行已落、信封未出、续办非空」**只由 Go 侧确定性错误到达**（`ErrInvalidEnvelope` / marshal / 键缺席），依赖故障会把事务打进中止态 → `WithinTransaction` 返 error → 未决 3 且版本行不落，故 27「重跑补发」在它唯一能到的因下恒假；CC 消费门有 ID 之外的幂等键（0021 主键租户 / 事实 / 版本 + `ReceiveFundsFact` 同版同内容答`已存在`），换 ID 形后同版本第二封零新行；`fingerprintEventID` 未导出且 SA 导 CC 包被 `boundaries_test` 挡，共用位先例 `internal/platform/outboxintent`；**15:2x 通道 1 按用户「自决」代裁（SA owner 口径），ready，Blocked by 31 进 main**：ID 指纹化、helper 提炼到 `outboxintent.FingerprintEventID` 导出、CC 三只改引零行为；`handOffFact` 只对 `ErrInvalidEnvelope` 分格——包 `ports.ErrFundsFactHandoffRejected` 返错整笔回滚、CLI 用法格 1 点名原因、端点 4xx，其余错误照旧折续办（事务入口下今天到不了，判断项写实）；CC 消费门幂等靠 0021 主键 + `已存在`，真库钉；完成判据七条写实；**16:2x 31 进 main（`0943f7da`），Blocked by 解、可派**；31 评审 Spec 非阻断 ①（admin-web「重发同一份即补发」词条）归本票改口；**19:5x 34 进 main（`6683f989`）：`outboxintent.FingerprintEventID` 已导出、CC 三只已改引，本票做法写实 (1)(2) 落空只引、判据里 CC 命中随 34 的数**（34 评审 Standards ① 那句 `ports` 口约头注同族，本票动 SA `ports` 头注时可顺手核 CC 那句）；**2026-09-16 10:5x–11:5x 通道 1 认领 → 完工 → 进 main**（用户「sa-cc/32 收掉」；作者 = 推送方 = 同一会话，隔离树分支 `mcp1-sacc32` 基 main `21bb1310` 直接 ff、SHA 不换：`0209195e` 认领 / `58edc520` 裁决 1 + 2 (a) 适配器 / `71ff0d3e` 编排 `handOffFact` 改 `(string, error)` / `57a0d7dd` CLI 用法格 1 / `90da4da2` 端点 400 `HANDOFF_ENVELOPE_REJECTED` + admin-web 词条 / `e91824b3` 真库两 ID 幂等 + CC 夹具头注 / `5a8080db` `cmd/parcel-api` 一处漏网改 ID / `3fe6bedf` 票面；远端 main = `3fe6bedf`）：**评审为推送方自审**（点名 11:13–11:17 零应答、`/code-review` 子代理两次鉴权错），Standards 0 阻断 / 2 非阻断（测试口名字面三处、`adoptedFact` 与 `existingFact` 同形）、Spec 0 阻断 / 1 判断项（判据 (3) 在生产依赖图上是间接观测）；tip 带 DSN 全量 115 ok / 0 FAIL / 16 无测试 / 0 cached（127 s）；清点零 diff（票面预报 +1/+1 已随 34 落）、迁移零 diff；34 残差那句 CC `ports.ExternalResultHandoff` 口约头注**未顺手**（本票只动 SA `ports`），仍归 34 尾巴 | SA（`external_funds_fact_handoff.go` + `map_external_funds.go` `handOffFact` + `cmd/parcel-settlement-register` 一格 + admin-web 词条一句） | 无 |
| [33](issues/33-sixth-batch-review-standards-tails-test-currency-code-count-words-sentinel-count-and-unanchored-byte-claim.md) | 第六批评审 Standards 尾巴合收（27 ← 通道 2 ① ② + Spec ②、29 ← 通道 6 ① ②）：`parcel-settlement-register` / `registrationjson` 夹具币种 `EUR` → 测试码 `XTS`；`registrationjson` 头注「采用四格」去数；`assemble_test.go` 一处头注「两只未决哨兵」实为三只、改「名单里每一只」；`fingerprintEventID` 头注「138 字节」锚 SHA 或去数。只夹具字面与注释，零行为。2026-09-15 14:4x 通道 1 立 ready——**resolved，2026-09-15 15:1x 进 main**（第七批，重放 `60721f60→3e0f1230`，批 tip `c84e81bd`；评审门推送方自审；此前 15:0x 通道 4 完工，task-c3f6cfd9，分支 `mcp4-sacc33` 基 `93840328` 单笔六件 + 票；138 字节锚 `a0cb6fef` 并写进四个引用字面；`translate_test` 多一句 `XTS` 是 ISO 4217 测试码；判断项四条见票面；`c84e81bd` 带 DSN 全仓 115 ok / 0 FAIL；清点零差） | SA / cmd 三份测试 + `registrationjson` 两处注释 + CC `assemble_test.go` / `external_result_handoff.go` 各一句 | 无 |
| [34](issues/34-cc-five-remaining-event-ids-still-concatenate-references.md) | **bug（resolved，CC owner）**：CC 其余五只交接信封 ID（`customsCaseEventID` / `manifestEventID` / `declarationSubmissionEventID` / `caseClosureEventID` / `externalResultEventID`）仍是引用串接形，与 29 修掉的三只同一潜伏缺口；`fingerprintEventID` helper 已在，做法候选五只一次同改 / 只改今天能从生产到达的；各消费方按信封 ID 去重时换形前后的幂等键要量；五只的失败响法立票时没读。2026-09-15 14:4x 通道 1 立（29 评审 ← 通道 6 Spec ④ 转记）；**15:05 取证 ← 通道 6 钉 `93840328` 已落票面**（`aec424a6→760332c7` 进 main）：五只里只有 `externalResultEventID` 有生产装配（`/customs/external-results`）且其事件类型零消费者，其余四只 `cmd/` 零构造；五只都不分格包 `ErrHandoffEnvelopeRejected`、失败全折续办、都不在重派路；`customsCaseEventID` / `externalResultEventID` 把 ID 直接当分区键；有消费者的两只 → VE 有 `FactKey` + 摘要幂等；grep 漏两只——`follow_up_handoff.go` 四维串接作 ID（第六只）、`restriction_handoff.go` 单一 `RestrictionID` 无上界；**15:3x 通道 1 按用户「自决」代裁（CC owner 口径），ready**：范围七只一次同改；`FingerprintEventID` 由本票提炼到 `outboxintent` 导出、29 三只改引删私有（32 只引）；ID 与分区键解耦——两只把 ID 当分区键的改取原可读串接、一字不变；确定性拒分格只做 `/customs/external-results` 那一路（照 32 裁决 2，4xx），其余六只入口未接不动、**残差记此**：接它们入口的票要同时照 32 裁决 2 分格；VE 幂等真库钉两只；完成判据七条写实——**resolved，2026-09-15 19:5x 进 main**（通道 6 代推送方，通道 1 两任 crash；分支 `mcp6-sacc34` 基 `0b3f0831`，代码 tip `e2941cde`、票面 tip `2c0aff8d`；重放 `ed1da549→6f4e4f85`（裁决 2：`FingerprintEventID` 导出 + 29 三只改引）/ `f07d65dd→cd8df62c`（裁决 1 / 3：七只口名常量 + 全维哈希，`customsCasePartitionKey` / `externalResultPartitionKey` 抽出保留可读串接）/ `74c44ac9→bc09d668`（裁决 4：`external_result_handoff` 分格包 `ErrHandoffEnvelopeRejected`，`handOff` 返 `ErrExternalResultHandoffRejected`，端点 400 `HANDOFF_ENVELOPE_REJECTED` 按 ADR-0022）/ `5646c6ad→033b0832`（判据 (1)(2) 补齐 + 评审 Spec ① / Standards ②）/ `e2941cde→650b6a66`（判据 (4) 两只 `Test…UnderTwoEnvelopeIDsDerivesOnce`：第 2 拍 3 封 = 重发那封 + 首封引出的两封派生信封被客户视图链定稿，改按 `event_id` 查 `status` + `countOutboxOfType(derived)==2`；两文件头注「不登记 tracking-projection.derived」核为 `49a2ab03` 前旧句改准）/ 票面 `2c0aff8d→6683f989`，清点 `d5b74eb7`（platform 17→18 / 16→17，CC 零变），批 tip `6683f989`；评审 ← 通道 3 裁决 1–3 两轴 0 阻断（17:1x）+ 通道 2 全支两轴 0 阻断（19:5x：Standards 2 非阻断 = `ports.ExternalResultHandoff` 口约头注缺 29 那句 / `Handle` 头注尾句未随分格，1 口味；Spec 1 非阻断 = 判据 (4) 格落同包投影用例文件而非 `assemble_test.go`）；带 DSN 全量 115 ok / 0 FAIL；**残差**：其余六只 `handOff*` 未分格归接入口的票（上句已记）、三份邻居用例头注同一旧句、评审两句注释归尾巴票或随 32；32 由此解阻） | CC（`adapters/postgres` 七只 handoff + `receive_external_result.go` + `customshttp` 外部结果端点 + 测试）+ `internal/platform/outboxintent`（新文件） | 无（32 晚于本票、只引） |
| [35](issues/35-admin-web-duty-verification-fixture-misses-funds-version-and-tsc-is-red-on-main.md) | **bug（既有，main 上 `tsc --noEmit` 红）**：19 给 admin-web `DutyVerificationRecord` 加了必填 `fundsVersion`，`register-rows.test.ts` 的 `verification()` 夹具没跟 → TS2322、`tsc` 退 2；CI 没有 admin-web 类型检查步，自 19 进 main（12:22）起无人看见，31 作者跑判据 5 时撞到、推送方在 `0b3f0831` 复现。做法：夹具加 `fundsVersion`，不改类型。只一份测试夹具零行为。2026-09-15 15:4x 通道 1 立 ready——**resolved，2026-09-15 16:2x 进 main**（第八批，重放 `2a1f4138→0943f7da`；评审门推送方自审；此前 15:3x 通道 5 完工（改派，原派通道 4 crash 零开工），task-1d4e6bef，分支 `mcp5-sacc35` 基 `15433da2` 单笔 +2 行；缺席的必填字段是 `fundsVersion` **和** `procedure` 两个、tsc 一次只报第一个，红自 22 `2e411f63` 起而非 19；`0943f7da` 上 `tsc --noEmit` 退 0、基线退 2）。**另记归用户**：要不要给 CI 加 admin-web `tsc` 步（计费） | admin-web 一份测试 | 无 |

**留空位、不立票的一件**：放行层的代码映射 `PAR-CUS-01/02`（`receive_external_result.go` 注释「真实代码映射属实例半边，没有它接入侧拆不出种类」）——登记册待提供，到位后接入侧译装出 `ReleaseContent`，编排不改。它在这里只占一行，不成票。

## 边界（七票共用）

- 七件全是**机制半边**：让事实能进、能出、能记。真实资金事实、真实凭证、真实付款条件与关联规则、真实程序的门禁清单属实例半边（`PAR-CUS-*` / `PAR-SET-*`），一律不写默认值。
- 不拆两侧已落的形状：SA 的 `map_external_funds.go` 四格负向代数、CC 的 `reconcile_duty_payment.go` 三方法与三表分层（UC-CC-009「七层对象必须分离」）都照用；本目录只接线、只补记、只开入口。
- 信封形状照仓内既有 handoff（`ports.*HandoffIntent` + `adapters/postgres/*_handoff.go` + `EnqueueOnce`），分区主体在各票「要裁的」里单列，不在 spec 代裁。
- 不改任何已 resolved 票的 Status；mech/06、mech/07 的「不在本票」节原样留着，本目录是它们的去处。

## Comments

- 2026-09-10 · 通道 4：立目录（task-9880bbc9）。七张子票的「要裁的」条数汇总见完工报；为零的由推送方直接转 ready-for-agent。
- 2026-09-10 · 通道 5（task-9a2ff746）：十三条要裁的分类（task-b941ce87：A 7 / B 5 / C 1）→ 用户 17:0x 授权后 B 类四条落 ADR-0137（六条越权风险点供 CC / SA owner 复核），A 类七条与 01-1 (c)、07-1 B 照推送方裁决写入各票「裁决」；立 08 / 09 / 10、子票表补三行。**只写 .md，未动代码。**
- 2026-09-10 · 通道 5（task-a93cb825）：08 两条（走信封；铸造 ID + 自然键唯一）与 10 一条（读签挂同族页：凭证 → customs-cases、协作与核对 → customs-restrictions）由通道 1 推送方代裁、写入并转 ready-for-agent；PP 无 inbox 先例，另立 11（PP 侧消费者，draft 要裁一条 PP 入口形状）作 08 的 Blocked by。本目录的缝由此跨到 PP 一角（01 / 08 / 11 是 SA↔PP 那条边）。**只写 .md，未动代码。**
