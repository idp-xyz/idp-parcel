# 39 失效版本到达后，已据前版收寄形成的非取消终局怎么重派生——先立 ADR，再谈代码

Category: enhancement
Status: resolved——2026-09-14 18:3x 通道 3（接手会话；作者为前一任通道 3 会话，15:52 提交 ADR-0138 草案 `0a25bb2a` 后 crash，原单 task-ea9c8da8 由推送方结 failed「承接方换人」，通道 1 派单 task-31d06b90 令本会话续做）：分支 `mcp3-lc39` 基远端 main `f04ad816`（main 自基起只有 `.scratch` 簿记，未动 `docs/adr/README.md` 与 lc spec，不必 rebase），六笔——`6ad26484` 认领、`0a25bb2a` ADR-0138 草案（`Status: Proposed`）、`2e66c0f3` README 行 + ADR-0135 / lc/25 追句 + lc spec 39 行、`30130fda` 完成记录、`2a1493f5` 收通道 2 评审 Standards ①–⑤ 改 0138 正文（仍 Proposed，Decision 语义不动）、本簿记笔（评审照录 + 判断项追加）；纯 .md，`go build ./...` 退 0；**非作者评审 ← 通道 2** 钉 `0a25bb2a`（取证 `f04ad816`）：**Standards 0 阻断 / 6 非阻断 · Spec 0 阻断 / 2 非阻断**，①–⑤ 已改于 `2a1493f5`，⑥ 与 Spec 两条不改稿、照录 Comments「评审 ← 通道 2」并列进完成记录「判断项」；**待推送方合并进 main**（本通道换会话仍算作者一侧；`2e66c0f3` 之后三笔未经非作者评审，推送方定要不要补看）；推荐候选、实现票的形与判断项见 Comments 末「完成记录」与「评审收口」。此前 in-progress——2026-09-14 16:1x 通道 3 按通道 1 派单 task-ea9c8da8 认领（`/domain-modeling`，PS owner 口径；分支 `mcp3-lc39` 基远端 main `f04ad816`，树 `D:/tops/idp-parcel-mcp3-lc39`；ADR 序号按 `docs/adr` 目录重取为 `0138`）。此前 ready-for-agent——2026-09-14 10:2x 通道 1 推送方按用户 10:1x「授权代裁」立票：lc/25「要裁的」2 是设计题不是一句话能裁的（ADR-0135 越权风险点 4 明写「归 PS owner，随 lc/25 实施时判」，lc/25 实施时按红线落了显式未决哨兵），本票的产物是**一份 ADR 草案**（`Proposed`），不是代码；ADR 被接受后实现另立票。取证锚 main `6bbf2bf0`。只写票面，未动代码、未动 `docs/**`。
Blocked by: 无（lc/25 与 lc/31 均已进 main；TF 半边失效版本的入队与 PS 按版本取回都在）
Type: grilling → ADR 草案（走 `/domain-modeling`，PS owner 口径；实现不在本票）

## 缺口（取证于 `6bbf2bf0`，逐符号名）

- TF 会对同一条「实际承运商首次有效收寄」事实发**失效版本**——依据被源更正为不再表达收寄（ADR-0135 决定六）。PS 侧 `internal/parcelshipment/adapters/transportfulfillment/judge_on_carrier_first_effective_pickup.go` 收到失效版本时停在未决哨兵 `ErrVoidedCarrierPickupRederivationUndecided`：不吸收、不重派生、不判、零写入；头注写明「owner 裁定后（ADR-0117 同来源更正的采用版本链是可循的形）在本适配器接上重派生，这一格随之消失」。用例 `judge_on_carrier_first_effective_pickup_test.go` 钉住「失效版本 → 未决且零写入」。
- 也就是说：一笔已据某版收寄判成「面单渠道服务终局已形成」的委托，在那版收寄失效之后，今天**永远停在未决**——消费门会按未决重投，直到失败预算耗尽。这是 AGENTS.md 红线「未确认规则保持显式未决」的正当停格，但停格本身不是答案。
- `LabelServiceFinalOutcome` 的各格（lc/38 条 2 已逐名点过）里没有「依据失效」这一格；`LabelServiceFinalAdopted` 形成后的采用记录（`FinalOutcomeStore` 那一处当前有效终局）今天没有「回退」或「被后版替代」的形。

## 语言从哪里来

- PS `CONTEXT.md`「实际承运商首次有效收寄即形成终局」——终局的**依据**是那条收寄事实；依据失效，终局的依据就没了，CONTEXT 没有说这时终局是什么。
- ADR-0135 决定六（TF 发失效版本）与越权风险点 4（「已据收寄形成的非取消终局在依据失效后是否形成新的采用判断版本回指前版（ADR-0117 的形）还是另有格——归 PS owner」）。
- ADR-0117：同来源更正在采用口形成新的采用判断版本并回指前版，AT-PS-049 不动——它裁的是**更正**（新值替旧值），不是**失效**（旧值作废、没有新值）；能不能直接循它，是本票要回答的第一问。
- ADR-0029：结果按恢复动作分格——「依据失效后重派生」的恢复动作是什么（等新收寄？回到未终局？形成一版「终局撤回」？）决定它该是哪一格。

## 要做的（ADR 草案，不写代码）

1. **`/domain-modeling`**：以 PS owner 口径，把「依据失效后的终局」放进 PS 统一语言里试三个场景——(i) 失效后再无新收寄；(ii) 失效后同一事实又来一版有效收寄（替代版本，ADR-0135 决定六另一支）；(iii) 失效到达时该委托已有受控关闭生效。每个场景写出运营看到的是什么、账上（`FinalOutcomeStore`）留下的是什么、下游（VE 投影 / SA 采用）收到的是什么。
2. **候选至少两条，各写反方**：甲 · 循 ADR-0117——失效版本形成一版新的采用判断（结论「终局依据失效 → 未终局」或「终局撤回」）回指前版，历史不改；乙 · 失效不是更正——终局一旦形成不因依据失效而退，只在账上追加「依据已失效」的标记并交人核（与 ADR-0135 决定四「不构成」不落版本的精神对照）；若还有丙，写。
3. **落 ADR 草案** `docs/adr/0138-*.md`（序号开工时按目录重取；`Status: Proposed`），Context 锚 SHA、Decision 按 ADR 形写、越权风险点单列；`docs/adr/README.md` 加一行；lc/25「要裁的」2 与 ADR-0135 越权风险点 4 各追一句指向它（追加不改写）。
4. **不做**：不改 `judge_on_carrier_first_effective_pickup.go` 与任何 `internal/**`；不改 PS `CONTEXT.md`（ADR 若要求改词条，写进 ADR Consequences，由实现票随 ADR 接受一并落）；不裁 TF 半边（ADR-0135 已裁）。

## 红线

- 未确认规则不写死：ADR 草案是 `Proposed`，哨兵 `ErrVoidedCarrierPickupRederivationUndecided` 在 ADR 接受与实现票落地之前一字不动。
- 不引行号、不数别处的东西；引 CONTEXT / ADR 用引文或决定号。
- 不为任何租户预设「失效后怎么办」的取值——这是机制的形，不是实例参数。

## 完成判据

1. `docs/adr/0138-*.md`（或重取的号）存在、`Status: Proposed`、Decision 至少覆盖「要做的」1 的三个场景与 2 的两条候选各自的反方；README 有行。
2. lc/25「要裁的」2 与 ADR-0135 越权风险点 4 各有一句指向新 ADR（`git grep -n '0138' -- .scratch/label-channel-service-first-release/issues/25-* docs/adr/0135-*` 各 ≥ 1，序号按实取）。
3. `git diff --stat main -- internal/ cmd/ apps/ migrations/` 为空。
4. 完成记录写清：推荐哪条候选、为什么、实现票该长什么样（地盘、判据、要动的哨兵与结果格）——推送方据此立实现票。

## 地盘

`docs/adr/`（新文件 + README 一行 + ADR-0135 追一句）、本票面、lc/25 票面「要裁的」2 一句、lc spec 子票表一行。**不动** `internal/**`、`cmd/**`、`docs/domain/**`。

## 参照

[lc/25](./25-external-carrier-first-pickup-triggers-label-final-judgment.md)「要裁的」2 与「裁决」；[lc/31](./31-carrier-first-effective-pickup-fact-registry-and-handoff.md)；[ADR-0135](../../../docs/adr/0135-carrier-first-effective-pickup-is-a-judged-control-fact-with-its-own-registry-and-enters-the-segment.md) 决定六 / 越权风险点 4；[ADR-0117](../../../docs/adr/0117-same-source-correction-forms-a-superseding-adoption-version-chained-to-the-current-responsibility-start.md)；ADR-0029；PS `CONTEXT.md`「面单渠道服务终局」一节；`internal/parcelshipment/adapters/transportfulfillment/judge_on_carrier_first_effective_pickup.go` `ErrVoidedCarrierPickupRederivationUndecided` 头注；`internal/parcelshipment/application/judge_label_service_final.go` `LabelServiceFinalOutcome`。

## Comments

- 2026-09-14 10:2x · 通道 1 推送方：立票并直接 ready（用户 10:1x「授权代裁」；这一条我**不一句裁**——它是 ADR-0135 明写留给 PS owner 的设计题，一句话裁掉等于在票面里藏一条难逆转决定）。**只写票面，未动代码与 `docs/**`。** 能力边界：读过哨兵头注与 lc/25「要裁的」2 / ADR-0135 越权风险点 4 / ADR-0117 摘要；没读 `FinalOutcomeStore` 的采用记录形与 VE / SA 对终局的消费路径——「要做的」1 的场景 (ii)(iii) 与下游那半靠作者量。
- 2026-09-14 18:3x · 通道 3（接手会话；作者为前一任通道 3 会话，15:52 提交 `0a25bb2a` 后 crash；通道 1 派单 task-31d06b90 令本会话续做，原单 task-ea9c8da8 由推送方结 failed「承接方换人」）——**完成记录，转 resolved，待非作者评审进 main。** 分支 `mcp3-lc39` 基远端 main `f04ad816`（main 自基起只有 `.scratch` 簿记、`docs/adr/README.md` 与 lc spec 未动，重放为纯 ff 或一次无冲突 merge 由推送方定），四笔：
  1. `6ad26484` docs(scratch)——认领：本票 Status → in-progress、lc spec 39 行（前一任）。
  2. `0a25bb2a` docs(adr)——[ADR-0138](../../../docs/adr/0138-voided-carrier-pickup-voids-the-label-service-final-as-a-third-ledger-record-and-rejudges-on-remaining-basis.md) 草案（`Status: Proposed`，取证锚 `f04ad816`）：Context（账上今天的形 / CONTEXT 已写「终局失效」只是没写账上的形 / 第一问 ADR-0117 能否直接循 / ADR-0029 的尺 / 三场景 (i)(ii)(iii) 各写运营看到、账上留、下游收到）、候选与反方（甲乙丙丁）、Decision 一～八、Consequences、Alternatives 另两条、越权风险点六条、Links（前一任）。
  3. `2e66c0f3` docs(lc/39 之三)——`docs/adr/README.md` 加 0138 一行（形照 0137 行、只加一行不动邻行；行首标「草案：Status 为 Proposed，尚非依据」照 ADR-0071 那行的成例，因为该节标题是「已接受决策」）；ADR-0135 越权风险点 4 追一句「→ 已由 ADR-0138 以 Proposed 起草……接受与否归 PS owner」（原句不改）；lc/25「要裁的」2 追一句指向 0138 与本票（原句不改，哨兵不动那句一并写明）；lc spec 39 行 → 完工待评审。
  4. 本记录笔——本票 Status → resolved + 本完成记录。
  **完成判据**：1 ✓ `docs/adr/0138-voided-carrier-pickup-voids-the-label-service-final-as-a-third-ledger-record-and-rejudges-on-remaining-basis.md` 存在、`Status: Proposed`；Decision 八与 Context 场景段覆盖「要做的」1 三场景（(i) 失效后再无新收寄 / (ii) 同一事实又来一版有效收寄，含乱序支 / (iii) 受控关闭已生效），「候选与反方」覆盖「要做的」2 的甲、乙各自反方并多出丙、丁；README 有行（笔 3）。2 ✓ 于 `2e66c0f3`：`git grep -c '0138' -- 'docs/adr/0135-*' '.scratch/label-channel-service-first-release/issues/25-*'` 两文件各 1，均 ≥ 1。3 ✓ 于 `2e66c0f3`：`git diff --stat f04ad816 -- internal/ cmd/ apps/ migrations/ docs/domain/` 为空，`git diff --stat main -- internal/ cmd/ apps/ migrations/` 亦为空（票面写 `main`，两种量法同为空）。4 ✓ 即下三段。
  **推荐哪条候选、为什么**（按 0138 Decision 如实转述，不另造）：**取甲的纪律半、弃其语义半**——失效不形成终局的新版本，形成终局采用账 `FinalOutcomeRecord` 的**第三种记录「终局失效」**（与「终局」「不采用」并列，不是第三种终局也不是不采用）：带失效锚（被失效的那一代 `F@v1`，取自失效版本回指的前版）、`prior_version`（当前终局恰以那一代为依据时指它并翻其 `is_current`，否则缺席、不翻任何行）、原因 `SOURCE_VOIDED/<失效版本>`、`is_current` 恒假；前版一字不改；包裹回到「当前无有效终局」并在**同一拍同一笔**以「无收寄」为输入重判一次（形成的终局是首派生、不挂 `prior_version`；只有真的翻了前版才重判）；迟到的被失效那一代按锚拒绝形成终局（不采用 `EVIDENCE_VOIDED/<失效版本>`）；同一事实的新有效版本照今天的首派生 / 同种类重派生走、不新造格。为什么不是原样的甲：ADR-0117 的语义半是「新值替旧值」，`Rederive` 要一份新的 `ResponsibilityOutcome` 而失效版本给不出（它不表达收寄，构造门的执行证据与业务时间一格填不出）；「结论未终局的终局版本」若为 current 撞 `final_outcome_current_only_finalized`、不为 current 就不是「新的采用判断版本」而是另一种记录；纪律半（按来源声明的关系回指成链）能循且该循，写法照 `final_outcome` 既有的翻当前位插新行（AT-PS-063），不照 0117 在 `intake_adoption` 上的「只插不改」（`2a1493f5` 按评审 ⑤ 改口）。**乙否决**：PS CONTEXT 已写「源事实更正或事件有效性变化使终局失效后……依据剩余有效决定历史重新派生」，「终局失效」是 CONTEXT 里已有的状态转换，乙让终局站着就是拒绝执行 CONTEXT；乙依赖的「终局一旦形成不回退」在 CONTEXT 里只有「不得回退为**取消**」一句，撑不起「不得失效」；TF 自己已落失效版本（ADR-0135 决定六），PS 若让据它形成的终局继续有效就是认 TF 已否认的事实；「交人核」把机制上可判的事推给运营且核完要做什么乙没定义。乙对的一半（失效不是取消、不是重开：不复活旧交易、不自动形成重开决定、不追溯生效被拒的重开）进了决定五。**丙否决**（不落账、读时去 TF 问链尾）：跨上下文读进每一次终局读取违 ADR-0005 / ADR-0025，VE 收不到任何信号，「当前终局」随 TF 链尾跳动、无一行可审计。**丁否决**（删或 UPDATE 前版行的 `finalized`）：CONTEXT「不得静默改写原决定历史」、AT-PS-063「历史不删」直接否决。
  **实现票该长什么样**（PS owner 立，ADR-0138 被接受为前提；本段是形、不是裁决）：**地盘**——PS `ports/ports.go` 的 `FinalOutcomeRecord` 由二居其一改三值（终局 / 不采用 / 终局失效）并加失效锚，`FinalOutcomeStore` 加按（租户，包裹，证据）查失效记录的读口；`domain/final_outcome.go` 加失效锚值对象与「终局失效」记录形（不在 `ParcelFinalOutcome` 上长「我不是终局」的格）；`application/form_parcel_final.go` 加「终局失效」命令（翻前版 `is_current` + 插失效行 + 同一拍重判，同一笔事务、后半失败整拍回滚）与「以证据为锚查失效记录」的采用前门（中 → 不采用 `EVIDENCE_VOIDED/<失效版本>`）；`application/judge_label_service_final.go` 的 `LabelServiceFinalOutcome` 长第六格 `LABEL_SERVICE_FINAL_VOIDED` 并加「失效 → 重判」入口；`adapters/postgres/final_outcome.go` 的 `Save` 对第三种行形的翻旧插新与新读口；`migrations/parcel_shipment/` 新迁移（序号实施时按目录重取）改 `final_outcome_result_shape` 与 `final_outcome_rederivation_coherent` 两条 CHECK 各加一支，`final_outcome_current_only_finalized` 不动（失效记录永不 current）；`adapters/labelfinal` 的处理方核多一个入口与消费翻译表一行；`adapters/transportfulfillment/judge_on_carrier_first_effective_pickup.go` 的 `CarrierPickupVoided` 分支接上决定二 / 三、译成消费结论「入账」，`ErrVoidedCarrierPickupRederivationUndecided` 连头注一起删，`judge_on_carrier_first_effective_pickup_test.go` 钉「失效版本 → 未决且零写入」的那例改为钉新行为；`cmd/parcel-dispatch/assemble.go` 的 `carrierFirstEffectivePickupJudgmentUndecidedSentinels` 名单去那一格、`assemble_test.go` 随改；`FinalOutcomeHandoff` 信封类型不变、记录种类显式三值出去。**不在实现票内、另立或占号**：VE `adapters/parcelshipment/derive_on_final_outcome.go` 的 `kindForFinalRecord` 译第三格（VE owner；今天按 `Finalized` 二分会把失效记录读成 `final-outcome-not-adopted`，语义相反——PS 票进 main 前 VE 票至少要占号，或 PS 票先让 VE 侧对第三格响亮拒绝而非误译）；PS CONTEXT Lifecycles「包裹身份与服务」加「→ 终局失效」转换与 GLOSSARY 是否补词条（0138 Consequences 已写好句子，随实现票同笔落 `docs/domain/**`）。**判据**——(a) 三场景各至少一例：(i) 失效后无新收寄 → 前版 `is_current` 翻假、失效行带 `prior_version` 与锚、`FindCurrentFinal` 答无、一封 formed 记录种类为终局失效、继续尝试判断派生开放或保持受控关闭；(ii) 失效后 F@v3 到 → 首派生或同种类重派生（`SOURCE_REVISED/F@v3`）、不新造格；(iii) 受控关闭已生效 → 同一拍出关闭路径终局（首派生、无 `prior_version`）或 `NOT_FINAL` 带原因；(b) 乱序到达末态一致：F@v3 先于 F@v2 → v2 落失效行、`prior_version` 缺席、不翻任何行、不重判，末态与顺序到达相同；(c) 锚拒采用：前版停在未决、失效先到、前版重投成功 → 不采用 `EVIDENCE_VOIDED/<失效版本>`、零终局；(d) 失效行与重判形成的终局同一笔事务，后半失败整拍回滚、无「失效已落、重判未做」中间态；(e) `ErrVoidedCarrierPickupRederivationUndecided` 于 `internal/` `cmd/` 零命中，哨兵名单用例随改；(f) `final_outcome` 上的 CHECK 对第三种行形的接受 / 拒绝各一真库例；(g) 带 DSN PS application / adapters/postgres / adapters/labelfinal / adapters/transportfulfillment + `internal/architecture` + 反向依赖 `cmd/parcel-api` / `cmd/parcel-dispatch` ok。**要动的哨兵与结果格**：删 `ErrVoidedCarrierPickupRederivationUndecided`；`LabelServiceFinalOutcome` 加 `LABEL_SERVICE_FINAL_VOIDED`；`FinalOutcomeRecord` 加「终局失效」。
  **判断项（0138 越权风险点，归 PS owner，本记录只转述不裁）**：1 同一拍重判还是分两拍（草案按同拍同笔；分拍则要补「失效已落、重判未做」的续办引用）；2 `LabelServiceOutcome` 与 `LabelServiceFailure` 是否同源可互相重派生（今天 `FormParcelFinalHandler` 按 `ResponsibilityOutcomeKind` 相等判同源，关闭路径先出「终局失败」、后收寄到达要换成「非取消终局」会落「异源不采用」；草案倾向同族视为同源，但改的是网络服务也走的同一段分派，未取）；3 迟到前版的拒绝词是不采用 `EVIDENCE_VOIDED` 还是未决（草案按 ADR-0029 取不采用：重投不会改变结果）；4 网络服务四格的来源失效是否同形（未量，重判入口不是 `JudgeLabelServiceFinal`）；5 委托完成摘要回退的下游后果（摘要是派生、不对外发信，PS 不为此多留一格）；6 失效锚查询按（租户，包裹，证据）还是（租户，证据）跨包裹（载运对象是集运单元时）。**读稿时未见自相矛盾处**：本会话通读 0138 一遍，重点核了决定三「只有真的翻了前版才重判」与决定二「`prior_version` 缺席那式不翻任何行」相容、决定四与场景 (ii) 相容、决定八对三场景的收束与 Context 场景段一致；CONTEXT 引句未逐字回对原文——那半归通道 2 评审。
  **接手方能力边界**：笔 1、2 由前一任写就、已提交并推分支，本记录按其提交信与全文复述，接手方通读 0138 一遍、未重跑 `/domain-modeling`；读过票面 39 全文、0138 全文、ADR-0135 越权风险点 4 原句与 Status 行、lc/25「要裁的」2 原句与「裁决」相邻段、README 0071 行与 0131–0137 各行、lc/35 完成记录（形的参照）；`git grep` 核过 `ErrVoidedCarrierPickupRederivationUndecided` / `carrierFirstEffectivePickupJudgmentUndecidedSentinels` / `FinalOutcomeRecord` / `LabelServiceFinalOutcome` / `final_outcome_*` CHECK / `kindForFinalRecord` 于 `2e66c0f3` 仍在原处；没读 `form_parcel_final.go`、`final_outcome.go` 全文——实现票形那一段的符号来自 0138 Consequences 与上述 grep，不来自逐行阅读。纯 .md，未跑 go test，`go build ./...` 退 0。非作者评审须由通道 3 以外的通道做（通道 2 已并行钉 `0a25bb2a`；笔 3 与本记录笔评审时一并看）。
- **2026-09-14 18:2x · 评审 ← 通道 2 · 钉 `0a25bb2a`（取证 `f04ad816`）**（推送方 18:2x 转来原文，通道 3 照录；两个轴标题改成行内粗体，其余一字不动）：**两轴均无阻断**。
  **Standards（非阻断）**：① 符号：`parcelsClosedByFinal` 在 f04ad816 不存在（Status 行与 Context 各一处）；`operate_label_transaction.go` 里读 `FindCurrentFinal` 的是 `LabelTransactionHandler.closedParcels`。其余 `ParcelFinalOutcome.Rederive` / `FindCurrentFinal` / `deriveCompletion` / `kindForFinalRecord` / `ErrVoidedCarrierPickupRederivationUndecided` 均在。→ 改符号名。② 指错决定号：「ADR-0134 决定五『入队失败回滚整步』」（Decision 三、Links 两处）——0134 决定五是「不做的，逐条写明」，回滚整步在其**决定三**与越权风险点 5。→ 改号。③ 跨文件计数（AGENTS「改文档」）：Status 行「0004 的四条 CHECK」——f04ad816 上 `final_outcome` 有**五条**（scope_not_blank / kind_closed / result_shape / rederivation_coherent / current_only_finalized），且该句在 SHA 锚之前；「四处读口 / 四个读口」今天数对但数不是论点，Consequences 处未锚。→ 两处去数换点名。④ 术语：PS CONTEXT 无「面单渠道服务终局」词条（Status 行声称读过），只有「面单服务终局边界」「终局服务结果」；「作废」全稿 16 处——CONTEXT 里它只指面单结果（成功作废 / 渠道作废），ADR-0135 决定六对收寄只用「失效版本」；本稿场景(iii)「均已作废 / 失效」取面单义、决定二「被作废的那一代收寄」取收寄义，一稿两义。→ 收寄义统一为「被失效的那一代」，Status 行词条改成 CONTEXT 里有的那一个。「撤回」(Withdrawal)、「不可逆失效」(`LapsedTransactions`) 区分正确，不动。⑤ 与所引 ADR：ADR-0117 Alternatives 原句把「翻 `is_current`」定性为「与只插不改相违」，本稿称循 0117「只插不改」却翻前版当前位——实质无误（`final_outcome` 既有形、AT-PS-063），措辞改为「循回指成链；写法照 final_outcome 既有翻当前位插新行」；「ADR-0117 明写『final_outcome 那条路不改』」与「ADR-0103『未知也是一个版本』」均非原文（0103 决定二原句「待确认必须是判断的一个版本」）。→ 引文改成可搜的原句或去掉引号改转述。⑥ CHECK 相容：决定一「第三种记录 is_current 恒假」与 `final_outcome_current_only_finalized`（`CHECK (finalized OR NOT is_current)`）相容；`result_shape` 的 NOT finalized 支与 `rederivation_coherent` 今天都容不下第三种行，稿内已认「各加一支」，与 Consequences 一致，无矛盾。→ 不改，照录。红线：Proposed ✓、无租户取值 ✓、无行号 ✓。
  **Spec（非阻断）**：判据①②④ ✓；③ 截至 0a25bb2a README 行 / 0135 追句 / lc25 追句 / 票 39 完成记录未做——正是你在做的，不算阻断。(1) AT-PS-063（UC-PS-004 原文「原有效交付因 POD 错误被失效 → 保留原终局历史，形成新判断版本并重新派生当前结果」）对「依据失效」给的形正是候选甲；`adapters/transportfulfillment/delivery_outcome.go` 头注称「POD 失效重派生的链路（AT-PS-063）由此接通」。本稿只借它「历史不删」，风险 4 写「没量」——接受前 owner 宜量网络路今天怎么给出新 `ResponsibilityOutcome`，并在 Consequences 补 UC-PS-004 AT 表一行 / 括注，否则同一 UC 下两路对「依据失效」形不同而无一句说明。→ 判断项，归 PS owner。(2) 场景(ii)乱序支「末态与顺序到达一致」只对当前终局成立，链形不同（乱序 T3 回指 T1；顺序 T3 首派生或回指 Tc），宜限定。→ 判断项，归 PS owner。
  **评审方跑了**：f04ad816 上 git grep 全部指定符号、0004 final_outcome DDL 全部约束、0014 kind 扩展、`label_service_final.go` 规则序、PS CONTEXT 全部引文与「失效 / 作废 / 撤回 / 不可逆失效」、0117/0029/0103/0134/0135 被引决定；未跑 go build/test（纯 docs）；未改一行、worktree 已移除。
- **2026-09-14 18:5x · 评审收口 · 通道 3（接手会话）**：推送方 18:2x 解除「不改 0138 Decision」红线（评审已读完），Standards ①–⑤ 单独一笔改于 `2a1493f5`（仍 `Status: Proposed`，Decision 语义不动；标题随 ⑤ 改口，`docs/adr/README.md` 0138 行链接文字与判据句同步）：① `parcelsClosedByFinal` → `LabelTransactionHandler.closedParcels`（Status 行、Context 各一处）；② 「ADR-0134 决定五」→ 决定三「入队失败即本步 error 上抛、事务回滚、调用方重放」+ 越权风险点 5（Decision 三、Links）；③ Status 行「四条 CHECK」→ 点名 `final_outcome_result_shape` / `final_outcome_rederivation_coherent` / `final_outcome_current_only_finalized`，Context「四处的读口」→「各自的读口」，候选甲「四个读口」→「各调用方」，Consequences「四个读口（四处调用方）」→「`FindCurrentFinal` 的各调用方」；④ Status 行与 Links 词条 → 「面单服务终局边界」「终局服务结果」；收寄义「作废」改「被失效的那一代 / 被它失效的 / 被源失效 / 失效是源说的」（第一问、场景 (ii) 乱序支、乙反方、决定一 / 二 / 三、Alternatives 第二条、越权风险点 3），面单义（场景 (iii)「均已作废 / 失效」「定案 / 作废」、决定五「已失败 / 已作废 / 已不可逆失效的旧交易」）保留、与 CONTEXT 一致；⑤ 标题与第一问「只循它的『……只插不改』」→「循回指成链的纪律，写法照 `final_outcome` 既有的翻当前位插新行（AT-PS-063），不照 ADR-0117 在 `intake_adoption` 上的『只插不改』」并点明 0117 Alternatives 自己把翻 `is_current` 定性为相违、把 `final_outcome` 留在原形，候选甲与决定八同改；ADR-0103 引文 → 决定二原句「待确认必须是判断的一个版本」。**⑤ 后半「`final_outcome` 那条路不改」未改**：`git grep` 于 ADR-0117 Alternatives 末句原文在（「同形照搬即可。`final_outcome` 那条路不改。」），是可搜的原句。改后全稿 `git grep -c` 「作废」只剩面单义那几处、「只插不改」只剩「不照 ADR-0117 在 `intake_adoption` 上的」那一处、`parcelsClosedByFinal` / 「决定五同一」/ 「未知也是一个版本」/ 「面单渠道服务终局」词条零命中。⑥ 不改稿，照录如上。**判断项追加（归 PS owner，接上文完成记录 1–6）**：7 网络路 AT-PS-063 与本记录对「依据失效」形不同——UC-PS-004 原文「原有效交付因 POD 错误被失效 → 保留原终局历史，形成新判断版本并重新派生当前结果」给的正是候选甲的形，`adapters/transportfulfillment/delivery_outcome.go` 头注称那条链路「由此接通」；接受前 owner 宜量网络路今天怎么给出新 `ResponsibilityOutcome`，并在 0138 Consequences 补 UC-PS-004 AT 表一行 / 括注，否则同一 UC 下两路对「依据失效」形不同而无一句说明（与完成记录判断项 4 同题、更具体）；8 场景 (ii) 乱序支「末态与顺序到达一致」只对当前终局成立，链形不同（乱序 T3 回指 T1；顺序 T3 首派生或回指 Tc），Context 场景段与决定八「乱序到达由决定二的锚保证末态一致」宜限定为「当前终局一致」——本会话未改稿，归 owner 随接受一并判。`2e66c0f3` / `30130fda` / `2a1493f5` / 本笔未经非作者评审，推送方定要不要补看。
