# lc/37 非作者评审 Standards 非阻断与 lc/25 裁决末条一笔收口：`ADR-0049 第三条` 统一为带引文的「决定三」、收寄路测试文件头「五值」计数换点名、`CarrierTrackingFactReference` 改名判

Category: chore
Status: resolved——**已进 main，2026-09-14 10:0x**（通道 1 推送方重放：main `7b4cffc1` 之上 `cherry-pick cdf17834..mcp4-tails` 九笔零冲突，本票四条 = `77ff4145` / `49f9654f` / `686279c1` / `9cf30fab` + 推送方清点 `69406e6d` + 本簿记笔；非作者评审 ← 通道 1 两轴 0 阻断（Standards 0 / 2 非阻断 · Spec 0 / 2 非阻断，全文见 Comments）；`9cf30fab` 带 DSN 全量 110 ok / 0 FAIL）；此前 resolved——完工待非作者评审进 main，2026-09-14 09:4x（分支 `mcp4-tails` 基远端 main `cdf17834`；三条对应三笔 `c425a2d8` / `17d7c205` / `96bd5d3a`，完成记录随条 3 同提交，见 Comments「完工」；条 3 判改名；清点预报零差）；**10:0x 追加条 4**（通道 1 09:0x 追派的 `commercialKindFrom` 头注，原消息未进队列、完工报后补到）随本笔同提交，见 Comments「补笔」；此前 in-progress——2026-09-14 08:5x 通道 4 按通道 1 派单 task-65ce5275 自立自做（先例 sa-cc/16、lc/37、psr/08：评审尾巴 A 类推送方派单、作者自立票自做），分支 `mcp4-tails` 基远端 main `cdf17834`（树 `D:/tops/idp-parcel-mcp4-tails`），与 [sa-cc/17](../../sa-cc-funds-and-credential-seams/issues/17-sa-nil-dependency-text-sa01-count-comments-and-pp-synthetic-evaluation-fixture.md) 同分支——两票都撞 `cmd/parcel-dispatch/assemble.go`，合派一个通道；要裁的为零
Blocked by: 无（lc/37 与 lc/25 均已进 main；三条出处全在两票 Comments）。撞点：通道 3 同期动 `internal/visibilityexception/**`、`cmd/parcel-api/assemble_claims.go`、ADR-0136，与本票零重叠；共享树不碰

## 缺口（出处逐条指到评审原话；取证于 `cdf17834`，开工先重量，与派单不符处如实记）

1. **`ADR-0049 第三条` 写法统一**（[lc/37](37-lc25-review-follow-ups-superseding-version-case-header-counts-and-exported-event-type.md) 评审 ← 通道 1 Standards ①、lc/37 作者判断项 ①、lc/37「进 main 记录」候选后继）。lc/37 条 3 只改了 lc/25 那一句，自此同文件对 ADR-0049 并存「第三条」与带引文的「决定三」两种写法；评审裁「留『决定三』不退回，目标写法是带引文的那种」，其余统一归 PS owner。**重量**：派单写「`assemble.go` 两处 + `direct_publisher_test.go` 两处，`assemble_test.go` 零命中」；`git grep -n -E 'ADR-0049 第三条' -- cmd/ internal/` 于 `cdf17834` 实得 **十四处、四个文件**——`cmd/parcel-dispatch/assemble.go` 十处（四条只投 VE 的路由头注、`wireDispatcher` 体内两句、四只装配函数头注）、`cmd/parcel-dispatch/assemble_test.go` 一处 `Covers:`、`internal/parcelshipment/application/advance_acceptance_chain_test.go` 一处 `Covers:`、`internal/platform/dispatch/direct_publisher_test.go` 两处 `Covers:`。派单的「两处 / 零命中」与实况不符，成因不查（多半是中文模式经 PowerShell 管道的解码坑，workflow.md 本机环境点名过），按实况全收：四处同一句、只改注释，`internal/platform/dispatch` 与 PS `application` 测试不是本票地盘但一并收，完成记录点名。目标写法照 lc/37 条 3 那一句：「ADR-0049 决定三」+ 原句引文「没有订阅者的事件类型显式失败并入账」——`git grep -c` 于 ADR-0049 单行命中 1，已核。
2. **收寄路测试文件头「五值」**（lc/37 评审 ← 通道 1 Standards ②）。`internal/parcelshipment/adapters/transportfulfillment/judge_on_carrier_first_effective_pickup_test.go` 文件头「五值要由真编排从夹具里判出来」的「五值」数的是 PS `application` 的 `LabelServiceFinalOutcome` 结果代数（派单写在 `domain`，以代码现名为准：`LabelServiceFinalAdopted` / `LabelServiceNotFinalOutcome` / `LabelServiceCancellationStandsOutcome` / `LabelServiceJudgmentNotAccepted` / `LabelServiceJudgmentUndecided`），另一文件的东西，第六格长出来这句无声变错（AGENTS.md「计数与行号同构」）。**重量**：`git grep -n '五值' -- internal/parcelshipment/adapters/transportfulfillment/` 于 `cdf17834` 实得三处——测试文件头那句、同文件 `TestNotFinalIsStructurallyUnreachableOnThePickupRoute` 头注「五值翻译表里那一格」（数的是 `labelfinal` 的翻译表，同样是别处的）、生产文件 `judge_on_carrier_first_effective_pickup.go` `ParcelLabelFinalJudge` 头注「五值译成消费结论」（同一个代数）。三处同一个数、同一个包、都只是注释，一并收；派单「同文件其他『五值』若是数本文件自己的用例不算」——三处没有一处数的是本文件的用例。
3. **`CarrierTrackingFactReference` 改名判**（[lc/25](25-external-carrier-first-pickup-triggers-label-final-judgment.md)「裁决」末条：头注已随 lc/25 改口为「指名 transport-fulfillment 拥有的一条实际承运商首次有效收寄事实（ADR-0135）」，「类型名是否随之改（如 `CarrierFirstEffectivePickupFactReference`）归实施时判」；lc/37「不在本票」；ADR-0135 决定七末句同一句话）。裁决权在票面交给实施者，本票判。**重量**：`git grep -n CarrierTrackingFactReference -- internal/ cmd/ docs/` 于 `cdf17834`——PS `domain/label_service_final.go`（类型、构造器、`CarrierFirstEffectivePickup` 的字段与 `Fact()`、`CarrierFirstEffectivePickupSpec.Fact`）、`domain/label_service_final_test.go`、`application/judge_label_service_final_test.go`、`adapters/transportfulfillment/judge_on_carrier_first_effective_pickup.go` 与其测试，全在 PS；`docs/` 只有 ADR-0135 两处（Status 行的能力边界清单、决定七那句「归实施时判」）；UC-PS-004 与 `production_wiring_baseline.txt` 零命中。同文件还有一枚同名族的 `CarrierTrackingFactVersion`，头注「是那条事实的版本」——指的是同一条事实，改名理由一字不差地对它也成立。

4. **（追加，通道 1 09:0x 经 send_to_session 追派、09:5x 完工报后补到——原消息未进本通道队列）`commercialKindFrom` 头注「两份镜像」被代码反证**（psr/09 非作者评审 ← 通道 2 Standards 非阻断 1，已落 psr/09 Comments）。`internal/parcelshipment/adapters/partycommercial/commercial_resolution_keys.go` `commercialKindFrom` 头注首句「名集与库内 `..._bases_closed` 白名单是同一集合的两份镜像」：名集含 `pcdomain.PriceRuleObject`（全部 `CommercialObjectKind.String()` 都译），而迁移 0022 重加的白名单没有 `PRICE_RULE`——那格由 `ResolutionKeyRegistration.validate`「需要键携带额外选择维度，本登记面不承载」与库内 CHECK 拦，既有用例 `TestKeyRegistrationRefusesDefaultsAndBareCalls`「价格规则仍在名集之外」钉着。**重量**：`cdf17834` 上 0022 的 CHECK 九个名、`String()` 十个名，差的恰是 `PRICE_RULE`，与评审所述一致。

**不在本票**：lc/25「要裁的」2 失效版本重派生（`ErrVoidedCarrierPickupRederivationUndecided` 等 PS owner 裁）；psr/09「必登」（PS owner）；`JudgeOnCarrierFirstEffectivePickupAdapter` 与 `AdoptOnEffectiveDeliveryAdapter` 同形三段抽 helper（lc/37「只记不抽」，照旧）；任何 CONTEXT / UC / ADR 正文改动——ADR-0135 里那两处旧名是已接受 ADR 的历史与它自己说「归实施时判」的那句，不改写（AGENTS.md「改文档」：不改写已接受 ADR 历史）。

## 做法

1. 十四处「ADR-0049 第三条」逐处改成「ADR-0049 决定三」，其中不带引文的补引文「没有订阅者的事件类型显式失败并入账」；已带 ADR-0049 原句片段的 `Covers:` 行（`direct_publisher_test.go`、`assemble_test.go`）只换「第三条」→「决定三」并把引文对齐到原句。一笔。
2. 三处「五值」：测试文件头改为逐名点五格；`TestNotFinalIsStructurallyUnreachableOnThePickupRoute` 头注「五值翻译表」→「结果代数翻译表」；生产头注「五值译成消费结论」→「把 `LabelServiceFinalOutcome` 译成消费结论」。一笔。
3. 判改名。理由见 Comments 完成记录；落法：`CarrierTrackingFactReference` → `CarrierFirstEffectivePickupFactReference`、`CarrierTrackingFactVersion` → `CarrierFirstEffectivePickupFactVersion`，构造器随之（`NewCarrierFirstEffectivePickupFactReference` / `NewCarrierFirstEffectivePickupFactVersion`），`newRequiredValue` 的口名串随之（`carrier first effective pickup fact reference` / `... version`——那两串只出现在拒空值的错误文本里，全仓无断言）；头注去掉「类型名沿用立票时的叫法，是否改名归另笔」那半句；全仓调用点跟上；夹具里 `TF-TRACK-FACT-7` 这类合成串是值不是名，不动。纯改名零行为，独立成笔、放最后。
4. 头注首句改口：名集是 `pcdomain.CommercialObjectKind.String()` 的反向、全集都译；白名单比它少 `PRICE_RULE`，那格由 `validate` 与 CHECK 拦在写入之前，两边不是镜像。只注释，一笔（追加在条 3 之后）。

## 红线

- 条 1 / 2 只许注释行；条 3 只许标识符与两串口名文本，判断分支、结果代数、`ReferenceCarrierFirstEffectivePickup` 的校验一字不动。
- 不动 `internal/transportfulfillment/**`、路由表行、任何 VE / CC / `cmd/parcel-api` 文件、`apps/`。
- 注释中文；引 ADR 用决定号 + 单行可搜的引文；不写行号、不数别处的东西。

## 完成判据

1. `git grep -n -E 'ADR-0049 第三条' -- cmd/ internal/` 零；`git grep -c '没有订阅者的事件类型显式失败并入账' -- docs/adr/0049*` 仍为 1（引文对得上原句）。
2. `git grep -n '五值' -- internal/parcelshipment/adapters/transportfulfillment/` 零；文件头那句逐名列出五格且与 `judge_label_service_final.go` 的常量名逐字相同。
3. `git grep -n -E 'CarrierTrackingFact(Reference|Version)' -- internal/ cmd/` 零；`git grep -n -i 'carrier tracking fact' -- internal/parcelshipment/` 零；ADR-0135 零 diff。
4. `gofmt -l` 空、`go build ./...` / `go vet ./...` 退 0；`go test -count=1` PS 全部包 + `internal/platform/dispatch` + `cmd/parcel-dispatch` + `internal/architecture`（PS `adapters/postgres` 与 `cmd/parcel-dispatch` 的真库用例带不带 DSN 如实记——本票三条都不碰持久化，DSN 由 sa-cc/17 条 6 那一跑顺带覆盖 `cmd/parcel-dispatch`）。
5. 完成记录随条 3 那一笔同提交（Status → resolved、逐笔 SHA、判据逐项、条 3 的判与理由、判断项）；清点预报零差（不增删文件、不加端口）。
6. 条 4：`git grep -n '两份镜像' -- internal/parcelshipment/adapters/partycommercial/` 零；头注含「PRICE_RULE」与「validate」两个词；`commercial_resolution_keys.go` diff 只有注释行。

## 地盘

`cmd/parcel-dispatch/assemble.go`（只注释）、`cmd/parcel-dispatch/assemble_test.go`（一句 `Covers:`）、`internal/parcelshipment/application/advance_acceptance_chain_test.go`（一句 `Covers:`）、`internal/platform/dispatch/direct_publisher_test.go`（两句 `Covers:`）、`internal/parcelshipment/adapters/transportfulfillment/judge_on_carrier_first_effective_pickup{,_test}.go`、`internal/parcelshipment/domain/label_service_final{,_test}.go`、`internal/parcelshipment/application/judge_label_service_final_test.go`、`internal/parcelshipment/adapters/partycommercial/commercial_resolution_keys.go`（条 4，只注释）、本票面、lc spec 一行。

## 参照

[lc/37](37-lc25-review-follow-ups-superseding-version-case-header-counts-and-exported-event-type.md) Comments「评审 ← 通道 1」Standards ① ②、判断项 ①、「进 main 记录」候选后继；[lc/25](25-external-carrier-first-pickup-triggers-label-final-judgment.md)「裁决」末条；[ADR-0135](../../../docs/adr/0135-carrier-first-effective-pickup-is-a-judged-control-fact-with-its-own-registry-and-enters-the-segment.md) 决定七；[ADR-0049](../../../docs/adr/0049-publish-channel-is-in-process-delivery-until-load-evidence.md) Decision；`internal/transportfulfillment/domain/carrier_first_effective_pickup.go`（TF 侧 `CarrierFirstEffectivePickupReference` / `CarrierFirstEffectivePickupVersion`，条 3 判改名的对照）；AGENTS.md「写代码注释」「改文档」。

## Comments

- 2026-09-14 08:5x · 通道 4（task-65ce5275）：立票，Status 直接 in-progress，作者自立自做。**只写票面，未动代码。** 三条出处抄自 lc/37 两份评审原话与 lc/25 裁决末条；三条都在 `cdf17834` 上重量过，与派单不符处（条 1 十四处非四处、条 2 三处非一处、条 3 多出一枚同族 `CarrierTrackingFactVersion`）已写进「缺口」。
- **2026-09-14 09:1x–09:4x · 通道 4（task-65ce5275）· 完工**。分支 `mcp4-tails` 基远端 main `cdf17834`，与 sa-cc/17 同分支（推送方重放后 main 上 SHA 会换，对照由推送方在「进 main 记录」补）。立票笔 `026dbbb0`（两票 + 两 spec 行，无代码）。
  - **条 1 `c425a2d8`**：十四处「ADR-0049 第三条」→「ADR-0049 决定三」+ 引文「没有订阅者的事件类型显式失败并入账」。`cmd/parcel-dispatch/assemble.go` 十处（四只 `ve*UndecidedSentinels` 头注、`wireDispatcher` 头注两句、四只 `derive*Consumer` 头注）、`assemble_test.go` 一句 `Covers:`、PS `application/advance_acceptance_chain_test.go` 一句 `Covers:`、平台 `internal/platform/dispatch/direct_publisher_test.go` 两句 `Covers:`（其中一句原带 ADR 原句片段「不静默丢弃也不『未知类型即放行』」，改为引文锚在「没有订阅者的事件类型显式失败并入账」、原句前后半另标）。**平台包与 PS application 测试不是本票地盘，同一句只改注释，一并收——本条点名。**
  - **条 2 `17d7c205`**：`judge_on_carrier_first_effective_pickup_test.go` 文件头逐名列 `LabelServiceFinalAdopted` / `LabelServiceNotFinalOutcome` / `LabelServiceCancellationStandsOutcome` / `LabelServiceJudgmentNotAccepted` / `LabelServiceJudgmentUndecided`（与 `application/judge_label_service_final.go` 常量名逐字核过）；`TestNotFinalIsStructurallyUnreachableOnThePickupRoute` 头注「五值翻译表里那一格」→ 点 `LabelServiceNotFinalOutcome` 那一格；生产文件 `ParcelLabelFinalJudge` 头注「五值译成消费结论」→「把 LabelServiceFinalOutcome 译成消费结论」。
  - **条 3 本笔——判改名，理由**：(i) 名字是一句错话——「轨迹事实」按 TF CONTEXT 本身不构成收寄，PS 消费的是 TF 判过并登记的收寄事实，头注自 lc/25 起已这么写、UC-PS-004 括注 lc/37 已为同一理由改口，类型名是最后一处仍说旧话的；(ii) TF 侧对应类型是 `CarrierFirstEffectivePickupReference` / `CarrierFirstEffectivePickupVersion`（`internal/transportfulfillment/domain/carrier_first_effective_pickup.go`），PS 的 `CarrierFirstEffectivePickupSpec` 已与 TF 同名镜像，改名后两侧按名对得上；(iii) 影响面全在 PS：`domain/label_service_final{,_test}.go`、`application/judge_label_service_final_test.go`、`adapters/transportfulfillment/judge_on_carrier_first_effective_pickup{,_test}.go`，`production_wiring_baseline.txt` / 类型可达性基线 / UC-PS-004 零命中，ADR-0135 那两处是历史与「归实施时判」本句、不改。**落法**：`CarrierTrackingFactReference` → `CarrierFirstEffectivePickupFactReference`、同族 `CarrierTrackingFactVersion` → `CarrierFirstEffectivePickupFactVersion`（头注「是那条事实的版本」——指同一条事实，理由一字不差地成立），构造器随之；取 lc/25 裁决举的那个名而不是与 TF 全同名，多出的「Fact」把它与本文件的 `CarrierFirstEffectivePickup`（事实 + 版本 + 有效时间的只读引用）分开，头注写明；`newRequiredValue` 的两串口名随之改为 `carrier first effective pickup fact reference` / `... version`（只出现在拒空值的错误文本里，全仓无断言，`git grep -n -i 'carrier tracking fact' -- internal/parcelshipment/` 零；TF / VE 里的「external carrier tracking fact」是它们自己那类事实的文本，不碰）；头注去掉「类型名沿用立票时的叫法，是否改名归另笔」。夹具串 `TF-TRACK-FACT-7` / `CFEP-1` 是值不是名，不动。
  - **验（本机，钉本笔）**：`gofmt -l`（暂存 blob）空；`go build ./...` / `go vet ./...` 0；不带 DSN `go test -count=1` PS 全部包 + `cmd/parcel-dispatch` + `cmd/parcel-api` + `internal/platform/dispatch` + `internal/architecture` 全 ok。**真库**：本票三条都不碰持久化；`cmd/parcel-dispatch` 带 DSN 那一跑在 sa-cc/17 条 3 之二（`6388e32b`，含条 1 / 2 的改动）上做过——PASS 181 / SKIP 0 / FAIL 0；条 3 改名之后未再带 DSN（纯标识符 + 两串错误文本，编译器与 vet 覆盖），如实记。未跑全量。
  - **判据逐项**：1 ✓ `git grep -n -E 'ADR-0049 第三条' -- cmd/ internal/` 零，`git grep -c '没有订阅者的事件类型显式失败并入账' -- docs/adr/0049*` = 1；2 ✓ `git grep -n '五值' -- internal/parcelshipment/adapters/transportfulfillment/` 零，五个常量名逐字同 `judge_label_service_final.go`；3 ✓ `git grep -n -E 'CarrierTrackingFact(Reference|Version)' -- internal/ cmd/` 零、`git grep -n -i 'carrier tracking fact' -- internal/parcelshipment/` 零、`docs/` 零 diff；4 ✓ 见上；5 ✓ 本条即；清点预报零差（本票不增删文件、不加端口、不加缝——sa-cc/17 条 3 之一那份 +1 归它记）。
  - **红线**：条 1 / 2 `--numstat` 增删相等、`git diff -U0` 里非 `//` 行零；条 3 staged diff 里每一行改动都含新旧名或口名串或是注释行（脚本核过零例外），`ReferenceCarrierFirstEffectivePickup` 校验、判断分支、结果代数零 diff；`internal/transportfulfillment/**`、路由表行、VE / CC / `cmd/parcel-api` / `apps/` 零 diff。
  - **撞点**：共享树 `D:/tops/idp-parcel` 上 `internal/parcelshipment/application/advance_acceptance_chain{,_test}.go` 有 ` M`，`git diff --numstat` 为空、mtime 2026-09-11 20:12——只是 CRLF 工作副本噪声，不是在途改动；本票条 1 改了那份测试的一句 `Covers:`，重放时不会撞内容。
  - **判断项（归评审）**：① 条 1 把 `internal/platform/dispatch` 与 PS `application` 两处非本票地盘的 `Covers:` 一并改了——四处同一句、只改注释；若评审认为平台包该另笔，退回那两句零行为。② 条 2 把生产文件 `ParcelLabelFinalJudge` 头注那处也收了（派单只点测试文件头）——同一个数、同一个包、只注释；不收则本票判据 2 的包级 grep 不为零。③ 条 3 取 `CarrierFirstEffectivePickupFactReference` 而非与 TF 全同名的 `CarrierFirstEffectivePickupReference`——理由见上「落法」；若评审倾向全同名（`CarrierFirstEffectivePickupSpec` 的先例），换名零行为。④ 条 3 连 `CarrierTrackingFactVersion` 一起改——派单只点 Reference；不改会让同一条事实的引用与版本两枚类型一新一旧。⑤ 条 3 改了 `newRequiredValue` 的两串口名——严格说是错误文本变化（派单「若判改名」那半给了口子）；不改则错误文本继续说「tracking」。
- **2026-09-14 10:0x · 通道 4 · 补笔（条 4）**：通道 1 09:0x 用 send_to_session 追派的第七条，本通道队列里没有它，09:5x 完工报后通道 1 重发才到——按「已推的历史不改写」在 `96bd5d3a` 之上再成一笔。`commercialKindFrom` 头注首句改为：名集是 `pcdomain.CommercialObjectKind.String()` 的反向、全集都译；库内 `..._bases_closed` 白名单比它少 `PRICE_RULE`（价格方向那一维本登记面不承载，迁移 0008 头注的理由），那格由 `ResolutionKeyRegistration.validate`「需要键携带额外选择维度，本登记面不承载」与库内 CHECK 拦在写入之前，两边不是镜像；ADR-0136 / 0022 那半句原样保留。同文件读口 `CommercialResolutionKeys.FormResolutionKey` 译不回名字那一支的注释「库内 CHECK 与领域封闭集是同一集合的两份镜像」是同一句错话（判据 6 的 grep 扫出的第二处），改为「CHECK 放行的名字都在封闭集里、只比它少 PRICE_RULE」，结论「分叉要人来看」不变。核过 `cdf17834`：0022 CHECK 的名集与 `String()` 的名集恰差 `PRICE_RULE` 一项。只注释；`gofmt -l`（暂存 blob）空、`go build ./...` / `go vet` 0、`go test -count=1` PS `adapters/partycommercial` + `internal/architecture` ok（未设 DSN，头注不碰持久化）。判据 6 ✓：`git grep -n '两份镜像' -- internal/parcelshipment/adapters/partycommercial/` 零，头注含「PRICE_RULE」「validate」。
- **2026-09-14 10:0x · 非作者评审 ← 通道 1 推送方**（钉 `c569ea36` = 重放 tip `9cf30fab` 同内容；通道 2 / 3 已 crash，无第三人；只读脚本核 + 通读 diff）：
  - **Standards**：阻断 无。非阻断 ① `internal/platform/dispatch/direct_publisher_test.go` `TestAnEnvelopeWithNoSubscriberFailsLoudly` 的 `Covers:` 行在引文里再嵌一层「」（「不静默丢弃，也不得回退成「未知类型即放行」」）——两段引文各自逐字单行命中 ADR-0049（`不静默丢弃，也不得回退成` 1 命中、`路由表是显式清单` 1 命中），嵌套引号是原文自带，可读性略降，不改。② 条 3 `newRequiredValue` 的两串口名是错误文本变化（作者判断项 ⑤自报），全仓无断言，接受。无发现：`ADR-0049 第三条` 于 `cmd/ internal/` **0**；引文「没有订阅者的事件类型显式失败并入账」于 ADR-0049 单行 1 命中；文件头点名的五个结果常量在 `application/judge_label_service_final.go` 各自命中；旧名 `CarrierTrackingFactReference|CarrierTrackingFactVersion` 于 `internal cmd apps` **0**——TF / VE 里的 `ExternalCarrierTrackingFact` 族是另一类事实（外部承运轨迹事实，按 TF CONTEXT 不构成收寄），未动，正确；`docs/` 零 diff（ADR-0135 历史与 lc/25「归实施时判」本句留旧名是对的）；`production_wiring_baseline.txt` 零差；条 4 引文「需要键携带额外选择维度，本登记面不承载」于同文件 `validate` 命中；`git diff -U0` 非注释差：条 1 / 2 / 4 **0**，条 3 每行含新旧名或口名串；注释全中文、无行号。
  - **Spec**：阻断 无。非阻断 ① 条 1 收了 `internal/platform/dispatch` 与 PS `application` 两处非本票地盘的 `Covers:`（判断项 ①）——同一句、只注释、十四处一并统一比留两处旧话强，接受。② 条 2 收了生产文件 `ParcelLabelFinalJudge` 头注（判断项 ②）——同一个数、同包、只注释，接受；否则包级 grep 不为零。判断项 ③ 取带「Fact」的名——头注把它与 `CarrierFirstEffectivePickup`（只读三元）分开的理由写清，接受；④ `...Version` 一并改——同一条事实的引用与版本两枚类型不该一新一旧，接受。无发现：判据 1–6 推送方脚本核同结果；「不在本票」四条零触碰——lc/25 要裁的 2 那格 `ErrVoidedCarrierPickupRederivationUndecided` 零 diff、psr/09 `validate` / `validateSettlement` 零 diff、同形三段 helper 未抽、CONTEXT / UC / ADR 正文零 diff；`apps/` 零 diff。
  - **结论**：可进 main；四条非阻断全接受、无后继。
- **2026-09-14 10:0x · 进 main 记录（通道 1 推送方）**：`merge-base(main, mcp4-tails) = cdf17834`，main 已前进 ve-disc/05 批到 `7b4cffc1` → `%TEMP%\idp-replay-tails-0958` detached `7b4cffc1`，`cherry-pick cdf17834..mcp4-tails` 八笔 + 补笔 `c569ea36` 一笔**零冲突**（与 ve-disc/05 批零重叠文件），tip `9cf30fab` 对作者分支 tip 除 main 自己的 ve-disc/05 批与簿记外零差；`gofmt -l` 空、`go build ./...` / `go vet ./...` 0；10:0x 占号，`9cf30fab` 带 DSN 全量 **110 ok / 0 FAIL / 16 无测试（`pptest` 无测试文件 +1）/ 0 cached**（133 s）；`-v` 探针 `TestARecordedBuyEvaluationStopsUndecidedAtTheExpectedCostSeamThroughTheRouteTable`（cmd 真库）/ `TestTheHandlerRefusesANilDependencyAtConstruction` / `TestARegisteredCarrierPickupIsFetchedAtTheEnvelopeVersionAndJudgedAsFinal` / `TestASupersedingCarrierPickupVersionIsJudgedAtItsOwnOccurrenceTime` 四例 PASS；清点 `tools/mechanism-inventory` 干净检出重生成 **PP 生产 88→89、合计 964→965、其余零差** → 推送方清点笔 `69406e6d`（数字全来自 sa-cc/17 条 3，本票零差）；释号。簿记一笔在其上（两票 Status「已进 main」前缀 + 评审 + 本记录；psr/09 Standards ① 标已落；tasks.md 节）；`ls-remote` 核 `7b4cffc1` 未动 → 共享树 `merge --ff-only` → `push <sha>:main`。**SHA 对照**（九笔）：`026dbbb0→a6d16d6d` / `c425a2d8→77ff4145` / `17d7c205→49f9654f` / `bbc156e3→517aa63b` / `e46decfd→ac179f5f` / `9693a4ee→6d546443` / `6388e32b→f1c21f93` / `96bd5d3a→686279c1` / `c569ea36→9cf30fab`。`mcp4-tails` → `merged/mcp4-tails`（指针留本地、远端删）；作者树 `D:/tops/idp-parcel-mcp4-tails`（干净 = origin）与重放树比内容后拆。**通道间消息丢失一次**：推送方 09:0x `send_to_session` 追派条 4 的消息从未进通道 4 队列（通道 4 自报，队列里确实没有），完工报回执里重发才到——推送方以后追派用 `dispatch_task` 而不是裸消息，任务记录不会丢。
