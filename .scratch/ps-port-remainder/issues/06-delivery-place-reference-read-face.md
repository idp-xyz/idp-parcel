# `parcel-shipment` 开一个按（租户，包裹身份）答「收件地点引用」的窄读口——`transport-fulfillment` 派送任务地点一格的提供方半边

Category: enhancement
Status: resolved——2026-09-10 12:3x 通道 2（单 task-422c077d-ac6f-4bb0-a0ec-0178a46e7490，接前任 task-0dab9f5a 现场），分支 `mcp2-psr06` 基 `84e89dc7`，tip `906f2f0f`，完成记录见 Comments 末条；待推送方派非作者评审后重放进 main。此前 in-progress（11:14 通道 2 认领）、ready-for-agent——由 [ADR-0130](../../../docs/adr/0130-delivery-place-reference-is-a-shipment-level-composite-reference-anchored-to-a-source-data-version.md) Consequences 第一条拆出（2026-09-09，通道 2，task-ddb77473，PS owner 口径代裁）；形状已裁，本票只落读口、值对象与 postgres 读面，不动 `transport-fulfillment/**`
Blocked by: 无

## 缺口

ADR-0114 决定三让 TF 按 `DeliveryPlaceSource.LoadDeliveryPlace(tenant, object)` 拉收件地点引用；ADR-0130 定了引用是什么、按什么键答、`待复核`与「没有」怎么分。PS 今天没有任何一条路从包裹身份走到「收件地点引用」：`ports` 上没有读口，`domain` 里没有这个值对象，收件地址只在接受基线快照与客户原始资料版本里作为内容出现。票 [tf/12](../../tf-segment-lifecycle-closure/issues/12-delivery-place-reference-seam-parcel-shipment.md) 的 TF 适配器等它。

## 要落的（形状照 ADR-0130，取舍已裁）

1. **值对象 `domain.DeliveryPlaceReference`**：四段——租户、委托、收件资料范围（`SourceDataScope` 形，委托必填、包裹可空、资料组）、资料版本锚（`SourceDataBasis` 形：接受基线，或某份 `SourceDataVersionID`）。`String()` 交规范串，串自带形状版本前缀（先例 `PSC-1:` / `PCC-1:`，取新号如 `DPR-1:`），四段之外不多一字，地址内容一字不进串；带重建门（ADR-0028），非规范串拒不规范化。
2. **读口 `ports.DeliveryPlaceReferenceView`**（名可议，落在 `ports` 新文件）：按（租户，`DeclaredParcelID` 或本上下文对正式包裹身份的既有引用）答封闭四格——`基线锚引用` / `已采用版本锚引用` / `收件地点未定`（范围上 `CurrentSourceDataAdoption` 为 `待复核`）/ `没有收件地点`（对象不属任何已接受委托的成员集合，含集运单元与不可见对象，按统一不可见结果，不区分不存在、他租户、未授权）。前两格带 `DeliveryPlaceReference`，后两格不带。**没有`不知道`格**：四本册子都是 PS 自己的，答不出就是读面坏了，上抛 error。
3. **包裹 → 委托的路**：经 `AcceptanceBaseline.covers` 走声明成员；包裹身份谱系（拆分 / 合并后的新包裹）今天领域里没有模型，本票先只按声明成员解析，谱系包裹会落「没有收件地点」——在读口头注写明这一格是「谱系未建模」的今日形状，谱系落地那票要补这一路（同 ps-port-remainder/05 NO 半边「从未关联 → 不在」那条越权风险点的处置）。
4. **收件资料范围怎么指名**：`SourceDataGroupReference` 是开放引用（ADR-0120 决定七），「收件」这个资料组在 PS 里今天没有自有词。本票在 PS `domain` 立一个自有常量作收件资料组的原词（机制半边——引用里要能写出这一段），矩阵登记侧（`PAR-COM-13`）用什么词是实例半边，两侧对不上时读口按 PS 原词查、查不到修订版本就答基线锚——这是如实答案（PS 没见过那个范围上的修订）而不是默认。
5. **postgres 读面**：一条按（租户，包裹）的查询——基线成员表反查委托，再按（委托，收件范围）取资料版本与派生采用判断；派生规则复用 `CurrentSourceDataAdoption`，不在 SQL 里第二套实现。需不需要新迁移（索引）由实施时量，只加索引不加列。
6. **不做**：按引用解析回地址内容的读口（第二个消费方，另立）；按委托或按引用的反查方法；向 TF 推送任何事件；改 `ShipmentRequestRepository` 一类写口。

## 完成判据

1. `domain.DeliveryPlaceReference` 构造 / 重建 / `String()` 往返测试：同一委托两个包裹串相等；基线锚与版本锚串不等；非规范串重建被拒。
2. `ports` 读口四格各一例用例测试（替身实现）：基线锚、已采用版本锚、`待复核`不给引用、不属成员集合答没有；`待复核`那例用两条分叉修订构造（`CurrentSourceDataAdoption` 已有的分叉路）。
3. postgres 适配器真库测试（带 DSN）覆盖同四格；DSN 缺席 SKIP 不 PASS。
4. 不动 `transport-fulfillment/**`、不改 ADR-0130 / 0114 / 0075 正文、不写真实地址。
5. 验证（作者层）：gofmt 空、`go build ./...` / `go vet ./...` 0、`go test -count=1` PS `domain` + `ports` + `adapters/postgres`（带 DSN）+ `./internal/architecture/...`；清点在干净检出重生成。

## 参照

ADR-0130 决定一 / 二与 Consequences；PS CONTEXT「收件地点引用」词条与 Rules 那条；`internal/parcelshipment/domain` 的 `AcceptanceBaseline`、`SourceDataScope`、`SourceDataBasis`、`CurrentSourceDataAdoption`；`internal/transportfulfillment/ports/delivery_requirement.go` 的 `DeliveryPlaceSource`（消费方形状，只读不改）；本目录 05（按包裹键读面的先例与「没有不知道格」的判据）。

## Comments

- 2026-09-09 · 通道 2（task-ddb77473）：立票（ready-for-agent）。形状在 ADR-0130，本票不再裁；ADR-0130 越权风险点 2（「没有」藏完整性问题）与 4（基线上已有值的首次更正走哪一格）若 owner 复核后改口径，本票随之改读口那一格，不回改 ADR。
- 2026-09-10 11:5x · 通道 2 新会话接手（单 task-422c077d-ac6f-4bb0-a0ec-0178a46e7490），从 `1d7affa5` + 树上四份未提交领域文件接续（`delivery_place_reference{,_test}.go`、`delivery_place_resolution{,_test}.go`，mtime 11:18–11:22）。按 parallel-sessions「接手别人在途产出时」先写自己的一片 red 再读现场：判据全对上（`DPR-1:` 前缀、四段顺序、重建门重渲染逐字比、`DeliveryPlaceDataGroup()` 原词、经 `baseline.covers` 走声明成员、`待复核`→未定、非成员/未接受→没有、派生复用 `CurrentSourceDataAdoption`）。一处形状差异：前任另立 `SourceDataVersionAnchor` 而非复用 `SourceDataBasis`——ADR-0130 原话是「同形」，且免于把 `NewSupplementOnAcceptanceBaseline` 一类修订意图词借给引用锚，接受。我方 red 里前任表中没有的输入（尾随空白/换行、小写前缀、外来前缀 `PSC-1:`、包裹范围 × 版本锚往返）并入前任测试，不留第二份镜像。
- 2026-09-10 12:3x · 通道 2（task-422c077d）：**完成记录**，分支 `mcp2-psr06` 基 `84e89dc7`、tip `906f2f0f`（= origin/mcp2-psr06），每笔提交后已推。
  - **每笔**：`eed326c5` 领域层四件（前任现场核过即收 + 我方并入的非规范串输入与一例往返）+ 票面接手记；`2dc0dd0d` `ports/delivery_place_reference_view.go` 新文件 + `production_wiring_baseline.txt` 加一行 `ParseDeliveryPlaceReference`（在 `eed326c5` 上两法同得 2→3，只对该检出成立；头部流水账一段）；`5a30093d` `adapters/postgres/delivery_place_reference_view{,_test}.go` 新文件；`42bf6570` 机制清点在 `5a30093d` 的干净 detached 检出重生成；`906f2f0f` 两处头注把「TF 端口只有两格」改为缺席陈述（作者自查的 Standards 发现）。
  - **规范串（供 tf/12 逐字对）**：`DPR-1:<租户>/<委托>/<资料组>[;<包裹>]/<baseline | version;<版本>>`。前缀拼写 `DPR-1:`（大写、连字符、冒号），四段顺序**租户 → 委托 → 收件资料范围 → 资料版本锚**，段分隔 `/`，子段分隔 `;`，每段内容 `url.PathEscape`（大写十六进制），零值引用交回空串。委托级实例：`DPR-1:tenant-1/request-1/DELIVERY_PLACE/baseline`、`DPR-1:tenant-1/request-1/DELIVERY_PLACE/version;dp-ver-2`；包裹级实例：`DPR-1:tenant-1/request-1/DELIVERY_PLACE;parcel-1/version;data-version-4`。收件资料组的 PS 原词是 `DELIVERY_PLACE`（`domain.DeliveryPlaceDataGroup()`）。TF 按 ADR-0130 只当不透明串存，不解析。
  - **触及**：`internal/parcelshipment/domain/delivery_place_reference{,_test}.go`、`delivery_place_resolution{,_test}.go`（新）；`internal/parcelshipment/ports/delivery_place_reference_view.go`（新）；`internal/parcelshipment/adapters/postgres/delivery_place_reference_view{,_test}.go`（新）；`internal/architecture/production_wiring_baseline.txt`（+1 行 + 理由 + 流水账）；`docs/product/MECHANISM-INVENTORY.md`（重生成）；本票面。**未碰**：`ports.go`、`adapters/postgres` 既有文件、`transportfulfillment/**`、`partycommercial/**`、ADR-0130 / 0114 / 0075 正文、PS CONTEXT、`cmd/*` 装配、`migrations/`（不加迁移：既有部分 GIN `shipment_request_accepted_parcels_gin` 够用，无新列）。
  - **验收对照（完成判据）**：1 ✓ `delivery_place_reference_test.go`——同一四段两次构造相等、同委托两成员串逐字同（resolution 测试）、基线锚 vs 版本锚不等、往返（含分隔符 / 非 ASCII / 百分号 / 包裹级 × 版本锚）、非规范串一表输入全拒（含转义大小写、多余转义、尾随空白/换行、外来与未来前缀，表在 `TestANonCanonicalDeliveryPlaceReferenceStringIsRefused`）。2 △ 四格各一例在 `delivery_place_resolution_test.go` 上对 `ShipmentRequest.DeliveryPlaceReferenceFor`（进程内实现，postgres 适配器只是取行 + 重建后委托它）：基线锚 / 已采用版本锚（链尾）/ `待复核`（两条修订各以 v1 为基准的分叉，`CurrentSourceDataAdoption` 既有路）不给引用 / 非成员与未接受答没有；**`ports` 包本身无测试文件**——全仓任何 `ports` 包都没有测试（接口即形状），05 的 CC/NO 半边同样把四格用例落在 domain + postgres，没有另造一份「替身实现」测试；这一条是给评审的判断题之一。3 ✓ `delivery_place_reference_view_test.go` 真库同四格 + 他租户统一不可见 + 仅已提交的成员答没有 + 两份已接受同时声明上抛 `ErrAmbiguousParcelTarget` + 缺键不查库；无 DSN 实测 SKIP 不 PASS。4 ✓ 见「未碰」；夹具无真实地址（标识全是 `tenant-1` / `request-1` / `parcel-N` / `dp-ver-N`）。5 ✓ 见验证。
  - **验收对照（要落的）**：① ✓ 四段 + `DPR-1:` + 重建门（重渲染逐字比，不规范化）；锚另立 `SourceDataVersionAnchor`（ADR「同形」，理由见接手记）。② ✓ `ports.DeliveryPlaceReferenceView.LoadDeliveryPlaceReference(ctx, 租户, 声明包裹) (domain.DeliveryPlaceResolution, error)`——四格封闭、无「不知道」、读面坏了上抛（含歧义）。③ ✓ 经 `AcceptanceBaseline.covers` 走声明成员；谱系未建模写在读口头注与 `DeliveryPlaceReferenceFor` 头注。④ ✓ `DeliveryPlaceDataGroup()` = `DELIVERY_PLACE`；别的资料组上的修订不动锚（domain 用例）；查不到修订答基线锚。⑤ ✓ 一条按（租户，包裹）的 SQL 走 0006 的部分 GIN 找行，`LIMIT 2` 分辨恰一 / 多于一，命中整份读回过重建门后由 domain 译四格，SQL 里无第二套派生；不加迁移。⑥ ✓ 未做按引用解析、反查方法、向 TF 推送、写口改动。
  - **验证强度**（作者层，带 DSN `-p 1 -count=1 -v`）：gofmt 空；`go build ./...` / `go vet ./...` 0；PS `adapters/postgres` **PASS 151 / SKIP 0 / FAIL 0**（5.4 s）；PS `domain` + `./internal/architecture/...` + `cmd/{parcel-api,parcel-commercial,parcel-dispatch}`（`go list` 反查出的 PS ports 反向依赖 cmd）**PASS 1079 / SKIP 0 / FAIL 0**；`ports` 无测试文件。未跑全量（推送方跑）。清点在 `5a30093d` 干净 detached 检出重生成、单独成笔。
  - **与 main 碰面干跑**（12:3x，`git fetch` 后 `origin/main = c59892e3`，`ls-remote` 同）：本分支与 `84e89dc7..origin/main` 无同文件；`git merge-tree --write-tree origin/main HEAD` 干净。**已知将来的一处重叠**：nr/03（已重放到 `f78fc500`，待推）在 `production_wiring_baseline.txt` 新插 network-routing 小节一行、未改头部计数；本笔在 PS 组加一行并写「在 `eed326c5` 上 2→3」——两笔不相邻、`merge-tree` 应能自动合，落地时推送方在 tip 重数即可（我那段流水账已预写这一句）。
  - **给评审的判断题**：(a) 判据 2「ports 读口四格各一例（替身实现）」以 domain 层对进程内实现的四格用例 + postgres 真库四格作答、不在 `ports` 包另造替身测试——按全仓惯例与 05 先例；评审若认为票面字面要求 `ports` 包内的替身测试，可要求补，形状是内存 map 替身包一组 `ShipmentRequest`。(b) `SourceDataVersionAnchor` 另立一型而非复用 `SourceDataBasis`——ADR 决定一原话「同形」，我读作允许分型；反方是「多一型多一套构造器」。(c) 歧义（两份已接受同时声明一件包裹）上抛 error 而不落「没有」——按票面「答不出就是读面坏了，上抛 error」与 ADR-0060；反方是 TF 会把它当技术故障重试。(d) `ParseDeliveryPlaceReference` 进接线基线而非改成未导出——判据 1 要求外部测试包能测「非规范串重建被拒」，且第二消费方要用它；反方是名单又多一行。
  - **越权风险点**（不改 ADR，随票记）：ADR-0130 越权风险点 2 / 4 未变；本票新增一条——**`Undetermined` 在 TF 端口上的落法**：tf/12 适配器今天只能把它译成 `RequirementMissing`（任务停在待形成，结果对但字面不对），是否加第三格归 TF owner（ADR-0130 越权风险点 1 原文）。
- **评审 ← 通道 1（推送方自跑 /code-review，非作者）· 钉 `c2a119c6`（基 `84e89dc7`，代码 tip `906f2f0f`）· 12:5x**。通道 3 / 4 均已崩且未重起，5 / 6 各在票上，无空闲非作者通道，按 09-08 规矩推送方自跑；子代理不可用，两轴由推送方窗口串行跑、互不引用。**Standards**：阻断 0；非阻断 2——(1) `production_wiring_baseline.txt` PS 组加行带理由与「在 `eed326c5` 上 2→3」，合规；进 main 时 tip 上 NR 那行已在，推送方实测 **4**，重放注见该文件 PS 段流水账下方。(2) 判断题一条：`ports/delivery_place_reference_view.go` 头注与基线理由行里「ADR-0130 Consequences 第一条 / 第三条」是按序位指别的文件里无标签的列表项——AGENTS.md「改文档」那条禁行号与计数，序位与行号同构（插一条就无声指错）；「决定一 / 二」有 `**一、**` 标签不在此列。ADR-0130 自身 Consequences 无标签，改起来要么给列表项加标签、要么改成引文；随 tf/12 或另起小笔，不挡合入。核过：注释全中文；两条 SQL 均 `tenant_id = $1`（ADR-0003）；重建门 `ParseDeliveryPlaceReference` 解四段 → 逐段构造 → 重渲染逐字比、不规范化（ADR-0028）；postgres 读面整份读回过 `RehydrateShipmentRequest` 再由 domain 译格，SQL 无第二套派生；smell baseline：`DeliveryPlaceReferenceFor` 取租户走 `currentVersion.SourceSubmission().Identity().TenantID()` 是一条 Message Chain，但读的是聚合自己的数据，判断题不改。**Spec**：阻断 0；非阻断 1——完成判据 2 字面「`ports` 读口四格各一例（替身实现）」未在 `ports` 包落替身测试，以 domain 层对进程内实现 `ShipmentRequest.DeliveryPlaceReferenceFor` 的四格用例（含 `待复核`用 `CurrentSourceDataAdoption` 既有分叉路构造）+ postgres 真库四格作答（判断题 a）——推送方认可：端口只有一只方法、语义全在 domain，全仓 `ports` 包无测试文件为既有惯例，替身测试测的会是替身；不补。要落的 ①–⑥ 逐项对上：① 四段 + `DPR-1:` + 重建门；锚另立 `SourceDataVersionAnchor`（判断题 b）——ADR 原话「同形」不是「同型」，避免把 `NewSupplementOnAcceptanceBaseline` 一类修订意图词借给引用锚，认可；② `(ctx, 租户, DeclaredParcelID) → (DeliveryPlaceResolution, error)` 封闭四格、无「不知道」、歧义上抛（判断题 c 认可：票面「答不出就是读面坏了」+ ADR-0060）；③ 经 `baseline.covers`，谱系未建模两处头注写明；④ `DELIVERY_PLACE` 原词，别的资料组修订不动锚，查不到修订答基线锚；⑤ 一条 SQL 走 0006 部分 GIN、`LIMIT 2` 分辨恰一 / 多于一、无迁移；⑥ 未做四条。`DeliveryPlaceReferenced` 由锚定格、采用判断未知取值上抛不默认——都对着 ADR-0130 决定二「`待复核`不给引用」与「没有默认」。判断题 d（`ParseDeliveryPlaceReference` 进基线）认可，理由行合登记纪律。**结论：两轴无阻断，进 main。**
- **进 main 记录（推送方 · 通道 1 · 12:5x）**：隔离树 `%TEMP%\idp-replay-psr06` 在 `346a6532`（= 当时 origin/main，nr/03 刚进）之上 cherry-pick：`1d7affa5→53469892` / `eed326c5→43919344` / `2dc0dd0d→70745894` / `5a30093d→ad1529aa` / `906f2f0f→60576319` / `c2a119c6→fcabde8e`；作者清点笔 `42bf6570` 与 main 上 nr/03 的清点同文件冲突，**跳过**，推送方在 tip 重生成为 `b734f8f5`（单独一笔）。`git diff c2a119c6 b734f8f5` 只剩 main 自己多出的文件（nr/03 十件 + tasks.md + awf/07 票面）与重生成的清点；本票十件逐字相同。`b734f8f5` 上 `gofmt -l` 零、`go build ./...` / `go vet ./...` 0；基线 `production_wiring_baseline.txt` 在该 tip 实测 4 条（非注释非空行）。占 55432 广播后**带 DSN** `go test -p 1 -count=1 -v ./...`：102 ok / 0 FAIL / 15 无测试，`--- PASS` 7897 / `--- SKIP` 1（仅 `pgtest.TestHelperTemplateOwnerProcess`）/ `--- FAIL` 0 / 0 cached，120 s，探针 `TestFreezeScopesAreInvisibleToEachOther` PASS，释号广播。本条 Comments 与基线重放注随推送方簿记一笔落在 `b734f8f5` 之上，ff 后 `push <sha>:main`，SHA 在推后广播里。psr/07 同链在 `c2a119c6` 上开，推送方按序重放。
