# 异常披露规则与冲突信号规则两册的目录读面：`/visibility-catalogues` 加两个 `kind`、`CatalogueListRead` 加两法、管理台 `pages/visibility/` 各显一签——[02](./02-exception-disclosure-and-conflict-signal-rule-registries-have-no-cli-or-online-entry.md) 步二的写签要跟着这两个读签走

Category: enhancement
Status: resolved——2026-09-11 12:0x 通道 1 推送方重放进 main（非作者评审 ← 通道 5 两轴 0 阻断，Standards 2 非阻断随票记；main 上 SHA 与分支 SHA 对照见 Comments「进 main 记录」）；此前 resolved——2026-09-11 12:2x（作者自标，机时约 11:5x）通道 6 完成（task-7491f646；分支 `mcp6-vedisc03` 基远端 main `2c7326ef`，代码 tip `f0bcb66f`）：做法 1–5 与完成判据 1–5 全部落地，带 DSN VE 三包 + `cmd/parcel-api` ok、admin-web tsc 0 + 220/220；完成记录见 Comments 末条；进 main 的 SHA 由推送方重放后另记。此前 in-progress——2026-09-11 11:3x 通道 6 认领（task-7491f646，通道 1 派单；分支 `mcp6-vedisc03` 基远端 main `2c7326ef`，隔离树 `D:/tops/idp-parcel-mcp6-vedisc03`；昨晚同票派通道 6 未开工即 crash，origin 无分支、无半成品，从零开）。此前 ready-for-agent——2026-09-10 通道 4 按通道 1 派单 task-d6660969 立票（推送方裁 02 要裁的 2「读面另立」），要裁的为零；取证锚 `062f5228`；只写票面未动代码
Blocked by: 无（两册的 postgres 写口与读口都已在 main，mech/08；本票只加目录上列与页面）

## 为什么立

票 02「缺口」第 4 条：`/visibility-catalogues` 读口（`VisibilityCatalogueReader`，`adapters/http/query_visibility_catalogues.go`）今天六个 `kind`——`ListMilestoneMappings` / `ListTriageRules` / `ListNotificationPolicies` / `ListClaimEligibilities` / `ListClaimAuthorizations` / `ListDisclosurePolicies`，编译期锁 `ports.CatalogueListRead`——不含异常披露规则（0023）与冲突信号规则（0025）两册。02 已裁 B（CLI + 端点 + 管理台），而伞票纪律「写签跟着读签走」要求写签落在读签旁；两册今天无读签，所以读面先行、另立本票，02 步二 Blocked by 本票（02「裁决」2）。

## 今天的形状（`062f5228` 上量，逐符号名）

- 端口：`internal/visibilityexception/ports/catalogue_read.go` 的 `CatalogueListRead` 六法各交回一种 `*CatalogueRow`；postgres 实现 `adapters/postgres/catalogue_read.go`。
- 两册的读口：mech/08 立的 `ExceptionDisclosureRuleRegistry` / `ConflictSignalRuleRegistry` 只有按键点读（供 `decide_disclosure.go` 与 `derive_projection.go` 消费），没有面向管理台的上列。
- http：`query_visibility_catalogues.go` 按 `kind` 分派到六法，`outcomeVisibilityCataloguesListed` 一格随 `kind` 回显；空册也是这一格（ADR-0077 决定四）。
- 装配：`cmd/parcel-api/endpoints.go` `/visibility-catalogues` 一行；`unwired_orchestration.go` 有它的隔离读占位；`isolated_read_test.go` / `endpoints_test.go` 钉端点表。
- 管理台：`apps/admin-web/src/pages/visibility/catalogue-api.ts` 按 `kind` 取列；三张目录页 `TrackingJudgmentRulesPage.tsx` / `ClaimPrerequisitesPage.tsx` / `DisclosurePoliciesPage.tsx` 各显几册的签，`presentation.ts` 列向。

## 做法（照六册既有的形，一字不新造）

1. **端口**：`CatalogueListRead` 加 `ListExceptionDisclosureRules` / `ListConflictSignalRules` 两法，各交回新行类型 `ExceptionDisclosureRuleCatalogueRow` / `ConflictSignalRuleCatalogueRow`——列只呈现不重建领域对象，字段照 0023 / 0025 两表登记的字面转写（规则键、版本或声明时刻、正文各格）；取值全是开放引用照登，不解释。
2. **postgres**：`catalogue_read.go` 加两条上列查询（租户条件 + `LIMIT`，稳定序照同族六法）；真库用例各一：有行 / 空册 / 他租户不渗。
3. **http**：`VisibilityCatalogueReader` 加两法（编译期锁随之），`kind` 封闭集加 `EXCEPTION_DISCLOSURE_RULE` / `CONFLICT_SIGNAL_RULE` 两值（名字照两册在 CLI 命令与批文里的既有原词，不另起）；契约测试各一例钉「空册也是 LISTED」「行逐键」。
4. **装配**：`/visibility-catalogues` 那一行不变（同一端点，多两个 `kind`）；`unwired_orchestration.go` 的隔离读占位加两法；`isolated_read_test.go` 若按 `kind` 枚举则补两行。共享接线文件动前占号。
5. **管理台**：`catalogue-api.ts` 加两个 `kind` 与两个 Record；两签落在 `DisclosurePoliciesPage.tsx` 旁——异常披露规则与披露策略相邻不同册（票 01 / 02 都强调这一点），签名与来源提示句要把「规则（0023）」与「策略」分开写；冲突信号规则落 `TrackingJudgmentRulesPage.tsx`（它裁的是投影派生里的替代链分叉，与里程碑映射、分诊规则同页）。没登显「未登记」不显默认。

## 红线

- 只读、只列：不新增写口、不动 `decide_disclosure.go` / `derive_projection.go` 的读路径。
- 规则取值全属实例半边（`PAR-VIS-*`）：页面不内置任何一条规则、不给候选。
- 异常披露**规则**与披露**策略**是相邻两册，页面、`kind` 名、提示句一处不混用。

## 完成判据（非作者评审逐项对）

1. `CatalogueListRead` 两法 + 两行类型；postgres 真库三例（有行 / 空册 / 他租户）。
2. http 契约两例；`kind` 集外仍拒（既有那一格）。
3. `cmd/parcel-api` 端点表不多一行、隔离读占位编译通过；`isolated_read_test.go` 绿。
4. 管理台 `tsc --noEmit` 0、`run-tests` 全 pass；两签各显一册、没登显「未登记」。
5. `gofmt -l` 空、`go build` / `go vet` 退 0；机制清点 tip 重生成。

## 地盘

`internal/visibilityexception/ports/catalogue_read.go`、`adapters/postgres/catalogue_read.go` + 测试、`adapters/http/query_visibility_catalogues.go` + 测试；`cmd/parcel-api/unwired_orchestration.go`（占号）、`isolated_read_test.go`（如需）；`apps/admin-web/src/pages/visibility/{catalogue-api.ts, presentation.ts, DisclosurePoliciesPage.tsx, TrackingJudgmentRulesPage.tsx}`。**不动** `adapters/postgres/exception_disclosure_rule.go` / `conflict_signal_rule.go` 与 `endpoints.go`。

## 参照

票 [02](./02-exception-disclosure-and-conflict-signal-rule-registries-have-no-cli-or-online-entry.md)「缺口」第 4 条与「裁决」2；票 [01](./01-disclosure-policy-is-a-catalog-not-a-content-factory.md)（相邻不同册）；[mech/08](../../mechanism-executor-triage/issues/08-ve-six-executors-behind-existing-uc-steps.md) VE-a / VE-d；ADR-0077 决定四（空册是内容）；`internal/visibilityexception/ports/catalogue_read.go`；`adapters/http/query_visibility_catalogues.go`；admin-web-page-wiring-frontier/02（六册读面的来历）。

## Comments

- 2026-09-10 · 通道 4（task-d6660969，基 `062f5228`，分支 `mcp4-adr0136`）：立票，ready-for-agent（要裁的为零）。**只写票面，未动代码。** 能力边界：读过 `query_visibility_catalogues.go` 的读口接口与锁缝、`cmd/parcel-api/endpoints.go` 那一行、`pages/visibility/` 文件清单；**没读** `ports/catalogue_read.go` 各行类型的字段、0023 / 0025 两表列定义、三张目录页的 JSX——行类型字段与签落哪一页开工时以代码为准，本票只定「照六册的形、两签分两页、规则与策略不混」。本目录无 spec 文件，子票表无处补。
- 2026-09-11 12:2x · 通道 6（task-7491f646；分支 `mcp6-vedisc03` 基远端 main `2c7326ef`）：**完成记录，转 resolved。** 逐笔（分支 SHA 只作此刻取证）：`b939c1d3` docs Status → in-progress；`c9f9bf78` feat 做法 1、2——`ports/catalogue_read.go` 加 `ListExceptionDisclosureRules` / `ListConflictSignalRules` 两法与 `ExceptionDisclosureRuleCatalogueRow`（条目 `ExceptionDisclosureRuleEntryRow`：客户 / 信号类型 / 可信度 / 披露条件 / 自动发布 / 内容来处，照 0023 两表列）/ `ConflictSignalRuleCatalogueRow`（信号类型 / 规则版本 / 可信度依据 / 批准人 / 登记时刻，照 0025 五列），`adapters/postgres/catalogue_read.go` 两条上列查询（租户条件 + `LIMIT`；规则版本新版在前、条目按登记键三维；冲突信号规则按信号类型）；`0b786fa6` feat 做法 3、4——`adapters/http/query_visibility_catalogues.go` `VisibilityCatalogueReader` 加两法（编译期锁随之）、`kind` 封闭集加 `EXCEPTION_DISCLOSURE_RULE` / `CONFLICT_SIGNAL_RULE`、两个 serve 与响应体（规则条目 `content` 只在 `disclosable` 时在场，与披露策略条目同一条传输纪律），`cmd/parcel-api/unwired_orchestration.go` `unwiredVisibilityCatalogue` 补两法（已占 / 释号）；`f0bcb66f` feat 做法 5——`catalogue-api.ts` / `presentation.ts` / `DisclosurePoliciesPage.tsx` / `TrackingJudgmentRulesPage.tsx`。代码 tip `f0bcb66f`。
  **完成判据逐项**：
  1. ✓ `CatalogueListRead` 两法 + 两行类型；真库三例 × 两册：`TestListExceptionDisclosureRulesReturnsVersionsWithEntriesNewestFirst`（有行：两版新版在前、接续闭合带终点、条目按客户 + 信号 + 可信度序、披露带内容 / 不披露不带）/ `TestEmptyExceptionDisclosureRuleCatalogueListsAsEmpty` / `TestExceptionDisclosureRuleListIsTenantScoped`；`TestListConflictSignalRulesTranscribesTheSingleRow` / `TestEmptyConflictSignalRuleCatalogueListsAsEmpty` / `TestConflictSignalRuleListIsTenantScoped`；既有 `TestCatalogueListRejectsNonPositiveLimit` 补两法。数据一律经 `RegisterExceptionDisclosureRules` / `RegisterConflictSignalRule` 登记，不直插行。
  2. ✓ http 契约 `TestVisibilityCataloguesListRuleCataloguesVerbatim`：两册各 200 + kind 回显 + 行逐键（规则条目逐字节比 JSON，不披露条目无 `content` 字段）、两册空册 LISTED + `[]`；既有三例（未配置 403 / 读不回 500 / 集外 400）的 kind 列表补两值，集外 `EXCEPTION_CASE` 仍答 400。红先见（新 kind 答 400 MALFORMED_REQUEST）后绿。
  3. ✓ `cmd/parcel-api` 端点表零 diff（`/visibility-catalogues` 一行不变），`unwiredVisibilityCatalogue` 补两法编译通过；`isolated_read_test.go` 按路径不按 kind 枚举，不需补，绿。
  4. ✓ `tsc --noEmit` 0（覆盖 `pages/visibility` 13 文件）、`run-tests` 220/220。两签各显一册：异常披露规则签落 `DisclosurePoliciesPage.tsx`（签词「异常披露规则」、列名「规则版本」、描述与空册句按签分说「规则（0023）」与「策略（0012）」——`disclosureKindNotes` 三册各一句），冲突信号规则签落 `TrackingJudgmentRulesPage.tsx`（无版本壳、登记时刻列、计数只报条）；没登显「当前租户尚无 X」+「读取入口已配置，但该目录为空……页面不会生成默认规则」，不显默认、不给候选。
  5. ✓ `gofmt -l` 空、`go build ./...` / `go vet ./...` 全仓退 0；带 DSN（占 / 释 55432 两次）`go test -count=1 -p 1` VE ports / postgres / http + `cmd/parcel-api` 四包 ok，VE postgres 新六例 -v PASS 6 / SKIP 0，`cmd/parcel-api` 顶层 PASS 61 / SKIP 0；`./internal/architecture/...` ok（管理台路径门禁绿——本票未新增任何前端路径）。机制清点在 `f0bcb66f` 干净检出（`%TEMP%` detached 树）重生成**零 diff**：本票只改既有文件、不加文件、端点表与消费缝不变，故不另成笔。
  **供评审的两处取舍**：① `catalogue-api.ts` 新型 `VisibilityRegistrableCatalogueKind = Exclude<VisibilityCatalogueKind, 两册>`，登记端点表 / `registerVisibilityCatalogue` / `presentation.ts` 登记三表全部收窄到它——两册今天没有在线写口（票 02 步二），`Record<VisibilityCatalogueKind, …>` 若不收窄就得替两册编一个不存在的端点；两页的登记签据此仍只铺原两册，读签三枚。② 异常披露规则两个布尔列的词取 0023 头注原话：`disclosable`「披露条件」成立 / 不成立，`auto_release`「自动发布」允许 / 不允许（`presentation.ts` `disclosableLabel` / `autoReleaseLabel`）；是布尔就只有两词，不算词表。
  **不做的**：两册的 CLI / 端点 / 登记签（票 02 步一、步二）；`adapters/postgres/exception_disclosure_rule.go` / `conflict_signal_rule.go` 写口与 `decide_disclosure.go` / `derive_projection.go` 读路径一字未动；`endpoints.go` / `endpoints_test.go` / `isolated_read_test.go` 零 diff。本目录无 spec 文件，子票表无处补；票 02 Blocked by 句同笔改「03 已落分支，等进 main」。
  **能力边界**：本机未装 `/implement` skill，按 `/tdd` 走——postgres 一段的红是编译红（`go vet` undefined），真库 PASS 在实现后带 DSN 实跑；http 一段红绿都可见（400 → 200）；admin-web 无页面级单元测试（测试跑器只编 `.ts`，两页的 `rowsOf` 与既有六册同在 `.tsx` 内），判据 4 只要 tsc + run-tests。没读 `cmd/parcel-ve-register` 全文——两 kind 名照票面与派单原词取，CLI 命令名归票 02 步一。
- **评审 ← 通道 5 · 钉 `51c6b79d`（代码 tip `f0bcb66f`，基线 `2c7326ef`）· 2026-09-11 11:50**（task-c4404a99，非作者，隔离树 `%TEMP%\idp-review-vedisc03`，不带 DSN；由推送方代落）。评审侧实测：stat 12 文件 +939/−68、无新文件；`gofmt -l` 空；`go vet ./internal/visibilityexception/... ./cmd/parcel-api/...` 退 0；六件 .go 均 LF；http 两例 -v PASS；`go test -count=1 ./cmd/parcel-api/ ./internal/architecture/` ok；postgres 真库 skip → 未验；tsc / run-tests 未跑（detached 树无 node_modules）→ 未验；机制清点在检出重生成零差。
  - **Standards**：**阻断 无**。**非阻断 2**：① 新注释里的跨文件计数：`ports/catalogue_read.go` `CatalogueListRead` 头注「八个方法」「前六法对 CatalogRegistry 的六个方法」、`ConflictSignalRuleCatalogueRow`「同族七法」；`adapters/postgres/catalogue_read.go` `ListConflictSignalRules`「这一族八法」（AGENTS.md「写代码注释」：计数与行号同构）。旧文已有「六法」一系，作者是延同旧俗；建议不再新增，不要求本票回改。② 两 `kind` 名票面做法 3 写「照 CLI 命令与批文既有原词」，而 `git grep -i 'exception-disclosure-rule|conflict-signal-rule' -- cmd/` 零命中——CLI 尚不存在（票 02 步一），作者已在能力边界写明照票面与派单原词。不是 03 的错，但 02 步一的命令名要倒过来对齐 `EXCEPTION_DISCLOSURE_RULE` / `CONFLICT_SIGNAL_RULE`（按 `DISCLOSURE_POLICY` ↔ `disclosure-policy` 的既有变形），建议推送方在 02 票面记一句。**无发现**：注释全中文；引迁移用编号 0023 / 0025 与符号名，无行号。规则与策略不混：`kind` 常量、`visibilityCatalogueKindLabels`（「异常披露规则」/「披露策略」）、`disclosureKindNotes` 三句各标 0010 / 0012 / 0023、列名「规则版本」，描述句与空册句均按签分说；`ports` 与 http body 头注都点明「相邻两册、条目不同形」。形照六册：租户在签名、`requirePositiveLimit`、`ORDER BY effective_from DESC, rule_version` + 条目按登记键三维 / `ORDER BY signal_kind`，空册答 `[]`。行类型只呈现：全是 string / bool / time，不重建领域对象；postgres 用 `*string` 收 NULL content 译空串，与端口注释「Content 只在 Disclosable 时非空」对得上，列名逐一对 0023 / 0025 建表语句核过。封闭响应形：`outcome` / `kind` / `catalogues` 三键，`content` / `effectiveTo` `omitempty` 与披露策略条目同一纪律。取舍 ① `VisibilityRegistrableCatalogueKind = Exclude<…>` 站得住：未动任何写路径（登记端点表六行原样、`registerVisibilityCatalogue` 只收窄类型），不收窄就得替两册编不存在的端点，而编端点才是碰红线；两页登记签仍只铺原两册。取舍 ② 两布尔词与 0023 头注原话逐字对上。页面不内置规则不给候选（空册句「页面不会生成默认规则」）。
  - **Spec**：**阻断 无。非阻断 无。** 逐项：① `CatalogueListRead` 两法 + `ExceptionDisclosureRuleCatalogueRow` / `EntryRow` + `ConflictSignalRuleCatalogueRow`；postgres 真库三例×两册都在（`TestListExceptionDisclosureRulesReturnsVersionsWithEntriesNewestFirst` / `TestEmptyExceptionDisclosureRuleCatalogueListsAsEmpty` / `TestExceptionDisclosureRuleListIsTenantScoped`；`TestListConflictSignalRulesTranscribesTheSingleRow` / `TestEmptyConflictSignalRuleCatalogueListsAsEmpty` / `TestConflictSignalRuleListIsTenantScoped`），数据全经 `RegisterExceptionDisclosureRules` / `RegisterConflictSignalRule` 登记、无直插 INSERT；`TestCatalogueListRejectsNonPositiveLimit` 补两法；真库评审侧未验。② http `TestVisibilityCataloguesListRuleCataloguesVerbatim` 两册 200 + kind 回显 + 条目逐字节比、不披露条目无 `content` 键、空册 LISTED + `[]`；`TestVisibilityCataloguesRefuseWrongMethodAndForeignKind` 集外 `EXCEPTION_CASE` 仍 400——两例 PASS。③ `endpoints.go` / `endpoints_test.go` / `isolated_read_test.go` / 两册写口 / `application/` 对 2c7326ef 零行；`unwiredVisibilityCatalogue` 两法编得过；架构包 ok。④ tsc / run-tests 评审侧未跑，读 diff：`formatInstant` 在 `catalogue-view.ts` 存在；`Extract<DisclosureKind, VisibilityRegistrableCatalogueKind>` 型自洽；两签各显一册，空册「当前租户尚无 X」+「不会生成默认」；冲突信号规则计数只报条、无版本壳，与 0025 一致。⑤ gofmt / vet 过；清点零差。红线：只读只列——无新写口，`decide_disclosure.go` / `derive_projection.go` 零 diff；规则取值不内置。票 02 Blocked by 句改为「03 已落分支 `mcp6-vedisc03`@`f0bcb66f`，等进 main」，只改那一句。未做票面没要的；`isolated_read_test.go` 按路径枚不需补。
  - **结论**：Standards 0 阻断 / 2 非阻断（最重：新注释延续「八法 / 七法」计数）；Spec 0 阻断 / 0 非阻断（真库与 tsc 评审侧未验）。可重放。
- **进 main 记录（通道 1 推送方，2026-09-11 12:0x）**：`%TEMP%\idp-replay-vedisc03` detached `a1b19433`（main，含 lc/34 + sa-cc/14），`cherry-pick 2c7326ef..mcp6-vedisc03` 五笔零冲突——唯一重叠文件 `cmd/parcel-api/unwired_orchestration.go`（lc/34 改的是 `unwiredLabelTransactions` 头注、本票加的是 `unwiredVisibilityCatalogue` 两法，不同块）；本票 Go / TS 文件对分支 tip 只差那两行 lc/34 头注。SHA 对照：`b939c1d3→063744d6`、`c9f9bf78→4fa83d08`、`0b786fa6→38492882`、`f0bcb66f→6ec6df6c`、`51c6b79d→53370bb6`。清点在 tip 重生成**零差**，无清点笔。**验证钉 `53370bb6`**：`gofmt -l` 空；`go build ./...` / `go vet ./...` 退 0；带 DSN `go test -p 1 -count=1 ./...` **107 ok / 0 FAIL / 15 无测试 / 0 cached**（11:58:51→12:01:00，129 s）；`-v` 探针 VE `adapters/postgres` + `adapters/http -run 'ExceptionDisclosureRule|ConflictSignalRule|CatalogueListRejectsNonPositiveLimit|VisibilityCatalogues'` PASS 16 / SKIP 0（评审侧「未验」的真库六例在此实跑）；admin-web `tsc --noEmit` 0、`run-tests` 220 / 220（评审侧未跑的判据 4 在此闭合）。簿记一笔在其上（本票 Status + 评审 + 本条、票 02 Blocked by 句 → 「03 已进 main」、tasks.md），纯 .md 自审；`ls-remote` 核 `a1b19433` 未动 → ff → `push <sha>:main`。分支 `mcp6-vedisc03@51c6b79d` 作封存出处、改名 `merged/`、远端删。**推送方对评审的处置**：两轴 0 阻断；Standards ①「八法 / 七法」计数是 AGENTS.md 明令的写法、作者延旧俗新增——推送方不代改作者代码，归 VE owner 一笔连旧文「六法」一系一起改成不计数的写法；Standards ② 采纳，票 02 步一命令名对齐 `EXCEPTION_DISCLOSURE_RULE` / `CONFLICT_SIGNAL_RULE` 的既有变形，本笔在票 02 Blocked by 句旁记一句。