# T1 门族五票对 3324ecb 重核汇总（PC/NR/CC/VE：03/04/05/06/09）

只读取证于 origin/main tip = `3324ecb`（隔离 worktree 检出，2026-08-20 晚）。四件口径：
仓储表 / 装载口 / 写入方（应用用例）/ 进程级登记口。逐票细节在各票 `## Comments` 的
「2026-08-20 对 3324ecb 重核」段；本页只留三行结论。状态由 MCP-1 改，此处只建议。

## 票 03 · PC 声明发布写入方（关键路径）

- **建议状态**：ready-for-agent（票面第 1 件先改写：仓储半边已被 `commercial_publication.go`
  的 `PublicationRegistry` 持久化面覆盖，26864d9，勿双做）。
- **仍缺的最小件**：发布**用例**（经 PublicationRegistry 写版本册 + 各 kind 声明表；六张
  声明表非测试 INSERT 仍为零）+ 进程级登记口 + `ResolutionKeySource` 实例登记面。
- **阻塞**：无机制面阻塞；声明内容属实例半边（PAR-COM-14/15/16/17）照红线留空。

## 票 04 · NR 网络定义登记册（NR 组，MCP-4 离线代核）

- **建议状态**：先改写票面再 ready——「无写入方也无解析层」已过时：NR-CATALOG-MECH
  （3b9f212，ADR-0068，迁移 0008 七表 + `LoadDefinitionsAt` + 七个适配器级 `Register*`）
  把模式设计与版本化存取做完了。
- **仍缺的最小件**：登记**用例** + 进程级登记口（`NewNetworkCatalog` 未接 cmd；0007
  `network_definition` 登记行仍无写入方，三证据视图恒答未配置的现状不变）。
- **阻塞**：解析层与服务区域/日历内容列被 PAR-NET-14 阻断（0008 头注明写后置），
  登记用例不被阻断；两件勿并一票。

## 票 05 · NR 自动改路事实目录（NR 组，MCP-4 离线代核）

- **建议状态**：ready-for-agent（2026-08-20 MCP-1 那条范围对齐关切可销：0008 头注明写
  冻结边界与改路条件不在 NR-CATALOG 内，无重叠）。
- **仍缺的最小件**：四件全部（表 + 装载口 + 写入方 + 登记口）+ `ReassessRouteHandler`
  接通；`assemble.go` 仍显式 `AutoReroute: nil` 注释原样，端口无任何实现。
- **阻塞**：机制面无阻塞；改善阈值与条件取值属 PAR-NET-14 待提供（合成条件 S 级验证）。

## 票 06 · CC 案件配置五类登记册

- **建议状态**：ready-for-agent（票面与代码零漂移，0006/0007/0008 与五视图自 32cc780
  后无人动过）。
- **仍缺的最小件**：版本化登记用例 + 写入方 + 进程级登记口（五类配置表非测试 INSERT
  为零）。
- **阻塞**：无；PAR-CUS-01..07 实例值待提供是常态。

## 票 09 · VE 五类规则/策略目录

- **建议状态**：ready-for-agent（两处票面事实先更新：MAP-KIND 已完（0014，1709872）；
  0018（0db1acf）新增授权目录两表，登记口范围一并覆盖）。
- **仍缺的最小件**：版本化登记用例 + 写入方 + 进程级登记口，覆盖五类目录现七张表；
  写入侧防重叠（`ErrAmbiguousCatalog` 同款）随建。
- **阻塞**：无；PAR-VIS-01/05/07/08/09 内容属实例半边待提供常态。

## 横向一句

五票同形（有装载无写入），但**起点已不同**：PC 差「用例+进程口」两层、NR 目录差
「用例+进程口」两层（存取口已在）、CC 与 VE 差「用例+写入方+进程口」三层、NR 改路
目录四层全差。若按一件解多墙排序，03（六墙同根）与 04（已备料最足）在前的判断与
development-plan 原排序一致。
