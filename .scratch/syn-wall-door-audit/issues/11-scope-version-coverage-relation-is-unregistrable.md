# 范围版本之间的覆盖关系无处登记，准入查询因此恒走保守暂停

Category: enhancement
Status: resolved

来源：票 02 交付过程中由 MCP-1 裁定分出（见
`.scratch/syn-wall-door-audit/issues/02-production-ownership-authority-has-no-adapter.md`
的 2026-08-21 裁定条）。**不是缺陷票**——今天的保守暂停是按 PILOT-SCOPE 明文办的诚实阻断，
本票要补的是让它有机会不保守的那份登记。

## 缺什么

`Suspensions.FindUnresumedSuspension` 现在三态回答准入：无未恢复暂停 → 开放；范围版本字面
相等 → 暂停（命中）；**有未恢复暂停但覆盖关系读不出来 → 暂停（保守）**。第三态之所以恒成立，
是因为登记册里没有任何地方记「范围版本 v1 与 v2 之间谁覆盖谁」。

`pgdomain.ScopeVersionReference` 是脱敏引用组合（客户、法人、产品、线路、伙伴等的引用），
真实值在参数登记册。从这个不透明串上**推**不出谱系——前缀、子串、版本号解析与时间序推断
一律禁止，那是从脱敏引用组合发明实例事实。所以关系只能**被登记**，不能被算出来。

## 今天的后果

登记册里只要还有一条已生效且未被恢复决定解除的暂停，任何范围版本问准入都答`暂停`。这不会把
系统钉死：一条未恢复暂停都没有时第一态成立，照常开放。但一旦有一条暂停立着，整个试点的新准入
就都拦住——这正是 PILOT-SCOPE 要的效果：

> 如果共享依赖、共同原因或证据不足导致影响范围无法可靠隔离，必须保守暂停整个试点的新准入，
> 不能仅拒绝当前报错的单个委托后继续放量。

## 要裁的

1. 覆盖关系记在哪一层：参数登记册（PAR-GOV-*）的一项，还是治理侧新增一张关系表？按 AGENTS.md
   「改试点实例状态 → 参数登记册」，倾向前者，但关系要被 `FindUnresumedSuspension` 读到就得
   在库面上有落点，两者可能都要。
2. 关系由谁登、凭什么证据登。范围版本升版本身是一次限量范围扩大的 `Go/No-Go`，覆盖关系是不是
   该作为那次决定的记录项一并落下——若是，它就不是独立登记而是 `Go/No-Go` 记录的一格。
3. 关系的形状：是「v2 承继 v1 的暂停」这种有向边，还是「v1 与 v2 互不相干」也要能显式登记。
   **两者都要能表达**——只能登「承继」的话，「确认不相干」就仍然读不出来，第三态照样恒成立。

## 红线

- **不得为了让默认路径回到「开放」而回填任何东西。** 没有租户就没有这份登记，恒走第三态是
  诚实阻断不是缺陷。
- 不得改成前缀、子串、版本号解析或时间序推断——理由见上，撞「敏感实例外置」红线。
- 关系登记不得成为一条绕过恢复决定的路：解除暂停只能靠 `RecordResumption`（原因解除证据、
  一致性核对、在途盘点、试点业务责任角色决定四件齐备）。登记一条「v2 不承继 v1」若能让 v1
  那条暂停对 v2 失效，那必须是**登记册事实**支持的结论，不是一次静默恢复。

## 参照

`docs/product/PILOT-SCOPE.md` 硬风险暂停段（隔离与保守暂停那句、恢复准入那句）；
`docs/product/PILOT-PARAMETER-REGISTER.md` PAR-GOV-03..07；ADR-0017；
`internal/pilotgovernance/domain/suspension_takeover.go` 的 `AdmissionSuspensionGround`。

## Comments

- 2026-08-21 MCP-5：按 MCP-1 裁定立票，未做任何实现。票 02 不阻塞在本票上——三态语义已交付，
  第三态是它今天的正确答案。本票解决后第三态会从「恒成立」变成「关系确实登不出来时才成立」。

- 2026-08-21 MCP-3（受用户委托裁断，「要裁的」三问出裁，转 `ready-for-agent`）：
  1. **落在哪层：两者都要，分工如下。** 权威事实落治理侧库面（新表）——关系要被
     `FindUnresumedSuspension` 读到，只有库面有落点；表结构属机制半边，由关系语义定死
     （范围版本引用对、边的种类、所属决定引用、登记时点），不含任何实例值，可援引 ADR-0068
     的结构先行——这里与渠道登记册不同，形状由第 3 问的语义背书，不取决于租户材料的样子。
     参数登记册一侧按 AGENTS「改试点实例状态」登脱敏索引与证据指针，不承载查询。
  2. **由谁登、凭什么：随 Go/No-Go 决定一并落，不设独立登记路。** 范围版本升版本身是一次
     限量范围扩大的 `Go/No-Go`，覆盖关系作为该次决定记录的一格随决定落库，登记人与证据即
     该次决定的登记人与证据。**无 Go/No-Go 决定就无关系登记**——届时恒走保守暂停照旧，
     那是诚实答案不是缺陷（票面红线第一条原样成立）。
  3. **形状：两种边都要能表达**（有向承继、显式互不相干），采纳票面论证——只能登「承继」
     则「确认不相干」永远读不出来，第三态照样恒成立。
  红线三条照单全收，尤其第三条：关系登记只影响「新范围版本是否承继旧暂停」的回答，
  解除暂停仍只走 `RecordResumption` 四件齐备，任何实现不得让关系登记变成静默恢复的旁路。
  主责上下文 pilot-governance；写口随治理登记册（阶段评审记录）走，读口给
  `FindUnresumedSuspension` 消费。

- 2026-08-21 MCP-6（受 MCP-2 派工执行，票 12 收口后顺延，同树另起一笔，票转 resolved）：
  按 MCP-3 三问裁定落地。
  - 库面：新表 `pilot_governance.scope_version_relation`（0005 迁移）——范围版本引用对
    （主键，有序）、边种类 CHECK 封闭二值、所属决定引用外键指回 `stage_review`（无
    Go/No-Go 决定就无关系登记，结构性成立）、登记时点；不含任何实例值（ADR-0068 结构
    先行，形状由第 3 问语义背书）。迁移不种默认行——没有租户就没有这份登记，红线第一
    条原样成立。
  - 域：`ScopeVersionRelation`（承继有向 / 互不相干对称，自指边拒绝）+
    `CompareScopeVersionRelations` 三格相容性判定（冗余/相悖/独立——互不相干与任一方向
    的承继相悖）；准入格新增 `AdmissionSuspendedByInheritedScope`，`Blocks()` 语义不变，
    PS 桥只问 Blocks() 不辨格，消费方零改动。
  - 写口：`RecordStageReviewCommand.Coverage` 随决定一并登记（后继一律取评审范围、决定
    引用取目标+候选组、时点取决定时点），不设独立登记路。相悖预检先于任何落库
    （`CoverageConflictBlocked` 带全部相悖对）；命令内自相矛盾未受理；边登记失败不翻
    决定、留 `CoverageContinuation` 续办引用，重放补登；「同一事实已在册」跳过不重登。
  - 读口：`FindUnresumedSuspension` 连关系表——登了承继按承继格拦（命中仍优先）、登了
    互不相干（任一方向，对称）那条暂停不及于所问版本、什么关系都没登第三态保守照旧。
    红线第三条落实：互不相干只隔开别的版本，暂停对写明的范围照旧拦着（真库用例点名证）；
    第二条落实：谱系零推断，SQL 只按登记行连接。
  - 参数登记册未动：无租户即无实例登记项，脱敏索引与证据指针待真实 Go/No-Go 时按 AGENTS
    「改试点实例状态」入册。
  - 验证：隔离树 gofmt 清、build/vet 0；治理侧含 PG 全绿（真库证承继/互不相干/保守/命中
    优先/逐条语义、FK 与 CHECK、无事务拒）；全仓 `go test -p 1 -count=1 ./...` 含 PG 结果
    与已验 SHA 见通道广播。

- 2026-08-24 MCP-3（死现场抢救合入，受用户裁定执行）：上条评论所属提交（`03a17f6`）
  从未落 main——它躺在 `t12-governance-register` 孤儿分支上（工作树 mtime 停在
  2026-08-21 23:47，其后未再响应），其下垫着该分支自己的票 12 实现（`4b5432a`），后者
  已被 main 上另一份票 12 实现（`6c5940c`，通道身份留痕表名与迁移 0004 均不同）顶替，
  不在抢救范围。本笔按票 13 先例「逐行复核＋验证由本笔完成」办：单摘 `03a17f6` 到
  main（cherry-pick 干净落地，`ports.go` 自动合并经人工复核；0005 序号在 main 上恰空）；
  逐行复核对照本票面 8-21 裁决三问与红线三条，逐格成立——读口三格定序命中>承继>保守、
  互不相干仅隔开所问版本而暂停本体照拦、写口后继强制取评审自身范围版本、相悖预检先于
  落库、消费方（PS 生产归属桥）只问 `Blocks()` 实测零改动。上条「见通道广播」的验证
  断言随死会话作废，不予采信；验证由本笔在隔离树重做，结果记于本笔提交信。
