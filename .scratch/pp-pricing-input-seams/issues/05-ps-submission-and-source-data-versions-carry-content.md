# PS 提交版本与客户原始资料版本留内容（测量 / 地址要素），使 02 / 03 两口能按基线锚与已采用版本锚答值：今天寄收件 name/value 条目只进 `PayloadDigest`、`CustomerSourceDataVersion` 只留痕不留内容——两只读口的「已采用版本锚」一格只能交锚，03 连基线锚一格也只能答「要素缺席」

Category: enhancement
Status: resolved——**已进 main，2026-09-15 14:4x 通道 1 推送方**（第六批，重放 `1d3fb469→3fee7544` / `636afbd3→6fffbd9e` / `6a620e4a→60635fe8` / `2c124b8e→3035765e` / `a893999a→969b2007` / 记录笔 `f7580e47→30113519`，批 tip `de822820`；评审 ← 通道 3 两轴 0 阻断（Spec 唯一「阻断」是清点未重生成，由推送方清点笔 `de822820` 兑）/ Standards 1 非阻断；`de822820` 带 DSN 全仓 115 ok / 0 FAIL；见 Comments「评审」与「进 main 记录」）。此前 resolved——**2026-09-15 14:3x 通道 5**（task-6dbb178b；分支 `mcp5-ppseams05` 基 main `bb7268f0`，代码 tip `a893999a`；代码五笔由通道 5 上一任会话 13:1x–13:43 落并逐笔推 origin，上一任 13:5x 占 55432 跑真库后会话结束、未释号未留数，本记录由通道 5 新会话 14:25 重跑后补写；非作者评审待推送方派）。此前 in-progress——**2026-09-15 13:1x 通道 5 认领**（分支 `mcp5-ppseams05` 基 main `bb7268f0`，隔离树 `%TEMP%\idp-parcel-mcp5-ppseams05`；按 /implement 走，判据 (1) 基线格与 (2) 已采用格先写 red）。此前 ready-for-agent——**2026-09-15 12:5x 通道 1 按用户「代裁」代裁（PS owner 口径），四条「要裁的」写入下方「裁决」节**：只留封闭要素（申报测量、地址要素）不留原文；走做法 1——两份既有 jsonb 快照各加一个可缺席的内容子段，零迁移、旧快照如实答缺；内容与摘要由 `CanonicalizeSubmissionPayload` 同一次调用产出、命令两样一起带、应用层不重算；CONTEXT「客户原始资料版本」词条「保留原始请求」仍指请求指纹，另加一句「保留封闭要素内容」。此前 draft——2026-09-15 11:3x 通道 5 立票（task-9cc0b1a6；通道 1 10:4x 追裁 [03](03-ps-origin-destination-postal-route-read-port.md) 取 A 时点名「顺手立 draft 05」）。只写票面未动代码；取证锚 main `3a21dab7`，代码事实在分支 `mcp5-ppseams02-03` 的 [02](02-billable-weight-actual-measurement-and-declared-dimensions-read-port.md) / 03 完成记录与判断项里逐符号可查。要裁的全归 PS owner，裁前不动代码
Blocked by: 无（02 PS 半边与 03 已在同分支落地——两口的形与答格已定，本票只让它们能答出值；两口的 PP 消费侧适配器票不等本票，先按「输入不可得」停）

**用词**：本票说的「内容」指客户原始资料里**已有消费方的那几格**——申报测量（[02](02-billable-weight-actual-measurement-and-declared-dimensions-read-port.md) 口）与地址要素（03 口，PS CONTEXT「地址要素」词条：邮编、国家 / 地区码）——不是客户原文整份。PS CONTEXT Rules「合成日志和证据索引只保留作用域引用、对象引用……不记录客户原文」那条纪律不因本票松动。

## 缺口（取证于分支 `mcp5-ppseams02-03`，逐符号名；两条都是 02 / 03 作者开工时量到、与两票裁决字面不符之处）

**一、提交版本不携带寄收件条目——地址要素在基线上也无物可读：**

- `git grep -n -E 'Scope\s+\[\]CanonicalContentEntry' -- internal/parcelshipment/domain/payload_canonicalization.go` → `SubmissionPayloadSpec.Scope` 是寄收件范围的 name/value 条目列表，它只被 `CanonicalizeSubmissionPayload` 编进 `PayloadDigest`。
- `git grep -n -E 'type versionDocument struct' -A6 -- internal/parcelshipment/adapters/postgres/shipment_request.go` → 提交版本落库的文档只有版本标识、来源指纹（含摘要）、成员、画像、时间；**没有条目**。`source_submission` 表（`migrations/parcel_shipment/0001_source_submission.sql`）只存 `payload_digest`。
- `git grep -n -E 'DeclaredProfiles|PayloadDigest' -- internal/parcelshipment/application/submit_shipment_request.go` → 命令收画像与摘要，**不收条目**；`adapters/http/isolated_write_intake.go` 的 `scopeEntries` 算完摘要即丢。
- 后果：03 的 `SubmissionVersion.addressElements`（`domain/address_elements_resolution.go`）今天恒答缺席，`ports.AddressElementsView` 对全部快照两段答 `NOT_PROVIDED`；`domain.AddressElementsOf`（按要素名读值的纯函数）无生产调用方，登在 `internal/architecture/production_wiring_baseline.txt` 上等本票。

**二、客户原始资料版本只留痕不留内容——两口的已采用版本锚一格只能交锚：**

- `git grep -n -E 'type CustomerSourceDataVersionSpec struct' -A12 -- internal/parcelshipment/domain/customer_source_data.go` → 版本收 `Request SourceSubmissionFingerprint`（身份 + `PayloadDigest` + 两时）与留痕清单（范围、基准、意图、原因、请求方、决定方、授权快照、适用 / 形成时间）；**没有那一版报了什么**。
- `git grep -n -E 'PayloadDigest\s+domain.PayloadDigest' -- internal/parcelshipment/application/amend_customer_source_data.go` → 修订命令同样只带摘要。`customer_source_data_version.snapshot`（`migrations/parcel_shipment/0003_customer_source_data.sql`）存的是这份留痕清单。
- 后果：`DeclaredMeasurementAnchoredOnAdoptedVersion` 与 `AddressElementsAnchoredOnAdoptedVersion` 两格只交锚不交值（两型头注写了为什么：交基线值等于把已被客户更正的申报当现行申报送出去）。PS CONTEXT「收件地点引用」词条许诺「旧锚永远解析到那一版内容」——版本今天没有内容可解析，这句在代码上不成立。

## 语言从哪里来

- PS `CONTEXT.md`「客户原始资料版本」：「针对明确委托、包裹、字段或资料范围形成的不可覆盖客户来源版本。版本保留**原始请求**、基础版本、补充/更正原因、对象范围、来源、业务时间和适用关系」——「保留原始请求」今天落成保留请求的**指纹**。
- PS `CONTEXT.md` Rules「客户寄收件、货物和申报原始资料在接受时形成快照。后续更正形成新版本」；「收件地点引用……旧锚永远解析到那一版内容」。
- PS `CONTEXT.md`「地址要素」（03 落的词条）：「要素的版本就是客户原始资料版本……要素在那一版上缺席即如实答缺」。
- PS `CONTEXT.md`「规范化业务内容摘要（`PayloadDigest`）」：摘要覆盖成员、范围、基础版本、服务要求与 `requestEffectiveAt`——本票不改它的算法（03 裁决 1 / 7）。

## 做法候选（两条以内，不选）

1. **只留封闭集、进既有 jsonb 快照**：提交版本从 `Scope` 条目里按 `AddressElementEntryName` 挑出寄 / 收两范围的封闭要素作规范化文档的一个子段，随 `shipment_request.snapshot` 的 `versionDocument` 落库（画像已是先例）；客户原始资料版本同法，在 `customer_source_data_version.snapshot` 里带上那一版的测量 / 地址要素子段；其余条目照旧只进摘要。不加列不加迁移；旧快照缺子段如实答缺。代价：修订版本的内容要由修订命令带进来——`AmendCustomerSourceDataCommand` 今天只带摘要，接单入口（隔离形态与将来的真渠道 Intake）都要把内容也交出来。
2. **内容另立表**：新迁移 `parcel_shipment/00NN` 一张「资料范围内容版本」表，键（租户、委托、范围、版本锚），值封闭要素子段；提交与修订两条路各写一行。代价：多一张表与一道迁移，两口读值多一跳；好处是内容与聚合快照分离、可按锚直接索引。

## 红线

- 只留有消费方的封闭要素，不留客户原文整份；日志与证据索引纪律不动。
- `PayloadDigest` 算法与既有摘要用例零改（03 裁决 1 / 7）；`0003` 不改，迁移只新序号。
- 两口（`DeclaredMeasurementView` / `AddressElementsView`）的形与封闭答格不动：本票让「已采用版本锚」一格开始带值、让 03 的基线格开始带值，格名与消费方契约不变。
- 修订版本的内容仍由客户声明、本上下文不替客户改值；节点实测、正式申报不覆盖它。
- 真实邮编 / 真实申报属实例半边；夹具全 `SYN-`。

## 完成判据（裁前草拟四条，留作出处；以下方裁决 7 写实的 (1)–(5) 为准，完成记录逐条对的是那五条）

1. 提交版本携带地址要素子段：接单带 `DELIVERY_PLACE.POSTAL_CODE` 一类条目 → `ports.AddressElementsView` 目的段答 `ANCHORED_ON_BASELINE` 带值；接单不带 → `NOT_PROVIDED`；旧形快照（夹具直写）→ `NOT_PROVIDED`；`domain.AddressElementsOf` 从 `production_wiring_baseline.txt` 出名单。
2. 客户原始资料版本携带内容：测量 / 地址要素范围上的修订被采用后，两口的已采用版本格 `Measurement()` / `Elements()` 第二返回值为真且值是那一版的；`待复核` 仍不给值。
3. 既有摘要用例（`payload_canonicalization_test.go`）零改；`internal/architecture` 门禁绿；`internal/parcelpricing/**` 零 diff。
4. `docs/product/MECHANISM-INVENTORY.md` 干净检出重生成（若走做法 2：迁移 +1）。

## 地盘

`internal/parcelshipment/{domain,ports,application,adapters/postgres,adapters/http}`、`migrations/parcel_shipment/`（仅做法 2，新序号）；`docs/domain/parcel-shipment/CONTEXT.md`（「客户原始资料版本」词条若要把「保留原始请求」写实成「保留封闭要素内容」）。不动 `internal/parcelpricing/**`、`internal/nodeoperations/**`、`internal/transportfulfillment/**`。

## 要裁的（全归 PS owner）

1. **留哪些内容**——只留 02 / 03 两口消费的封闭要素（测量、邮编、国家 / 地区码），还是把寄收件两范围的全部条目留成内容？前者是「有消费方才留」，后者会把客户原文整份落进快照，与日志 / 证据索引的纪律要对齐。
2. **留在哪里**——做法 1（既有 jsonb 快照的子段，零迁移）还是做法 2（另立内容版本表）。判据是「按锚直接读」的读法与聚合快照的体量。
3. **修订版本的内容从哪里来**——`AmendCustomerSourceDataCommand` 今天只带摘要；要带内容，接单入口（隔离形态 `isolated_write_intake.go` 与将来的真渠道 Intake）要把 `SubmissionPayloadSpec` 的条目交出来。摘要算法不动是定的（03 裁决 1 / 7），但「内容与摘要必须由同一份规范化输入产出」要不要成为构造门（防止内容与摘要各说各话）。
4. **CONTEXT「客户原始资料版本」词条「保留原始请求」那句**——是改成「保留请求指纹与封闭要素内容」，还是让「原始请求」继续指指纹、内容另立一句。

## 裁决（2026-09-15 12:5x 通道 1 按用户「代裁」代裁，PS owner 口径；依据是本票两条缺口的取证、02 / 03 完成记录与判断项、06 评审残差，钉 `e1ab9fb5`）

1. **要裁的 1——只留封闭要素，不留原文。** 内容 = 该资料范围上**由本上下文定名的封闭要素**：测量范围留 `DeclaredMeasurement`（重量 + 可缺席尺寸，02 已定形），寄 / 收两范围留 `AddressElements`（`POSTAL_CODE` / `COUNTRY_CODE`，03 已定形）；其余 name/value 条目照旧只进 `PayloadDigest`。**为什么不留整份**：CONTEXT Rules「不记录客户原文」那条纪律写的是日志与证据索引，快照不在字面内，但把寄收件条目整份落进快照等于让快照替代来源系统当原文库——机制半边没有任何消费方读它，实例半边（租户字段映到要素名）却要靠它长；「有消费方才留」让每多留一格都要先有词条，与 03 裁决 1「PS 定要素名的封闭集」同一把尺。封闭集要扩只走 CONTEXT「地址要素」词条加要素名，不走「顺手多留」。
2. **要裁的 2——做法 1：两份既有 jsonb 快照各加一个可缺席的内容子段，零迁移。** 提交版本 `versionDocument` 加 `elements`（按资料范围分的封闭要素 name/value，寄 / 收各一段，缺席即 `omitempty`）——画像 `Profiles` 已是「内容随提交版本进快照」的先例（ADR-0048）；客户原始资料版本 `sourceDataVersionDocument` 加 `content`（按该版本的 `DataGroup` 只带一种：测量范围带 `profileDocument` 同形的测量，寄 / 收范围带要素 name/value）。**为什么不另立表**：两口今天的读法是「先解析锚、再从锚指的那一版读内容」（`AddressElementsFor` / `addressElementsInGroup`、`DeclaredMeasurementAnchoredOnAdoptedVersion`），锚已经是版本身份，「按锚直接读」在 jsonb 上就是读那一行的子段，另立表多一跳、多一道迁移却不多任何一种读法；内容体量是几个要素，不是聚合快照的负担。**重开条件**：封闭集长到一只手数不过来、或出现「按要素值反查委托」的消费方——那时再立表，本票不预留。旧形快照缺子段 → 如实答缺（判据 1 第三句），不回填。
3. **要裁的 3——内容与摘要由同一次规范化产出；命令两样一起带；应用层不重算，也不设领域构造门。** `domain.CanonicalizeSubmissionPayload` 扩成一次调用交回**摘要 + 内容**（返回一个成对的结果类型，摘要算法零改、既有用例零改——加返回值不改哈希），寄 / 收要素由既有 `AddressElementsOf` 从同一份 `entries` 挑出、测量由既有画像路。接单入口（隔离形态 `isolated_write_intake.go` `scopeEntries` 处，将来真渠道 Intake 同位）**只调这一次**，把两样一起放进命令：`SubmitShipmentRequestCommand` 加 `DeclaredElements`（与 `DeclaredProfiles` 并列、同「允许缺席或部分覆盖」口径）；`AmendCustomerSourceDataCommand` 加 `Content`（按 `Scope` 的范围只允许一种形：测量范围一份 `DeclaredMeasurement`、寄 / 收范围一份 `AddressElements`，形与范围不配 → `未受理`；`Intent` 为显式清空时内容必须为空，非空 → `未受理`——清空就是「那一版上要素缺席」，两口的已采用格随之如实答缺）。**为什么不做领域构造门**：领域对象要验「内容与摘要出自同一份输入」就得拿到整份载荷重算摘要，与要裁的 1 相抵；结构上只留一条产出路径（唯一调用点 + 成对返回）比事后校验更守得住，接单入口用例断言「同一份 `SubmissionPayloadSpec` 一次调用得到的摘要与内容一起进了命令」。真渠道 Intake（PAR-INT-01）落地时照同一条路，本票不替它写。
4. **要裁的 4——「保留原始请求」仍指请求指纹，内容另立一句。** 「原始请求」在 ADR-0014 与 `SourceSubmissionFingerprint`（身份 + 摘要 + 两时）上已是「请求身份」的读法，改它会让引用这个词的地方一起换义；在词条末补一句：「版本另保留该资料范围上由本上下文定名的封闭要素内容（申报测量、地址要素），持锚方按锚解析到那一版内容；其余条目只进摘要，不留原文。」「收件地点引用」词条「旧锚永远解析到那一版内容」由此在封闭要素上成立，不改字。
5. **顺带收进本票（06 评审残差，PS owner 口径）**：`AddressElements` 头注「AddressElementsOf 读在场只认这一种缺席」收成「值层面只认这一种缺席」（同名矛盾条目那一路缺席由 `AddressElementsOf` 自己的头注讲）；`AddressElementsOutcome` 头注「所以对今天全部快照口都答 NOT_PROVIDED」随本票落地删掉「今天恒缺席」的整句——它不再为真。
6. **做法写实**：(1) `domain`：成对返回类型 + `DeclaredElements`（寄 / 收两段 `AddressElements`）+ `SourceDataVersionContent`（按范围一种形，构造门如裁决 3）；`CustomerSourceDataVersionSpec` 加 `Content`；`SubmissionVersion.addressElements` 从版本自己的要素子段读（删「今天恒为缺席」头注）；`DeclaredMeasurementAnchoredOnAdoptedVersion` / `AddressElementsAnchoredOnAdoptedVersion` 两格开始带值（第二返回值为真），`待复核` 仍不给值。(2) `application`：两条命令加字段，`SubmitShipmentRequest` 把要素随版本落、`AmendCustomerSourceData` 把内容随版本落，不重算摘要。(3) `adapters/postgres`：两份文档各加子段，`omitempty`；读回缺子段即零值。(4) `adapters/http` 隔离形态：`scopeEntries` 处改为一次调用取成对结果。(5) `internal/architecture/production_wiring_baseline.txt`：`AddressElementsOf` 那一行随生产调用点出现而出名单。(6) CONTEXT 词条一句（裁决 4）。**不动** `PayloadDigest` 算法、`0003`、两口的形与格名、`internal/parcelpricing/**`。
7. **完成判据写实**（替换上方「待裁后写实」四条）：(1) 接单带 `DELIVERY_PLACE.POSTAL_CODE` 一类条目 → `ports.AddressElementsView` 目的段 `ANCHORED_ON_BASELINE` 带值；不带 → `NOT_PROVIDED`；夹具直写旧形快照 → `NOT_PROVIDED`；`AddressElementsOf` 从接线基线出名单（`internal/architecture` 门禁绿）。(2) 测量范围与收件范围各一条修订被采用后，`DeclaredMeasurementView` / `AddressElementsView` 已采用格第二返回值为真且值是那一版的；显式清空的版本被采用后已采用格如实答缺；`待复核` 仍不给值；形与范围不配、清空带内容 → `未受理`。(3) `payload_canonicalization_test.go` 既有用例零改、摘要哈希零变（新用例只加）；接单入口用例断言摘要与内容出自同一次调用。(4) `internal/parcelpricing/**` 零 diff；迁移零新增；`docs/product/MECHANISM-INVENTORY.md` 在 tip 干净检出重生成（新增 domain 文件则生产 +N，作者预报）。(5) 带 DSN PS adapters/postgres + application + `cmd/parcel-api` PASS 非 SKIP。
8. **能力边界**：裁的是留什么、留在哪、从哪来、词条怎么写；具体不变式（`SourceDataVersionContent` 对「范围 × 形」的封闭组合、清空版本的内容零值怎么表达、`versionDocument.elements` 的 JSON 键名）归作者按代码定并写进判断项。读过：本票全文、02 / 03 完成记录与判断项、06 评审、`versionDocument` / `sourceDataVersionDocument` / `CustomerSourceDataVersionSpec` / 两条命令的结构体段、CONTEXT「客户原始资料版本」「地址要素」「收件地点引用」词条；**没读**：`isolated_write_intake.go` 正文、`amend_customer_source_data.go` 编排正文、ADR-0014 / 0048 正文、PAR-INT-01 交接。作者量到与代码不符，以代码为准并写进判断项，不回头等我。

## 完成记录（2026-09-15 通道 5，task-6dbb178b，分支 `mcp5-ppseams05` 基 main `bb7268f0`；代码五笔由通道 5 上一任会话 13:1x–13:43 落，本记录由通道 5 新会话 14:2x 补写）

**接手说明**：上一任 13:5x 广播占 55432 跑真库后会话结束，未释号、未留数、未写本记录（通道 4 14:0x 占号广播里「通道 5 占号后未见释号」即此）。本会话无在途记忆，材料只有分支五笔、派单原文（task-6dbb178b）与票面裁决；下面「逐条对判据 / 裁决」是**对 `a893999a` 的 diff 逐文件读出来的**，「验证」是本会话 14:25 重跑量得的，不转引上一任任何数。代码一行未改。

| 笔 | 时刻 | 内容 |
|---|---|---|
| `1d3fb469` | 13:13 | 认领：Status → in-progress |
| `636afbd3` | 13:2x | **domain 半边**。`payload_canonicalization.go` 加 `CanonicalSubmission`（`Digest()` + `DeclaredElements()`）与 `CanonicalizeSubmission`（内部调既有 `CanonicalizeSubmissionPayload` 取摘要、`AddressElementsOf` 对寄 / 收两范围各挑一次），既有函数一字未动；`address_element.go` 加 `DeclaredAddressElements`（寄 / 收两段，`InGroup` / `Empty`）；新文件 `source_data_version_content.go`：`SourceDataVersionContent`（`NewMeasurementContent` / `NewAddressElementsContent` / `Measurement` / `AddressElements` / `Empty` / `FitsAmendment`）；`customer_source_data.go`：`CustomerSourceDataVersionSpec.Content`、`CustomerSourceDataVersion.Content()`、`FormCustomerSourceDataVersion` 过 `FitsAmendment` 门、`ShipmentRequest.sourceDataVersionByID`；`shipment_request.go` / `submission_supersession.go` / `rehydration.go`：`SubmissionVersion.elements` 与 `DeclaredElements()`，三条 Spec 各加 `Elements`；`address_elements_resolution.go` / `declared_measurement_resolution.go`：两只 `*OnAdoptedVersion` 构造器加内容参数、`Elements()` / `Measurement()` 在已采用格随内容在场、`addressElementsInGroup` / `DeclaredMeasurementFor` 已采用路从 `sourceDataVersionByID` 取那一版内容、`SubmissionVersion.addressElements` 改从子段读、删「今天恒缺席」诸头注；新 `source_data_version_content_test.go`，两份 `_resolution_test.go` 改口 |
| `6a620e4a` | 13:27 | **application 半边**。`SubmitShipmentRequestCommand.DeclaredElements`、`FormNewSubmissionVersionCommand.DeclaredElements`、`AmendCustomerSourceDataCommand.Content` 三格，各原样进 Spec；`AmendCustomerSourceDataHandler.Handle` 在授权前先问 `FitsAmendment`（不配 → `ErrInvalidCustomerSourceDataVersion`，与意图未声明同格）；新 `amend_customer_source_data_content_test.go` |
| `2c124b8e` | 13:35 | **adapters/postgres 半边**。`shipment_request.go`：`versionDocument.Elements *elementsDocument`（`sender` / `delivery` 两段 `addressElementsDocument{postalCode, countryCode}`，全程 omitempty），`elementsDocumentOf` / `declaredElements` 往返，读回经 `AddressElementsOf`；抽出 `measurementDocumentsOf` / `declaredMeasurementFrom` 供画像与资料版本内容共用；`source_data_version.go`：`sourceDataVersionDocument.Content *sourceDataContentDocument`（`measurement` 或 `elements` 两段互斥），`sourceDataContentDocumentOf` / `content` 往返，要素段按版本自己的范围读；`address_elements_view.go` 头注改口；`shipment_request_test.go` 抽出 `submittedShipmentRequestSpec` 夹具；新 `source_data_version_content_test.go` 真库六条 |
| `a893999a` | 13:43 | **代码 tip。** `adapters/http/isolated_write_intake.go`：`submissionDraft` 加 `senderPostalCode` / `senderCountryCode` / `recipientPostalCode` / `recipientCountryCode` 四格，`scopeEntries` 用 `AddressElementEntryName` 拼封闭名，`IntakeSubmission` 只调一次 `CanonicalizeSubmission` 并把 `Digest()` 与 `DeclaredElements()` 一起放进命令；`_test.go` 加一条；`production_wiring_baseline.txt` 剪 `AddressElementsOf`（同段流水账记剪前 2 / 剪后 1，钉该检出）；PS `CONTEXT.md`「客户原始资料版本」词条末加裁决 4 那一句；`ports/address_elements_view.go` / `declared_measurement_view.go` 头注改口；`address_element.go` 头注按裁决 5 收尾 |
| 本笔（tip） | 14:3x | 本完成记录；Status → resolved；「完成判据」节标题改指裁决 7；未动 `spec.md` / `tasks.md` / 清点（派单「不碰」） |

**逐条对判据（裁决 7）**：
- **(1) ✓** 接单带 `DELIVERY_PLACE.POSTAL_CODE` 一类条目 → 目的段 `ANCHORED_ON_BASELINE` 带值：domain `TestAMemberOfARequestAcceptedWithElementsAnswersThemAnchoredOnTheBaseline`（两成员同答；寄件段只报邮编时国家 / 地区码不在场），真库 `TestAMemberOfARequestAcceptedWithElementsAnswersThemFromTheSnapshot`；不带 → `NOT_PROVIDED`：`TestAMemberOfAnAcceptedRequestWithoutElementsAnswersNotProvidedOnBothEnds`（03 那条改名改口）与 `TestARequestDeclaringOnlyTheDestinationLeavesTheOriginNotProvided`（一段有一段无）；夹具直写旧形快照 → `NOT_PROVIDED`：真库 `TestAnOldShapeSnapshotWithoutTheElementsSegmentAnswersNotProvided`（`snapshot #- '{currentVersion,elements}'` 抹掉子段后两段皆缺、不报错不回填）；`AddressElementsOf` 出名单：`git grep -c "^internal/parcelshipment/domain " -- internal/architecture/production_wiring_baseline.txt` 在 `2c124b8e` 为 2、在 `a893999a` 为 1（只作此刻取证），`./internal/architecture/...` 带 DSN ok。
- **(2) ✓** 测量范围与收件范围各一条修订被采用后已采用格带那一版的值：domain `TestAnAdoptedDeliveryPlaceCorrectionAnswersItsOwnElementsOnTheAdoptedVersion`（链尾 `20097`，不是基线 `10115` 也不是首版 `20095`；修订没报的国家 / 地区码不拿基线 `DE` 顶）/ `TestAnAdoptedMeasurementCorrectionAnswersItsOwnMeasurementOnTheAdoptedVersion`（`3.25`，外廓不顶），真库同题 `…FromTheSnapshot` 两条；显式清空被采用后如实答缺：`TestAnAdoptedExplicitClearLeavesTheAdoptedCellWithoutAValue`（要素与测量两口）与真库 `TestAnAdoptedExplicitClearAnswersTheAdoptedAnchorWithoutAValueFromTheSnapshot`；`待复核` 仍不给值：`TestAForkedChainWithContentStillWithholdsBothAnchorAndValue`；形与范围不配、清空带内容 → 未受理：domain `TestASourceDataVersionOnlyCarriesTheContentShapeOfItsOwnGroup`（拒 / 收两组用例表）+ application `TestAmendmentContentThatDoesNotFitTheScopeOrIntentIsRefusedBeforeAuthorization`（授权零次、版本号零签、聚合零写、来源事实仍保全）；登记册往返 `TestASourceDataVersionRegistryRoundTripsTheContent`。
- **(3) ✓** `git diff --stat bb7268f0..a893999a -- internal/parcelshipment/domain/payload_canonicalization_test.go` 空；`CanonicalizeSubmissionPayload` 与 `canonicalSubmissionDocument` 零改（diff 在该文件只有增行）；摘要一致由 `TestCanonicalizingASubmissionYieldsTheDigestAndTheClosedElementsTogether` 钉（成对产出的 `Digest()` 与只算摘要那条路对同一份输入一字不差；算不出摘要时成对结果同拒）；接单入口一次调用：`TestIsolatedSubmissionIntakeCarriesTheClosedElementsWithTheDigestFromOneCanonicalization`（带邮编摘要变、要素同值；新字段留空摘要不变、要素缺席；同草案重发两样同）。
- **(4) ✓** `git diff --stat bb7268f0..a893999a -- internal/parcelpricing/ internal/nodeoperations/ internal/transportfulfillment/ migrations/` 空；清点**未随笔提**（派单「不碰清点」），本会话在干净检出 `a893999a` 上重生成量得：`parcelshipment` 生产 186 → 187 / 测试 178 → 181，合计 988 → 989 / 938 → 941，其余列（应用编排、适配器、Outbox、缝、端口两口径）零变；只作预报，推送方在批 tip 兑底。
- **(5) ✓** 见「验证」。

**逐条对裁决**：
- **裁决 1（只留封闭要素）** 内容类型只有两形：`SourceDataVersionContent` 的 `measurement DeclaredMeasurement` / `elements AddressElements`；`DeclaredAddressElements` 只有 `sender` / `delivery` 两段 `AddressElements`（头注写了为什么不按 `SourceDataGroupReference` 开放成表）；提交路上其余 `Scope` 条目照旧只进 `PayloadDigest`——`CanonicalizeSubmission` 从 `spec.Scope` 里只经 `AddressElementsOf` 挑封闭名，不留条目；`versionDocument` / `sourceDataVersionDocument` 都没有任何 name/value 列表段。
- **裁决 2（做法 1，零迁移，旧快照如实答缺）** `versionDocument.Elements`（键 `elements`）与 `sourceDataVersionDocument.Content`（键 `content`）两个指针段全程 `omitempty`，`migrations/` 零 diff；读回 `nil` 即零值（`elementsDocument.declaredElements` / `sourceDataContentDocument.content` 的 nil 分支），旧形快照真库例钉住；测量内容与画像共用 `measurementDocumentsOf` / `declaredMeasurementFrom`，「与 `profileDocument` 同形」落成 `measurementContentDocument`（去掉成员那一格——成员已在版本范围里指名）。
- **裁决 3（一次产出、两样一起带、应用层不重算、不设领域构造门；形与范围 / 清空的门）** `CanonicalizeSubmission` 一次交回 `CanonicalSubmission`；`IntakeSubmission` 只调它一次，`PayloadDigest: canonical.Digest()` 与 `DeclaredElements: canonical.DeclaredElements()` 同出；三条命令各加一格，`SubmitShipmentRequestHandler` / `FormNewSubmissionVersionHandler` / `AmendCustomerSourceDataHandler` 原样搬进 Spec，无一处重算摘要或核对；**摘要 ↔ 内容不设门**（分支上没有任何「拿内容重算摘要比对」的代码）；**形 × 范围 × 意图的门**在 `SourceDataVersionContent.FitsAmendment`，`FormCustomerSourceDataVersion` 拒不配的（`ErrInvalidCustomerSourceDataVersion`），`AmendCustomerSourceDataHandler.Handle` 在授权前早问同一道门（判断只有 domain 一处）。真渠道 Intake（PAR-INT-01）未写，见判断项 ⑤。
- **裁决 4（「保留原始请求」仍指指纹，另加一句）** PS `CONTEXT.md`「客户原始资料版本」词条末加「版本另保留该资料范围上由本上下文定名的封闭要素内容（申报测量、地址要素），持锚方按锚解析到那一版内容；其余条目只进摘要，不留原文。」一字不差；「原始请求」与「收件地点引用」词条未改字。
- **裁决 5（06 两条残差）** `AddressElements` 头注「AddressElementsOf 读在场只认这一种缺席」→「值层面只认这一种缺席……同名多条互相矛盾那一路的缺席由 AddressElementsOf 自己的头注讲」；`AddressElementsOutcome` 头注删「今天这一格同时覆盖『条目未落库』……对今天全部快照口都答 NOT_PROVIDED」整段，改为「要素缺席答的是租户没报」。
- **裁决 6（做法写实 (1)–(6)）** 逐项与 SHA 表对得上：(1) `636afbd3`、(2) `6a620e4a`、(3) `2c124b8e`、(4)(5)(6) `a893999a`。**不动**清单核过：`PayloadDigest` 算法、`0003`、两口的形与格名（`AddressElementsView.LoadAddressElements` / `DeclaredMeasurementView` 签名零改，五格名零改）、`internal/parcelpricing/**`。

**判断项**（裁决 8 点名三条 + 量到与裁决字面不符之处）：
- **① `SourceDataVersionContent` 对「范围 × 形」的封闭组合**（裁决 8 点名）：测量内容只落 `DECLARED_MEASUREMENT` 范围，要素内容只落 `SENDER_PLACE` / `DELIVERY_PLACE` 两范围，缺席落任何范围（含 `CONSIGNEE_ADDRESS` 一类开放范围——那些范围上的修订本上下文没有词条，只留痕）；显式清空只许缺席。落在 `FitsAmendment` 一处，domain 构造器与 application 编排两处共用。要素内容不区分它是寄件还是收件那一段——范围由版本自己的 `Scope.DataGroup()` 说，读回时 `sourceDataContentDocument.content(group)` 按版本范围拼条目名，所以 `content.elements` 文档里不带范围键。
- **② 清空版本的内容零值怎么表达**（裁决 8 点名）：`SourceDataVersionContent{}` 零值即「那一版上要素缺席」，没有单独的「清空」标记——清空是 `Intent`（`ExplicitClearIntent`）说的话，内容层只需缺席；`NewAddressElementsContent` 收到两格都缺的 `AddressElements` 交回零值（「报了但两格都空」与「没报」在读口上本来就是同一格），`NewMeasurementContent` 拒没有重量的测量（`ErrInvalidDeclaredMeasurement`）；落库 `content` 段整段省略。03 判断项 ④ 留给本票的「显式清空要不要单独成格」——**不成格**：已采用格带锚不带值就是它的答法，两口五格名零改。
- **③ `versionDocument.elements` 的 JSON 键名**（裁决 8 点名）：`elements: { sender: { postalCode, countryCode }, delivery: { … } }`，每层 omitempty；键取要素名的驼峰、不取条目名 `DELIVERY_PLACE.POSTAL_CODE`——文档是本适配器的私有形，条目名只在领域读值那一步出现；`content: { measurement: { weight, dimensions } | elements: { postalCode, countryCode } }` 两段互斥。缺席的要素不写键，在场的值恒非空串，所以读回经 `AddressElementsOf` 重读一遍得到同一份在场标志。
- **④ 「`CanonicalizeSubmissionPayload` 扩成一次调用交回摘要 + 内容」落成新增同包 `CanonicalizeSubmission`，既有函数签名不动**——与裁决 3 字面（「扩成」「返回一个成对的结果类型」）不同，理由是同条裁决自己的另一半「既有用例零改」：改返回类型会让 `payload_canonicalization_test.go` 里每一处调用都得改；新函数内部只调既有函数取摘要，摘要只有一条产出路径。`CanonicalizeSubmissionPayload` 的生产调用方因此只剩 `CanonicalizeSubmission`（同包裸标识符算引用，接线基线未拦）。
- **⑤ 「命令两样一起带」今天只有提交路有生产填写方**：`SubmitShipmentRequestCommand.DeclaredElements` 由 `IsolatedSubmissionIntake` 填；`FormNewSubmissionVersionCommand.DeclaredElements` 与 `AmendCustomerSourceDataCommand.Content` 的**唯一非测试构造点是 `adapters/http/unconfigured_intake.go`**，它对两条命令都答 `ErrAccessChannelNotConfigured`——这两条路今天没有接单入口，不是本票漏接，是 PAR-INT-01 真渠道 Intake 未落（裁决 3 末句「本票不替它写」）。判据 (2) 因此在 domain / application / postgres 三层各自成立，**没有一条从 HTTP 到快照的修订全程用例**；真渠道 Intake 落地时要照 `IntakeSubmission` 那条「只调一次 `CanonicalizeSubmission`」的路把 `Content` 填上，届时再补全程例。
- **⑥ 隔离接单草案长出四格，`apps/admin-web` 那张页面没跟**：`submissionDraft` 加 `senderPostalCode` / `senderCountryCode` / `recipientPostalCode` / `recipientCountryCode`（本仓自己的页面草案字段，不是任何租户的渠道字段名）；`git grep -E "recipientPostalCode|senderPostalCode" a893999a -- apps/` 零命中——管理台提交页今天不发这四格，经它进来的委托 `DeclaredElements.Empty()`，基线格照旧答「要素缺席」。页面补字段归 admin-web 侧（不在本票地盘），是 03 / 05 两口在隔离形态上真有值的最后一格。
- **⑦ 已采用版本锚指向的版本不在本委托上 → 上抛 error，不落任何一格**：`addressElementsInGroup` / `DeclaredMeasurementFor` 经 `sourceDataVersionByID` 找不到时以 `ErrInvalidAddressElementsResolution` / `ErrInvalidDeclaredMeasurementResolution` 包着上抛——采用判断只在本委托自己的版本上派生，找不到就是读面坏了，与两口头注「没有『不知道』格」同一条理由。
- **⑧ 两只已采用格构造器的「缺席」纪律与基线格相反**：`AddressElementsOnBaseline` / `DeclaredMeasurementOnBaseline` 两格都缺即不是本格（拒），`*OnAdoptedVersion` 允许内容为零值——缺席在那里是那一版说的话（清空、或修订改的是无词条条目）；`AddressElementsResolution.Elements()` 的第二返回值因此从「格 == 基线」改为「要素非空」，`DeclaredMeasurementResolution.Measurement()` 原判法（`declared()`）不变，只是已采用格现在可为真。
- **⑨ 测量那一半不在 `CanonicalSubmission` 里**：裁决 3「测量由既有画像路」——`Profiles` 本就是 `SubmissionPayloadSpec` 的强类型段、命令原样带走，成对结果只配摘要 + 地址要素；修订路的测量内容由 `AmendCustomerSourceDataCommand.Content` 走 `NewMeasurementContent`。

**验证**（本会话 14:2x，隔离 worktree `%TEMP%\idp-parcel-mcp5-ppseams05` 检出 `a893999a`，`git status` 空）：`gofmt -l ./internal/ ./cmd/` 空；`go build ./...` / `go vet ./...` 退 0；14:2x `check_messages(waitMs 0)` 队列空 → 广播占 55432 → 带 DSN `go test -p 1 -count=1 -v ./internal/parcelshipment/... ./internal/architecture/... ./cmd/parcel-api/...` **2219 PASS / 0 FAIL / 0 SKIP**，退出码 0，14:25:02–14:25:26（24 s；PS adapters/postgres 7.8 s），21 包 ok、2 包无测试；本票新写的真库六条与 intake / domain / application 探针逐条 `--- PASS` 非 SKIP（`--- PASS` / `--- FAIL` / `--- SKIP` 三个 ASCII 子串在 UTF-8 读入的日志上计数，无锚）→ 释号广播。`-race` 本机无 cgo 未跑。**非作者评审待推送方派**（上一任无自评记录，本会话也不自评——完成记录里的逐条对照是读 diff 写的核对，不是评审）。

**能力边界**（本会话）：读过派单原文、票面全文、分支五笔完整 diff、03 完成记录（对格式）、`parallel-sessions.md` / `issue-tracker.md` / `workflow.md` 本机环境节；**没读** `payload_canonicalization.go` 既有部分正文、`resolveSourceDataAnchor` 与采用判断派生的正文、ADR-0014 / 0048 / 0130 正文、PAR-INT-01 交接、`apps/admin-web` 的提交页源码（只 grep 了字段名）。判断项 ①–③ 写的是代码里已经定下的形，不是本会话新裁；④–⑨ 是本会话对 diff 量到的事实。上一任为什么这么定，只能从头注与提交信读，本记录不替它补理由。

## 参照

[02](02-billable-weight-actual-measurement-and-declared-dimensions-read-port.md) 完成记录判断项 ①（已采用版本锚只交锚的理由）；[03](03-ps-origin-destination-postal-route-read-port.md) 裁决 7（取 A、内容落库归本票）与完成记录；[spec](../spec.md)「边界（三票共用）」「不在本目录」；`internal/parcelshipment/domain/payload_canonicalization.go`（`SubmissionPayloadSpec.Scope`、`AddressElementsOf`）、`address_elements_resolution.go`（`SubmissionVersion.addressElements` 头注「是 pp-seams/05 接内容的那一处」）、`declared_measurement_resolution.go`（`DeclaredMeasurementAnchoredOnAdoptedVersion` 头注）、`customer_source_data.go`（`CustomerSourceDataVersionSpec`）；`internal/parcelshipment/adapters/postgres/shipment_request.go`（`versionDocument`）、`source_data_version.go`（`sourceDataVersionDocument`）；`internal/parcelshipment/application/submit_shipment_request.go` / `amend_customer_source_data.go`；`internal/parcelshipment/adapters/http/isolated_write_intake.go`（`scopeEntries`）；`internal/architecture/production_wiring_baseline.txt`（`AddressElementsOf` 那一行）；PS `CONTEXT.md`「客户原始资料版本」「地址要素」「规范化业务内容摘要」「收件地点引用」；ADR-0014（规范化版本）、ADR-0048（画像随版本）、ADR-0130（收件地点引用与锚）。

## Comments

- 2026-09-15 14:3x · 通道 5 新会话（task-6dbb178b）：完成记录见上，代码一行未改。接手链：上一任通道 5 会话 13:1x 认领 → 13:2x–13:43 五笔落齐并逐笔推 origin → 13:5x 广播占 55432 跑真库 → 会话结束（未释号、未留数、未写完成记录）；本会话 14:1x 应通道 1 点名与「pp-seams/05 收尾」派单接手，14:25 重跑并补记。判断项九条里 ⑤（修订路无生产填写方，等 PAR-INT-01）与 ⑥（admin-web 提交页未长四格）是两条越出本票地盘的残差，归 PS owner / admin-web 侧另立，不挡合入。
- 2026-09-15 11:3x · 通道 5（task-9cc0b1a6）：立票，未动代码。两条缺口都是 02 / 03 开工时对 `3a21dab7` 量到的、裁决字面没预见的事实（02 裁决 3 / 5 与 03 裁决 1 都默认快照里有那一版的内容）；02 按裁决 7 以代码为准落成「已采用格只交锚」，03 经通道 1 10:3x 取 B → 10:4x 改 A（理由：基线与修订两半是同一道「版本留不留内容」的领域题，要一次定、不从一只读口里只开一半）。能力边界：读过 02 / 03 全文与两票实现、`payload_canonicalization.go` / `shipment_request.go` / `source_data_version.go` / `customer_source_data.go` / `submit_shipment_request.go` / `amend_customer_source_data.go` / `isolated_write_intake.go` 正文；**没读** 真渠道 Intake 的设计交接（PAR-INT-01），「要裁的」3 里接单入口那半靠 PS owner 与作者开工时量。
- **2026-09-15 14:4x · 评审 ← 通道 3 · 钉 `a893999a`**（基线 `bb7268f0` = merge-base；隔离检出只读；`go build ./...` 与 `go vet` PS + architecture 皆 0；未跑 go test）：**Standards 阻断 无；非阻断 1**：夹具真实邮编 / 国家码——`adapters/http/isolated_write_intake_test.go`、`domain/source_data_version_content_test.go`、`adapters/postgres/source_data_version_content_test.go`、`application/amend_customer_source_data_content_test.go` 用 `10115`（柏林）、`20095` / `20097` / `20099`（汉堡）、`DE`；票红线「夹具全 `SYN-`」，寄件格已用 `SYN-200000` 而收件格没跟。判非阻断：`10115` 在 main（03 已合）`address_element_test.go` / `address_elements_resolution_test.go` 已有先例，且为公开邮编非租户实例。**无发现（核过）**：注释全中文、无行号；计数仅 `production_wiring_baseline.txt` 剪除注记「剪前 2 剪后 1」且锚父提交 `2c124b8e`，合 AGENTS 例外与该文件惯例；跨文件引用皆符号名（`CanonicalizeSubmission`、`AddressElementsOf`、`FitsAmendment`）；新领域文件 `source_data_version_content.go` 零 import，领域包无 HTTP / pgx；06 残差两处已改（`AddressElements` 头注「值层面只认这一种缺席」、`AddressElementsOutcome` 头注「今天恒缺席」整段删换）。**Spec 阻断 1**：判据 (4)「`MECHANISM-INVENTORY.md` 在 tip 干净检出重生成」未做——照 CI 步骤 `tools/mechanism-inventory` 重生成后 `git status --porcelain` 非空（parcelshipment 生产 186→187、测试 178→181），CI「Mechanism inventory is current」门在 `a893999a` 必红。**非阻断 无。无发现（逐条）**：裁决 1——`CanonicalizeSubmission` 只经 `AddressElementsOf` 按寄 / 收 × `POSTAL_CODE` / `COUNTRY_CODE` 挑，`SourceDataVersionContent` 仅 measurement / elements 两形，`elementsDocument` / `sourceDataContentDocument` 无原文键。裁决 2——`versionDocument.Elements`、`sourceDataVersionDocument.Content` 均指针 + `omitempty`；`migrations/` 零 diff；真库旧形用例 `TestAnOldShapeSnapshotWithoutTheElementsSegmentAnswersNotProvided`。裁决 3——成对 `CanonicalSubmission` 内部调既有 `CanonicalizeSubmissionPayload`（非测试全仓唯一调用处），`payload_canonicalization_test.go` 不在 diff，新例断言摘要与旧路一字不差；三条命令字段原样透传无重算；`FitsAmendment` handler 早问 + `FormCustomerSourceDataVersion` 构造门 → `ErrInvalidCustomerSourceDataVersion`，授权前拒（`authorizer.calls == 0`）；`isolated_write_intake.go` 单次调用、`Digest()` + `DeclaredElements()` 同入命令并有例；无重算摘要的领域门。裁决 4——CONTEXT 仅追加一句，「保留原始请求」不动。判据 (1)(2)——基线带值 / 不带与旧形 `NOT_PROVIDED` / 已采用格 `20097`、测量 `3.25` / 清空答缺 / `待复核` 不给值皆有例。PP / NO / TF 零 diff；接线基线仅 `AddressElementsOf` 一行出名单。生产侧 `AmendCustomerSourceDataCommand` 唯一产出处为 `unconfigured_intake.go`，修订内容随真渠道 Intake，合裁决 3「本票不替它写」。**结论：不可进 main——唯一阻断是判据 (4) 清点重生成；补该提交后本轮无其他阻断，可进。**
- **2026-09-15 14:4x · 进 main 记录 · 通道 1 推送方**：**推送方对评审的处置**：Spec 唯一阻断（清点未重生成）按本仓流程由推送方在重放 tip 重生成、单独成笔 `de822820` 兑——PS 186→187 / 178→181 与评审、作者预报逐格相符（同批 27 也增七件、作者同样未重生成，一并兑）；票面判据 (4) 由此满足，不回作者。Standards ① 另立 [07](07-ps-content-fixtures-use-real-postal-and-country-codes-instead-of-syn.md)（chore，PS owner，ready）；作者判断项 ⑥（admin-web 提交页未长四格）记入 pp-seams spec 残差、归 admin-web 侧，⑤ 等 PAR-INT-01 不立票。**重放**：`%TEMP%\idp-replay-wave6` @ `cbffc269`，`cherry-pick bb7268f0..mcp5-ppseams05` 五笔 + 记录笔 `f7580e47` 零冲突（分支动的 28 件与 main 自 `bb7268f0` 起两笔——30 的两份 CC 测试文件 + .md——零文件重叠）；SHA 对照：`1d3fb469→3fee7544` / `636afbd3→6fffbd9e` / `6a620e4a→60635fe8` / `2c124b8e→3035765e` / `a893999a→969b2007` / `f7580e47→30113519`；`internal/parcelshipment` / `internal/architecture` / `docs/domain` 对作者 tip 零 diff、票面对分支 tip 零 diff。同批 sa-cc/27 / 29，三票零文件重叠。**验证钉 `de822820`**：`gofmt -l` 空；`go build` / `go vet` 0；14:34 占号，带 DSN `go test -p 1 -count=1 ./...` **115 ok / 0 FAIL / 16 无测试 / 0 cached**（136 s）；`-v` 探针 `TestAnOldShapeSnapshotWithoutTheElementsSegmentAnswersNotProvided` 与 PS postgres / http 的 `Elements|Content` 各格 PASS 非 SKIP；14:37 释号。簿记一笔在其上，纯 .md 自审；`ls-remote` 核 `cbffc269` 未动 → `merge --ff-only` → `push <sha>:main`。分支 `mcp5-ppseams05@f7580e47` 作封存出处、改名 `merged/`、远端删；`%TEMP%\idp-parcel-mcp5-ppseams05` 由通道 5 比内容后拆。**下游**：02 / 03 两口的已采用格与 03 的基线格开始能答值；PP 消费侧适配器票「输入不可得」的前提解除一半（真渠道 Intake 仍等 PAR-INT-01）。
