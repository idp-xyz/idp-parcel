# 实际移动事实登记册——八例编排里没有一例处理实际移动

Category: enhancement
Status: resolved——四层齐（表 + 重建门 + 端口 + 编排 + 真库适配器），棘轮那行已剪且
`transport-fulfillment` 组至此清空；更正入口未接，不在本票判据内，理由见文末
Blocked by: 01（已 resolved）

## 现状（锚 `9d6063c`）

`RecordMovementFact` 在 `domain/load_assignment_movement.go`，有完整实现与测试：事实身份、
班次、种类、地点、来源、版本、发生时间、门禁放行引用，以及更正关系（`corrects` / `correctedAt`）。

**TF 八例编排里没有任何一例处理实际移动。** 无表、无端口。

而 `transport-fulfillment` 的 CONTEXT 开篇就把「实际移动」列为本上下文管理的对象之一：

> 本上下文管理国际小包网络中的场外揽收、节点间运输、具体班次与容量、外部运输委托、运输交接、
> **实际移动**和末端派送。

## CONTEXT 的相关纪律

> 路由计划和计划履约段不预占班次、容量或运输资源。运输委托、订舱、承运接受和装载分配表达
> 执行准备，也不能提前制造实际履约段。

> 车辆故障、运输中断、失联、疑似丢失或异常案件建立本身不转移控制，也不自动结束实际履约段。

> 协作事项取消、替代或缩小范围时，停止尚未消耗的委托、订舱、预占和分配范围；**已经形成的
> 权威交接、实际移动和费用责任继续保留**。

最后一句是这一票的核心：实际移动是**已发生的事实**，上游意图取消不能抹掉它。库面因此必须
不可回写，更正走版本链。

## 要做的

**表**：新开迁移。移动事实的更正关系（`corrects` / `corrected_at`）成对在场或成对缺席。
门禁放行引用（`GateClearanceReference`）可空——CONTEXT 说门禁出发可阻断，但不是每次移动都
经门禁。**可空与「没有门禁」要分得开**，别用空串表达两种含义。

**端口 + 适配器**。

**编排**：需要一个登记用例。它与票 01 的段有关系——移动事实发生在某个实际履约段的控制期内，
所以标 `Blocked by: 01`。但**移动事实不结束段**（CONTEXT：中断不结束），两者的关系是引用
不是状态机。

## 陷阱

- **别让移动事实推导控制转移。** CONTEXT 逐项列举过：扫描、车辆到场、物理装载都不能替代
  控制边界。移动是段**内**的事实，不是段的成立或结束依据。
- 别把「装载分配」（意图）与「实际装载」「实际移动」（事实）合并。领域已经把意图与事实分开，
  库面合并会让那条分立失效。

## 完工判据

`RecordMovementFact` 有生产调用路径，棘轮基线那一行可剪。

## Comments

- 2026-09-03 · MCP-3：**本票 resolved，两刀。** `220619a`（端口 + 编排，剪 `RecordMovementFact`）、
  `d9b56aa`（迁移 `0010` + 重建门 + 真库适配器）。

  **票面点名的三处，逐条交代：**

  **一、更正关系成对在场或成对缺席。** 库内 CHECK 与重建门各守一遍，另加两条票面没列但同源的：
  前版引用不得指向自己（那是一条读不动的链）、更正不得早于发生（更正的是对同一件已发生的事的
  记述，不是把它挪到更早）。

  **二、门禁放行引用可空，而「可空」与「没有门禁」要分得开。** 库面用 NULL 不用空串，正是为了
  让两者分得开——下一个人看到 NULL 该读作「这一次没有门禁约束」，不是「这一行没填完」。

  **动笔时多分出一格票面没写的**：`GateRequired` **不进表**。它是判断的输入而不是事实的属性——
  一条已经成立的出发要么带着放行依据、要么本来就不受门禁管，两者在库里的区别就是那一列有没有
  值。**把判断过程当成事实存起来，下一次规则变了那一列就开始说谎。** 连带后果：重建门传
  `GateRequired=false`，因为装回时门禁那一问早有答案，再问一遍会把一条早已成立的出发判成「被门禁
  挡住」。

  **三、编排。** 结果代数里 **`门禁未放行`独立成格，不并进`输入未受理`**：两者的续办动作相反——
  前者要去取放行结果（判断本身属 `customs-compliance`，本上下文只认它的放行引用、不重建门禁
  机制），后者要去改输入。领域已用 `ErrDepartureGateBlocked` 把它与一般不合法分开，编排照着分。

  **陷阱两条都守住了，而第一条在库面上多守了一层**：编排既不读段也不写段；**表上也没有履约段
  外键**——加了会让「段必须先在」成为写入前提，而事实可能先于段的登记到达，那时正确的做法是照实
  记下事实而不是拒收它。意图与事实没有合并：装载分配在 `0009`、移动事实在 `0010`，两张表之间没有
  任何列互指状态。

  **未做且不在判据内**：更正入口（新版本回指前身）。领域与库面都装得下——写口只插不改、版本链读得
  回来——但那要一条编排，本票判据只到 `RecordMovementFact` 上生产调用路径 + 棘轮那行剪掉。

  **`transport-fulfillment` 那一组至此清空**（本批开工时 7 条）。**但基线注释里同时写了一句反话，
  这里重复一次因为它容易被误读**：名单清空只判名单清空，判不了「TF 都接上了」——本门禁只量导出
  工厂，一条编排没接它是量不到的。票 05 剪掉两行之后 `PerformOffsitePickupHandler` 仍不立段，而
  名单再不会为它红（那件另立了票 08）。

  **验证**：真库实测 `-run 'MovementFact|Departure' -v` **RUN 15 / PASS 15 / SKIP 0 / FAIL 0**。
  架构门禁（含棘轮）、迁移与 TF 四包 ok，`gofmt -l` 空，SQL 零 CR、零行号前缀。

  **一件报了不修的**：跑 `go build ./...` 时全仓红在 `cmd/parcel-api/unwired_orchestration.go`
  （`undefined: time`）。**不是本票的文件**——该目录当时在另一会话手上做端点接线，量到时 mtime 距今
  13 秒，是活的中间态。按 `parallel-sessions` 不去修、也不当成自己验证失败；只单独验了本票那几包并
  向对方报了一声。**所以本票没有声称「全仓构建绿」**。
