# 履约参与关系逐对象成立与结束

Category: enhancement
Status: resolved——逐对象成立（票 02/08 已落）与逐对象结束三来源齐，头号红线有测试钉住；
「实际承运商保持待确认」那一件不属本票，理由见文末
Blocked by: 01（已 resolved）, 02（已 resolved）

## CONTEXT 要求什么

> 一个载运对象参与某个实际履约段的对象级关系，保存该对象关联的计划履约段、实际控制起止、
> 结果及依据。**共享实际履约段中的每个载运对象分别成立、结束和更正，不能由整段结果覆盖成员
> 差异。**

> 实际履约段是共同执行与控制责任范围；一个实际履约段可以承载多个载运对象，每个对象必须通过
> 履约参与关系分别关联自己的计划履约段、控制起止和结果。

> 一条计划履约段可以尚未执行，也可以由一个或多个实际履约段完成；一个实际履约段也可以同时
> 承载来自不同包裹计划段的兼容对象。**任何关联都不得用实际事实覆盖原计划。**

> 可验证的实际承运责任或运输控制边界发生变化时，结束原载运对象的履约参与关系并形成下一实际
> 履约段。外部网络内部转包不可验证时保持未知，不虚构承运商或内部实际履约段。

## 为什么它单独一票而不并进票 01

票 01 建段册时会顺带建参与关系表（两者一次迁移落）。**但「逐对象分别成立、结束、更正」这条
纪律的执行器不在表里，在编排与聚合方法上**：谁在什么时点为哪个对象开一条参与关系、谁结束它、
一个对象的结果如何不污染同段其它成员。那是一整套行为，比建表大，且要在票 02 的两条立段入口
落地之后才谈得上。

领域侧 `FulfillmentParticipation` 与段上的 `JoinWithPickup` / `JoinWithHandover` 已经实现了
这些不变量。

## 要做的

- 逐对象加入既有段（首个对象立段由票 02 管，后续对象走 `JoinWith*`）。
- 逐对象结束：结果封闭多值，**不得只有「已完成」**（CONTEXT：达成计划 / 中断 / 折返 /
  提前终止 / 异常转交分立）。
- 控制边界变化时结束原关系并形成下一段——这条把票 01 与本票连成环，是本批最后落地的一件。
- 实际承运商可保持**待确认**，后续获得证据时形成带业务时间和来源的判断，**不追溯覆盖**原来的
  未知期间。

## 陷阱

- **整段结果不得覆盖成员差异**，这是本票的头号红线。任何「按段批量更新成员」的写法都违反它，
  哪怕当下所有成员结果确实相同。
- 「保持未知」与「没有」要分得开：外部网络内部转包不可验证时是**未知**，不是无承运商。

## 完工判据

参与关系可逐对象成立与结束，且有测试钉住「同段两成员结果不同时互不覆盖」。本票没有对应的
棘轮条目要剪——`JoinWith*` 是聚合方法不是导出工厂，本就不在棘轮网内。

## Comments

- 2026-09-03 · MCP-3：**本票 resolved，两刀**（`b9194c7` 交接与终止两路，`8a775d4` 交付一路与
  控制边界变化）。**逐对象成立那一半不在本票的两笔里**——它随票 02 与票 08 已经落地，本票补的是
  结束那一半。

  **四件要做的，逐条交代：**

  **一、逐对象加入既有段**：票 02 的 `enterFulfillmentSegment` 已做，本票不重做。

  **二、逐对象结束，结果封闭三来源**（有效交付 / 下一次权威交接 / 明确控制终止），**不是一个
  「已完成」**。三路共用一条判据：**结束的依据必须是一条读得回来的控制事实，不是调用方自报的
  引用串**——交接与交付各自按键读回登记，读不回来答`控制事实不在册`且参与关系一动不动。
  **终止那一路的依据只能由调用方给**（它指向本上下文之外的处置决定），那是三路真正的不对称，
  不是疏漏。

  **三、控制边界变化时结束原关系并形成下一段。** 下一段的段引用由调用方显式给，缺席就只结束
  不立新段——**owner 本日确认沿用票 02 那条裁定**。进下一段走的是与两条立段入口同一道门，不另
  写一份。**交付与终止不得带下一段**：两者之后都没有下一段，允许它就能表达「已交付但又进了
  下一段」；谓词 `carriesControlOnward` 存在就是为了让那句话写不出来。

  **四、实际承运商保持待确认——不在本票，且它不是遗漏。** 那是一条独立的判断（CONTEXT 有自己的
  「实际承运商」词条与「不追溯覆盖原来的未知期间」纪律），领域上没有承运商判断这个对象，参与
  关系上也没有承运商字段。**它要的是一个新的模型而不是本票这条编排上的一格**，硬塞进来就是把
  两个身份合成一个。本票不做，也不假装它做了。

  **头号红线（整段结果不得覆盖成员差异）有测试钉住，但要如实标出它靠什么守住的。**
  测试是真的（一个走终止、一个走下一次交接，两种结果并存且起点互不改写），**但这条性质不是本票
  实现出来的**：ADR-0097 那个窄写口在类型上就表达不出批量写，领域 `end()` 也只改匹配的那一条。
  本票加的测试是把它钉在编排缝上的**回归守卫**，防的是下一个人往这里加一条批量写口。

  **顺带把段登记册替身里那个占位补成了真的**：`EndParticipation` 此前一直返回 `Invalid`。
  **替身若按段整批写回，「整段结果覆盖成员差异」这一类就永远测不出来**——替身比真库宽容，测的
  就不再是真库那个口。

  **一条测试是写另一条时撞出来的，留下了**：交付夹具的到场时刻比两成员段的参与起点早几小时，
  领域当场拒了「终点早于起点」。那不是夹具凑巧，是 CONTEXT「实际控制起止」的直接后果——一次发生
  在对象进段之前的交付，结束不了它此后才成立的参与。既给交付那几例配了更早进段的成员，也把这条
  拒绝**单独钉成一条用例**而不是绕开它。

  **未做**：结束参与之后不自动关段。CONTEXT 生命周期④要求「全部有效参与关系已经结束**且不再
  接受新对象**」，后半句是一个决定不是一个可推导的状态；自动关段等于替人做了那个决定。
  `CloseSegment` 的编排入口因此仍未接，它要先有那个「不再接受新对象」的声明口。

  **验证**：TF 四包与架构门禁 ok，`go vet` 退 0，`gofmt -l` 空，`go build ./...` 退 0。
  未跑真库（本票无适配器层改动），`-race` 未跑。
