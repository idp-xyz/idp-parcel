# T2 量尺重核（对 `main=7d68475`，批务票 05）

r27 自记「T2 量尺（棘轮普查/消费清单）r27 未重取，下次接线批开工前按量尺重核」——本批即那
「下次接线批」，本文承载重核结果。口径全取
[census-d5e5d20](../production-wiring-ratchet-gate/census-d5e5d20.md)：git 侧匹配、大小写敏感、
调用点扣**全部**声明行（按包+名，不按「命中数 ≤ 1」）、四族不加总。

## 四族对照（基线 d5e5d20 → 本轮 7d68475）

| 族 | 基线 总/零 | 本轮 总/零 | delta |
|---|---|---|---|
| 一族 outbox 交接口 `NewOutbox*Handoff` | 46 / 41 | 47 / 33 | +1 总；零降 8（已接从 5 口升至 14 口） |
| 二族 应用层处理器 `New*Handler` | 62 / 53 | 68 / 35 | +6 总；零降 18 |
| 三族 领域工厂（十二前缀） | 72 / 13 | 72 / 13 | **完全持平，名单逐字一致** |
| 四族 ports 适配器（按接口断言认） | 66 / 46 | 84 / 20 | +18 总；零降 26 |

三个「零降」数字不叫还债进度——基线自注仍然作数：**零调用点绝大多数是十八墙里还没建门的
口，属缺席而非在场且错**；一族终点也仍不是 47 类全接（ADR-0049「接不住的类型登记比不登记
更糟」）。

## 各族要点

- **一族**：装配点已从基线的单一 `cmd/parcel-dispatch/assemble.go` 扩为多处（`cmd/parcel-api`
  的六个 `assemble_*.go`、`cmd/parcel-governance-register`）——重跑者别再拿「唯一装配处」当
  前提。零调用点 33 口全数对映
  [消费清单](../outbox-handoff-consumption-map/report.md)「应有未开」两栏的缝，A/B 票对的
  候选池就是它。
- **二族**：零 35 张里绝大多数是墙后写侧编排（SA/TF/CC 的命令处理器等墙降或等消费接线）；
  本批新增的 `NewRegisterPartyIdentityHandler` 已由 `cmd/parcel-commercial` 两个子命令调用，
  不在零单上。
- **三族**：13 张名单与基线逐字一致。**基线自记的前缀集缺口原样存在**：8-21 以来全部新领域
  函数走 `New*` / `Rehydrate*` 前缀，十二前缀集一格没长——下一个想拿三族当量尺的人先读基线
  「⚠ 这个 13 是下界」两节，26 才是当时的下界，本轮未重扫盲区（`Rehydrate*` 族 43+ 个）。
- **四族**：断言文件 97→123、构造函数 66→84；零调用点 46→20。降 26 全部由读面接线批吃掉
  （admin-web-page-wiring-frontier 八子票 + 本批 01/03 读口）。**名单里唯一的新面孔：
  `NewScopeVersionRelations`**（pilot-governance 治理关系表，审计票 11 产物）——非测试 0、
  测试 2，正是棘轮门禁要的「只被测试接线」那一格；`cmd/parcel-governance-register` 已存在
  但只装配授权区间三类，关系表读写没有进程入口。**下一张治理接线票的现成候选**，本轮只
  点名不开票。
- 其余 19 张四族零单全是基线 46 的遗留子集，无新增悬空。

## 消费清单（outbox-handoff-consumption-map）侧

基线表自注保质期「消费本表前按新 HEAD 重取证」。本轮以一族数字刷新其**装配面**：已接
14 口（基线 5 口），零单 33 口与该表「应有未开·跨上下文 26 + 同上下文 19」的缝对映关系
未逐行重验——**46 行×4 栏的逐行重取证不在本票范围**，谁开下一张 A/B 票对谁按表内自注
先重取证该行。

## 本轮没做的事（如实记）

- 三族盲区（前缀集外 132 个非 `New*` 导出函数、`Rehydrate*` 族）未重扫。
- 四族「无断言适配器」盲区（基线记 110 个含 `New*` 的无断言文件）未重扫。
- 消费清单 46 行逐行 CONTEXT-MAP/UC 判据未重验。

取证命令与索引脚本同基线「取证方法」节；本轮索引：调用名 1096、声明名 958（`internal/` +
`cmd/` + `migrations/`，非测试，含泛型声明格）。
