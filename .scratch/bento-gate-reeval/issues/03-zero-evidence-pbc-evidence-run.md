# 六项零证据 PBC 的取证推进：先复核 PBC-02/03 建模阻塞，再按简报取证序走

Category: enhancement
Status: resolved

来源：[票 01](./01-reeval-verdict-bento-gate-stays-blocked.md)「差什么」第 3 条，
2026-08-21 MCP-3 受用户委托裁断开票。

## 现状（票 01 取证）

`PBC-02/03/04/05/07/09` 零证据：`RunRepositoryContract` 全仓零调用（ADR-0031 的断言，
于 `b9f6cba` 复核仍成立）；`tests/bentocontract/` 只有 `candidate*`（PBC-01）与
`outbox_inbox_test.go`（PBC-06）。各适配器自己的回滚/事务模板测试是将来取证 PBC-03 的
原料，但没有任何一项已按候选登记为 PBC 证明。

## 步骤（按依赖序，第一步纯取证）

1. **复核 PBC-02/03 的建模阻塞现状**：简报记录的阻塞是聚合版本字段与端口签名，而
   ADR-0030/0031 已动过这一带——阻塞可能已消。产出：逐项「仍阻塞/已消」判定与证据，
   录本票 Comments。
2. 阻塞已消的项，把既有原料（各 `*FollowsTheTransactionalTemplate`、回滚测试等）按候选
   登记为对应 PBC 证明；缺的补。
3. 按简报「下一步」的取证序推进 `PBC-04/07`、`PBC-05`、`PBC-09`。
4. 九项对同一候选（`v0.1.0-rc.2` + checksum）全部通过后，按完成门禁登记 `B-06`——
   **那一步是闸门动作，另行报批，不在本票内**。

## 边界

- 每一步的证据对同一候选登记；实现存在不等于通过（ADR-0026），本票不宣称闸门状态。
- 第 1 步结论若是「仍阻塞」，后续步骤按票 01 口径继续等，本票以第 1 步产出收口，不硬推。

## 参照

票 01；ADR-0026、ADR-0030、ADR-0031、ADR-0017；`docs/design/` 持久化简报（闸门表与取证序）。

## Comments

- 2026-08-21 MCP-3：随票 01 收口裁定开票。
- 2026-08-21 MCP-4（领票，第 1 步取证完成）：**PBC-02/03 的建模阻塞已消**，逐项判定
  （对 main=`3756eeb` 实测；简报所记三处相扣缺口逐一对现行代码）：
  1. 「`domain.ShipmentRequest` 字段全未导出、外部包重建不出任意合法状态」→ **已消**。
     重建门已立：`domain/rehydration.go` 的 `RehydrateShipmentRequest`（ADR-0028/0030，均
     Accepted），今天放行`已提交`与`已接受`两状态，`已拒绝`/`已撤回`由
     `ErrRehydrationStateNotSupported` 显式挡住——ADR-0030「按快照表达力逐状态放行」正是此意。
     合同要跑的插入/加载/版本冲突/作用域隔离在可构造状态内够用。
  2. 「聚合无版本字段且 `Save` 不收预期版本，这一口在端口签名上」→ **已消，且「端口签名」
     这半个问题被 ADR-0031 的逐符号实测解散**。聚合现携 `revision int64`
     （`domain/shipment_request.go`，一次保存一次推进、状态转移不动它）；框架合同实测收的是
     `RepositoryContractScenario[K,A]` 的三个分开接口（`Loader`/`Inserter`/
     `VersionedUpdater.Update(…, expectedRevision)`），**不要求 `Save` 改名或改签名**——绑定处
     写一个把 `request.Revision()` 递给 expectedRevision 的场景壳即可。
  3. 「归档谱系的聚合另带 `EventBuffer` 而当前不带」→ **不再阻塞**。PS 域内 `EventBuffer`
     零出现，而框架合同形状（上述三接口）不含事件缓冲——那是归档谱系自己的形状，不是合同
     要求（ADR-0031 逐符号核过框架模块，非推断）。
  另两件当年缺、现在在的：`migrations/parcel_shipment/0002_shipment_request.sql` 在
  （ADR-0031 Consequences 写「`shipment_request` 表还不存在」的时点已过，该表及 0003-0006
  俱在）；`adapters/postgres/shipment_request.go` 的 `ShipmentRequests` 仓储在。
  **边界**：本判定只回答「阻塞已消」，不宣称任何 PBC 通过（ADR-0026：实现存在不等于通过）。
  步骤 2/3 就此解锁，MCP-4 续办。

- 2026-08-21 MCP-2（接管 MCP-4 死会话现场续办，步骤 2/3 收口）：**六项零证据 PBC 已全部取证**，
  对同一候选 `v0.1.0-rc.2`（checksum 由 PBC-01 用例逐字核对 `go.sum`）在真实 PostgreSQL 16.14
  上通过。用例全落 `tests/bentocontract/`；逐项证据、绑定方式与判据的展开已按取证录入简报
  「实现准入检查」节（`PBC-02`/`PBC-03`、`PBC-04`/`PBC-05`/`PBC-07`、`PBC-09` 三段），此处只记
  票内事实：
  1. `PBC-02` 委托聚合仓储过框架 `RunRepositoryContract` 三组子用例；`PBC-03` 真实仓储与真实
     Outbox 同一事务成功同现、回滚双消——第 1 步「阻塞已消」的判定经实测坐实，`Save` 未改签名。
  2. `PBC-04`/`PBC-05`/`PBC-07` 共用真实 `SubmitShipmentRequestHandler` 提交管线（事务边界按
     简报两段拍）取证：重放返原/异摘要冲突/八协程并发恰一份委托一份意图；信封过框架
     `Validate`、载荷八键无多无缺、同委托稳定分区、租约过期重投逐字节同；`ErrCommitUncertain`
     可认出、不自动重放、稳定关联可查回、恢复走重放不再开事务。
  3. `PBC-09` 产出半边：`ProduceConsumerProof` 九字段严格格式（无未知字段、无尾随值、
     RFC3339Nano），七格钉死所锁候选、只产 `PASS`；真实提交号在 B-06 协调时传入，不在本票。
  4. 为第 2 条取证新增「委托已提交」意图端口与 Outbox 适配器（`ShipmentRequestSubmittedHandoff`
     / `OutboxShipmentRequestSubmittedHandoff`），事件类型/Source 随本上下文既有家族，简报信封
     基线表随实现更新；适配器自带 PBC-08 行为面逐适配器五条证明（原子提交、回滚双消、重发幂等、
     无事务拒收、缺身份响亮）。生产接线不随本票发生——两个事务边界壳只住在合同夹具里。
  验证：`tests/bentocontract` 与 `internal/parcelshipment/adapters/postgres` 两包全量真库跑
  （不 `-run` 子集）全 PASS；全仓 `go test -p 1 -count=1 ./...` 设 DSN 零 FAIL（含 PG）。
  **边界**：本票不宣称闸门状态——闸门按票 01 裁定维持阻断；`PBC-08` 行为面收口归
  [票 02](./02-pbc08-behavior-face-closure.md)；九项齐备后登记 `B-06` 是闸门动作，另行报批。
  步骤 2/3 就此收口，票转 resolved。
  ——【落库注】上一条完成于分支 `mcp4-bento-pbc02` @ `1f7462b`（2026-08-21 23:47），当日未合入
  main、未广播；随 2026-08-24 MCP-1 的采用裁定入库（见下），内容原文保留，验证语句所述为
  其 08-21 基线上的结果。

- 2026-08-24 MCP-1（步骤 2/3 取证落地 + 一项事后发现，两件分开记；当时不知上一条的存在）：

  **一、取证证据 `6355d12`**（验证于该提交态的隔离 worktree：gofmt/build/vet 零信号，
  全仓 `go test -p 1 -count=1 ./...` 零 FAIL，DSN 已设、PG 各包实跑非跳过，255 秒）。
  六项就此不再零证据，证据全部落 `tests/bentocontract/`、对同一候选（`v0.1.0-rc.2`
  + checksum）登记：PBC-02 场景壳绑定（ADR-0031 结果代数 ↔ 哨兵错误、expectedRevision
  经重建门盖回聚合）、PBC-03 同事务同生共死、PBC-04 四面取三（第四面「不能创建第二个
  EventID」当轮判为不可取证：生产发射器在 internal/ 零存在）、PBC-07 两方向按稳定关联
  收敛、PBC-05 形状面（发射器缺席记边界）、PBC-09 九字段产出器。
  **二、事后发现（当时转 needs-info 的原因）**：证据落库后清点工作树，发现未合入的平行
  完成——分支 `mcp4-bento-pbc02` @ `1f7462b`，即上一条 Comment 的出处。该现场于
  2026-08-24 16:5x 量得：工作树干净、目录 mtime 停在 2026-08-21 22:28——全提交的死现场，
  分支指针即凭据。本轮领票前提（「第 1 步已由 MCP-4 完成，待办步骤 2/3」）漏点了这棵
  树——「换人之后先清点」的又一实例。去留裁定权上交，重启条件记为该裁定。

- 2026-08-24 MCP-1（用户令全面解决，裁定并执行采用，票转 resolved）：
  上一条「重启条件」由用户指令兑现，裁定**整体采用** MCP-2 的平行完成并随本笔落库：
  机制半边（「委托已提交」意图端口 + Outbox 适配器 + 行为面五条证明）、两段式事务夹具、
  六件取证用例、九字段产出器、简报信封基线定版与证据登记全部采回（采自 `1f7462b`，
  分支指针保留供事后补验）。与其重叠的本轮 `6355d12` 六件（场景壳、单段事务 rig 与
  草案形状信封夹具）同笔退役——同一合同一套证据，两套并存只多一份维护面；PBC-02/03/
  04/07 的取证效力由采用件等价或更强地承接（采用件的信封走生产适配器、并发面带意图
  唯一性、PBC-07 带提交事务计数）。唯一保留的本轮增量：`ErrCommitUncertain` 的
  「提交实回滚」方向用例（采用件只证了实落地方向），已按采用件夹具重写并入——该方向
  的口径按简报幂等表如实断言：保全已独立提交，重发同命令判重放、答`已有结果`且不附
  委托（「只有已经建单时才附已有委托结果」）、不再开提交事务；把保全后中断的提交续成
  委托属续办机制，不在本票。PBC-04 第四面与 PBC-05 发射器半边随机制落地而取证齐全，
  先前两处未决就此消除。简报勘正一处随笔：票 02 现状为 ready-for-agent，闸门表「行为面
  收口在办」半句改为「已开票待领」。
  边界照旧：闸门维持阻断（票 01 裁定）、`PBC-08` 行为面归票 02、`B-06` 是闸门动作另行
  报批、生产接线不随本票发生。验证见本笔提交信。
