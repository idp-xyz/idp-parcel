# 覆盖地平线与被挂起评价联动的读面

Category: enhancement
Status: in-progress——**05a（第 1 项）已落 `1cacc72` + `aa54a4b`；第 3 项摘要条已落 `f4c2996`，
「一键跳到复核动作」已落 `b98368d`（「跳到登记」与「挂起评价数」那一格未做，见末条 Comment）；
05b（第 2 项）两件裁决已由 [ADR-0105](../../../docs/adr/0105-evaluation-issue-carries-a-structured-series-subject-and-lands-in-a-child-table.md)
答完（`e53428c`），可开工。认领前先读 Comments，第 2 项的范围与票面所写不同**
Blocked by: 无（03 的代码已是主线祖先 `f62d619`）；第 2 项另有一道**未裁的领域改动**，见 Comments

## 要建什么

让缺口在当天被看见，而不是月底对账时。沿 ADR-0077 读面通例（独立查询端点、租户级作用域、未配置即拒、空册如实答空）：

1. **伴生读口 `ReferenceSeriesCoverageRead`**（`ports/catalogue_read.go` 同款，不拓宽写口）：按租户列出每条序列——种类、标识、在用版本引用、在用版本末期止点（开放则为空）、距 `now` 剩余覆盖时长、最近一次复核（时刻 / 结论）、未复核版本数。
2. **被挂起评价联动**：从评价库按待判断且原因码 ∈ {`REFERENCE_SERIES_UNRESOLVED`, `EXCHANGE_RATE_UNRESOLVED`} 计数并按序列种类分组——核 `ports/evaluation_read.go` 有没有可复用的列面；没有就在本票补最小读口，不扫 JSONB（ADR-0060 同款判据）。
3. **管理台**：在「计价参考序列」页顶部加覆盖摘要条（每序列一格：剩余覆盖天数、未复核版本、挂起评价数）；一键跳到该序列的登记 / 复核动作。
4. **告警阈值不在本票**：「剩余覆盖低于 N 天告警」「日环比超 X% 标黄」都是租户参数——机制只把数算出来摆在读面；阈值格留在配置里显式未决，页面文案说「阈值未配置」。

## 红线

- 读面不形成判断、不重建领域对象；只转写列面。
- 无租户时空列表是正常答案，不是故障。

## 验证

真库读口用例；`tsc --noEmit` 绿。

## Comments

- 2026-09-03 · MCP-1：**没动代码。第 2 项踩到两道票面没写的前提，第二道是领域改动，
  超出我拿到的授权，交回给 owner 裁。第 1、3、4 项不受影响。**

  票面第 2 项写的是「核 `ports/evaluation_read.go` 有没有可复用的列面；没有就在本票补最小
  读口，不扫 JSONB」。**「补一个最小读口」做不到**，理由有两层，取证如下（锚 `297d723`）。

  **第一层：那一列不存在。** `ports.EvaluationCatalogueRow` 只有 `Status`（五格封闭：
  COMPLETED/PENDING/CONFLICT/FAILED/UNRATABLE），没有原因码；`migrations/parcel_pricing/0001_evaluation.sql`
  的列也只有 `status`，**原因只活在 `snapshot` 那个 jsonb 里**，而票面自己禁止扫 JSONB。
  读口读不出一个不存在的列，所以这一项的真实前提是**先加列**，不是补读口。这一层人已授权。

  **第二层（真正的拦路者）：「按序列种类分组」在今天的领域里没有数据可分。**
  `domain.EvaluationIssue` 只有 `code` 与 `message` 两格（`evaluation.go`）。产出待判断那两格
  的地方是 `newEvaluationIssue("REFERENCE_SERIES_UNRESOLVED", seriesErr.Error())` 与
  `newEvaluationIssue("EXCHANGE_RATE_UNRESOLVED", …)`——**序列种类只以自由文本混在 message 里**。
  要按种类分组，就得给 `EvaluationIssue` 加一格结构化的种类，**那是领域模型改动**，不是加一列
  库面。按 AGENTS.md，难逆转的取舍走 ADR 或至少由 owner 裁；我拿到的授权是「加那一列」，
  不含这一条，所以停在这里。

  **还有一件形状问题，一并摆出来供裁**：一次评价带的是**一组** issue 不是一个
  （`withOutcome` 之前可能已追加过 `EXCLUDED_BY_RATE_CARD` 一类）。所以「加一列 reason_code」
  也不对形——要么 `text[]`，要么一张 `evaluation_issue` 子表（一行一条 issue）。子表是正规解，
  但它把本票从「补读口」变成「建表 + 改评价写侧 + 改领域」。

  **建议的拆法**（不是裁决，是给 owner 的选项）：

  - **05a**：第 1 项（`ReferenceSeriesCoverageRead` 覆盖地平线读口 + 真库用例 + 端点）。
    自足、不碰领域、不建表，随时可做。
  - **05b**：第 2 项。先裁两件——issue 要不要带结构化的序列种类（领域）、issue 落库用子表
    还是数组（库面）。裁完再动手。
  - 第 3 项（管理台摘要条）依赖 05a 与 05b；第 4 项（阈值）票面已明定不在本票。

  **这一格与今晚另外四件同族，写下来是因为它最难被撞见**：票面那句「没有就补最小读口」读起来
  像已经核过——它具体、有位置、给了退路。而**退路本身预设了那件事补得出来**。真去补的人会先
  写好读口再发现库面没有那一列，再发现领域没有那一格，两次都在已经动了手之后。

- 2026-09-03 · MCP-5：**切片 05a 交付（`1cacc72`）：覆盖地平线读口 + 8 个真库用例。第 1 项完，
  第 2、3 项未动。**

  **先答上一条那个问题：第 1 项的前提是真的，与第 2 项不同。** 逐格核过（锚 `b6db989`）：种类与
  标识 ← `reference_series_version.kind` / `series_id`；末期止点 ← `effective_to`（NULL 即无上界）；
  最近一次复核 ← `reference_series_review.reviewed_at` / `decision`；未复核版本数 ← 两表左连接可数。
  **不缺列，也不缺领域格**，所以这一项按票面做得下来。

  **落点**：`ports/catalogue_read.go` 追加 `ReferenceSeriesCoverageRow` 与 `ReferenceSeriesCoverageRead`
  （不动既有两口，理由同该文件头注：扩写既有接口会拆全部替身）；`adapters/postgres/reference_series_coverage.go`
  新开 `ReferenceSeriesCoverage` 类型（另立而不给 `OperationsCatalogue` 加方法——那两口只读版本表
  一张，本口要连版本与复核两张）。

  **在用版本的选择规则没有第二处定义。** `0004` 的头注写明「在用」不是版本表上的状态列，而是按
  时刻从复核记录派生的结论。所以本口只用 SQL 取候选，交 `domain.SelectInForceSeriesVersion` 挑,
  与 `ReferenceSeriesReviews.ResolveInForce` 同一条规则同一处。**在读口里用 `ORDER BY` 排出「最新
  通过的那版」会让同一个问题有两套答案，而它们只在恰好同时被改时才一致。**

  **三处与票面字面不同，理由逐条写在这里而不是只写在代码里：**

  1. **没有「距 `now` 剩余覆盖时长」那一格**，虽然票面第 1 项列了它。末期无上界时那个数既不是 0
     也不是无穷，是「没有终点」，**折成一个时长就把这个区别丢了**；而算差值要挑时区与舍入口径，
     那属于呈现面。改交 `InForceEffectiveTo` 加显式布尔 `HasInForceEffectiveTo`，页面自己算。
     判据与 `PriceCardCatalogueRow` 用显式布尔而不是零值判断那条同源。
  2. **「未复核版本数」拆成两格。** `UnreviewedVersionCount` 是一条复核都没有的版本（等复核人），
     `ReturnedVersionCount` 是复核过但至今未通过的版本（等登记方更正）。**两者的续办动作不同，
     折成一格会让它们在页面上长同一张脸**——与本仓反复记的那个形状同族。
  3. **行的键是（序列标识 + 种类）而不只是标识。** 在用解析的入口本来就按两者问，而版本表没有
     任何约束把一条标识钉死在一个种类上。同标识两种类时各出一行是照实转写；折成一行就得挑一个
     种类，**那是读面替人裁一件库面没裁的事**。

  另两件形状取舍：`limit` 数的是**序列条数**不是行数（一条多版序列不该把整页占满）；`at` 在方法
  签名上、不取适配器内的时钟（在用是「该时刻之前通过的最新版本」，读口自取 `now` 会让同参数的
  两次查询答不同的话，而这一口正要被当证据看）。零时刻与非正 `limit` 都在读口上拒绝，不静默
  答一个没人决定过的取值。

  **不读快照**：候选所需的版本引用由（标识、版本、`content_digest`）三列构造，符合目录读口那条
  「权威内容在快照、目录只转写列面」的纪律。

  **验证（在 `1cacc72` 的干净 detached 检出上，不在共享树——树上此刻压着别人的在途改动）**：
  `gofmt -l .` 空、`go build ./...` 退 0、`go vet ./...` 退 0、`go test -count=1 ./...` **93 包 ok、
  零 FAIL**。**含真库且按开关取证**：8 个 `TestCoverage*` 带 `-v` 单跑全部 `--- PASS` 非 SKIP
  （DSN 指门禁容器 55432）。**`-race` 未跑，不声称跑过。** 验证树已拆（不带 `--force`，退 0 且
  实测目录已不在）。

  **本片未做、交回去的：**
  - **端点。** MCP-1 建议的 05a 含端点，但那要碰 `cmd/parcel-api/endpoints.go`（共享接线文件）。
    本刀不占它，好让做票 04b 的人不必等；**接线另切一刀，占号时另发一条**。
  - **第 3 项（管理台摘要条）** 依赖端点，故也未动。
  - **第 2 项** 仍卡在上一条那两件裁决上，本片一个字没碰。

  **一件本片自己撞到、值得下一个人知道的**：新建的 `.go` 文件被工具写成了 CRLF（`gofmt -l` 当场
  报出来），按字节级 CRLF→LF 回写修掉后才提交，入库为 LF。成因与复发范围见今晚频道里 MCP-4 的
  取证——**改既有文件不会，新建文件会**。新建 `.go` 之后顺手量一次 CR 字节数比等 `gofmt` 报便宜。

- 2026-09-03 · MCP-5：**05a 第二刀交付（`aa54a4b`）：覆盖读口进端点表。第 1 项至此端到端可用。**

  `GET /pricing-reference-series-coverage`，一条序列一行。`adapters/http` 新开
  `query_reference_series_coverage.go`（读口接口 + 端点 + 响应体），`cmd/parcel-api` 五处接线：
  端点表一行、`main.go` 造适配器并传入、`assembleBusinessEndpoints` 多一个入参（**位置紧跟
  `referenceSeries` 之后，改那个函数的人注意参数序**）、探针表一行、隔离读放行表一行。unwired
  占位另立类型不并进 `unwiredPricingCatalogue`（判据同评价册：并成一个会让装配测试盖不住
  「这本册接错了适配器」）。

  **`asOf` 回显不是装饰。** 「在用」是按时刻从复核记录派生的结论；不把所用的那一刻说出来，页面上
  就会出现一个只对服务端某一瞬成立、却看起来永久的权威答案。**同一条判据下，目录页的状态列被裁为
  不含「在用」**（票 04b，owner 裁）——那一页没有正当的时刻源；本端点有，代价就是必须回显。两处
  裁决方向相反而理由同一条，值得并读。

  **响应体防一处折叠，这是本刀最要紧的一格。** 若把止点平铺成一个可缺席的键，「这条序列没有在用
  版本」与「有在用版本但它没有终点」**都表现为止点缺席**，而两者一个是「今天没得用」、一个是
  「用着且不会到期」，续办动作相反。改成 `inForceResolved` 布尔 + 可缺席的 `inForce` 子对象，
  子对象里再用 `openEnded` 只答第二问；布尔与子对象成对的判据同 party-commercial 的
  `caliberDeclared`（让调用方分得开「服务端说没有」与「这个键没序列化出来」）。有一条传输层用例
  专钉这一格。

  **不开 `?at=`**：历史问法今天没有调用方，现在加就是发明一个没人要的形状；端口侧已把 `at` 留在
  签名上，要时加得上。时刻源在组合根选定（`systemClock{}`），端点自己收 `CoverageClock`，所以
  传输层测试注入固定时钟即可。

  **进隔离读放行表**，与兄弟端点 `/pricing-reference-series` 同待（ADR-0078 放行的是运营查阅面）；
  **写行不入格这一条不变**。

  **验证（在 `aa54a4b` 的干净 detached 检出上）**：`gofmt -l .` 空、`go build ./...` 退 0、
  `go vet ./...` 退 0、`go test -count=1 ./...` **93 包 ok、零 FAIL**（含真库 DSN）；7 个
  `TestCoverageEndpoint*` 传输层用例全绿。**`-race` 未跑，不声称。**

  **一格如实报红**：`ci.yml` 的「Mechanism inventory is current」在 `aa54a4b` 上**是红的**——本笔
  加了一个端点与两个文件，清点必然陈旧。**未在本笔重生成**，照全组已收敛的规矩：那道门只在推的
  那一刻需要绿，由推的人最后重生成一次（任何增删文件的提交都会让它重新陈旧，今晚已自证两次）。

  **第 3 项（管理台摘要条）现在不再被端点挡着**，谁接都行；数据面与 `asOf` 都在响应体里。
  **第 2 项**仍卡在那两件裁决上，本刀一个字没碰。

- 2026-09-03 · MCP-4：**第 3 项摘要条交付（`f4c2996`）：接 `aa54a4b` 的端点，摆在「参考序列」目录
  之上。本项两件未做，逐条写在下面；第 1、2 项一个字没碰。**

  **落点**：`apps/admin-web/src/pages/pricing/` 下 `CoverageSummaryBar.tsx`（只负责摆）、
  `coverage-rows.ts`（判读，纯函数）、`coverage-rows.test.ts`（node:test）、`api.ts` 追加
  `ReferenceSeriesCoverageRecord` 与 `listReferenceSeriesCoverage`、`ReferenceSeriesPage.tsx` 挂一行。
  判读与摆分开，判据同 operations 的 handover-scope-summary、party 的 policy-rows：这一层要钉的是
  「哪两种状态不得折成一句」，用测试钉得住、用组件钉不住。

  **先把 05a 那条「三处与票面字面不同」的第 1 项补成完整的一句，两半合起来才是一个决定。**
  那里写的是「算差值要挑时区与舍入口径，那属于呈现面」——**它读起来像「后端不算」，而真正的理由是「算在能把那两个
  选择说出口的地方」**。后端因此不出「剩余时长」那一格而只给 `effectiveTo`；前端挑了，并且挑的
  就是这个：**两个时刻都取服务端给的 RFC3339，在同一条时间轴上按 UTC 作差，向下取整到自然天；
  可以为负——已过期是一种真实状态，不夹逼到 0**（按 0 显示会把「已过期」伪装成「今天到期」）。
  口径钉在 `coverage-rows.ts` 的 `remainingDaysBetween` 与同名用例上。没有这后半句，那格数字在
  页面上仍然会出现，只是没人说得清它按什么算的——那正是 05a 不肯在后端算它的原因。

  **三态照后端的形状读，不折回去。** 响应体把在用那一组做成 `inForceResolved` 布尔 + 可缺席的
  `inForce` 子对象、子对象里再用 `openEnded` 答第二问，前端判读因此是三种答案而不是两种：`open`
  （在用且没有终点——**不是「剩余很多天」**）、`bounded`（在用且有止点，剩余天数只在这里有意义）、
  `none`（今天没有在用版本——**与 `bounded` 且剩余为 0 不是一回事**：前者去登记或去复核，后者去
  登记下一版）。三种答案三句话，不共用一个破折号。**先看布尔再看子对象**：布尔说有而子对象缺席、
  或 `openEnded` 为假而止点缺席，都是响应不合契约，按 `none` 如实作答而不按 0 天算——按 0 算是
  页面凭空造出一个业务结论。若后端当初把止点平铺成一个可缺席键，前端分不出前两种，**而且不会
  知道自己分不出**；这一格只能在定形状那一刻防，上一条防了。

  **另三处刻意不做，与票面第 4 项和上一条同源**：不标黄不标红（阈值 N 是租户参数，没有 N 先上
  颜色等于替租户定了一个；页面文案写「阈值未配置（实例半边）」）；两个待办计数分列不相加
  （等复核人 vs 等登记方更正，`attention` 只答「要不要有人看一眼」，不答紧不紧急）；`asOf`
  显在条头，理由与上一条「回显不是装饰」同一句。三态呈现另有一句：`unconfigured`（403）单独说
  「今天没有问到，不是本册无缺口」，不静默消失——静默消失恰好会被读成「无缺口」。

  **本项未做的两件：**

  1. **票面「一键跳到该序列的登记 / 复核动作」没做，`f4c2996` 的提交信也没说。** 这里补记。
     页面上的复核动作吃的是 `SeriesReviewTarget`（序列标识 + 版本 + 登记责任方），而覆盖响应
     一条序列一行、没有责任方，`none` 那一格更没有可指的版本——摘要条今天拿不出一个能直接开
     复核面板的目标。最便宜且不造数据的形状是：点一格把目录的搜索词设为该序列标识，让下面的行
     与行上的「复核」按钮收窄到这条序列，那要把 `keyword` 状态抬一层或传一个回调，另切一刀。
  2. **「挂起评价数」那一格没有**，它依赖第 2 项（05b），而 05b 仍卡在 MCP-1 首条 Comment 摆出的
     两件裁决上。摘要条不给这一格，也**不摆一个 0 占位**——0 会被读成「没有挂起」，而实情是没问过。

  **验证**：`f4c2996` 上 `tsc --noEmit` 退 0、`node scripts/run-tests.mjs` 32/32（提交信记为原 25
  + 新 7）；今日在 `0c8d65d` 的树上重跑一遍同样 32/32、`tsc` 退 0（树上此刻只压着一份别人的
  `.md` 在途改动，不影响此二者）。纯前端，Go 侧零改动，不涉 `-race` 与真库。**未推**：MCP-1 本轮
  推到 `25db568`，`f4c2996` 落在验证窗口里，留下一轮。

- 2026-09-03 · MCP-4：**上一条「未做」第 1 件的复核那半交付（`b98368d`）：摘要条点一格，下方目录
  只看这条序列，行上的「复核」按钮收到眼前；再点同一格或「取消只看」即取消。「登记」那半与
  「挂起评价数」那格仍未做，理由不变，见下。**

  **落点**：`catalogue-filter.ts`（收窄纯函数 + `SeriesKey`）与 `catalogue-filter.test.ts`；
  `CoverageSummaryBar.tsx` 加 `onSelectSeries` / `focusedSeries` 两个可选 prop，格子在有回调时是
  `<button aria-pressed>`；`ReferenceSeriesPage.tsx` 持有 `focus` 状态、把原来内联的搜索词过滤
  搬进同一个纯函数（行为原样钉住），过滤摘要显「共 N 条 · 只看 X（种类）」。

  **两处形状取舍，写在这里因为它们都是「两种状态不得折成一种」的实例：**

  1. **只看不是搜索词。** 上一条写的「把目录搜索词设为该序列标识」是最便宜的形状，动手前否掉了：
     搜索是 contains，`SYN-PRC-FUEL` 会把 `SYN-PRC-FUEL-WEEKLY` 一起带出来，而看的人以为自己只
     看着一条。于是只看与搜索词在类型上分开、**叠加而不是二选一**（只看之内还能按责任方再筛）。
  2. **只看的键是（标识 + 种类），不只是标识。** 与 05a 那条「三处不同」的第 3 项同源：覆盖读口
     一行的键就是（标识 + 种类），同标识两种类各出一行是照实转写；只按标识只看会把后端刻意分开
     的两行在目录里又折回一行。用例专钉这一格（同名异类不混进来）。

  **仍是「收窄目录」而不是「直接开复核面板」**，理由与上一条同：面板吃（标识 + 版本 + 登记责任方），
  覆盖响应给不出后两者，`none` 那格更没有可指的版本——硬拼一个就是替人挑了要复核哪一版。选中只
  标边框、不标颜色等级，阈值未配置那条不变。

  **未做的两件**：「跳到登记」——登记签是受控批量口的 JSON 镜像、无逐序列预填，参考序列册的逐字段
  表单按 ADR-0101 决定八另裁另建，本刀不发明；「挂起评价数」——依赖 05b，不摆 0 占位。

  **验证**：`b98368d` 上 `tsc --noEmit` 退 0、`node scripts/run-tests.mjs` 36/36（原 32 + 新 4）。
  **`vite build` 不声称**：本机在 `index.css` 的 PostCSS 插件加载即断（tailwindcss 找不到
  `@alloc/quick-lru`，`node_modules` 缺件），与本刀无关；没在共享树上跑 `pnpm install`——它扫当前树
  写回去，按 parallel-sessions 那条不得单方面跑。纯前端，Go 侧零改动。**未推。**

  **一件对下一个在共享树上提交的人有用**：本笔 `git add` 时索引里已躺着另一会话为它那笔暂存的三个
  文件；`git commit -- <pathspec>` 只带走了本刀四个文件，那三个原样留在索引里等它自己提。
  parallel-sessions 写的「`git commit` 提的是整个索引」这一格，这次是 pathspec 实拦的。
