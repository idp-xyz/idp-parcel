* text=auto

*.go text eol=lf
*.mod text eol=lf
*.sum text eol=lf
*.md text eol=lf
*.sql text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# bash 读不了 CRLF：`set -uo pipefail\r` 报 `invalid option name`，`trap ... EXIT\r` 报
# `invalid signal specification`，脚本在第一行就死。而本机 core.autocrlf=true，检出时
# 把 LF 换成 CRLF——于是 Windows 上一次全新检出就得到一批跑不动的脚本，且报的是
# 语法错，不是「行尾不对」。取证脚本坏在这上面尤其难认：WSL 里 Edge 取回的是
# Chromium 自己的错误页，看起来跟「页面没接上」一模一样。
*.sh text eol=lf

# git 钩子无扩展名，text=auto 在 Windows 检出会给 CRLF，复制进 .git/hooks 后 sh 读 #!/bin/sh\r 就死——与上面 *.sh 同一条病。
scripts/git-hooks/* text eol=lf
