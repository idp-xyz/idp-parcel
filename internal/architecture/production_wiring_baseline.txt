# 生产接线棘轮基线。每行「包 名」，`#` 起注释。
#
# 这里躺着的是**此刻容忍的**未接线领域工厂，不是「应当如此」的清单。冻结它是为了让门禁能判
# 方向：名单变长要有人写一行理由，名单里的条目失效要有人剪掉。
#
# 加一条时把理由写在它上面——你在加的当下知道它归哪个 PN 切片、为什么现在还不接，而三个月
# 后来读的人不知道。
#
# 初版这 33 条实测于 2026-08-21，两套独立方法各数一遍同得 33 且名单逐条相同：一套是 git 侧
# 正则（排注释行、cmd 计数），一套是本门禁的 go/ast 解析。**当日随后删掉两条死代码为 31；
# 同日晚随审计票 14 新增两条（PS 载荷规范化摘要一对），现为 33；2026-09-01 随票 08 面单
# 交易域模型新增三条为 36，同日票 08 的仓储落地后重建那条接上生产调用路径剪掉，记为 35；
# 2026-09-02 随 label-channel/06 写侧编排落地剪掉面单交易那两条，记为 32。
#
# 2026-09-02 随 label-channel/13 新增两条（渠道择优比较器与批量评价口），改后**重数实测 34**
# ——不是拿 32 加 2 得来的，照下面那两段的教训办。
#
# **上面那个 34 后来被当成基准用过一次，而它当时已经过期了**——记它的人（本会话）没写它是在
# 哪一笔上量的，读它的人据以推算就会错。它的准确说法是「在 e03efd3 上量得 34」；此后
# 3c4efe5 与 fa98df1 各剪一条，到 71daac5 已是 32。**所以本文件此后每记一个数都连 SHA 一起
# 记**：这份名单一晚内被四个会话改过，「干净检出上量的」已经不足以定位——干净检出本身每几
# 分钟就是一个新的。
#
# 2026-09-02 随 label-channel/12 的择优编排落地，剪掉 SelectChannelCandidateByCost 一条，
# **在 a5a32cc 上改后重数实测 32**（不是拿任何先前的数加减出来的）。
#
# 2026-09-02 随 label-channel/12 的成本取数适配器落地，剪掉 EvaluatePricingAcrossPlans 一条，
# **在 7ae3c7a 上改后重数实测 31**。这两笔之间隔了别的会话的若干提交，所以 32 与 31 不是
# 相邻的两个数——**它们各自只对自己那个 SHA 成立**，中间那段本文件被谁改过要看 git 历史。
#
# 2026-09-02 随 party-commercial-context-gaps/04 第二笔新增两条（客户服务规则版本的两格适用
# 声明），**在 c1e8f68 上改后重数实测 33**。理由行按现行要求写「谁是它应该的调用方、那一层
# 何时落」（调用方是票 05），不写「切片还没到」——那句已于本日作废。
#
# 2026-09-02 随 party-commercial-context-gaps/01 剪掉 PublishChannelAccountUseAuthorization
# （渠道账号使用授权补齐执行器，接上生产调用路径）。本笔的计数见下面那段——它写于同一次
# 改动，因为这一剪恰好撞上了一种此前没记过的失效。
#
# 一件该留档的现场：本段与下面那段曾在 HEAD 上与被剪的条目**脱节**——两段注释先随别人的
# 提交入了库，条目本身还留在名单里，于是文件有一阵子自己说剪了而实际没剪。成因是共享索引
# （见 parallel-sessions 的卷带那节）。这正好是本文件反复在防的东西的镜像：以往是名单变了
# 注释没跟上，这次是注释先走了名单没跟上，两者一样让读的人信一句假话。
#
# 2026-09-02 随 tf-unwired-seven/03 剪掉 SummarizeHandovers（交接范围汇总读面落地，
# application/summarize_handover_scope.go 是它的生产调用方），**本笔单独作用于其父提交
# 后重数实测 33**。按本门禁报错时要求的三分先分过成因：全仓 `SummarizeHandovers` 只有一处
# 声明（domain/transport_handover.go），不存在「别处同名声明造成误判」那一种，因此是第二种
# ——真的接上了包外非测试引用。
#
# **这一笔撞上了本文件此前没记过的一种失效，写下来因为它比上面两次都难防。** 上面各句都写
# 「剪后重数实测 N」，那句话默认**树上只有一个版本的名单**。共享工作树上不成立：同一时刻可以
# 有好几处未提交的增减各自躺着，你量到的 N 是它们叠加后的数，而每个人都会把它当成自己那一笔
# 的结果。本笔实测过这一格三次，三次都对而三个数不同：先在共享树上量得 33（当时压着另一处
# 未提交的剪），后来那一剪先入了库、HEAD 变成 33，本笔加上去**实测 32**。
# **三个数都不假，量的不是同一个东西。**
#
# 所以记数时要连「在哪量的」一起记。要一个能给下一个人用的数，只有一处量得准——**在干净检出
# 上量**，那里不含任何人的在途改动。逐块核（`git add` 前 `git diff HEAD -- <file>`）能在提交
# 之前逼出这个区别：本笔就是在那一步才发现自己写下的 32 记的不是自己那一剪。
#
# 2026-09-02 随 tf-unwired-seven/01 **新增**一条 RehydrateActualFulfillmentSegment（理由写在
# 那一行上方）。**这一笔是上面那条处方第一次被照着用**：动手前先 `git diff` 该文件确认树上
# 无他人在途改动（为空），在那棵干净树上量得底数 **32**，加后实测 **33**——两个数因此锚在
# 同一棵树上，不是一个减一个加出来的。
#
# 2026-09-02 随 tf-unwired-seven/04 剪掉 ChargeOccurrenceForFailedAttempt（理由写在那一组
# 的注释里）。**量在一棵 detached 隔离 worktree 上**（检出 `f5a0f38`，不含任何人的在途
# 改动）：`git show` 取该检出的本文件数得剪前 **32**，剪后 **31**。按上面那条处方，这两个数
# 只对这个检出成立；谁要拿一个数当底，请在自己动手那一刻重取 HEAD 再数。
#
# 2026-09-02 随同一票下一层（段册适配器）**又把那条剪掉**，加它时写的「适配器落地那天出
# 名单」当天兑现。**两处各量各的，都记在这里，因为它们是两个不同的数而不是一个数量错了**：
# 隔离 worktree（检出 `d54fe5d`）上剪前 32 剪后 31；本笔实际提交的父提交 `b321885` 上剪前
# 33 剪后 **32**。两棵树相差一条，是因为中间别人各有增减。
#
# **而这一条本身栽了一次，就在写下它的那一分钟里。** 我先按更早的共享树读数顺手写了「剪后
# 32」，跑完隔离树才发现那里实测是 31。上面那条处方我自己刚写完就没照做：**底数不是拿来
# 记住的，是每次都要在你正操作的那个检出上重取的**。留着这句不删——它比处方本身更能说明
# 那条处方为什么必要：知道规则并不会让你自动照做，只有每次真去量才会。
#
# **那个 32 又错了一格，同一种错第二次。** 2026-09-02 晚 label-channel/10 加这一条之前重数，
# 实测 31——某次增减（隔离写面那批的名单改动）只改了名单没改这句。加入登记册重建入口一条后
# **实测 32**，这个数是数出来的不是从 31 加出来的**
# ——这句逐次记增减而不是只写一个数，是因为下一次增减时只改一处会让另一处变错。
#
# **而它已经错过一次，留在这里当实例。** 2026-09-02 剪那两条之前实测条目数为 34，而上一句
# 当时写着「现为 35」——某次增减只改了名单没改这句。所以上面的 32 是**剪后实测**得来的，
# 不是拿 35 减 2 算出来的；照那句算会得 33，继续错下去。逐次流水账防不住这个，它只是让
# 错误可追；**下一个改名单的人请重数一遍再改这句**。
#
# 2026-09-03 随 label-channel/10 的登记册仓储适配器（adapters/postgres/continued_attempt_register.go）
# 落地，剪掉 RehydrateContinuedAttemptRegister——加它时写的「读它的仓储适配器落地那天这一条出
# 名单」兑现。成因三分先分过：全仓该名字只有一处声明（domain/continued_attempt_register.go），
# 非测试调用点就是上述适配器，是第二种。**在 `0d4eb0d` 的干净内容上（`git show` 取本文件）数得
# 剪前 31、剪后 30**；两个数只对该检出成立，下一个改名单的人在自己那一刻的 HEAD 上重取。
#
# 2026-09-03 随 tf-unwired-seven/02 剪掉 EstablishSegmentWithHandover（交接登记编排接上立段，
# application/register_transport_handover.go 的 establishSegment 是它的生产调用方）。成因三分
# 先分过：全仓该名字只有一处声明（domain/actual_fulfillment_segment.go），不存在「别处同名
# 声明造成误判」那一种，是第二种。**剪后重数实测 29**，不是拿上一个数减一得来的。
#
# **这个 29 量在工作副本上而不是干净检出上，所以要连怎么量的一起说清。** 动手前先
# `git status -- internal/architecture/` 确认树上无他人在途改动（为空），改完再
# `git diff HEAD -- 本文件` 复核只有本笔两处 hunk——两步合起来才等价于「本笔单独作用于其父
# 提交」。父提交 `28e2990`，它未碰本文件。**并且 HEAD 在本次编辑期间真的往前走过一笔**
# （从 `a318f0e` 到 `28e2990`，另一个会话的票 06），这正是上面反复记的那格竞态；这次没咬到
# 是因为那两笔碰的是 cmd/parcel-api 与 apps/admin-web，不是运气好，是查过。
# 两套独立方法各数一遍同得 29：一套按 UTF-8 逐行读后滤非空非注释，一套在字节层数行首非 `#`
# ——后者不经文本解码，防的是本机把中文按 GBK 解时并掉换行导致的静默偏小。
#
# 2026-09-03 同票下一刀，剪掉 EstablishSegmentWithPickup（收寄登记编排接上立段，
# application/register_offsite_pickup.go 的 establishSegment 是它的生产调用方）。成因三分同上
# 一段先分过：全仓该名字只有一处声明。**剪后重数实测 28**，两法同得，量法同上一段（动手前
# `git status -- internal/architecture/` 为空，改完 `git diff HEAD -- 本文件` 只有本笔的 hunk），
# 父提交 `0054a13`。
#
# **这两刀之间隔的不只是时间，还有一句注释的两次失真**，记下来因为门禁对它完全无声：同一组里
# 「转换门与访问器本就不在名单上——EstablishSegmentWith* 各占一行（仍未接）」那句，第一刀后
# 「仍未接」对 Handover 已假，第二刀后「各占一行」也假。TestWiringBaselineHasNoStaleEntry 只比
# 条目行与未接线名单、**不读 `#` 注释**，两次都全绿。两次都是剪的人顺手改的，而不是被谁报出来的
# ——这正是 AGENTS.md「改文档」那条说的、结构上守不住只能靠路过的人的那一类。
#
# 2026-09-03 随 tf-unwired-seven/05 剪掉 OpenDispatchTask（揽派任务建立编排落地，
# application/open_dispatch_task.go 是它的生产调用方）。**剪后重数实测 27**，两法同得，量法与
# 上两段同（动手前 `git status -- internal/architecture/` 为空，改完 `git diff HEAD -- 本文件`
# 只有本笔的 hunk），父提交 `426f3fb`。
#
# **本笔的成因三分值得多写一句，因为它差一点就该是第三种。** 同一笔里新增了
# OpenDispatchTaskHandler / Command / Result / Deps 四个以 `OpenDispatchTask` 为前缀的标识符
# ——那正是「别处新出现同名符号把它误判成已接线」的形状。**去读了本门禁的匹配码才敢剪**：
# `countProductionReferences` 比的是 `typed.Sel.Name == entry.name`，**精确相等不是子串**，
# 四个前缀名一个都不命中。所以这一剪立在真调用上（`domain.OpenDispatchTask(spec)`），不是
# 立在名字上。下一个剪条目时同时引入近名符号的人，照这一步先去读那段匹配码。
#
# 2026-09-03 随 tf-unwired-seven/05 第三刀剪掉 FormLoadAssignment（装载分配形成编排落地，
# application/form_load_assignment.go 是它的生产调用方）。成因三分同上：全仓该名字只有一处
# 声明；同笔又新增了四个以 FormLoadAssignment 为前缀的标识符，而门禁比的是精确相等不是子串
# （上一段已核过匹配码，此处不重复读）。**剪后重数实测 26**，两法同得，父提交 `e6a66ed`。
#
# 2026-09-03 随 tf-unwired-seven/06 剪掉 RecordMovementFact（实际移动事实登记编排落地）。
# 成因三分同上；全仓该名字只有一处声明。**剪后重数实测 25**，两法同得，父提交 `06322cf`。
#
# **`transport-fulfillment` 这一组至此清空——本批（tf-unwired-seven）开工时它有 7 条。**
# 但别把「这一组空了」读成「TF 都接上了」：本门禁只量**导出工厂**，一条编排没接它是量不到的。
# 实测过一次：票 05 剪掉两行之后 `PerformOffsitePickupHandler` 仍不立段，而名单再不会为它红
# （那件另立了票 08，已 resolved）。**名单清空只判名单清空。**
#
# 2026-09-04 随 mechanism-executor-triage/06 的 SA-b 剪掉 IncludeAdjustmentInSubsequentPeriod
# （理由写在 settlement-accounting 组那一段）。量法同上几段：动手前 `git status -- internal/architecture/`
# 为空，改完 `git diff HEAD -- 本文件` 只有本笔的 hunk；**父提交 `8ab9835` 上两法同得剪前 25、
# 剪后 24**——两法是 UTF-8 逐行读后滤非空非注释，与字节层数行首非 `#`。两个数只对该检出成立。
#
# 2026-09-07 随 wiring-baseline-remainder（MCP-6）剪掉 parcel-pricing 组三条：MarshalPricingPlanSnapshot、
# RehydratePricingPlanSnapshot、ParseCanonical，**三条都是成因第一种（已删）**，不是接上——理由与
# 出处写在该组那两段。同笔把 ReplayPricingEvaluation 的理由行改写成「谁是调用方、那一层何时落」。
# 量在一棵隔离 worktree 上（分支基于 `0f84c0ec`，不含任何人的在途改动，动手前
# `git status -- internal/architecture/` 为空）：`git show` 取该父提交的本文件两法同得剪前 **9**，
# 本笔单独作用于其上剪后 **6**。两个数只对该检出成立；本笔重放进 main 时父提交若不是 0f84c0ec，
# 下一个改名单的人在自己那一刻的 HEAD 上重取，别拿这两个数加减。**这一段自己就改过一次 SHA**：
# 先在 `2efef58e` 上量得同样的 9→6，随后 main 前进四笔、本分支重放到 `0f84c0ec` 之上，于是在新
# 父提交上重量一遍再改这句——两次相等是量出来的，不是因为"中间没人改过"就推出来的。
# 同日接管会话为重放再量一次：main `08f54867` 上本文件的 blob 与 `0f84c0ec` 逐字节相同，两法同得
# 剪前仍 **9**；分支 tip `28ea268b` 两法同得 **6**。这两个数同样只对这两个检出成立。
#
# 下面那些理由行的取证强度曾经不齐：初版逐条读过签名与调用点的只有 parcel-pricing 与
# party-commercial 两组、以及 AssessSafeHandoff 与 FormSupplierExpectedCost 两条，其余按组写。
# 2026-08-24 已逐条补核（锚 b44dd51，方法与逐条结果见 production-wiring-ratchet-gate 票 01
# Comments 末条）：组注释的语义断言逐条成立；唯一例外（DecideDisclosure 的包外测试引用）与
# 几处组注释计数失配已就地改在各组注释里。
#
# 一件初版就该说清的事：这 33 条里 25 个**连 domain 包都没出过**——不只是没有生产调用点，
# 而是没有应用层用例、没有适配器、没有端口，包外零引用。**所以这份名单不是一张缺陷清单，
# 是一条基准线**；它里面有多少是「接过线后来烂了」、多少是「切片还没到」，本门禁分不出，也不
# 打算分。这一段原有一句「按本仓机制先行实例后到的顺序，切片还没开工的能力本来就该是这样」，
# 2026-09-04 按 mechanism-executor-triage/spec.md「处置裁决」第 5 条删去——八切片全数记为达标之后
# 它不再成立（同目录票 01 已证），而 32 条逐条核过没有一条能归到「切片还没到」（同目录票 03）。


# —— parcel-shipment ——
# 产出带校验的安全交接评估，测试里调 9 次，包外零引用。
internal/parcelshipment/domain AssessSafeHandoff
# 审计票 14 的规范化摘要机制件：按 ADR-0014 版本化形状（PSC-1）产出 PayloadDigest。
#
# 这里原有两条——摘要函数 CanonicalizeSubmissionPayload 与它的版本报出口——并预言「同一条
# 路径的两端，接上那天一起出名单」。**那个预言没兑现，摘要函数已于 ADR-0091 单独出名单。**
# 接它的是隔离形态的提交 Intake（IsolatedSubmissionIntake），不是原先设想的真渠道 Intake：
# 真渠道那笔工作仍等 PAR-INT-01。原注释还写着「adapters/http 侧不得出现调用它的『开发用』
# 实现」，那句仍然有效且未被违反——它禁的是采信自报身份、从请求内容铸信封的那一种，而隔离
# Intake 整组注入身份，且生产装配里没有通往它的代码路径（ADR-0091 判据三）。
#
# 版本报出口留在名单上：至今零生产调用点，接它的仍是真渠道那笔工作。
internal/parcelshipment/domain CurrentPayloadCanonicalizationVersion
# 票 08 的面单交易域模型（ADR-0084）曾在这里占两条（EstablishLabelTransaction 与
# EstablishPriorLabelTransactionLink），理由是「写入方是渠道适配器，渠道墙未降前登记零行」，
# 并写明「写编排落地那天，两条一起出名单」。ADR-0088 把面单渠道服务纳入首发，
# label-channel/06 的写侧编排（parcelshipment/application/operate_label_transaction.go）
# 随之落地并引用这两扇门，2026-09-02 按上句剪掉。
#
# **剪掉前按本门禁的提示逐条分过成因**：两个名字在全仓各只有一处声明（均在
# parcelshipment/domain/label_transaction.go），不存在「别处同名声明造成误判」那一种，
# 因此是第二种——真的接上了包外非测试引用。
#
# 一句要说清的边界：本门禁量的是**domain 包外的非测试引用**，而那条编排此刻**尚未进
# cmd/parcel-api 的装配**。所以「出名单」等于「有了应用层调用方」，不等于「生产可达」。
# 装配随取面单出向端口（label-channel/07）那笔一起落。
# `面单继续尝试决定`登记册（ADR-0084 决定六留待、label-channel/10）的重建入口
# RehydrateContinuedAttemptRegister 曾在这里，理由是「这一票只落了领域层，读它的仓储适配器落地
# 那天出名单」——2026-09-03 该适配器（adapters/postgres/continued_attempt_register.go）落地并
# 调用它，按上句剪掉。追加与派生两个入口（Append、Judge）从未在名单上——它们是聚合方法而非
# 导出工厂，本门禁只量后者。
# 渠道候选择优比较器 SelectChannelCandidateByCost 曾在这里（票 `label-channel/13`），预言
# 「票 12 落地那天这一条出名单」——2026-09-02 票 12 的择优编排
# （parcelshipment/application/select_channel_candidate.go）落地并调用它，按上句剪掉。
#
# **剪之前按本门禁的提示分过三种成因**：这个名字在全仓只有一处声明（domain/
# channel_candidate_cost.go）、一处非测试调用点（上述编排），其余命中全在测试里，
# 因此不是「别处同名声明造成误判」那一种，是真的接上了。


# —— visibility-exception ——
# 以下各条均产出带不变式校验的领域对象或领域决定；除 DecideDisclosure 外包外零引用——
# 它的包外引用全在测试里（三个 postgres 适配器测试与一个应用层测试），与
# FormSupplierExpectedCost 同属仅有的两个走出 domain 层的，另一半注在 SA 组那条上。
#
# EstablishCase 曾在这里，2026-09-04 随 mechanism-executor-triage/08 VE-b 剪掉：信号编排
# （application/raise_signal.go 的 establishCaseFor）在分诊走向为自动建案时建立案件，案件与
# 发作期、结论同一记录落库。成因三分先分过：全仓该名字只有一处声明（domain/exception_case.go），
# 同笔新增的符号（ExceptionCaseSnapshot、CaseIdentityFactory）都不与它同名，门禁比的是精确
# 相等不是子串，是第二种。**在 c3b4311 的干净内容上（工作树对 HEAD 无差）数得剪前 23、
# 剪后 22**，两法同得（按 UTF-8 逐行滤非空非注释，与字节层数行首非 `#`）；两个数只对该检出
# 成立，下一个改名单的人在自己那一刻的 HEAD 上重取。同票余五条随 VE-a / VE-c / VE-d 各自出名单。
#
# DecideDisclosure 曾在这里，2026-09-04 随同票 VE-a 剪掉：新用例 application/decide_disclosure.go
# 从发作期、反查到的客户归属与异常披露规则（0023 登记面）形成三态决定并入册，
# application/notify_customer.go 改为引用已登记的决定；adapters/postgres/disclosure_decision.go
# 读回时再过一遍同一道构造门。头注那句「与 FormSupplierExpectedCost 是仅有的两个走出 domain
# 层的」到此两条都已接上。成因三分先分过：全仓该名字只有一处声明（domain/customer_disclosure.go），
# 同笔新增的 DecideDisclosureHandler / Command / Deps / Result 是前缀名，门禁比的是精确相等不是
# 子串，是第二种。**在 acb4975 的干净内容上（工作树对 HEAD 无差）数得剪前 16、剪后 15**，
# 两法同得；只对该检出成立。
#
# SubmitEvidence 与 PrepareDisclosure 曾在这里，2026-09-04 随同票 VE-c 一笔剪掉：新编排
# application/manage_evidence.go 的 ManageEvidenceHandler.SubmitEvidence 与 .PrepareDisclosure
# 是它们的生产调用方（UC-VE-007 步 2 证据到达与证据对外披露版本），adapters/postgres/evidence.go
# 读回披露版本时再过一遍 PrepareDisclosure 那道门。成因三分先分过：两个名字在全仓各只有一处
# 声明（domain/evidence.go），同笔新增的 SubmitEvidenceCommand / PrepareEvidenceDisclosureCommand
# 是近名不是同名，门禁比的是精确相等不是子串，是第二种。**在 8468b53 的干净内容上（工作树对
# HEAD 无差）数得剪前 15、剪后 13**，两法同得；只对该检出成立。
#
# ResolveByBusinessTime 与 RaiseConflictSignal 曾在这里，2026-09-04 随同票 VE-d 一笔剪掉：投影
# 派生编排 application/derive_projection.go 的 judgeForks / raiseConflict 是它们的生产调用方——
# 替代链分叉按业务时间裁决，裁不了的按已登记的冲突信号规则（0025）形成信号、直调
# RaiseSignalHandler 进 UC-VE-004 分诊（derive_projection_test 里那句「接 RaiseConflictSignal 是
# 另一张票」到此兑现）。成因三分先分过：两个名字在全仓各只有一处声明（domain/fact_conflict.go），
# 同笔新增的 ConflictSignalRule* 一族与 SupersessionForks 都不与它们同名，门禁比的是精确相等，
# 是第二种。**在 7058d92 的干净内容上（工作树对 HEAD 无差）数得剪前 13、剪后 11**，两法同得；
# 只对该检出成立。
#
# **visibility-exception 这一组至此清空——票 mechanism-executor-triage/08 开工时它有 6 条。** 与 TF
# 那组同一句提醒：名单清空只判名单清空，本门禁只量导出工厂；本组三个新登记面（异常披露规则、
# 冲突信号规则各自的写入口，与分诊条目的团队维）的 CLI / 在线登记口尚未接，那件另立票。


# —— transport-fulfillment ——
# 以下各条均产出带不变式校验的领域对象，包外零引用。
#
# ChargeOccurrenceForFailedAttempt 曾在这里，2026-09-02 随 tf-unwired-seven/04 剪掉：
# application/register_failed_attempt_charge.go 是它的生产调用方。**它没有接进揽收编排**
# ——ADR-0098 裁了不在那里形成，理由是 CONTEXT 禁「任一对象的成功、失败或取消被压缩成其他
# 对象的状态」，且揽收登记必须照常成功、塞进去会造出一个静默不发生且无人重试的派生步骤。
#
# EstablishSegmentWithHandover 与 EstablishSegmentWithPickup 曾在这里，2026-09-03 随
# tf-unwired-seven/02 分两笔剪掉：生产调用方分别是 application/register_transport_handover.go
# 与 application/register_offsite_pickup.go 的 establishSegment，两者共用
# application/enter_fulfillment_segment.go 那道进段门。段引用作显式入参进命令，**缺席时不立段**
# ——实际履约段不等同于交接范围、计划段、班次或订舱，段身份由谁铸出至今没有裁决，编排不拿
# 手边任一引用顶替。**PerformOffsitePickupHandler（多对象到访）尚未接**，它不在本门禁量的范围里
# （只量导出工厂），所以剪掉这两行之后没有任何信号会提醒它还欠着——记在这里就是那个提醒，
# 它另有一张票：.scratch/tf-unwired-seven/ 下的
# 08-multi-object-pickup-attempt-establishes-no-segment.md（ready-for-agent）。
# **票与注释两条路都留着，因为它们被不同的人看见**：票会被从跟踪器名单里看见，这句会被改这份
# 名单的人看见，而那不是同一批人。
#
# OpenDispatchTask 与 FormLoadAssignment 曾在这里，2026-09-03 随 tf-unwired-seven/05 分两刀剪掉：
# 生产调用方分别是 application/open_dispatch_task.go 与 application/form_load_assignment.go。
# 同批的 RecordMovementFact 仍未接，它归票 06。
#
# RecordMovementFact 曾在这里，2026-09-03 随 tf-unwired-seven/06 剪掉：
# application/record_movement_fact.go 的 RecordMovementFactHandler.Record 是它的生产调用方。
# **至此 transport-fulfillment 这一组清空了**——本组开头那句「以下各条均产出带不变式校验的
# 领域对象，包外零引用」现在没有条目可指，留着它是为了让下一个往本组加条目的人看见这段历史。
# 实际履约段的重建门曾在这里（ADR-0028，tf-unwired-seven/01），2026-09-02 同票下一层落地后
# 剪掉：`adapters/postgres/fulfillment_segment_registry.go` 的 FindByKey 是它的生产调用方。
# 加它那天写的是「适配器落地那天这一条出名单」，当天兑现。
#
# 转换门与访问器本就不在名单上——JoinWith*、EndParticipationWith*、CloseSegment 是聚合方法
# 而非导出工厂，本门禁只量后者。段的两个导出工厂（EstablishSegmentWith*）本日已双双出名单，
# 出处见本组上方那段。


# —— settlement-accounting ——
# 本组开工时四条均产出带不变式校验的领域对象、除 FormSupplierExpectedCost 外包外零引用；2026-09-04
# 随 mechanism-executor-triage/06 三笔全部剪掉，现在没有条目可指——留着这一段是为了让下一个往本组
# 加条目的人看见这段历史。
#
# IncludeAdjustmentInSubsequentPeriod 曾在这里，2026-09-04 随 mechanism-executor-triage/06 的 SA-b
# 剪掉：application/cut_off_publish_statement.go 的 CutOffPublishStatementHandler.IncludeAdjustment
# 是它的生产调用方（UC-SA-003 步 7 的调整半边——费用半边早已由 IncludeLateCharge 接上，同一步
# 此前只接了一半）。成因三分先分过：全仓该名字只有一处声明（domain/customer_statement.go），
# 是第二种。
#
# FormAuditedPayable 与 FormSupplierCreditNote 曾在这里，2026-09-04 随同票 SA-a 一笔剪掉：
# application/audit_supplier_bill.go 的 ReceiveSupplierBillHandler.Audit 与 .Credit 是它们的生产
# 调用方（UC-SA-004 步 5–6，步 7 的两份发布并进 supplier_bill_handoff.go 同分区）。成因三分先
# 分过：两个名字在全仓各只有一处声明（domain/supplier_bill.go），同笔新增的 AuditBillLineCommand
# / FormSupplierCreditNoteCommand 是近名不是同名，门禁比的是精确相等（tf-unwired-seven/05 那段核过
# 匹配码），是第二种。**在 e8ffe32 的干净内容上（`git show` 取本文件）两法同得剪前 21、剪后 19**；
# 两个数只对该检出成立，下一个改名单的人在自己那一刻的 HEAD 上重取。
#
# FormSupplierExpectedCost 曾在这里，2026-09-04 随同票 SA-c 剪掉：application/form_supplier_expected_cost.go
# 的 FormSupplierExpectedCostHandler.Handle 是它的生产调用方（UC-SA-002 步 5 的 BUY 方向）。它与
# DecideDisclosure 曾是全部条目里仅有的两个**已经走出 domain 层**的（适配器与应用层都在、两边都有
# 测试、唯独没有生产调用路径，基线预言「真烂会先烂在这两条上」）——这一条今天接上了；BUY 评价到
# 本上下文的入向缝在 SA 侧只落了端口与编排，消费侧适配器等提供方的金额取整槽（票面记着）。成因
# 三分先分过：全仓该名字只有一处声明（domain/supplier_expected_cost.go），同笔新增的
# FormSupplierExpectedCostHandler / Command / Deps 是前缀名不是同名，是第二种。**在 42f584a 的干净内容
# 上两法同得剪前 17、剪后 16**，只对该检出成立（这一剪本该随 1ea2597 同笔，落成了下一笔——那笔的
# 说明里「已随 616646d 入库」是误判，成因见本笔提交信）。**至此 settlement-accounting 这一组清空**——本票
# （mechanism-executor-triage/06）开工时它有 4 条。名单清空只判名单清空：本门禁只量导出工厂，
# UC-SA-002 步 2「结算提交主要范围…请求评价」那条编排今天仍不存在，它不是工厂，这里量不到。


# —— customs-compliance ——
# 以下各条均产出带不变式校验的领域对象，包外零引用。
#
# RegisterCredential 曾在这里，2026-09-04 随 mechanism-executor-triage/07 CC-a 剪掉：监管凭证
# 登记面（application/register_credential.go）是它的生产调用方，读它的第一个判断是同笔的
# application/judge_credential_applicability.go（UC-CC-003 步 7 的判断半边）。成因三分先分过：
# 全仓该名字只有一处声明（domain/compliance_judgment.go），同笔新增的 RegisterCredentialHandler
# / Command / Deps 是前缀名，门禁比的是精确相等不是子串（tf-unwired-seven/05 那段核过匹配码），
# 是第二种。**在 024cb5f 的干净内容上（`git show` 取本文件）数得剪前 24、剪后 23**；两个数只对
# 该检出成立，下一个改名单的人在自己那一刻的 HEAD 上重取。
#
# ReceiveReleaseOutcome 曾在这里，2026-09-04 随同票 CC-b 剪掉：application/receive_external_result.go
# 的 ReceiveExternalResultHandler.Handle 在放行层调它（UC-CC-006 步 5 的放行那一格）——读过
# InterpretExternalResult 正文后判定它**不是**平行第二写法：那个函数只记原始语义与规则引用，
# 不把放行语义解释进任何形状，所以这是接线不是二选一（票 03「没能确认的」第一条至此答完）。
# 成因三分先分过：全仓该名字只有一处声明（domain/duty_release.go），是第二种。**在 a3adb75 的
# 干净内容上数得剪前 22、剪后 21**，只对该检出成立。
#
# FormDutyCollaboration 与 VerifyDutyPayment 曾在这里，2026-09-04 随同票 CC-c 一笔剪掉：
# application/reconcile_duty_payment.go 的 DutyPaymentReconciliationHandler 是两者的生产调用方
# （FormCollaboration 接 UC-CC-009 步 4–5，VerifyPayment 接步 7；步 6 外部资金事实的入向缝以
# 同文件 ReceiveFundsFact 为 CC 半边——SA 事实采用不发信封，取证于 b3d3343，SA 侧另立票）。
# 成因三分先分过：两个名字在全仓各只有一处声明（domain/duty_collaboration.go、domain/duty_release.go），
# 同笔新增的 FormDutyCollaborationCommand / VerifyDutyPaymentCommand 是前缀名，门禁比的是精确相等
# （tf-unwired-seven/05 那段核过匹配码），是第二种。**在 3f4a428 的干净内容上两法同得剪前 19、
# 剪后 17**，只对该检出成立。
#
# **customs-compliance 这一组至此清空**——本票（mechanism-executor-triage/07）开工时它有 4 条。
# 与 TF 组清空时同一句告诫：本门禁只量导出工厂，「这一组空了」不等于「CC 都接上了」——
# 凭证适用性的持久化门禁记录、放行层的代码映射、核对向 settlement-accounting 的步 8 交接都
# 还没有执行器，它们在票面的完成记录里，不在这份名单上。


# —— party-commercial ——
# CustomerServiceRuleAppliesToCustomerContract / CustomerServiceRuleAppliesToServiceProduct 曾在这里
# （票 party-commercial-context-gaps/04 第二笔落的两格适用声明，理由行明写「票 05 落地时两行一起
# 出名单」），2026-09-04 随票 05 剪掉：客户服务规则正文进了 0023 与 `SaveCustomerServiceRule`，
# 点读口 `CustomerServiceRuleContents` 按两列折回两格时调它们，批文口 `customerServiceRuleBodyFrom`
# 同样调它们——两条都是生产路径。**在 cfc4858d 的干净内容上数得剪前 11、剪后 9**，只对该检出成立。
# PublishChannelAccountUseAuthorization 曾在这里，2026-09-02 随 party-commercial-context-gaps/01
# 剪掉：渠道账号使用授权补齐了执行器（0018 登记册、ports 登记面、application 写侧编排），
# 发布门的生产调用方是 application/register_channel_account_use.go。
# **剪之前按本门禁的提示逐条分过成因**：这个名字在全仓只有一处声明（domain/
# channel_account_use_authorization.go），不存在「别处同名声明造成误判」那一种，因此是第二种。
internal/partycommercial/domain ResolveCreditPolicy
# 下面两条落在网里但不是工厂：ManualReviewRequirementFor 返回 bool 是个谓词，
# ValidateBeforeDecision 返回同型 Resolution 是变换不是构造。**留着不排除是有意的**——
# 留在名单里的代价只是两行，漏掉一个工厂的代价是它永远隐形。两边代价不对称，够了。
internal/partycommercial/domain ManualReviewRequirementFor
internal/partycommercial/domain ValidateBeforeDecision


# —— parcel-pricing ——
# 在用序列版本的选择规则 SelectInForceSeriesVersion（ADR-0099 决定三）曾在这里，票
# pricing-reference-series-operations/01 加入时写「票 02 的真库读口落地那天出名单」——2026-09-03
# 票 02 的 adapters/postgres/reference_series_review.go（ResolveInForce）落地并调用它，按上句剪掉。
# **剪之前按本门禁的提示分过三种成因**：全仓该名字只有一处声明（domain/reference_series_review.go），
# 非测试调用点就是上述适配器，是第二种。加时在 42846ed 上数得 30→31；剪时**在 2a9a76a 的干净内容
# 上（`git show` 取本文件）数得剪前 31、剪后 30**——两组数各自只对自己那个检出成立（本笔提交
# 的父提交若不是 2a9a76a，请在自己那一刻的 HEAD 上重取，别拿这两个数加减）。
# 同一票的 NewSeriesReview / NewReviewedSeriesVersion / PeekReferenceSeriesRegistrationReference
# 不在名单上：前两个按名字被门禁跳过（`New` 前缀），第三个由同一适配器调用。
#
# 评价重放门（CONTEXT「重放必须使用新的评价引用……重算结果与原评价不一致时，结果为冲突」那几条
# 的执行器）。**调用方是 PN-08 W02「历史回放」的执行编排**：取回原评价与它记录的原方案版本、
# 按原输入快照与原版本清单重算、结果记 `R`——EvidenceKind 里那格 EvidenceReplay 就是为它留的；
# CONTEXT 点名的「争议复核」走同一扇门。**那一层今天缺三件，三件同票落地那天这一条出名单**：
# 按版本引用取回原方案的读口（PriceCardCatalog 只有按方向+范围+时点的 LoadApplicable，重放要的
# 是「原评价用的那一版」而不是「此刻适用的那一版」）、应用编排（回放结果不交结算：它不是新费用）、
# 治理触发面。票在 .scratch/wiring-baseline-remainder/issues/06-pp-replay-pricing-evaluation-has-no-executor.md。
# 别把 application/evaluate_pricing.go 的 settleAgainstExisting 读成它：那是同标识重复请求的
# 冒名比对，纯函数重算后比摘要、不铸新评价引用，与本条守的规则是两件事。
internal/parcelpricing/domain ReplayPricingEvaluation
# MarshalPricingPlanSnapshot 与 RehydratePricingPlanSnapshot 曾在这里，理由行写着「快照序列化与
# 重建是同一条未接线路径的两端：适配器 price_card_catalog.go 正文没用它们……一次修好会同时去掉
# 两行」。**那句预言没兑现，成因在预言自己**：适配器正文后来确实在存取快照，走的却是登记层那一对
# （registered_price_card.go 的 MarshalPriceCardRegistration / RehydratePriceCardRegistration），
# 它们直接用未导出的 pricingPlanDocumentOf / pricingPlanFrom——路径接上了而这两行没跟着消失，
# 因为修路径的人绕过了它们，方案层那对成了同一形状的平行第二个公开写法（mechanism-executor-triage/
# spec.md「parcel-pricing（4）」一节 2026-09-02 核出）。2026-09-07 按该 spec 处置裁决第 2 条**删去**
# （wiring-baseline-remainder，MCP-6）：全仓非测试引用为零、测试引用改经登记门（证的才是生产真走的
# 路径）。成因三分：是**第一种**（已删），删前全仓各只有一处声明，不存在同名误判。
#
# ParseCanonical 曾在这里，理由行写着「值与解析助手，造的是值对象不是领域对象……留两行更便宜」。
# 它的函数注释说「用在序列化边界上——那里不允许同一个数的不同写法产生不同的内容摘要」，而它从没
# 接到过任何边界：本包所有文本入口收的是外部写法、必须宽收再规范化；快照存的是 (coefficient, scale)
# 两个字段不是文本，那道门要拦的是非规范的字段组合，字符串解析器管不到（domain/
# decimal_canonical_rebuild_test.go 四格钉住）。同日**删去**，成因三分同上是第一种。**它自称要守
# 的那件事没随它消失**——percentShare 按字段移位真会产出非规范写法、重建门原样收回、语义摘要
# 因此对同一个数可能出两个串；收紧与否要连摘要可比性一起裁，票在同目录
# 07-pp-decimal-rebuild-boundary-accepts-non-canonical-spellings.md。
# 批量评价口 EvaluatePricingAcrossPlans 曾在这里（票 `label-channel/13` 缝一）。它比 PS 侧
# 那条择优比较器晚一步出名单，因为两者缺的调用方本就不是同一个：比较器缺编排，而它缺的是
# 把编排接到自己身上的那个适配器（`application` 不得导入 `parcel-pricing`，编排只能经
# `ports.ChannelCandidateCostSource` 取成本）。2026-09-02 那个适配器随票 `12` 落地
# （parcelshipment/adapters/parcelpricing/cost_source.go），按上句剪掉。
#
# **剪之前按本门禁的提示分过三种成因**：全仓一处声明（parcelpricing/domain/
# batch_evaluation.go）、一处非测试调用点（上述适配器），其余命中全在测试里，不是同名误判。
# 初版这里还有两条与其余不同类的：DecimalFromInt64 与 Evaluate 全仓无人调用、测试里也是零，
# 不是「只被测试接线」而是死代码，各自都是一行转发给同文件里的正主（NewDecimalFromInt64 与
# EvaluatePricing，两个正主分别有 11 与 25 处引用）。**已删**，见本文件的 git 历史。
#
# 留这段是因为：本门禁按判据本就不管这一类，删掉之后**不会再有任何机制提醒下一个人这里曾有
# 过一对影子函数**。而它俩当初能长出来，正是因为同文件里已有正主却没有东西拦住第二个写法。
