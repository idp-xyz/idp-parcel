# UC-PS-001 生产接单业务决策简报

状态：已确认；八项接单机制已纳入领域与应用基线，真实实例参数仍待证据登记

## 目的

本简报记录 [`UC-PS-001`](./UC-PS-001-SUBMIT-SHIPMENT-REQUEST.md) 中八项已经整体确认的接单业务机制，并说明它们在领域模型、应用编排和试点治理中的落点。开发可以据此实现稳定业务骨架，但不得把尚未登记的真实参数、角色、字段或阈值当作产品默认值。

本简报不确定 API、字段、错误码、技术超时、重试次数、客户 SLA，也不填写真实客户、合同、线路、关务区域或资金参数。

## 已确认决策摘要

| ID | 已确认结论 | 当前状态 | 主要落点 |
|---|---|---|---|
| `BD-PS-001` | 建立独立、可续办的接受判断任务；未决不伪装为拒绝 | 已确认 | 应用任务、续办、通知与状态投影 |
| `BD-PS-002` | 版本化硬规则全部通过时自动接受；人工复核必须由规则显式要求；主动拒绝仅允许授权角色按结构化依据形成 | 已确认 | 接受命令、权限、审计与事件 |
| `BD-PS-003` | 采用版本化接受前财务控制策略，可装配预付冻结、信用校验、明确无控制或合同明确的组合 | 已确认，真实策略待登记 | 接单与结算协作及补偿 |
| `BD-PS-004` | 在领域接受前判定试点生产归属；排除范围交给当前唯一生产权威，不能安全交接时保持归属未决 | 已确认，试点参数待登记 | 入口路由与生产权威治理 |
| `BD-PS-005` | 已提交未决采用同一委托的新提交版本；已拒绝后修正建立关联新委托；已接受后走独立变更用例 | 已确认 | 身份、版本、幂等与客户回执 |
| `BD-PS-006` | 按产品、合同、法人、服务/关务范围和有效期装配版本化校验规则；渠道错误映射为稳定业务原因 | 已确认，真实规则待登记 | 规则装配、校验器与错误语义 |
| `BD-PS-007` | 不支持委托成员级部分接受；失败成员必须在接受前显式纠正或重组 | 已确认 | 委托不变量、接受基线与重组流程 |
| `BD-PS-008` | 分离五类时间；独立商业选择锚点先解析规则包，再按规则逐项形成下游 `asOf`，提交前失效的关键依据必须重新判断 | 已确认；真实锚点与逐项策略待登记 | 版本选择、并发裁决与审计 |

八项决策的落点不同：`BD-PS-001`、`BD-PS-002`、`BD-PS-005`、`BD-PS-007` 和 `BD-PS-008` 是长期接单机制；`BD-PS-003` 和 `BD-PS-006` 只能先确认通用机制，真实合同模式和字段仍由证据决定；`BD-PS-004` 是试点运行治理，不写成长期领域不变量。

## 逐项确认口径

### `BD-PS-001` 未决接受判断如何续办

**已确认决定**

- 为已经建立的委托创建独立、可追溯的接受判断任务；任务是应用处理机制，不新增委托领域状态。
- 依赖不可用、权威判断尚未形成、并发冲突或可安全恢复的处理失败时，本次返回`尚未决定`，委托保持`已提交`，并从已保存阶段继续判断。
- `network-routing` 返回`资料不足`时必须保留原语义。适用产品、合同和资料规则可以决定补充、纠正或确定性拒绝，应用层不得统一把它映射为不可达或固定拒绝。
- 客户可补充的资料缺口进入待补充动作；系统依赖故障进入查询或重试，两者使用不同原因和续办方式，不能都归为“等待客户”。
- 判断任务必须保存当前阶段、缺少的权威结果、已经采用的版本、最后处理结果和可安全续办引用。只有版本化产品或合同规则明确规定补充期限及届满后果时，届满复核才可以形成确定性拒绝；否则只能升级给授权角色继续处置。通知渠道、补充时限和升级参数等待真实合同与 `PAR-NFR-06` 参数化。
- 无法建立最小委托身份的输入继续使用`输入未受理`，不能为了进入任务机制创建占位委托。

**边界示例**

商业规则服务暂时不可用时保持未决并重试；适用规则已经明确某项必要资料缺失且该产品不允许补充时，才形成确定性业务拒绝。

### `BD-PS-002` 自动接受与主动拒绝边界

**已确认决定**

- 当前适用的版本化硬规则、商业限制和包裹级可达性要求全部通过后，由系统自动形成接受决定，不再增加无业务依据的人工审批门槛。
- 规则未通过时按权威规则形成拒绝、待补充或未决结果；人工操作不能把硬规则失败改成通过，也不能跳过缺少的权威判断。
- 只有适用规则本身明确要求人工业务判断时，才进入人工复核；人工复核不是所有委托的默认步骤。
- 运营企业主动拒绝只允许适用责任法人授权的角色形成，并必须记录结构化原因、证据、适用范围、授权依据、实际决定方和决定时间。
- 主动拒绝只作用于尚未接受的委托。客户在决定前撤回整份待决委托进入 [`UC-PS-005`](./UC-PS-005-WITHDRAW-SUBMITTED-SHIPMENT-REQUEST.md)，不是运营拒绝；已经接受后的停止服务、取消或变更进入 [`UC-PS-006`](./UC-PS-006-CANCEL-PARCEL-OR-COORDINATE-POST-INTAKE-DISPOSITION.md)等独立用例，不回写为接单拒绝。
- 主动拒绝不为全部委托增加等待窗口。它只在委托仍为`已提交`时与自动接受竞争同一个决定提交边界；一个决定合法提交后，另一方只能读取既有结果，不能追加相反决定。
- 是否针对某个产品或客户保留人工接受门槛，必须作为版本化商业规则显式配置；默认不创建一条隐藏的人工接单路径。

**边界示例**

规则全部通过但运营企业因已授权且有证据的风险原因不承担请求时，可以主动拒绝；普通客服备注或临时口头决定不能产生拒绝事实。

### `BD-PS-003` 接受前财务控制策略

**已确认决定**

- 每个适用合同版本必须引用版本化的接受前财务控制策略。策略可以装配`预付冻结`、`账期信用校验`、明确的`接受前无财务控制`，或者合同明确要求且相互不冲突的控制组合；不得把它们硬编码成三个永久互斥枚举，未配置也不能被解释为默认放行。
- 策略组合必须明确每项控制的适用范围、共同通过条件、判断顺序和失败处置；某一范围明确无财务控制不能静默取消同一合同对其他范围明确要求的控制。
- 适用预付冻结时，在接受前取得版本化估价并形成足额资金冻结结果；冻结失败时不得接受。接受提交失败时必须按同一业务关联请求释放冻结，不能遗留无委托责任的占用。
- 预付冻结在委托被拒绝、放弃或冻结依据失效时也必须显式释放；重复查询、重试和补偿不得形成重复冻结或重复释放。
- 适用信用校验时，在接受前校验适用信用额度、信用暴露、逾期或暂停规则；是否同时冻结资金只由同一策略的明确合同依据决定。判断不通过时按合同政策拒绝或进入授权处置，不能用人工备注绕过。
- 接受前明确无财务控制的适用范围必须保存合同版本和“不适用”依据；它不代表后续不计费、不结算或没有其他信用治理。
- 任何必需的财务判断无法确定时保持未决，不得按余额充足、额度可用或无需控制处理。
- 金额、余额、冻结和信用暴露由 `settlement-accounting` 提供；商业政策、合同适用性和定价方案绑定由 `party-commercial` 提供，可执行价卡和纯 `PricingEvaluation` 由 `parcel-pricing` 提供；`parcel-shipment` 只保存接受时采用的结果与依据。
- 跨上下文采用可补偿编排，不要求分布式事务。接受决定只有在适用商业前置结果成立后才能提交；接受提交完成后，事件投递失败不得释放已经合法占用的资金或回退接受决定。

**边界示例**

预付冻结成功但接受提交并发失败时执行关联释放；接受已经提交但回执丢失时查询原决定，不得再次冻结或再次接受。合同同时要求信用校验和预付冻结时，两项结果都必须满足策略才可接受；合同对明确范围规定接受前无财务控制时，本用例记录不适用，不调用冻结或信用判断冒充额外门槛。

### `BD-PS-004` 试点生产归属与业务接受分离

**已确认决定**

- 在进入 `idp-parcel` 的领域接受流程前，对完整拟受理委托形成版本化生产归属结果。
- 生产归属同时记录“权威身份”和“准入门禁”两个正交维度。未纳入试点或当前暂停新准入时，不建立 `idp-parcel` 的业务拒绝决定；暂停不是第四种权威身份，也不改变已经形成的权威身份、已纳入委托或在途责任。其他系统承接时，该范围交给当时已经明确的唯一生产权威系统处理；不得把某个旧系统永久写成默认权威。无法确定权威方或无法证明请求可以安全交接时，只保留`生产归属未决`的接入记录，不向任一系统伪造接受或拒绝。
- 客户首先取得渠道中立的接收或查询关联，最终业务结果由被选中的唯一权威系统返回。生产权威交接、结果回传和失败兜底由 `PAR-GOV-03` 至 `PAR-GOV-07` 及上线方案参数化。
- 同一逻辑委托在同一业务时点只能有一个生产权威接受方。入口必须携带稳定关联，禁止同时向多个候选生产系统投递后由结果先到者决定归属。
- 归属规则按完整委托及拟接受成员判断，不以包裹级拆分规避试点范围。已经由 `idp-parcel` 接受的委托不因后续暂停被迁回或改写。

**边界示例**

试点容量已满时，该输入由该范围当前唯一生产权威系统继续处理；`idp-parcel` 不能记录“客户委托已拒绝”，也不能只接受其中部分包裹。若当前权威或安全交接无法确认，则保持生产归属未决。

### `BD-PS-005` 纠错、重提与委托身份

**已确认决定**

- 委托处于`已提交`且尚未决定时，客户纠错在同一委托下形成新的提交版本并重新判断；原始版本、变更原因、提交主体和判断历史永久保留。
- 同一委托同一时刻只有一个待判断的当前提交版本；旧版本继续可追溯，但不能与新版本并行取得接受决定。
- 只有客户范围、委托边界和最小成员身份仍可确认属于同一业务请求时，才沿用原委托身份。改变客户账户、合同责任范围、寄收件关系或目的服务范围的修正必须重新确定委托边界。
- 普通资料补充可以沿用原委托；为了剔除失败成员、拆分成员或改变成员共享约束而进行的重组，必须形成一份或多份关联新委托，并明确原成员去向，不能伪装成同一委托的字段纠错。
- 委托已经`已拒绝`后，修正请求建立关联的新委托，并明确来源、替代或重提关系；不把终局拒绝原地重开为已提交。
- 委托已经`已接受`后，资料更正进入 `UC-PS-002`，成员取消或收寄后停止服务进入 `UC-PS-006`；改变共享服务范围或新增服务件建立关联新委托，不复用接单纠错通道覆盖接受基线。
- 去重应区分同一版本的重试与明确的新提交版本。相同版本返回已有结果，不同版本必须保留关联并重新校验。

**边界示例**

已提交委托补充缺少的申报资料可以形成新提交版本；已拒绝委托补齐资料后建立关联新委托；已接受委托新增包裹必须形成关联新委托。

### `BD-PS-006` 可执行校验规则与稳定原因

**已确认决定**

- 建立由服务产品、客户合同、关务区域、适用法人、服务范围和有效时间共同选择的版本化校验规则组合。组合只装配各权威上下文拥有的规则，不建立一个可以覆盖其他上下文事实的“万能规则库”。
- 规则组合至少区分接入最小身份、委托通用不变量、产品与合同资料、监管原始资料和跨字段条件五层；正式关务判断仍由 `customs-compliance` 拥有，不能前移为接单字段校验。
- 每次判断保存实际采用的规则标识、版本、有效区间、输入事实版本和结果；新版本不追溯改写既有接受或拒绝依据。
- 每项结构化结果至少表达适用对象或字段范围、结果确定性、是否可纠正、建议处置和客户可见消息键；`输入未受理`、业务拒绝和未决使用不同原因目录。
- 对外接入字段由适配器映射为稳定业务语义。渠道字段名、枚举和临时错误不能进入核心领域模型成为永久规则。
- 业务结果使用稳定、可分级的原因标识，并保留原渠道或外部错误作为来源证据；不得用自由文本代替可统计原因，也不得丢失原始返回。
- 必填字段、跨字段条件和真实原因目录只能在锚点产品、合同、线路及关务参数提供后发布，当前不得用行业经验填充默认值。

**边界示例**

两个渠道用不同字段表达同一申报价值时，适配器分别映射到同一业务语义；具体产品规则决定是否必填，渠道字段名不进入接受不变量。

### `BD-PS-007` 多包裹委托是否部分接受

**已确认决定**

- 不支持成员级部分接受。一份委托的当前提交版本只形成一个委托级接受或拒绝决定，接受基线覆盖该版本的全部声明包裹。
- 任一成员确定性不满足接受条件时，整份当前提交版本不能接受；可纠正问题按 `BD-PS-001` 和 `BD-PS-005` 形成补充版本或关联重提。
- 某个成员不满足接受条件时，不得静默删除该成员后接受剩余成员，也不得先接受再补做拆分。
- 客户或授权运营角色可以在接受决定前显式纠正成员；需要删除、拆分或重新组合成员时，必须按委托共享约束形成一份或多份关联新委托，并保留原提交、成员去向、重组原因和关联。
- 重组后的每份委托分别校验和决定。提交批次仍允许不同委托分别接受、拒绝或未决，不扩大为整批原子处理。
- “不支持成员级部分接受”及接受前重组边界已经写入 [`parcel-shipment` 权威上下文](../../domain/parcel-shipment/CONTEXT.md)，`UC-PS-001` 只引用并编排该规则，不另建第二套定义。

**边界示例**

三个包裹中一个明确不可达时，系统不能只接受另外两个。客户可以在决定前明确重组，重组后的委托各自取得完整决定。

### `BD-PS-008` 判断有效时点与提交失效

**已确认决定**

- 分别记录来源发生时间（`occurredAt`）、系统接收时间（`receivedAt`）、客户请求生效时间（`requestEffectiveAt`）、判断依据时点和接受/拒绝决定时间，不使用一个通用“业务时间”代替。`occurredAt`/`receivedAt` 是来源信封元数据，不进入规范化内容摘要；其变化不单独造成冲突，也不建立二者的先后硬约束。
- 客户请求生效时间（`requestEffectiveAt`）是客户声明的业务适用时间；其值及缺失/显式存在状态进入 `PayloadDigest`，可以参与服务日历、地址范围和可达性判断，但不能用 `occurredAt` 或 `receivedAt` 回填，也不能通过回填恢复已经失效的商业资格。
- 客户关系、合同、产品、授权、信用和接受权限必须在形成接受决定的业务时点有效。首次商业解析使用独立于候选规则包的版本化商业选择锚点；只有 `party-commercial` 唯一选出产品、合同和规则包后，`parcel-shipment` 才为当前提交版本按该规则包逐项形成下游判断的具体 `asOf` 语义和值，各权威判断上下文校验并回显。不得让规则包决定自身选择时点，不得使用一个全局时间代替不同判断的时点语义，客户也不能通过回填时间恢复旧合同资格或绕过当前限制。
- 每个跨上下文判断必须返回判断时间、明确的 `asOf` 语义、采用版本、有效区间和当前修订标识；`parcel-shipment` 保存实际采用的引用与快照。
- 决定提交前复核所有关键判断仍覆盖所选依据时点且当前未被有效限制推翻。依据失效、被替代或出现并发冲突时重新判断；暂时无法完成时返回未决，不以旧结果接受。
- 已经合法提交的接受或拒绝不因规则后来更新而被静默重写；纠错或有效性变化通过新版本和明确关系处理。

**边界示例**

可达性判断完成后、接受提交前线路被有效关闭时，必须重新判断；接受提交完成后才发布的新线路版本不能追溯改写原接受依据。

## 组合后的接单机制

八项决策整体采用后，接单编排形成以下稳定顺序：

1. 入口先判定生产归属并保全原始提交；同一委托只进入一个生产权威。
2. 建立委托及提交版本，创建或续办接受判断任务。
3. 先按独立商业选择锚点唯一解析产品、合同和规则包，再按已选规则包逐项形成并由权威提供方校验判断依据时点，取得资料、可达性和适用资金/信用结果。
4. 失败成员在决定前显式纠正或重组，不进行成员级部分接受。
5. 硬规则全部通过时自动接受；确定性不通过按适用规则处置，依赖未决保持已提交。
6. 主动拒绝只由授权角色按结构化依据形成。
7. 接受、拒绝、未决、纠错和重提都保留来源、版本、决定与事件发布关联。

该顺序是业务机制，不等于 API 调用顺序、数据库事务布局或消息拓扑。

## 确认范围与参数边界

- `BD-PS-001` 至 `BD-PS-008` 的业务机制已经整体确认；权威领域文档和应用用例必须以这些结论为准，本简报不另建第二套领域状态。
- `BD-PS-003`、`BD-PS-004` 和 `BD-PS-006` 的通用机制已经确认，但真实合同策略、上线方案、校验规则和试点参数仍须在参数登记册中取得证据后才能进入生产。
- `BD-PS-001`、`BD-PS-002` 和 `BD-PS-008` 所需的真实补充时限、通知渠道、人工复核条件、授权角色、商业选择锚点、逐项判断时点策略和 SLA 同样保持未配置，不能从确认的机制反推出默认值。
- 接单规则、授权和判断时点统一由 `PAR-COM-14` 取证，接受前财务控制由 `PAR-COM-15` 取证，生产归属与交接继续使用 `PAR-GOV-03` 至 `PAR-GOV-07`；不得在本简报维护第二套实例状态。
- 本次确认不批准任何真实金额、阈值、角色名单、渠道 Schema、超时、重试次数或 SLA。
