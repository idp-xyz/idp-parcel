# UC-PS-002 补充或更正已接受委托的客户原始资料

状态：开发基线草案；稳定业务边界已形成，允许字段、阶段规则和授权入口由 `PAR-COM-13` 与待确认业务决策管理

## 目标与边界

本用例从已接受委托或包裹的客户、或被授权代表客户的运营角色提出资料补充/更正请求开始，到 `parcel-shipment` 形成可追溯的客户原始资料新版本、明确拒绝/未受理结果，或者记录本次尚未形成业务结果为止。

本用例包含：

- 校验目标委托、包裹、资料范围、请求方、实际决定方、授权和请求所依据的源版本。
- 保全原始请求、原因、来源、来源发生时间（`occurredAt`）、系统接收时间（`receivedAt`）和资料适用时间（`requestEffectiveAt`）。前两者是来源信封元数据，不单独形成冲突；资料适用时间属于规范化资料内容。
- 形成不可覆盖的客户原始资料新版本，并派生当前可供下游消费的资料版本采用判断。
- 区分资料补充、资料更正、来源版本更正/撤销、无变化重复请求、业务拒绝、输入未受理、业务未决和技术未形成。
- 支持逐包裹、逐字段和批量部分成功，同时维持跨客户、法人、委托和包裹范围隔离。
- 将新版本引用交给关务、路由、节点或结算上下文重新作出各自判断。

本用例不执行：

- 修改委托接受基线、客户账户、责任法人、合同、服务产品、寄收件关系的共享约束或包裹身份。
- 自由增删已接受委托成员；新增服务件进入关联新委托，对已有成员只能通过 [`UC-PS-006`](./UC-PS-006-CANCEL-PARCEL-OR-COORDINATE-POST-INTAKE-DISPOSITION.md) 逐包裹请求取消或停止服务，并形成适用的包裹取消或收寄后服务处置结果。
- 覆盖节点实测、重新包装、实际收寄、装袋、运输、监管事实、正式申报资料、提交版本或最终费用。
- 自动换单、重新包装、改路、重新申报、提交授权、重新提交、放行、解除内部限制、重开案件或重新计费。
- 直接判定客户资料真实与否、关务专业结论、运输可行性、节点作业结果或财务金额。

## 元数据

| 项目 | 内容 |
|---|---|
| 用例 ID | `UC-PS-002` |
| 主责上下文 | `parcel-shipment` |
| 主要参与者 | 货主客户，或当前有效授权范围内的运营代表 |
| 业务决定所有者 | `parcel-shipment`；字段与阶段适用性依据来自已登记规则 |
| 触发 | 客户或授权运营角色提出已接受委托后的资料补充/更正请求 |
| 起始边界 | 修订请求到达客户资料受控入口并被保全 |
| 成功结束 | 客户原始资料新版本已形成，且当前资料版本采用判断可供下游引用 |
| 业务未决结束 | 资料冲突、基础版本过期、规则或授权无法确定，未形成新采用判断 |
| 业务拒绝结束 | 请求已明确违反成员、合同、阶段或其他适用业务规则 |
| 输入未受理结束 | 无法识别目标委托/包裹/资料范围或请求授权，未建立修订请求结果 |
| 技术未形成结束 | 原始请求已保全，但版本或发布意图未完成提交，可安全续办 |

## 权威依据

本文件只拥有应用编排和开发契约。术语、规则、生命周期和所有权发生差异时，以下文档优先：

| 引用键 | 权威内容 | 来源 |
|---|---|---|
| `PS-LANG` | 客户原始资料、资料版本、资料补充/更正请求和当前资料版本采用判断 | [小包托运：Language](../../domain/parcel-shipment/CONTEXT.md#language) |
| `PS-RULE` | 接受基线不可覆盖、成员约束、来源版本和下游资料边界 | [小包托运：Rules and invariants](../../domain/parcel-shipment/CONTEXT.md#rules-and-invariants) |
| `PS-LIFE` | 委托接受、资料版本和客户承诺生命周期 | [小包托运：Lifecycles](../../domain/parcel-shipment/CONTEXT.md#lifecycles) |
| `PC-RULE` | 客户、责任法人、合同、产品、授权和委派版本 | [参与方与商业](../../domain/party-commercial/CONTEXT.md#rules-and-invariants) |
| `CC-DATA` | 正式申报资料只能由关务消费来源版本后形成 | [UC-CC-002](../customs-compliance/UC-CC-002-FORM-DECLARATION-UNIT-AND-DATA.md) |
| `CC-READY` | 新资料后重新评估申报就绪，不能沿用旧判断 | [UC-CC-003](../customs-compliance/UC-CC-003-ASSESS-DECLARATION-READINESS.md) |
| `CC-ACTION` | 已提交后的补充、更正、撤销重报和替代分类 | [UC-CC-007](../customs-compliance/UC-CC-007-MANAGE-POST-SUBMISSION-ACTIONS-AND-REPLACEMENT.md) |
| `CC-CLOSE` | 关闭后迟到资料不自动重开案件 | [UC-CC-010](../customs-compliance/UC-CC-010-CLOSE-REOPEN-AND-ESTABLISH-FOLLOW-UP-CASE.md) |
| `ADR-FACT` | 来源事实、有效性关系和派生结果分离 | [ADR-0005](../../adr/0005-source-facts-effective-events-derived-state.md) |
| `PARAM` | 真实入口、字段、阶段、授权和时间参数 | [首发试点参数与证据登记册](../../product/PILOT-PARAMETER-REGISTER.md) |

## 参与者与上下文协作

| 参与者或上下文 | 本用例职责 | 明确不负责 |
|---|---|---|
| 货主客户 | 提供补充/更正请求、资料内容、原因和资料适用时间（`requestEffectiveAt`） | 不能修改接受基线、节点实测或正式申报资料 |
| 授权运营代表 | 在客户合同或运营委派允许的范围内代录或提出请求 | 登录能力不等于资料决定或跨客户授权 |
| 接入适配器 | 认证入口、保全原始请求和来源语义 | 不直接覆盖当前资料或形成接受后资料结果 |
| `parcel-shipment` | 校验范围和授权，形成资料请求结果、客户原始资料版本和采用判断 | 不形成关务、节点、运输、路由或财务决定 |
| `party-commercial` | 提供当前客户、责任法人、合同、产品、委派和资料修改权限 | 不形成具体资料版本 |
| `customs-compliance` | 消费新资料版本，形成正式申报资料、就绪和后续关务动作 | 不修改客户原始资料 |
| `node-operations` | 提供节点实测、作业和物理差异事实 | 不接受客户更正覆盖实测 |
| `network-routing` | 依据新资料重新判断网络资格或剩余路线 | 不直接改写客户资料或自动改路 |
| `settlement-accounting` | 依据适用计费规则重新判断费用影响 | 不以客户资料版本直接改最终金额 |
| `visibility-exception` | 在资料差异需要协调时建立异常信号或案件 | 不判定资料采用、关务结果或责任 |

## 输入语义契约

输入是稳定业务语义，不是 API、文件或数据库字段清单。省略字段表示“不变”，不表示清空；显式清空必须由字段和阶段规则允许。

| 输入组 | 客户或代理可声明 | 权威来源 | 最低业务语义 |
|---|---|---|---|
| 来源信封 | 否 | 接入适配器与接入来源 | 请求身份、来源主体、来源发生时间（`occurredAt`）、系统接收时间（`receivedAt`）、原始内容或不可变引用、重试关系；两类时间不进入规范化内容摘要 |
| 目标范围 | 是 | 客户请求，目标身份由 `parcel-shipment` 解析 | 委托、包裹、字段或资料组范围；不得以批次、袋、总单或客户名称模糊指代 |
| 基础版本 | 是 | 客户请求引用，现行版本由 `parcel-shipment` 核对 | 请求方认为正在修订的源版本或明确“补充缺失资料”的基准 |
| 修订意图 | 是 | 客户请求 | 补充、更正、显式清空、来源更正或撤销关系及原因 |
| 资料内容 | 是 | 客户原始声明 | 寄收件、货物、申报原始资料和服务要求范围内的声明内容 |
| 业务时间 | 可声明 | 适用业务规则与 `parcel-shipment` | 资料何时在业务上成立或适用；不能仅以接收时间替代 |
| 授权上下文 | 否 | `party-commercial` 与接入身份 | 客户账户、责任法人、委托关系、实际决定方、授权角色、委派和有效时间 |

客户资料版本必须关联明确委托或包裹范围。跨客户批量请求只是操作归组，每个目标分别判断、分别形成结果，不能形成批量级当前资料状态。

## 资料范围与阶段边界

具体字段、字段组、阶段和允许动作由 `PAR-COM-13` 与真实合同、产品、线路和关务规则登记。未登记时只能形成未决或业务拒绝，不能以系统便利推断允许。

| 业务阶段 | 可形成的结果 | 不能由本用例自动完成 |
|---|---|---|
| 已接受、尚未收寄 | 补充或更正客户原始资料新版本 | 修改接受基线、成员、承诺、路由或服务产品 |
| 已收寄或已测量 | 保留客户声明新版本并关联节点差异 | 覆盖实测、作业、身份或计费事实 |
| 已制签或已装袋 | 形成新源版本并通知相关消费方重新判断 | 自动换单、重包、拆袋、重封或改路 |
| 关务资料形成中、尚未提交 | 供 `UC-CC-002` 形成新的正式资料版本 | 自动形成就绪、授权或提交 |
| 已提交关务 | 由 `UC-CC-007` 分类补充、更正、撤销重报或替代 | 修改或重发既有提交版本 |
| 案件已关闭或服务已完成 | 保全迟到版本及其适用时间 | 自动重开案件、回退运输、承诺或结算 |

## 结果语义契约

| 结果 | 业务含义 | 必须保留的依据 | 禁止行为 |
|---|---|---|---|
| 已记录并采用 | 新版本已形成，当前资料版本采用判断明确可供下游引用 | 请求、原始内容、基准版本、版本范围、原因、业务/适用时间、决定方、授权快照和关系 | 不得改写接受基线、旧版本或下游正式资料 |
| 已记录但待下游判断 | 新版本已形成，但字段或阶段影响需要关务、路由、节点或结算重新判断 | 版本和待判断范围、下游责任方、当前依据 | 不得把版本形成解释为就绪、放行、改路或计费结果 |
| 待补充/待复核 | 缺少必要内容、基础版本过期、字段冲突或规则需要人工判断 | 缺失/冲突范围、当前版本、待补足原因和续办引用 | 不得按“无变化”或最后到达内容采用 |
| 业务拒绝 | 请求违反成员、账户、合同、产品、字段或阶段规则 | 结构化原因、规则版本、适用时间、决定方和授权 | 不得删除原始请求或写成技术失败 |
| 输入未受理 | 目标、范围、来源或授权无法建立最小修订请求 | 原始请求引用、确定性错误和解析依据 | 不得创建占位版本或跨客户猜测目标 |
| 已有结果 | 同一请求身份与相同内容已经形成结果 | 原请求和已有版本/结果关联 | 不得重复形成版本 |
| 请求冲突 | 同一请求身份携带不同内容、范围、基准或资料适用时间（`requestEffectiveAt`） | 原请求、冲突字段和纠正入口 | 不得把 `occurredAt`/`receivedAt` 的变化当作冲突，也不得按最后到达覆盖 |
| 技术未形成 | 原始请求已保全，但版本提交或发布意图未完成 | 处理阶段、失败位置和安全续办引用 | 不得返回已采用或业务拒绝 |

`已记录并采用`只表示 `parcel-shipment` 的资料版本判断完成，不表示下游已经采用。下游必须按各自上下文的业务时间和门禁重新判断。

## 应用流程

| 步骤 | 责任方 | 应用动作 | 结果与下一步 |
|---|---|---|---|
| 1 | 接入适配器 | 认证入口并保全请求、原始内容、来源时间和接收时间 | 无法保全时形成技术未形成，不进入资料版本处理 |
| 2 | `parcel-shipment` | 识别重复、重试、内容冲突和关联请求 | 相同内容返回已有结果；不同内容进入请求冲突 |
| 3 | `parcel-shipment` | 解析客户账户、委托、包裹、字段范围和基础版本 | 目标不明、跨客户越权或范围不可解释时输入未受理 |
| 4 | `party-commercial` / `parcel-shipment` | 校验请求方、实际决定方、授权角色、委派和适用时点 | 授权不足形成业务拒绝；依赖无法确定形成待复核 |
| 5 | `parcel-shipment` | 校验接受基线共享约束、成员和包裹身份不变量 | 增删成员、改账户/法人/合同/产品/服务范围拒绝并转关联路径 |
| 6 | `parcel-shipment` | 按字段、范围、阶段和规则判断补充、更正、显式清空或来源更正 | 不允许的阶段或字段形成业务拒绝；缺失或冲突形成待补充/待复核 |
| 7 | `parcel-shipment` | 形成不可覆盖客户原始资料版本及其关系 | 记录原因、基准版本、对象范围、业务时间、决定方和授权快照 |
| 8 | `parcel-shipment` | 派生当前资料版本采用判断 | 只覆盖明确范围；不删除旧版本，不产生下游专业结果 |
| 9 | `parcel-shipment` | 将版本引用交给适用下游 | `UC-CC-002`、`UC-CC-003`、`UC-CC-007`、路由、节点或结算分别重新判断 |
| 10 | 接入适配器 / 下游责任方 | 返回版本结果或提供查询 | 发布失败只重试同一发布意图，不重复形成资料版本 |

## 下游交接边界

| 下游 | 新资料版本的消费方式 | 明确不允许 |
|---|---|---|
| `UC-CC-002` | 对未提交范围引用新的客户原始资料版本，形成新的正式申报资料版本 | 直接修改客户原始资料、覆盖旧正式资料或假定已就绪 |
| `UC-CC-003` | 对受影响申报单元重新评估申报就绪，保留旧判断历史 | 由资料补充自动恢复就绪、授权或提交 |
| `UC-CC-007` | 对已提交范围分类补充、更正、撤销重报或替代，决定是否建立后续动作 | `UC-PS-002` 不创建新的提交版本或重发外部申报 |
| `UC-CC-010` | 关闭或已完成后保全资料版本，由其判断原案后续办、重开或不改变生命周期 | 资料版本自动重开案件或回退关闭决定 |
| `node-operations` | 将客户声明与实测差异作为输入，继续由节点拥有测量和作业事实 | 客户版本覆盖实测、收寄、装袋或重包事实 |
| `network-routing` | 重新判断资格、剩余路由或资料影响 | 自动改路、恢复旧路线或覆盖已发生移动 |
| `parcel-pricing` / `settlement-accounting` | `parcel-pricing` 依据新资料和已解析依据形成新的评价；`settlement-accounting` 再按费用确认条件判断费用影响 | 资料版本直接覆盖评价、最终费用或对账结果 |

客户资料更正不自动解除 `UC-CC-011` 的内部合规限制，不自动改变外部监管结果、放行门禁或异常案件；这些结果仍由其责任用例重新判断。

## 版本、并发与迟到规则

- 同一请求身份、相同规范化内容摘要、相同目标范围和基础版本的重试返回已有结果；不能创建第二个资料版本。规范化摘要必须包含资料适用时间（`requestEffectiveAt`）的值及缺失/显式存在状态。
- 同一请求身份携带不同内容、范围、基础版本或 `requestEffectiveAt` 形成请求冲突；`occurredAt`/`receivedAt` 变化只追加来源观察，不单独形成冲突，不得按最后到达覆盖。
- 请求引用过期基础版本且与当前版本存在重叠字段时，形成冲突或待复核；只有登记了明确合并规则的非重叠范围才可合并，首发默认保持待复核。
- 资料版本提交顺序由明确的领域顺序、基础版本关系和业务有效时间裁决；消息到达、数据库写入或客户端时间不能单独决定当前版本。
- 资料来源被更正、撤销或确认无效时，保留原版本并追加关系，重新派生当前资料版本采用判断；不删除历史。
- 资料适用时间早于接收时间的迟到请求，按适用规则重新派生受影响的当前判断；不能把历史接受基线、正式申报资料或提交版本改写成使用了迟到版本。不得用 `occurredAt` 或 `receivedAt` 默认补齐缺失的 `requestEffectiveAt`。
- 已形成的物流、关务、监管、财务和承诺事实不因资料版本变化回退。需要重新申报、换单、重包、改路或调整费用时，由对应上下文形成新的决定和版本。
- 部分成功只在明确对象和字段范围内成立；批量操作不得形成“全部已采用”或“全部已拒绝”的总结果。

## 权限、隐私与审计

- 请求方、实际决定方和授权角色分别记录；登录操作人不能替代实际决定方，客户请求也不能越过运营企业或合同授权边界。
- 每次版本必须关联集团租户、责任法人、客户账户、委托、包裹和明确字段/资料范围，跨客户合报不得扩大可见范围。
- 客户只能看到自身授权范围内的请求、版本和结果；错误、查询、导出和批量回执不得泄露其他客户的资料或限制。
- 审计至少包括原始请求、来源、请求/接收/业务/适用时间、目标解析、基础版本、旧新内容或不可变引用、原因、规则版本、决定方、授权快照、版本关系、下游交接和发布结果。
- 人工纠错必须形成新请求和新版本；不得直接覆盖接受基线、源版本、当前判断或下游结果。
- 资料内容按最小必要原则披露；日志和指标使用关联标识，不输出不必要的地址、货物或申报明文。

## 验收示例

| ID | 场景 | 预期结果 |
|---|---|---|
| `AT-PS-014` | 接受后补充缺失的客户申报原始资料 | 形成新客户原始资料版本，接受基线不变，并标明下游待判断范围 |
| `AT-PS-015` | 更正已接受的寄件、收件或货物声明值 | 保留旧版本和更正关系；当前判断引用新版本，不覆盖接受基线 |
| `AT-PS-016` | 同一请求身份以相同内容重试 | 返回原结果和版本，不重复形成新版本 |
| `AT-PS-017` | 同一请求身份携带不同内容或范围 | 形成请求冲突；原请求和原版本不被覆盖 |
| `AT-PS-018` | 无法识别目标委托、包裹或资料范围 | 输入未受理，不创建占位版本 |
| `AT-PS-019` | 请求方无客户、委托或字段范围授权 | 业务拒绝，按最小必要原则返回，不披露其他客户资料 |
| `AT-PS-020` | 省略字段与显式清空分别提交 | 省略字段保持不变；显式清空只有在字段和阶段规则允许时才形成版本 |
| `AT-PS-021` | 过期基础版本与当前版本在同一字段重叠 | 保持冲突或待复核，不按最后写入覆盖 |
| `AT-PS-022` | 多包裹批量请求中 P1 合法、P2 待复核、P3 越权 | 逐包裹、逐范围返回结果；不形成批量总状态 |
| `AT-PS-023` | 请求新增或删除已接受委托成员 | 拒绝普通资料更正，转取消、重组或关联新委托路径 |
| `AT-PS-024` | 请求改变客户账户、责任法人、合同、服务产品或共享服务范围 | 拒绝普通资料更正；不得改变接受基线 |
| `AT-PS-025` | 客户重量/尺寸更正与节点实测冲突 | 双方事实分别保留；由节点和关务等责任方重新判断，不覆盖实测 |
| `AT-PS-026` | 已制签、收寄、装袋或运输后客户更正 | 形成版本并交下游；不自动换单、重包、改作业或回退运输事实 |
| `AT-PS-027` | 关务尚未提交时形成客户资料更正 | `UC-CC-002` 引用新源版本形成新的正式资料，`UC-CC-003` 重新评估；不自动提交 |
| `AT-PS-028` | 关务已提交后形成客户资料更正 | 交 `UC-CC-007` 分类后续动作；不修改或重发原提交版本 |
| `AT-PS-029` | 关务案件已关闭或服务已完成后收到更正 | 保全迟到版本；由 `UC-CC-010` 分类是否后续办，不自动重开 |
| `AT-PS-030` | 业务有效时间早于系统接收时间的迟到更正 | 按业务时间重派生受影响判断；不倒改接受、正式申报或提交历史 |
| `AT-PS-031` | 资料版本已形成但下游事件首次发布失败 | 版本只形成一次；仅重试同一发布意图 |
| `AT-PS-032` | 跨客户查询、导出和批量更正 | 严格按客户账户、法人、委托、包裹和授权隔离；不得泄露共同集运或合报之外的资料 |

## 待确认业务决策

以下项目不改变本用例的不可变版本和所有权边界，但阻塞真实生产字段、阶段、授权和时间规则：

| ID | 待确认决策 | 当前建议 | 影响 |
|---|---|---|---|
| `BD-PS-009` | 接受后资料修订的生产入口、请求方和实际决定方 | 优先复用唯一生产接入渠道；客户可提交请求，授权运营角色可代录，版本决定仍由本用例按权限形成 | 接入、权限、审计和客户回执 |
| `BD-PS-010` | 字段/资料组与接受、制签、收寄、装袋、运输、申报和完成阶段的允许动作 | 采用版本化阶段矩阵；未登记的字段和阶段不默认允许 | 校验、拒绝、下游交接和验收 |
| `BD-PS-011` | 客户声明与节点实测、关务正式资料或计费资料冲突时的处理 | 双层保留来源事实，由对应上下文重新判断；不以客户更正覆盖其他来源 | 节点、关务、路由和结算协调 |
| `BD-PS-012` | 业务有效时间、迟到更正、来源撤销和并发合并规则 | 分离业务时间、适用时间和接收时间；重叠过期版本默认待复核，不按最后到达覆盖 | 当前判断、历史解释和并发处理 |

## 开发拆分建议

在 `PAR-COM-13` 和 `BD-PS-009..012` 未全部确认前，可以先实现：

1. 受控入口的原始请求保全、请求身份和冲突保护。
2. 委托/包裹/字段范围解析和客户账户隔离。
3. 不可覆盖资料版本、来源关系、业务时间和当前采用判断的领域骨架。
4. 已记录、待补充、业务拒绝、未受理、冲突和技术未形成结果语义。
5. 向 `UC-CC-002`、`UC-CC-003`、`UC-CC-007` 及其他消费方的版本引用交接。
6. `AT-PS-014..032` 的领域与应用测试骨架；真实字段、角色、阶段和阈值使用显式未配置状态。

未确认前不得通过人工数据库改值、接受基线覆盖、自动重报或全局“资料已更新”状态绕过本用例。
