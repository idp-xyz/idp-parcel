# UC-TF-005 形成权威运输交接与实际履约

状态：稳定业务骨架待实现；真实节点/运输方交接、实际承运商、移动事实和外部可见范围参数待提供

## 目标与边界

本用例从节点侧交出、运输方接收或其他适用边界证据到达开始，到 `transport-fulfillment` 按载运对象形成权威运输交接、实际承运商判断、实际履约段、出发/移动/到达及下一次控制边界为止。

本用例是运输事实的唯一权威形成入口。节点扫描、车辆离场、装载分配、外部状态码、单证列入或消息到达都只是来源证据，不能单独替代权威交接。有效交接成立前，原控制方继续承担控制；已开始的实际履约段不能取消回未开始。

本用例包含：

- 收集节点、运输方、封签、点验、交出和接收证据，并逐对象裁决已交接、已拒收或待确认。
- 只为已交接对象建立实际履约段和履约参与关系；对封签集运单元形成受控派生，不伪造成员逐件事实。
- 依据保留原始来源的有效证据判断实际承运商；证据不足时保持待确认。
- 记录出发、实际移动、到达、资源变化、中断、折返、改降和控制责任变化。
- 在下一次权威交接或有效交付后结束相应对象的履约参与关系。
- 以稳定事实引用交给追踪、异常、节点、路由和包裹终局消费方。

本用例不执行：

- 节点作业、节点物理装卸、封签制作或成员变更。
- 路由计划选择、容量规则、客户承诺、关务决定、异常案件或费用结算。
- 派送任务和 POD 有效性判断（由 [`UC-TF-006`](./UC-TF-006-PERFORM-DELIVERY-AND-CAPTURE-POD.md) 负责）。
- 包裹终局、客户取消或服务终止。

## 元数据

| 项目 | 内容 |
|---|---|
| 用例 ID | `UC-TF-005` |
| 主责上下文 | `transport-fulfillment` |
| 主要参与者 | 节点运营、运输运营、实际执行方、伙伴接入、网络规划、异常运营 |
| 业务决定所有者 | `transport-fulfillment` 拥有权威交接、实际承运商、实际履约段和移动事实 |
| 触发 | 节点侧交出/运输方接收证据、运输事件或控制边界变化 |
| 交接成功结束 | 明确对象控制转移到运输方，形成履约参与和实际段 |
| 交接非成功结束 | 对象形成拒收、待确认或未受理结果，原控制保持 |
| 履约结束 | 对象形成下一次权威交接、有效交付或明确控制终止 |
| 未形成结束 | 来源、并发或技术处理未完成；不伪装为拒收或运输失败 |

## 权威依据

| 引用键 | 权威内容 | 来源 |
|---|---|---|
| `TF-LANG` | 载运对象、权威交接、实际承运商、实际履约段和移动 | [运输履约上下文：Language](../../domain/transport-fulfillment/CONTEXT.md#language) |
| `TF-RULE` | 交接证据、实际承运商和履约段不变量 | [运输履约上下文：Rules and invariants](../../domain/transport-fulfillment/CONTEXT.md#rules-and-invariants) |
| `TF-LIFE` | 实际履约段和权威交接生命周期 | [运输履约上下文：Lifecycles](../../domain/transport-fulfillment/CONTEXT.md#lifecycles) |
| `TF-COMMISSION` | 委托、订舱、接受和分配准备 | [`UC-TF-004`](./UC-TF-004-COMMISSION-AND-ACCEPT-TRANSPORT.md) |
| `NO-HANDOFF` | 节点控制、集运、封签和节点侧交出事实 | [节点作业上下文](../../domain/node-operations/CONTEXT.md) |
| `NR-PLAN` | 当前计划段、执行意图和冻结边界 | [网络与路由上下文](../../domain/network-routing/CONTEXT.md) |
| `PS-SERVICE` | 包裹身份、服务责任、收寄和承诺 | [小包托运上下文](../../domain/parcel-shipment/CONTEXT.md) |
| `ADR-FACT` | 来源事实、有效事件和派生状态分离 | [ADR-0005](../../adr/0005-source-facts-effective-events-derived-state.md) |

## 输入语义契约

| 输入组 | 权威来源 | 最低业务语义 | 使用约束 |
|---|---|---|---|
| 节点侧交出证据 | `node-operations` | 对象/集运单元、节点控制、点验、装载、封签、交出范围、业务时间和来源 | 只能作为交接证据，不自动转移控制 |
| 运输方接收证据 | 运输方/伙伴接入 | 接收方、对象范围、地点、时间、来源、拒收/待确认原因 | 来源身份和版本必须保全 |
| 运输准备 | `UC-TF-004` | 班次、容量、委托、承运接受、装载分配和条件 | 准备结果不证明实际接收 |
| 包裹/集运身份 | `parcel-shipment` / `node-operations` | 包裹永久身份、集运成员快照和谱系 | 不创建临时包裹或覆盖节点实物 |
| 实际移动来源 | 自营执行/外部伙伴 | 出发、位置、到达、中断、资源和时间事实 | 外部不透明范围不得虚构内部轨迹 |
| 当前限制 | `customs-compliance` / 其他限制源 | 对象、动作、有效区间和解除结果 | 只阻断明确受门禁动作 |

## 结果语义契约

| 结果 | 业务含义 | 必须保留 | 禁止行为 |
|---|---|---|---|
| 权威交接已形成 | 明确对象控制转移到运输方 | 双方证据、对象范围、业务时间、结果版本和控制边界 | 不用整批汇总覆盖成员差异 |
| 权威交接已拒收 | 本次交接不成立，原控制继续 | 拒收对象、原因、来源和后续入口 | 不等于取消、退运或交付 |
| 权威交接待确认 | 证据不足或冲突无法裁决 | 全部来源、冲突范围、当前控制和复核入口 | 不因装载/离场/消息顺序转移控制 |
| 实际履约段已成立 | 已交接对象进入共同运输控制范围 | 履约段、对象参与关系、实际承运/未知、起点和依据 | 不从订舱或单证推导 |
| 实际承运商已确认 | 有效证据明确真实执行方 | 责任主体、来源、业务时间和判断版本 | 不把签约方/品牌/代理商自动改写 |
| 实际承运商待确认 | 控制成立但身份证据不足 | 已知控制范围、未知期间和补证入口 | 不虚构伙伴内部网络 |
| 移动/到达事实已形成 | 运输事实发生并可引用 | 对象/段、位置、业务时间、资源、来源和更正关系 | 不形成交付或关务完成 |
| 控制边界已结束 | 下一次交接、有效交付或明确终止成立 | 结束原因、对象范围、时间和后续责任 | 不用班次关闭或异常案件代替 |
| 未形成 | 事实保存或派生未完成 | 已形成事实和恢复引用 | 不伪装为未移动 |

## 应用流程

| 步骤 | 责任方 | 应用动作 | 结果与下一步 |
|---:|---|---|---|
| 1 | 运输接入 | 保全交接请求、来源、对象、业务时间和已有控制 | 无法关联时未受理 |
| 2 | `transport-fulfillment` | 识别重复、迟到、冲突和更正版本 | 重复返回原结果；冲突保留并待复核 |
| 3 | `transport-fulfillment` | 核对节点控制、准备范围、运输接受、封签和适用门禁 | 形成逐对象候选交接结果 |
| 4 | `transport-fulfillment` | 依据规则形成已交接、拒收或待确认 | 未交接对象控制不变 |
| 5 | `transport-fulfillment` | 为已交接对象建立履约参与和实际履约段 | 记录实际承运商或未知 |
| 6 | 执行方/伙伴接入 | 接收出发、移动、到达、中断和资源变化事实 | 保存来源，不直接修改计划 |
| 7 | `transport-fulfillment` | 形成下一次交接或控制结束结果 | 结束相应对象参与关系，其他对象继续 |
| 8 | `transport-fulfillment` | 发布稳定事实引用给路由、追踪、异常和 `parcel-shipment` | 发布失败只重试同一意图 |

## 交接与实际履约规则

- 权威交接按载运对象形成，允许部分成功；整袋/整笼/整箱只有在封签连续且相符时才能派生“随单元受控”，不能伪造成员逐件接收。
- 封签缺失、破损或不一致时，可以判断集运单元本身的交接，但成员只保留来源链和责任主张，当前实物控制形成待确认/冲突并等待开封和实际核对；不得继续把已经没有保管能力的原节点记为成员当前控制方。
- 扫描、点验、签字、车辆离场、物理装载、卸载、位置变化或外部状态码中的单项都不能在没有合格规则时自动转移控制。
- 实际履约段由首个有效收寄或权威交接成立；只有已交接对象形成履约参与关系，其他对象不得被段级状态吞并。
- 实际承运商与渠道服务方、签约服务商、代理商、底层品牌和结算相对方分离；证据不足时允许未知，后续有效证据只形成新的身份判断版本。
- 班次首个有效出发/移动前可以取消或替代；开始后只能形成中断、折返、改降或后续旅程，不能回写为未开始。
- 停运、失联、车辆故障或异常案件不自动结束控制；没有下一次交接、有效交付或明确终止依据时当前责任继续存在。
- 退运、替代和改路后的新履约段拥有独立身份并关联原旅程，不倒退原段、原承诺或原交付。

## 一致性、幂等与并发

- 同一对象、交接身份和来源版本只形成一个有效交接结果；同一移动事实重复到达不重复累计里程或转移控制。
- 早期交接更正必须沿后续交接、移动、到达和交付关系重新评估控制来源链；后续事实继续不可覆盖。当前实物方明确而来源链矛盾时保存控制冲突，不把控制简单回退给早期原方。
- 交接与取消、容量释放、改路和异常并发时按业务时间、有效边界和已发生事实裁决，不按消息顺序覆盖。
- 交接事实保存成功但派生/发布失败时从同一事实恢复，不要求再次交接或重复移动。
- 部分交接、对象拒收和未知承运商不回滚同一段中其他对象已经成立的结果。
- 任何人工更正保留原证据、原判断、原因、决定方和生效时间；不能直接编辑当前控制字段绕过版本。
- 跨客户共同运输只向执行方提供当前履约所需的最小范围，不能通过整批汇总暴露其他客户。

## 首发试点叠加条件

- `PAR-NET-08`、`PAR-NET-13/15` 提供主履约方、节点/运输来源、交接、控制和责任边界；`PAR-INT-03` 提供外部交接、实际承运和移动来源，`PAR-INT-07` 只提供上游运输准备交互引用。
- `PAR-NET-04..06` 提供节点控制、集运、封签和节点侧交出证据；`PAR-NET-14` 提供计划段和执行边界。
- 适用关务限制只由 `PAR-CUS-01..04` 或其他限制源提供最小门禁投影，不在本用例内解除。
- `PAR-NFR-02/04/06` 提供事件规模、新鲜度、处理和恢复目标。
- 未确认时可以实现逐对象结果、未知承运商、封签派生、冲突和恢复骨架；不得默认“车辆离场即交接”或外部状态码映射。

## 验收示例

| ID | 场景 | 预期结果 |
|---|---|---|
| `AT-TF-051` | 节点交出和运输方接收均满足规则 | 逐对象形成已交接并建立实际履约段 |
| `AT-TF-052` | 一批对象只有部分被接收 | 已接收对象进入履约，拒收对象继续由节点控制 |
| `AT-TF-053` | 车辆离场但双方证据冲突 | 保持待确认，原控制不转移 |
| `AT-TF-054` | 封签连续的集运单元整体交接 | 形成单元控制和成员随单元受控派生，不伪造逐件扫描 |
| `AT-TF-055` | 封签破损但单元被接收 | 单元交接可独立成立；成员进入控制待确认/冲突并等待核对，不继续挂在无实际保管能力的原节点 |
| `AT-TF-056` | 渠道品牌已知但实际承运商未知 | 建立实际段，承运商保持待确认 |
| `AT-TF-057` | 后续取得真实承运商收寄证据 | 形成新的身份判断版本，未知期间和原来源继续保留 |
| `AT-TF-058` | 班次出发后资源故障 | 保留开始事实，形成中断/替代，不回写为取消 |
| `AT-TF-059` | 运输中断但没有控制终止证据 | 当前运输方继续承担控制，异常不自动结束实际段 |
| `AT-TF-060` | 目的节点接收证据充分 | 形成下一次权威交接并结束原对象参与关系 |
| `AT-TF-061` | 交接事实重复到达 | 返回原结果，不重复转移控制 |
| `AT-TF-062` | 迟到更正使原交接证据失效，之后已有移动和下一跳交接 | 保留原判断及全部后续事实，重算控制来源链；允许当前实物方明确但来源冲突，不简单回退早期控制 |
| `AT-TF-063` | 跨客户共同运输查询 | 严格按对象和授权隔离，不泄露其他客户范围 |

## 给开发的交接

实现必须把“节点/运输方来源证据 → 权威交接结果 → 实际承运商判断 → 履约参与关系 → 实际履约段 → 移动事实”分开保存。禁止使用一个 `in_transit` 或 `handover_status` 覆盖对象差异，也禁止由段级状态反向生成包裹交付或终局。

完成本用例只证明运输控制和实际履约事实可追溯；派送交付、POD、退运和包裹终局由后续用例分别形成。
