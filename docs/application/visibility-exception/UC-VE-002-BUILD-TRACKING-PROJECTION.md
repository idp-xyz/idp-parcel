# UC-VE-002 接收事实并形成全程追踪投影

状态：稳定业务骨架待实现；真实来源覆盖、映射版本和冲突样本待提供

## 目标与边界

本用例从各源上下文提交已经接受、范围明确且可追溯的事实或有效性变化开始，到 `visibility-exception` 形成标准追踪里程碑、物流/控制/关务/交付等维度投影和摘要，或明确返回重复、冲突、未归类、待补充或未形成结果为止。

本用例不接受原始报文、截图、自由备注或客户意见为事实，不修改源事实、路由、节点、运输、关务、交付、包裹终局或费用，也不形成 ETA、异常案件、客户通知或普通客户查询结果。客户全程追踪视图由 [`UC-VE-008`](./UC-VE-008-PROVIDE-CUSTOMER-END-TO-END-TRACKING-VIEW.md) 在本投影之上按客户账户和对象授权形成。

## 元数据

| 项目 | 内容 |
|---|---|
| 用例 ID | `UC-VE-002` |
| 主责上下文 | `visibility-exception` |
| 参与者 | 各源责任方、伙伴接入、轨迹运营、客户服务 |
| 业务决定所有者 | `visibility-exception` 拥有里程碑/投影；源上下文拥有事实有效性 |
| 触发 | 已接受事实、迟到/更正、身份谱系变化或映射规则生效 |
| 成功结束 | 形成可追溯投影版本和适用里程碑 |
| 待补充/冲突 | 身份、范围、时间、映射或裁决依据不足 |
| 未形成 | 持久化或并发处理失败，不能伪装为无变化 |

## 权威依据

| 引用 | 内容 | 来源 |
|---|---|---|
| `VE-RULE` | 来源接受、映射、冲突、迟到和外部不透明履约规则 | [全程追踪与异常上下文](../../domain/visibility-exception/CONTEXT.md) |
| `VE-LIFE` | 投影版本和更正生命周期 | [全程追踪与异常上下文](../../domain/visibility-exception/CONTEXT.md#lifecycles) |
| `FACT` | 来源事实、有效事件和派生状态分离 | [ADR-0005](../../adr/0005-source-facts-effective-events-derived-state.md) |
| `IDENTITY` | 包裹永久身份和谱系 | [小包托运上下文](../../domain/parcel-shipment/CONTEXT.md) |

## 输入和结果

| 输入/结果 | 业务语义 |
|---|---|
| 源事实 | 来源身份、对象范围、事实类型、来源版本、接受结果和业务时间；只消费已接受事实 |
| 有效性变化 | 更正、撤销、替代或迟到关系；追加后重新派生，不删除原结果 |
| 投影已形成 | 映射版本、维度、里程碑、范围、业务/有效/接收时间和派生版本均可追溯 |
| 部分/未归类 | 只有部分可映射或没有可靠映射；保留原因和续办入口 |
| 冲突待确认 | 有效事实无法按权威范围、时间和因果裁决；保留各事实和冲突关系 |
| 已有/未形成 | 重复返回原结果；处理失败保留失败阶段和恢复引用 |

## 主流程

1. 保全来源身份、原始引用、对象范围、业务时间、有效时间和接收时间。
2. 校验来源上下文已接受事实，并按 `parcel-shipment` 核对永久身份、外部标识版本和谱系。
3. 选择当前适用的事实映射版本；没有可靠映射则形成未归类。
4. 按事实类型、权威范围、业务时间、有效时间和因果顺序分别派生各维度，不压缩成单一状态。
5. 对重复、迟到、更正和冲突形成追加版本；无法裁决时保持待确认，不按最后消息覆盖。
6. 返回投影已形成、部分、未归类、冲突、已有结果或未形成结果，供 ETA、异常和客户全程追踪视图用例消费。

## 规则、权限与隔离

- 不能建立全局来源排名；裁决必须引用事实类型、控制范围、时间和源上下文权威范围。
- 集运、同批或共同来源不合并包裹身份；拆分/合并沿 `parcel-shipment` 谱系继续投影。
- 外部不透明履约段只展示已知入口、出口和实际获得的里程碑，不虚构伙伴内部节点。
- 跨客户投影按客户账户和责任法人隔离；共同来源事故不泄露其他客户对象或数量。
- 投影规则版本变化不得删除原映射、原判断或已发布客户信息。

## 首发约束与验收

`PAR-INT-03`、`PAR-VIS-01` 和 `PAR-NFR-04/06` 未确认时只做骨架、回放或影子验证。`VIS-01` 按 `P+R/S` 取证：正常来源和已发布映射取得 `P`，冲突、迟到、更正及未归类分支使用 `R/S`。

| ID | 场景 | 预期结果 |
|---|---|---|
| `AT-VE-038` | 已接受节点收寄事实首次进入投影 | 形成来源引用和节点里程碑，不修改节点事实 |
| `AT-VE-039` | 同一来源版本重复到达 | 返回原投影，不重复里程碑 |
| `AT-VE-040` | 来源身份携带不同对象范围 | 形成冲突并待确认 |
| `AT-VE-041` | 外部状态码尚未被业务接受 | 不形成标准里程碑 |
| `AT-VE-042` | 已接受事实没有可靠映射 | 形成未归类，不写成运输中 |
| `AT-VE-043` | 两个有效事实无法裁决 | 保留双方和冲突关系 |
| `AT-VE-044` | 迟到/更正事实到达 | 追加投影版本，原版本保留 |
| `AT-VE-045` | 包裹真实拆分 | 后继包裹分别投影，共同历史只保留谱系 |
| `AT-VE-046` | 跨客户共同集运 | 物理来源可共同存在，客户投影隔离 |
| `AT-VE-047` | 外部段无内部节点明细 | 不虚构中间节点 |
| `AT-VE-048` | 保存结果不确定 | 返回未形成和恢复引用 |

## 给开发的交接

把“已接受来源 → 映射判断 → 各维度投影 → 标准里程碑/摘要”分别建模。完成本用例只证明内部投影可解释和可重派生，不证明客户有权查询、客户视图已经发布、ETA、异常、通知或源动作完成。
