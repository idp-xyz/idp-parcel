# UC-VE-008 提供客户普通全程追踪视图

状态：稳定业务骨架待实现；真实客户账户授权、对客里程碑、地点粒度、ETA 门槛和终局展示规则待提供

## 目标与边界

本用例从已认证请求方查询一个或多个包裹开始，到在明确货主客户账户和对象授权范围内返回普通全程追踪视图，或返回无权查询、标识冲突、信息待确认、当前无可见结果或未形成为止。

本用例把 [`UC-VE-002`](./UC-VE-002-BUILD-TRACKING-PROJECTION.md) 的当前有效追踪投影、[`UC-VE-003`](./UC-VE-003-FORM-ETA-AND-VISIBILITY-GAP.md) 已满足对客门槛的 ETA、`parcel-shipment` 的客户服务终局以及已经批准披露的客户可见异常编排为客户账户隔离的只读视图。

本用例不接受或修改源事实，不决定 ETA、包裹终局、异常披露或通知义务，也不发送主动通知。普通查询、门户展示、客户可见异常和 [`UC-VE-006`](./UC-VE-006-FORM-CUSTOMER-DISCLOSURE-AND-NOTIFICATION.md) 的主动通知始终是不同结果。

## 元数据

| 项目 | 内容 |
|---|---|
| 用例 ID | `UC-VE-008` |
| 主责上下文 | `visibility-exception` |
| 参与者 | 货主客户授权用户、客户服务、客户门户或客户查询接入 |
| 业务决定所有者 | `visibility-exception` 拥有客户视图编排、ETA 对客可见性和异常披露决定；各源上下文拥有各自源事实，`parcel-shipment` 拥有客户服务终局 |
| 触发 | 授权客户查询、当前投影/ETA/终局/客户可见异常变化、客户归属确立，或来源更正触发客户视图重派生 |
| 成功结束 | 返回当前客户账户及授权对象范围内可解释的客户追踪视图 |
| 负向结束 | 请求方或对象明确无权查询，且外部结果不泄露对象是否存在 |
| 待判断结束 | 外部标识冲突、身份谱系待确认、授权范围或当前有效来源不足 |
| 未形成结束 | 视图派生、授权判断或结果保存未完成，不能伪装为无轨迹或无权查询 |

## 权威依据

| 引用 | 内容 | 来源 |
|---|---|---|
| `VE-RULE` | 客户全程追踪视图、账户隔离、公开语义和更正规则 | [全程追踪与异常上下文](../../domain/visibility-exception/CONTEXT.md) |
| `PROJECTION` | 当前有效标准里程碑、各维度投影和更正关系 | [`UC-VE-002`](./UC-VE-002-BUILD-TRACKING-PROJECTION.md) |
| `ETA` | 客户可见 ETA 判断及其版本 | [`UC-VE-003`](./UC-VE-003-FORM-ETA-AND-VISIBILITY-GAP.md) |
| `OUTCOME` | 包裹客户服务终局及其来源关系 | [`UC-PS-004`](../parcel-shipment/UC-PS-004-FORM-PARCEL-FINAL-SERVICE-OUTCOME.md) |
| `DISCLOSURE` | 已批准的客户可见异常和异常通知边界 | [`UC-VE-006`](./UC-VE-006-FORM-CUSTOMER-DISCLOSURE-AND-NOTIFICATION.md) |
| `ACCOUNT` | 货主客户账户、参与方关系和授权依据 | [参与方与商业上下文](../../domain/party-commercial/CONTEXT.md) |

## 输入和结果语义

| 输入/结果 | 业务语义 |
|---|---|
| 查询主体 | 已认证人员或系统身份，以及其被授予的客户账户和对象范围；认证成功不自动拥有全部客户数据 |
| 查询标识 | 包裹内部引用、授权客户参考号或版本化外部标识；标识只用于定位候选，不是访问凭证 |
| 客户追踪视图 | 客户允许看到的包裹身份、公开标准里程碑、追踪摘要、适用 ETA、客户服务终局、更正说明及已批准异常信息 |
| 当前无可见结果 | 对象已授权，但当前没有满足对客展示条件的里程碑、ETA 或终局；不表示未收寄、未运输或遗失 |
| 信息待确认 | 投影冲突、身份谱系、终局更正或授权依据尚不能可靠裁决；不选择最乐观或最严重结果 |
| 无权查询 | 请求方、客户账户或对象范围不匹配；对外结果不得帮助推断其他客户对象是否存在 |
| 更正/替代视图 | 来源有效性、ETA、终局或披露依据变化后形成的新版本；原已发布版本保留但不再作为当前结果 |

## 主流程

1. 识别并校验请求方身份、当前货主客户账户、委派关系、允许查询的对象范围和用途。
2. 按客户账户和查询标识定位包裹候选，核对外部标识版本、客户归属和身份谱系；零匹配、多匹配和无权范围分别处理。
3. 读取 `UC-VE-002` 当前有效投影，只采用已经映射且允许对客展示的标准里程碑、摘要和公开地点语义。
4. 读取 `UC-VE-003` 的客户可见性判断；只有达到适用质量与合同门槛的 ETA 才进入视图，不能使用计划时间或内部预测填充。
5. 读取 `parcel-shipment` 当前有效客户服务终局；交付扫描、退运动作或异常案件不能替代终局所有者的结果。
6. 如 `UC-VE-006` 已形成当前有效且授权范围匹配的客户可见异常，只读展示批准内容；普通查询不自行形成披露决定或消息意图。
7. 按客户可见规则过滤内部节点、路线、控制、供应商商业信息、调查、其他客户和受限监管内容，形成当前客户视图版本。
8. 返回当前视图、当前无可见结果、信息待确认、无权查询、标识冲突或未形成，并保留采用版本和更正/替代关系。

## 规则、权限与隔离

- 货主客户账户是本用例的首要隔离边界。集团用户、客户服务人员或系统接入只有在明确委派范围内才能跨账户查询，不能因属于同一集团租户而默认获得所有客户视图。
- 外部运单号、客户订单号、收件信息或其他可猜测字段不能单独作为授权。未认证收件人或公开匿名查询不属于本用例；如未来支持，必须建立独立且明确的授权与最小披露规则。
- 同一外部标识存在多个有效候选、跨客户复用或版本冲突时保持待确认/冲突，不任选一个候选，也不向请求方列出其他客户候选。
- 共同集运、同班次、共同异常案件或共同来源不能扩大查询范围。客户只看到本账户获授权对象及其公开信息，不能推断其他客户存在、数量、线路或异常影响。
- 客户视图不是内部全程追踪投影的完整副本。内部控制位置、非公开节点、路由策略、合作伙伴商业角色、调查记录、证据和未经确认责任默认不展示。
- 标准里程碑只能来自 `UC-VE-002` 当前有效投影；客户服务终局只能来自 `parcel-shipment`。显示“已交付”里程碑不能自行形成委托完成或其他终局。
- ETA 只有在 `UC-VE-003` 已明确允许对客展示时才显示。ETA 缺失或停止展示不能用客户承诺、计划到达或内部估计替代。
- 已批准客户可见异常可以进入普通视图，但查询动作不形成异常披露、主动通知、送达或客户确认。门户展示能否满足特定通知义务仍由 `UC-VE-006` 按合同和策略判断。
- 来源迟到、更正、撤销、替代、POD 失效、身份谱系或终局变化必须触发当前视图重新派生。原发布内容保留更正关系，不得静默删除，也不得继续把失效内容作为当前事实展示。
- 客户归属确立必须触发视图形成。货主客户账户迟于包裹源事实才可确定时，视图在归属确立时按当前有效投影形成，不得等待下一份源事实；归属不可确定期间不形成视图，也不得发明账户或表达为无轨迹。
- 某一维度信息不足时只降低该维度的展示，不把整个包裹写成“运输中”“异常”或“已完成”。批量查询逐对象返回结果，一个对象冲突或失败不扩大其他对象授权，也不回滚合法结果。

## 首发约束与验收

`PAR-VIS-01/03/09`、`PAR-COM-07`、`PAR-NFR-06` 未确认，或 `VIS-11 P+S` 未完成时，只能使用隔离账户进行回放或影子验证，不开放生产客户查询。真实来源覆盖、对客里程碑、地点粒度、ETA 门槛、终局展示、更正规则、客户账户与对象授权和查询新鲜度必须由业务责任方提供，不能以样例默认值替代。

| ID | 场景 | 预期结果 |
|---|---|---|
| `AT-VE-150` | C1 授权用户按本账户包裹查询 | 只返回 C1 当前有效客户视图和采用版本 |
| `AT-VE-151` | C1 使用属于 C2 的有效外部标识查询 | 返回不泄露存在性的无权结果，不暴露 C2 身份或轨迹 |
| `AT-VE-152` | 同一外部标识存在多个有效候选 | 形成标识冲突/待确认，不任选候选或列出其他客户对象 |
| `AT-VE-153` | 请求方只有外部运单号但没有客户账户授权 | 不受理查询，标识本身不作为访问凭证 |
| `AT-VE-154` | 集团用户只获委派 C1、C3 | 只允许 C1/C3，对其他集团客户不默认授权 |
| `AT-VE-155` | C1/C2 包裹处于同一集运单元、班次或共同案件 | C1 视图不披露 C2 的存在、数量、路线或异常范围 |
| `AT-VE-156` | 当前投影包含已批准公开里程碑 | 展示标准语义和公开地点，不复制内部来源状态 |
| `AT-VE-157` | 投影含内部控制位置、非公开节点和供应商商业信息 | 按规则过滤，不进入客户视图 |
| `AT-VE-158` | 只有内部 ETA 或质量未达对客门槛 | 不展示 ETA，也不使用计划或承诺时间填充 |
| `AT-VE-159` | `parcel-shipment` 已形成当前有效客户服务终局 | 展示终局及其依据版本，不由摘要反推 |
| `AT-VE-160` | 只有有效交付扫描但终局尚未形成 | 可展示获准交付里程碑，不显示包裹服务终局已完成 |
| `AT-VE-161` | 来源更正使原里程碑或终局失效 | 形成追加更正/替代视图，原版本保留且不再作为当前结果 |
| `AT-VE-162` | 包裹发生真实拆分或合并 | 按当前有效身份谱系和授权展示后继对象，共同历史不重复计算 |
| `AT-VE-163` | 一个维度存在无法裁决的事实冲突 | 该维显示待确认，其他可靠维度继续展示，不虚构统一状态 |
| `AT-VE-164` | `UC-VE-006` 已批准当前客户可见异常 | 只读展示批准范围，不形成新的披露或主动通知 |
| `AT-VE-165` | 客户普通查询时不存在客户可见异常 | 返回正常追踪内容，不自动建案、披露或通知 |
| `AT-VE-166` | 合同要求异常主动送达，但客户已在门户查看 | 保留查询/展示结果；是否满足通知义务仍由 `UC-VE-006` 判断 |
| `AT-VE-167` | 未认证收件人尝试公开查询 | 本用例不受理，不默认开放最小轨迹 |
| `AT-VE-168` | 授权或视图结果保存失败 | 返回未形成和恢复引用，不伪装为无轨迹或无权查询 |
| `AT-VE-169` | 包裹全部源事实已入账后，货主客户账户关系才建立 | 在客户归属确立时按当前投影形成客户视图，不等待新的源事实 |

## 给开发的交接

把内部全程追踪投影、客户全程追踪视图、客户可见异常和主动通知分别建模。客户视图必须同时受客户账户、对象授权和信息公开规则约束，并只引用当前有效里程碑、客户可见 ETA、终局和更正关系。完成本用例只证明授权客户可以安全查询普通全程轨迹，不证明异常已经通知、客户已经确认或任何源事实已经改变。
