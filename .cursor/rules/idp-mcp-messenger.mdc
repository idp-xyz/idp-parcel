---
description: IDP MCP 规则与强制收尾
globs: "**/*"
alwaysApply: true
---

# IDP MCP

侧栏管理通道，增删后需「同步到 mcp.json」。每个对话窗口绑定一个 `idp-mcp-N`。

# 绝对规则：每轮回复后必须调 `check_messages`

**无论你做了什么（回答问题、完成任务、寒暄、确认），回合的最后一步必须是调用当前 `idp-mcp-N` 的 `check_messages`。没有例外。**

## 调用方式

**默认（无消息时永久等待，不返回 `[continuation]`）：**

```
check_messages({ blockUntilMessage: true })
```

不要传 `maxBlockMs`，单次调用会在空队列上一直轮询，直到有消息可消费或客户端取消。

**仅当** Cursor 或传输对单次工具调用有硬超时、长等会被掐断时，再使用分段长等，例如：

```
check_messages({ blockUntilMessage: true, maxBlockMs: 600000 })
```

- 仅在**使用 `maxBlockMs` 分段**时，空队列到期会返回 `[continuation]` → 立即用**相同参数**再调一次以续等
- 调用失败/超时 → 立即重试，最多 3 次
- **必须**附带 `reply`（本轮回复摘要，供对话面板展示）

## 执行顺序

1. 完成文字回复和所有工具调用
2. **最后一步** → `check_messages({ blockUntilMessage: true, reply: "本轮回复的简短摘要" })`

**`reply` 参数**：每次调用 `check_messages` 时**必须**附带 `reply`（1-2 句话概括本轮做了什么）。面板中"同步回复"开关开启时，回复会自动显示在对话面板中。

## 其它工具速查

- 跨通道：`send_to_session` / `broadcast`
- 阻塞提问：`ask_question`（等待 `answer.json`）
- 进度：`save_progress` / `load_progress` / `send_progress`
- 锁定：`set_channel_lock`

## 点名：收到 `[点名]` 广播必须立刻应答

派发方派任务前会先广播一条以 `[点名 ← 通道 M · 截止 HH:MM]` 开头的消息。收到后**本轮只做一件事**：用 `send_to_session({ target: M, message })` 回一行，格式固定：

`[应答] 通道 N · 状态：空闲 | 在做 <票号> · 地盘：<目录，无则写无> · 余量：充足 | 将满`

然后立即回到 `check_messages`。不要借这一轮顺手干别的——窗口很短，晚到等于没到。

截止之后才发出的应答**不算认领**：派发方名单在截止那一刻定死，晚到的等下一次点名。你正在干活、本轮结束才读到点名，属正常——不必补答，也不要因此中断手上的票。

派发方那半（为什么心跳不够、窗口怎么定、未应答怎么记）见仓内 `docs/agents/parallel-sessions.md`「派发前先点名」一节。

**再次强调：不管做了什么，回合结尾必须调 `check_messages`。**
